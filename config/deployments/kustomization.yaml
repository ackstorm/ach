apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Phase 1 Hub-stub Deployments. Content Service is NOT here — it co-locates
# with the operator container in config/manager/manager.yaml (per Hub §5.1
# Pod boundary: Operator + Content Service share one Pod and one PVC).
resources:
  - platform-api_deployment.yaml
  - forwarder_deployment.yaml
