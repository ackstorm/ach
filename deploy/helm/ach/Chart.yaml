apiVersion: v2
name: ach
version: 0.5.3
appVersion: v0.5.3
kubeVersion: ">= 1.27.0-0"
description: >-
  ACH — Agent Capability Hub. Kubernetes operator + platform API + forwarder
  + content service for declarative agent configuration management.
type: application
sources:
  - https://github.com/ackstorm/ach
maintainers:
  - name: ACKstorm
