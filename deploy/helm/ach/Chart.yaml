apiVersion: v2
name: ach
version: 0.2.0
appVersion: v0.2.0
kubeVersion: ">= 1.27.0-0"
description: >-
  ACH — Agent Configuration Hub. Kubernetes operator + platform API + forwarder
  + content service for declarative agent configuration management.
type: application
sources:
  - https://github.com/ackstorm/ach
maintainers:
  - name: ACKstorm
