# P1 Docs Gaps Implementation Plan (G1, G2-docs, G10, G12, G13-docs, G14, G17, G18-docs)

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Close the docs-only half of the P1 ("close v1alpha1") bundle from `docs/external-review-resolutions.md`: honest framing for budget (G1), GitOps-only write path (G2), gateway-as-optional-edge (G12), the transient `default_team_missing` window (G14), the dropped `nbf` claim (G17), and the bare-email `sub` (G18-docs); plus author the kind-lifecycle contract (G10) and `pk_`-vs-`ek_` guidance (G13-docs).

**Architecture:** ACH's in-repo docs are a lean `CLAUDE.md` navigation hub + deep narrative under top-level `references/` + task docs under `docs/` (runbooks, developer-guide, getting-started). The frozen/delivered specs live in a SEPARATE sibling repo `/home/jcm/Projects/ach-spec/` — spec edits are noted per-gap but committed there separately, NOT in this repo.

**Tech Stack:** Markdown. One Go code-comment edit (G2). No tests beyond `markdownlint`/link sanity and the doc-build (`make gen-crd-ref-docs` is unaffected here).

---

## Ground-truth corrections (trust THESE over the decisions doc)

Verified against the working tree 2026-06-16:

1. **G1 — no stale budget wording exists in README.md / CLAUDE.md / `references/`.** The decisions doc names a "Five Pillars / Usage Governance … Environment groups … limits" target, but those phrases are ABSENT from the in-repo files (already removed or never present). The only in-repo budget-adjacent action is the `pk_` warning reword — that's a Go string, handled in the CODE plan (Task 13). G1's remaining work is entirely in the sibling spec (`ach-spec-final-delivered.md §6.3/§8.6`).
2. **G10 — `docs/references/` does NOT exist; top-level `references/` DOES.** Per the decisions doc's "match convention" note, create the new doc at `references/adding-a-cr-kind.md` (NOT `docs/references/`).
3. **G10 — `.github/pull_request_template.md` does NOT exist** — create it.
4. **G17/G18 — the in-repo JWT docs are already mostly correct.** `docs/runbooks/writing-an-mcp-backend.md`, `docs/developer-guide/jwt-forwarder.md`, and `test/e2e/mcp-echo/README.md` already omit `nbf` and already say `sub` = bare email. The work is ADDITIVE notes ("`nbf` intentionally absent — verify `iss/aud/exp` only"; "legacy `<ns>/<email>` form is gone"). The stale `signer.go` comments are in the CODE plan (Task 3).
5. **G2 — the live stale claim is in `references/troubleshooting.md:381-392`** (the `ConflictWithUIRow` failure-mode entry presents it as reachable) + the `CLAUDE.md:271` cross-reference. The one-line code comment goes on `internal/controller/ach/conditions.go:156-161`.

**Spec edits (sibling repo `/home/jcm/Projects/ach-spec/`):** each gap below lists spec sections that also need updating. Make + commit those in that repo separately. They are out of scope for this repo's commits but listed so nothing is dropped.

**Verify commands:** there are no unit tests for docs. After each task: re-read the edited section to confirm accuracy, and run `make qa-lint-changed` only if a `.go` file changed (G2 comment). Final: `git diff --stat` to confirm only intended files changed.

---

## Task 1: G10 — author the kind-lifecycle contract

**Files:**
- Create: `references/adding-a-cr-kind.md`
- Create: `.github/pull_request_template.md`
- Modify: `CLAUDE.md:124-143` (MANDATORY Reading Table — add a row)

**Step 1: Create the reference doc**

`references/adding-a-cr-kind.md`: the full kind-lifecycle checklist + archetype applicability matrix from G10 of the decisions doc (the 11-row table). State the load-bearing principle up top: **"every `*.ach.ackstorm.ai` CR kind is a first-class citizen — it MUST satisfy every applicable row of the matrix, in the same PR; no second-class kinds."** Archetypes: **object** (Plugin/Skill/Artifact/Prompt), **discovery** (PluginMarketplace/SkillMarketplace), **governance** (Environment/BackendIdentityPolicy), **config singleton** (LiteLLMConnection). Reproduce the 11 surfaces verbatim (CRD+CEL; projection table+`WithTxNotify`; status conditions incl. `Synced=False/NameConflict` on duplicate identity (G15) + `origin`/`locked` if UI-writable; reconciler+finalizer; content pipeline F1; admin inventory; admin refresh; hydrate; metrics; docs/spec; tests). Add a "retroactive audit" note: `Skill`/`SkillMarketplace` (G8 parity) + `LiteLLMConnection`.

**Step 2: Create the PR template**

`.github/pull_request_template.md`: a contributor checklist. Include a top "## Summary" + "## Testing" section and a "## New CR kind? (kind-lifecycle checklist — see `references/adding-a-cr-kind.md`)" section with a per-surface tick-list (the 11 rows as `- [ ]` items), prefaced "skip this section if no new CR kind."

**Step 3: Add the CLAUDE.md MANDATORY row**

In the MANDATORY Reading Table (CLAUDE.md:124-143), add:

```
| Adding/auditing a CRD kind (any archetype) | `references/adding-a-cr-kind.md` (kind-lifecycle checklist + archetype matrix) |
```

**Step 4: Verify + commit**

Re-read all three. Confirm the matrix matches G10 exactly and the new doc path is `references/` (not `docs/references/`).

```bash
git add references/adding-a-cr-kind.md .github/pull_request_template.md CLAUDE.md
git commit -m "docs(process): add kind-lifecycle contract + PR-template checklist (G10)"
```

---

## Task 2: G2 — frame v1alpha1 as GitOps/CRD-only; mark UI path reserved

**Files:**
- Modify: `references/troubleshooting.md:381-392` (the `ConflictWithUIRow` entry)
- Modify: `CLAUDE.md:271` (cross-reference)
- Modify: `internal/controller/ach/conditions.go:156-161` (one-line doc comment)

**Step 1: Reframe the troubleshooting entry**

In `references/troubleshooting.md:381-392`, relabel the `### ❌ Operator condition: Synced=False reason=ConflictWithUIRow` entry: prepend a note that **the UI write path does NOT exist in v1alpha1**, so `origin='ui'` rows are never produced and this condition is **unreachable / dormant** (reserved for the future UI Objects API, #34; the promotion half is unbuilt — see `docs/external-review-resolutions.md` G2). Keep the WHY (column/UPSERT mechanics) as background, but change the resolution from an actionable fix to "N/A in v1alpha1 — reserved machinery."

**Step 2: Fix the CLAUDE.md cross-reference**

At `CLAUDE.md:271`, where `ConflictWithUIRow` is listed among live failure modes routed to troubleshooting, annotate it as "(dormant/reserved — no UI write path in v1alpha1)" so agents don't treat it as a live capability.

**Step 3: Add the code doc comment**

On `internal/controller/ach/conditions.go:156-161` (`ReasonConflictWithUIRow`), append one line:

```go
// Reserved for the future UI write path (#34); the promotion half is unbuilt
// and origin='ui' rows are never produced in v1alpha1 — dormant. See G2 in
// docs/external-review-resolutions.md.
```

**Step 4: Verify + commit**

```bash
make qa-lint-changed   # conditions.go changed
git add references/troubleshooting.md CLAUDE.md internal/controller/ach/conditions.go
git commit -m "docs(g2): label UI write path + ConflictWithUIRow as reserved/dormant (G2)"
```

**Spec note (external):** in `ach-spec-final-delivered.md` + README, state "v1alpha1 write path is GitOps/CRD only; UI Objects API + export + promotion are reserved/future (P2)." Don't present `ConflictWithUIRow`/`origin='ui'` as shipped.

---

## Task 3: G12 — reframe gateway as an optional edge proxy

**Files:**
- Modify: `CLAUDE.md:90-97` (mode table) + `:81-85` (architecture prose)
- Modify: `internal/gateway/doc.go:1-17`

**Step 1: Annotate the mode table**

At `CLAUDE.md:90-97`, change the gateway row's `Owns` cell to mark it optional, e.g.:

```
| gateway         | `ach gateway`         | **Optional** edge reverse proxy — single-origin front for the HTTP surfaces (no auth, no /metrics, no /dex); disable via `gateway.enabled=false`, use per-service Ingress instead |
```

Add a one-line note before/after the table: "The architecture is **5 logic modes** (operator, platform-api, forwarder, content-service, migrate); `gateway` is an optional, logic-free packaging convenience, not a co-equal mode."

**Step 2: Soften the architecture prose**

At `CLAUDE.md:81-85`, add "optional" to the gateway description and note per-service Ingress is the alternative when `gateway.enabled=false`.

**Step 3: Note optionality in the package doc**

In `internal/gateway/doc.go`, add a sentence: the gateway is optional packaging (not a 6th logic mode); toggled by Helm `gateway.enabled`; per-service Ingress is the supported alternative.

**Step 4: Verify + commit**

```bash
make qa-lint-changed   # doc.go changed
git add CLAUDE.md internal/gateway/doc.go
git commit -m "docs(g12): reframe gateway as optional edge proxy, not a 6th mode (G12)"
```

**Spec note (external):** `ach-spec-final-delivered.md` + README — present gateway as optional edge/packaging, annotate the mode table likewise.

---

## Task 4: G14 — document the transient `default_team_missing` startup window

**Files:**
- Modify: `references/troubleshooting.md` (append a new entry; `default_team_missing` is currently absent)

**Step 1: Add the troubleshooting entry**

Append (after the last entry, ~line 494):

```
### ❌ First SSO login returns `500 default_team_missing`
A login that arrives BEFORE the operator's first successful `EnsureDefaultTeam`
(operator just started / `LiteLLMConnection` not yet `Ready` / LiteLLM briefly
unreachable) can 500 with audit `outcome=default_team_missing`.
✅ It SELF-HEALS — the next LiteLLMConnection reconcile creates the canonical
`default` team (idempotent list-first → `POST /team/new`), and subsequent logins
succeed. No manual action needed; the operator bootstraps the team proactively
(`litellmconnection_controller.go`), so in steady state it is present before any
login. WHY: ACH does NOT lazily create the default team in the SSO path — that is
a fail-loud signal (`auth/doc.go`) — but the operator's proactive bootstrap closes
the gap once the control plane converges. The 500 is transient, not a dead-end.
```

**Step 2: Verify + commit**

```bash
git add references/troubleshooting.md
git commit -m "docs(g14): document transient default_team_missing startup window (G14)"
```

---

## Task 5: G17 + G18-docs — JWT claim notes (`nbf` absent, bare `sub`)

**Files:**
- Modify: `docs/runbooks/writing-an-mcp-backend.md`
- Modify: `docs/developer-guide/jwt-forwarder.md`
- Modify: `test/e2e/mcp-echo/README.md`

**Step 1: `writing-an-mcp-backend.md`**

After the "Validate `iss`, `aud`, `exp`" verification step, add an explicit note:

```
> ACH does NOT emit `nbf` (intentionally — with a 120s `exp` it adds only
> clock-skew risk; `exp` is the sole time bound). Verify `iss`, `aud`, `exp`
> only — do NOT require `nbf`. ACH also emits no `jti`.
```

Add a `sub` compat note near the `sub` row: "`sub` is the bare `<owner-email>` (no namespace prefix). The legacy `<namespace>/<email>` form is gone (pre-release hard-cut); do not split `sub` on `/`. Key on `iss`+`sub`."

**Step 2: `jwt-forwarder.md`**

Below the JWS claims table (lines ~70-82), add a note: `nbf` is intentionally omitted (clock-skew footgun with the 120s TTL; `exp` is the sole time bound); minted set is `iss/sub/aud/iat/exp` (+`email`), no `nbf`, no `jti`. Add the `sub` compat note (legacy `<ns>/<email>` form removed).

**Step 3: `test/e2e/mcp-echo/README.md`**

Add a short "JWT claims" note: backends verify `iss/aud/exp` only; `nbf` is absent; `sub` is the bare owner email; `email` mirrors `sub`.

**Step 4: Verify + commit**

```bash
git add docs/runbooks/writing-an-mcp-backend.md docs/developer-guide/jwt-forwarder.md test/e2e/mcp-echo/README.md
git commit -m "docs(g17/g18): note nbf absent + bare-email sub in JWT backend docs (G17/G18)"
```

**Spec note (external):** `ach-spec-final-delivered.md §9/§9.1` — remove `nbf` from the claims block + the "backends MUST verify" list; state minted set is `iss/sub/aud/iat/exp` (+`email`); confirm bare `sub` + retained `email`.

---

## Task 6: G13-docs — `pk_`-vs-`ek_` guidance

**Files:**
- Modify: `docs/user-guide/index.md` (or add a section to `references/troubleshooting.md` — pick the user-facing one)

**Step 1: Add the dev-vs-agent guidance section**

In `docs/user-guide/index.md`, add a short "Choosing a key: `pk_` vs `ek_`" section:

```
- **`pk_` (Personal Key)** — dev / personal use. Authorizes against the UNION of
  your capabilities; NOT bound to any Environment (no access-group scoping, no
  Environment attribution tag). Convenient for local experimentation.
- **`ek_` (Environment Key)** — agents / CI / workloads. Environment-scoped:
  capability-gated to one Environment and carries its attribution tag. Use this
  for anything reproducible or governed.
```

Add a line: "A recommended Prometheus alert: watch `forwarder_requests_total{key_type=\"pk\"}` on runtime routes to catch `pk_` used where an `ek_` belongs." (No server-side `pk_`-forbid toggle — honoring the frozen permanent decision; enforcement, if wanted, is the deployer's LiteLLM choice.)

**Step 2: Verify + commit**

```bash
git add docs/user-guide/index.md
git commit -m "docs(g13): add pk- vs ek- dev-vs-agent guidance + alert tip (G13)"
```

---

## Task 7: G1 — budget framing (spec-only; in-repo confirmation)

**Files:**
- (in-repo) none beyond confirming — the `pk_` warning reword is in the CODE plan (Task 13)

**Step 1: Confirm no in-repo budget wording remains**

Grep to confirm (these should return only the decisions doc / unrelated fuzz-budget / the correct KEY-10 ROADMAP line):

Run: `git grep -nE "tag budget|Environment groups|Usage Governance|per-Environment budget" -- ':!docs/external-review-resolutions.md'`
Expected: no live stale hit (fuzz "time budget" in `references/makefile.md` and the correct `ROADMAP.md` KEY-10 line are fine).

**Step 2: Record the finding (no commit needed if clean)**

If the grep is clean, G1's in-repo work is already done (only the CODE-plan `pk_` reword remains). No commit. If a stale hit surfaces, reword it to: "Governance = capabilities/identity/forwarding/attribution; budget = LiteLLM user (now) → key (future)," then commit `docs(g1): purge per-Environment budget wording`.

**Spec note (external):** `ach-spec-final-delivered.md §6.3/§8.6` — reframe the env tag as attribution-only; remove "tag budget" language; state budget = user-level (now), key-level (future).

---

## Final gate

```bash
git diff --stat              # confirm only intended files changed
make qa-lint-changed         # only relevant if conditions.go / gateway doc.go touched
```

External-repo spec edits (collected from the per-task "Spec note" lines) are tracked separately in `/home/jcm/Projects/ach-spec/`:
- §2.1/§2.3 (G4 — CODE plan), §5.8 drop + §5.9 deliver (G6 — CODE plan), §5.12+AppendixA (G9 — CODE plan), §6.3/§8.6 (G1), §9/§9.1 (G17/G18), §18.5 metrics (G7 — CODE plan), audit-field table (G20 — CODE plan), GitOps-only framing (G2), gateway-optional framing (G12).
