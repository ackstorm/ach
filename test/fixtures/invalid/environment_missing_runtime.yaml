# FIXTURE: invalid. Expected rejection at admission.
# Rule violated: Environment.spec.runtime REQUIRED (CRD-02 / Plan 02 Task 2)
# Expected error substring: "runtime"
#
# This manifest omits spec.runtime entirely. CRD-02 enforces that both
# runtime and context blocks are present via:
#   has(self.spec.runtime) && has(self.spec.context)
# The kubebuilder marker also stamps runtime as Required at OpenAPI level —
# admission rejects with the CEL message:
#   "Environment.spec must declare both runtime and context blocks (CRD-02)"
apiVersion: ach.ackstorm.ai/v1alpha1
kind: Environment
metadata:
  name: invalid-missing-runtime
  namespace: ach-system
spec:
  authorizedTeams:
    - default
  context:
    prompts: []
    plugins: []
    artifacts: []
