# FIXTURE: invalid. Expected rejection at admission.
# Rule violated: memory.achMemory.auth type=bearer needs the user key.
# Expected error substring: "secretRef is required when type=bearer"
#
# The bearer arm talks to ach-memory DIRECTLY with a minted user key, so the
# operator must be told which Secret holds it. Without a secretRef there is no
# env var to inject and the harness would send no Authorization header at all —
# a silent downgrade to the no-auth path, not an error.
apiVersion: ach.ackstorm.ai/v1alpha1
kind: ACHAgent
metadata:
  name: invalid-memory-bearer-no-secret
  namespace: ach-system
spec:
  profileRef:
    name: standard
  identity:
    secretRef:
      name: ops-ek
      key: ek
  capability:
    environment: engineering-prod
  channels:
    - name: tick
      type: cron
      cron:
        schedule: "* * * * *"
  memory:
    type: ach-memory
    achMemory:
      endpoint: http://ach-memory.ach.svc:8000/mcp/
      auth:
        type: bearer
        # secretRef deliberately omitted → CEL rejection
