apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Early declarative base: the e2e namespace layout. Applied by
# `scripts/cluster.sh` (create_namespaces) via `kubectl apply -k` BEFORE the
# helm installs, so every chart's target namespace exists.
resources:
  - namespaces.yaml
