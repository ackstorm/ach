# ToolHive operator (vendored MCP servers).
#
# Used by:  scripts/cluster.sh hydrate_toolhive
# Charts:   oci://ghcr.io/stacklok/toolhive/toolhive-operator-crds
#           oci://ghcr.io/stacklok/toolhive/toolhive-operator
#
# CRDs and operator live on independent version streams (0.0.x vs 0.5.x) —
# they are NOT co-versioned. Bump both deliberately and re-run e2e.

crdsChartVersion: 0.28.3
operatorChartVersion: 0.28.3
