---
phase: 02.2-phase-02-cleanup-gap-g1-fix-litellm-real-uat-path-invariant-
plan: 02
type: execute
wave: 2
depends_on: ["02.2-01-PLAN"]
files_modified:
  - docker-compose.yml
  - docker-compose.spike.yaml
  - scripts/litellm-config.yaml
  - scripts/spike-litellm-config.yaml
  - scripts/uat-sc5.sh
  - scripts/uat-g1.sh
autonomous: true
requirements: [OP-13]
must_haves:
  truths:
    - "Root docker-compose.yml ships litellm-db + litellm services behind profiles: [litellm], off by default — `make dev-up` (no profile) brings up only postgres + redis (D-04)"
    - "docker-compose.spike.yaml is deleted from the repo; one canonical compose file going forward (D-05)"
    - "LiteLLM image pin is ghcr.io/berriai/litellm-database:v1.83.10-stable, verbatim from the prior spike file (D-06, [[feedback_litellm_version_control]])"
    - "Separate litellm-db service (postgres:16-alpine) exists with no published host port; only the litellm container reaches it on the compose-internal network (D-07)"
    - "Config file renames from scripts/spike-litellm-config.yaml to scripts/litellm-config.yaml; env var renames from LITELLM_SPIKE_PORT to LITELLM_PORT; default 14000 preserved (D-08)"
    - "scripts/uat-sc5.sh is updated in lockstep: $COMPOSE drops -f docker-compose.spike.yaml and adds --profile litellm; every LITELLM_SPIKE_PORT reference renames to LITELLM_PORT (D-05, D-08)"
    - "scripts/uat-g1.sh is a new single-mode runner mirroring uat-sc5.sh --full structure (set -euo pipefail, RANDOM_PEPPER, trap cleanup, dev_run helper, $COMPOSE up -d with --profile litellm) (D-09)"
    - "uat-g1.sh exercises the backdate+grep scenario end-to-end against real LiteLLM v1.83.10 and asserts JSON-form grep `\"audit\":true.*\"outcome\":\"revoked\".*\"user_id\":\"u-uat-g1\"` against the operator stdout audit emitter (D-10, BLOCKER 1 fix — emitter is slog.NewJSONHandler per internal/audit/handler.go:45)"
    - "uat-g1.sh hard-requires `jq` (early `command -v jq` check; exits 2 with diagnostic if absent) for capturing the LiteLLM /key/generate response token"
    - "Strict exactly-one-event audit-stream parsing and post-revocation /key/info 404 verify are NOT shipped (D-11)"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Two new services litellm-db + litellm gated by profiles: [litellm]"
      contains: "profiles: [\"litellm\"]"
    - path: "scripts/litellm-config.yaml"
      provides: "Renamed LiteLLM config (formerly spike-litellm-config.yaml)"
      contains: "master_key: sk-spike-mk"
    - path: "scripts/uat-g1.sh"
      provides: "Gap G1 end-to-end UAT runner against live LiteLLM"
      contains: "\"audit\":true.*\"outcome\":\"revoked\".*\"user_id\":\"u-uat-g1\""
    - path: "scripts/uat-sc5.sh"
      provides: "Updated UAT runner using the unified compose stack with --profile litellm"
      contains: "COMPOSE=\"docker-compose --profile litellm\""
  key_links:
    - from: "scripts/uat-g1.sh"
      to: "docker-compose.yml litellm service"
      via: "$COMPOSE up -d brings up postgres + redis + litellm-db + litellm under --profile litellm"
      pattern: "COMPOSE=\"docker-compose --profile litellm\""
    - from: "scripts/uat-g1.sh"
      to: "internal/orphan/runnable.go OutcomeRevoked + internal/audit/handler.go JSONHandler"
      via: "JSON-form grep of operator audit log: \"audit\":true + \"outcome\":\"revoked\" + \"user_id\":\"u-uat-g1\""
      pattern: "\"outcome\":\"revoked\".*\"user_id\":\"u-uat-g1\""
    - from: "docker-compose.yml litellm service"
      to: "scripts/litellm-config.yaml"
      via: "volume mount ./scripts/litellm-config.yaml:/app/config.yaml:ro"
      pattern: "./scripts/litellm-config.yaml:/app/config.yaml"
---

<objective>
Establish a real-LiteLLM end-to-end UAT path that exercises the Gap G1 fix (Plan 02.2-01) against LiteLLM v1.83.10. Merge the two spike-only services (`litellm`, `litellm-db`) into root `docker-compose.yml` behind `profiles: [litellm]` so day-to-day `make dev-up` stays lean; retire `docker-compose.spike.yaml`; rename `scripts/spike-litellm-config.yaml` → `scripts/litellm-config.yaml` and env var `LITELLM_SPIKE_PORT` → `LITELLM_PORT`; update `scripts/uat-sc5.sh` in lockstep so the repo never has a broken-UAT window; ship a new standalone `scripts/uat-g1.sh` that seeds an ACH personal_keys row, creates an orphan LiteLLM key, backdates it to bypass the 10-minute OrphanAgeFloor, runs one operator tick, and JSON-form-greps the operator stdout for the revocation audit event.

Purpose: With Plan 02.2-01's G1 fix in place, the orphan loop's wire-format call now succeeds against real LiteLLM 1.83.10. This plan is the live verification path for OP-13 (orphan-cleanup audit event per revocation) — the assertion was deferred-by-design in Phase 02 (`02-HUMAN-UAT.md` SC#5.4) because Phase 02 lacked a real LiteLLM harness. Plan 02.2-02 provides that harness.

Output: One edit to root `docker-compose.yml` (add 2 services + profiles gate), one deletion (`docker-compose.spike.yaml`), one rename (`scripts/spike-litellm-config.yaml` → `scripts/litellm-config.yaml`), one edit to `scripts/uat-sc5.sh` (compose+port var rename), one new file `scripts/uat-g1.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/02.2-phase-02-cleanup-gap-g1-fix-litellm-real-uat-path-invariant-/02.2-CONTEXT.md
@.planning/phases/02.2-phase-02-cleanup-gap-g1-fix-litellm-real-uat-path-invariant-/02.2-PATTERNS.md

@docker-compose.yml
@docker-compose.spike.yaml
@scripts/uat-sc5.sh
@scripts/spike-litellm-config.yaml
@internal/audit/handler.go

<interfaces>
<!-- Existing surfaces this plan consumes. The Plan 02.2-01 contract is the dependency. -->

From Plan 02.2-01 (must be merged first — wave 1):
- `db/migrations/000003_litellm_token.up.sql` adds nullable `litellm_token text` to `personal_keys` and `environment_keys`.
- `internal/orphan/runnable.go` defines `OutcomeRevoked = "revoked"` constant (and `OutcomeSuccess` is REMOVED); success-path audit event emits `outcome=revoked`.
- `internal/litellm/types.go` `UserKeyInfo.Token` (json:"token") is what the orphan loop set-differences against.

From internal/audit/handler.go (line 45 — the canonical Phase 2 audit emitter):
```go
h := slog.NewJSONHandler(w, &slog.HandlerOptions{Level: slog.LevelInfo})
return slog.New(h).With(slog.Bool("audit", true))
```
Audit lines on stdout are JSON. Expected shape (verified empirically against internal/orphan/runnable.go:228-232 emit-site declaration order):
```
{"time":"…","level":"INFO","msg":"operator.orphan-cleanup","audit":true,"target.kind":"litellm_key","target.name":"<token>","outcome":"revoked","user_id":"u-uat-g1"}
```
The With-attribute `audit:true` is emitted first by slog.JSONHandler; the call-site attributes follow in declaration order, so `outcome` precedes `user_id`. The grep regex MUST therefore be the JSON form (BLOCKER 1 fix — the prior `key=value` regex would never match).

From docker-compose.yml current shape (lines 21-48):
```
services:
  postgres:        # ach-dev-postgres, postgres:16-alpine, port 5432
  redis:           # ach-dev-redis, redis:7-alpine, port 6379
volumes:
  postgres_data:
```

From docker-compose.spike.yaml (lines 24-62 — verbatim source for the merge):
```
services:
  litellm-db:    # ach-spike-litellm-db (RENAME → ach-dev-litellm-db), postgres:16-alpine, no published port
  litellm:       # ach-spike-litellm (RENAME → ach-dev-litellm), ghcr.io/berriai/litellm-database:v1.83.10-stable
                 # volume: ./scripts/spike-litellm-config.yaml:/app/config.yaml:ro (RENAME path)
                 # port: ${LITELLM_SPIKE_PORT:-14000}:4000 (RENAME env var → LITELLM_PORT)
```

From scripts/uat-sc5.sh (line 62-65, 128-129, 141, 176):
- Line 62: `LITELLM_SPIKE_PORT="${LITELLM_SPIKE_PORT:-14000}"`
- Line 65: `COMPOSE="docker-compose -f docker-compose.yml -f docker-compose.spike.yaml"`
- Line 128: `echo "uat-sc5 [$MODE]: bringing up compose stack (LITELLM_SPIKE_PORT=${LITELLM_SPIKE_PORT})..."`
- Line 141: `URL="http://localhost:${LITELLM_SPIKE_PORT}/health/readiness"`
- Line 176: `ACH_LITELLM_BASE_URL=http://localhost:${LITELLM_SPIKE_PORT}` (and similar at line 244)

UAT runner skeleton primitives to copy verbatim from uat-sc5.sh (lines 35-123) into uat-g1.sh:
- `set -euo pipefail`
- `REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"; cd "$REPO_ROOT"`
- `RANDOM_PEPPER="uat-g1-$(head -c8 /dev/urandom | base64 | tr -d '/=+' | head -c12)"`
- `LOG_DIR="$(mktemp -d "${TMPDIR:-/tmp}/ach-uat-g1-XXXXXX")"`
- `cleanup()` + `trap cleanup EXIT INT TERM`
- `dev_run()` helper (lines 105-123)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Merge LiteLLM services into root docker-compose.yml; retire docker-compose.spike.yaml; rename config file</name>
  <files>docker-compose.yml, docker-compose.spike.yaml, scripts/litellm-config.yaml, scripts/spike-litellm-config.yaml</files>
  <read_first>
    docker-compose.yml,
    docker-compose.spike.yaml,
    scripts/spike-litellm-config.yaml,
    .planning/phases/02.2-phase-02-cleanup-gap-g1-fix-litellm-real-uat-path-invariant-/02.2-CONTEXT.md,
    .planning/phases/02.2-phase-02-cleanup-gap-g1-fix-litellm-real-uat-path-invariant-/02.2-PATTERNS.md
  </read_first>
  <action>Three coordinated filesystem operations. (A) Append two new service blocks to `docker-compose.yml` under the existing `services:` map (after the `redis:` block, before the `volumes:` block). The two blocks lift the existing `docker-compose.spike.yaml` services verbatim with five edits per D-04, D-05, D-06, D-08: (1) add `profiles: ["litellm"]` to BOTH the `litellm-db` block and the `litellm` block as the first key under the service name (per D-04 — off by default, opt-in via `--profile litellm`); (2) rename `container_name: ach-spike-litellm-db` → `container_name: ach-dev-litellm-db` and `container_name: ach-spike-litellm` → `container_name: ach-dev-litellm` to match the `ach-dev-postgres` / `ach-dev-redis` convention already in the root file; (3) rename the `litellm` service's volume mount from `./scripts/spike-litellm-config.yaml:/app/config.yaml:ro` → `./scripts/litellm-config.yaml:/app/config.yaml:ro` (per D-08); (4) rename the port env-var from `${LITELLM_SPIKE_PORT:-14000}:4000` → `${LITELLM_PORT:-14000}:4000` (default 14000 preserved); (5) drop the top-of-file `# Phase 02 UAT spike — ...` comment block from the spike file (do not copy it into root compose). Keep the LiteLLM image pin `ghcr.io/berriai/litellm-database:v1.83.10-stable` VERBATIM per D-06. Keep both healthchecks verbatim (litellm-db: pg_isready every 2s; litellm: wget /health/readiness with `Authorization: Bearer sk-spike-mk` every 5s). Keep the `command: ["--config", "/app/config.yaml", "--port", "4000"]` line verbatim. Keep the `LITELLM_MASTER_KEY: sk-spike-mk` env var verbatim (matches the healthcheck Authorization header literal). (B) Delete the file `docker-compose.spike.yaml` from the repo. (C) `git mv scripts/spike-litellm-config.yaml scripts/litellm-config.yaml`; in the new file, edit the leading comment block (lines 1-10): replace the first line `# Minimal LiteLLM config for Phase 02 UAT spike.` with `# Minimal LiteLLM config for ACH local UAT runs (scripts/uat-sc5.sh + scripts/uat-g1.sh).` and drop the `Phase 02 SC#5` framing — leave the `general_settings` / `litellm_settings` blocks (master_key, store_model_in_db, drop_params) byte-identical. Verify via `docker-compose --profile litellm config -q` (or `docker compose --profile litellm config -q`) exits 0 with the new compose file (does NOT fetch images — pure YAML validation). WARNING 5 fix: the verify step suppresses BOTH stdout and stderr (`>/dev/null 2>&1`) — rely on exit status only; do not let YAML body leak into UAT stdout.</action>
  <verify>
    <automated>test ! -e docker-compose.spike.yaml && test -f scripts/litellm-config.yaml && test ! -e scripts/spike-litellm-config.yaml && grep -q 'profiles: \["litellm"\]' docker-compose.yml && grep -q 'container_name: ach-dev-litellm-db' docker-compose.yml && grep -q 'container_name: ach-dev-litellm$' docker-compose.yml && grep -q 'image: ghcr.io/berriai/litellm-database:v1.83.10-stable' docker-compose.yml && grep -q './scripts/litellm-config.yaml:/app/config.yaml:ro' docker-compose.yml && grep -q '\${LITELLM_PORT:-14000}:4000' docker-compose.yml && ! grep -q 'LITELLM_SPIKE_PORT' docker-compose.yml && (docker-compose --profile litellm config -q >/dev/null 2>&1 || docker compose --profile litellm config -q >/dev/null 2>&1)</automated>
  </verify>
  <acceptance_criteria>
    - File docker-compose.spike.yaml does NOT exist
    - File scripts/spike-litellm-config.yaml does NOT exist
    - File scripts/litellm-config.yaml exists with byte-identical `general_settings` / `litellm_settings` content (only leading comment changed)
    - docker-compose.yml contains exactly TWO `profiles: ["litellm"]` occurrences (one per new service)
    - docker-compose.yml contains `container_name: ach-dev-litellm-db` (exact)
    - docker-compose.yml contains `container_name: ach-dev-litellm` (exact, not -db suffix)
    - docker-compose.yml contains `image: ghcr.io/berriai/litellm-database:v1.83.10-stable` (verbatim)
    - docker-compose.yml contains `./scripts/litellm-config.yaml:/app/config.yaml:ro` (NOT `spike-litellm-config.yaml`)
    - docker-compose.yml contains `${LITELLM_PORT:-14000}:4000` (NOT `LITELLM_SPIKE_PORT`)
    - docker-compose.yml retains existing `postgres` + `redis` service blocks (verified by `grep -c "container_name: ach-dev-postgres" docker-compose.yml` returns 1 and `grep -c "container_name: ach-dev-redis" docker-compose.yml` returns 1)
    - `docker-compose --profile litellm config -q >/dev/null 2>&1` OR `docker compose --profile litellm config -q >/dev/null 2>&1` exits 0 (WARNING 5: silent verify — no YAML body leak)
    - `docker-compose config -q >/dev/null 2>&1` OR `docker compose config -q >/dev/null 2>&1` (no profile) exits 0
  </acceptance_criteria>
  <done>Root docker-compose.yml has the two new services under `profiles: [litellm]`; the old spike file is gone; the config file is renamed; YAML validates cleanly with and without the litellm profile, with stdout suppressed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update scripts/uat-sc5.sh — drop -f docker-compose.spike.yaml + add --profile litellm + LITELLM_PORT rename</name>
  <files>scripts/uat-sc5.sh</files>
  <read_first>
    scripts/uat-sc5.sh,
    .planning/phases/02.2-phase-02-cleanup-gap-g1-fix-litellm-real-uat-path-invariant-/02.2-CONTEXT.md,
    .planning/phases/02.2-phase-02-cleanup-gap-g1-fix-litellm-real-uat-path-invariant-/02.2-PATTERNS.md
  </read_first>
  <action>Three surgical edits in `scripts/uat-sc5.sh`. (1) Line 62: `LITELLM_SPIKE_PORT="${LITELLM_SPIKE_PORT:-14000}"` → `LITELLM_PORT="${LITELLM_PORT:-14000}"`. (2) Line 65: `COMPOSE="docker-compose -f docker-compose.yml -f docker-compose.spike.yaml"` → `COMPOSE="docker-compose --profile litellm"`. (3) Global sweep — replace every remaining `LITELLM_SPIKE_PORT` reference in the file with `LITELLM_PORT` (lines 128, 141, 176, 244 per pattern-mapper grep). Use `grep -n "LITELLM_SPIKE_PORT" scripts/uat-sc5.sh` post-edit to confirm zero matches remain. Preserve `set -euo pipefail` (line 35), `cleanup()` + `trap cleanup EXIT INT TERM` (lines 84-98), `dev_run` helper (lines 105-123), the `RANDOM_PEPPER` construction (line 75), and every `$COMPOSE down -v --remove-orphans` invocation (line 95). Do NOT switch from `docker-compose` (hyphen) to `docker compose` (v2 subcommand) — Phase 01-10 SUMMARY documents that the hyphen form is the host-portable choice; the Plan 01-10 `DOCKER_COMPOSE` knob convention is honored implicitly by uat-sc5.sh referencing `docker-compose` directly. WARNING 4 fix: the verify step does NOT assume `--help` exists on uat-sc5.sh; use a pure syntax-and-shebang check.</action>
  <verify>
    <automated>! grep -q 'LITELLM_SPIKE_PORT' scripts/uat-sc5.sh && ! grep -q '\-f docker-compose.spike.yaml' scripts/uat-sc5.sh && grep -q 'COMPOSE="docker-compose --profile litellm"' scripts/uat-sc5.sh && grep -q 'LITELLM_PORT="${LITELLM_PORT:-14000}"' scripts/uat-sc5.sh && bash -n scripts/uat-sc5.sh && head -2 scripts/uat-sc5.sh | grep -q '^#!'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "LITELLM_SPIKE_PORT" scripts/uat-sc5.sh` returns 0
    - `grep -c "docker-compose.spike.yaml" scripts/uat-sc5.sh` returns 0
    - scripts/uat-sc5.sh contains the exact line `COMPOSE="docker-compose --profile litellm"`
    - scripts/uat-sc5.sh contains the exact line `LITELLM_PORT="${LITELLM_PORT:-14000}"`
    - `bash -n scripts/uat-sc5.sh` exits 0 (syntax check)
    - First two lines of scripts/uat-sc5.sh include a shebang `#!` (WARNING 4: no `--help` flag assumption)
    - scripts/uat-sc5.sh retains `set -euo pipefail` (grep returns >=1)
    - scripts/uat-sc5.sh retains `trap cleanup EXIT INT TERM` (grep returns 1)
    - scripts/uat-sc5.sh retains the `dev_run()` helper definition (grep returns 1)
  </acceptance_criteria>
  <done>scripts/uat-sc5.sh runs against the merged compose file via `--profile litellm`; no stale `LITELLM_SPIKE_PORT` or `docker-compose.spike.yaml` references remain; bash syntax is clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author scripts/uat-g1.sh — Gap G1 end-to-end revocation UAT against live LiteLLM</name>
  <files>scripts/uat-g1.sh</files>
  <read_first>
    scripts/uat-sc5.sh,
    docker-compose.yml,
    internal/orphan/runnable.go,
    internal/audit/handler.go,
    .planning/phases/02.2-phase-02-cleanup-gap-g1-fix-litellm-real-uat-path-invariant-/02.2-CONTEXT.md,
    .planning/phases/02.2-phase-02-cleanup-gap-g1-fix-litellm-real-uat-path-invariant-/02.2-PATTERNS.md
  </read_first>
  <action>Create new executable file `scripts/uat-g1.sh` (mode 0755) mirroring `scripts/uat-sc5.sh --full` structure as a single-mode runner (no `--smoke` / `--full` dispatch). Leading shebang `#!/usr/bin/env bash` + leading 30-line comment block describing what G1 is (`ListUserKeys` endpoint mismatch — Phase 02 HUMAN-UAT.md §Gaps G1), what this script does (full backdate+grep cycle against real LiteLLM v1.83.10), expected wall time (~8-12 min), re-runnable note (trap drops every compose service and volume). Then `set -euo pipefail`. Then the standard header (lines 37-79 of uat-sc5.sh adapted): `REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"; cd "$REPO_ROOT"`, support `--help|-h` (prints the top comment block via `sed -n '3,33p' "$0"; exit 0`); reject unknown flags with `exit 2`.

INFO 9 fix — `jq` hard requirement: immediately after the `set -euo pipefail` line and before any other initialization, add:
```bash
command -v jq >/dev/null 2>&1 || { echo "uat-g1: jq is required but not found in PATH (install with: apt-get install jq / brew install jq)"; exit 2; }
```

Config block: `LITELLM_PORT="${LITELLM_PORT:-14000}"`, `LITELLM_MASTER_KEY="${LITELLM_MASTER_KEY:-sk-spike-mk}"`, `HEALTH_TIMEOUT_S="${UAT_HEALTH_TIMEOUT_S:-180}"`, `COMPOSE="docker-compose --profile litellm"`, `ACH_DB_URL="postgres://ach:ach@localhost:5432/ach?sslmode=disable"`, `RANDOM_PEPPER="uat-g1-$(head -c8 /dev/urandom | base64 | tr -d '/=+' | head -c12)"`, `LOG_DIR="$(mktemp -d "${TMPDIR:-/tmp}/ach-uat-g1-XXXXXX")"`, `OPERATOR_LOG="$LOG_DIR/operator.log"`, `OPERATOR_PID_FILE="$LOG_DIR/operator.pid"`. `cleanup()` function copied from uat-sc5.sh lines 84-97 verbatim (kills `$OPERATOR_PID_FILE` PID with `kill -TERM`, waits for exit, prints `uat-g1: tearing down compose stack (exit=${rc}, logs at ${LOG_DIR})...` to stderr, runs `$COMPOSE down -v --remove-orphans`) followed by `trap cleanup EXIT INT TERM`. `dev_run()` helper copied verbatim from uat-sc5.sh lines 105-123.

Scenario steps per D-10 (mirror uat-sc5.sh `--full` mode shape):
- (Step 1) `$COMPOSE up -d` + wait for ACH Postgres `pg_isready` (DEADLINE=60s) and LiteLLM `/health/readiness` (DEADLINE=$HEALTH_TIMEOUT_S) using the same curl-and-grep pattern as uat-sc5.sh lines 132-156.
- (Step 2) Apply ACH migrations: `dev_run "ACH_DB_URL=${ACH_DB_URL}" "ACH_MIGRATIONS_PATH=/workspace/db/migrations" go run ./cmd/migrate 2>&1 | tail -10`.
- (Step 3) Seed ACH personal_keys row via `$COMPOSE exec -T postgres psql -U ach -d ach` HEREDOC: `INSERT INTO personal_keys (key_id, credential_hash, owner_email, status, litellm_user_id, litellm_token, expires_at) VALUES ('pkid_uat_g1', 'uat-g1-fake-hash-' || md5(random()::text), 'uat-g1@example.com', 'active', 'u-uat-g1', 'placeholder-token-pending-litellm-generate', now() + interval '7 days') ON CONFLICT (key_id) DO NOTHING;`.
- (Step 4) Create LiteLLM orphan key under `u-uat-g1` via curl `POST /key/generate` with body `{"user_id":"u-uat-g1","duration":"24h","metadata":{"created_by":"uat-g1"}}`; capture the response field `key` into shell variable `TOKEN` using `jq -r .key` (jq is hard-required per INFO 9 — the `command -v jq` gate at the top of the script guarantees presence). Echo the captured TOKEN to stdout for diagnostic visibility.
- (Step 5) `UPDATE personal_keys SET litellm_token='$TOKEN' WHERE key_id='pkid_uat_g1';` via `$COMPOSE exec -T postgres psql -U ach -d ach -c …`.
- (Step 6) Backdate the LiteLLM key: `$COMPOSE exec -T litellm-db psql -U litellm -d litellm -c "UPDATE \"LiteLLM_VerificationToken\" SET created_at = now() - interval '11 minutes' WHERE token = '$TOKEN';"`.
- (Step 7) Launch operator in background: `dev_run "ACH_DB_URL=${ACH_DB_URL}" "ACH_LITELLM_BASE_URL=http://localhost:${LITELLM_PORT}" "ACH_LITELLM_MASTER_KEY=${LITELLM_MASTER_KEY}" "ACH_ORPHAN_CLEANUP_INTERVAL=5m" "ACH_CREDENTIAL_HASH_PEPPER=${RANDOM_PEPPER}" "ACH_PLUGIN_MAX_SIZE_MIB=50" "ACH_CACHE_ROOT=/tmp/ach-uat-cache" go run ./cmd/operator > "$OPERATOR_LOG" 2>&1 &`; immediately `echo $! > "$OPERATOR_PID_FILE"` so cleanup() can kill the process.
- (Step 8) `echo "uat-g1: waiting up to 6 minutes for one operator tick (5min interval + 1min slack)..."; sleep 360`.
- (Step 9) BLOCKER 1 fix — JSON-form grep. The operator audit emitter is `slog.NewJSONHandler` (internal/audit/handler.go:45) so audit lines are JSON, not key=value. Use this exact bash block (single-quoted regex; `grep -E` does the alternation; the three field tokens are independent because JSON object key order is not part of the slog contract — use independent matches via `awk` for robustness):
```bash
if grep -E '"audit":true.*"outcome":"revoked".*"user_id":"u-uat-g1"' "$OPERATOR_LOG" >/dev/null; then
  echo "uat-g1: PASS — revocation audit event observed (ordered match)"
  exit 0
elif awk '/"audit":true/ && /"outcome":"revoked"/ && /"user_id":"u-uat-g1"/' "$OPERATOR_LOG" | grep -q .; then
  echo "uat-g1: PASS — revocation audit event observed (order-independent awk match)"
  exit 0
else
  echo "uat-g1: FAIL — no audit event with audit:true + outcome:revoked + user_id:u-uat-g1 observed"
  echo "uat-g1: last 30 lines of operator log:"
  tail -30 "$OPERATOR_LOG"
  exit 1
fi
```
Rationale: slog JSONHandler emits With-attributes (`audit:true`) first, then call-site attributes in declaration order (`target.kind`, `target.name`, `outcome`, `user_id` per runnable.go:228-232). The ordered `grep -E` is the primary check; the `awk` fallback is the robustness check in case slog ever reorders or Go runtime changes ordering semantics. Either match passes.

D-11: do NOT attempt strict exactly-one-event parsing; do NOT issue `GET /key/info?key=<token>` post-revocation for LiteLLM-side 404 verification. `chmod 0755 scripts/uat-g1.sh`. Verify `bash -n scripts/uat-g1.sh` and `scripts/uat-g1.sh -h` both exit 0.</action>
  <verify>
    <automated>test -f scripts/uat-g1.sh && test -x scripts/uat-g1.sh && bash -n scripts/uat-g1.sh && scripts/uat-g1.sh -h >/dev/null && grep -q 'set -euo pipefail' scripts/uat-g1.sh && grep -q 'command -v jq' scripts/uat-g1.sh && grep -q 'RANDOM_PEPPER="uat-g1-' scripts/uat-g1.sh && grep -q 'LITELLM_PORT="${LITELLM_PORT:-14000}"' scripts/uat-g1.sh && grep -q 'COMPOSE="docker-compose --profile litellm"' scripts/uat-g1.sh && grep -q 'trap cleanup EXIT INT TERM' scripts/uat-g1.sh && grep -q '"audit":true.*"outcome":"revoked".*"user_id":"u-uat-g1"' scripts/uat-g1.sh && grep -q 'awk .*audit.:true.*outcome.:.revoked.*user_id.:.u-uat-g1' scripts/uat-g1.sh && grep -q "pkid_uat_g1" scripts/uat-g1.sh && grep -q "u-uat-g1" scripts/uat-g1.sh && grep -q "LiteLLM_VerificationToken" scripts/uat-g1.sh && grep -q "interval '11 minutes'" scripts/uat-g1.sh && grep -q "ACH_ORPHAN_CLEANUP_INTERVAL=5m" scripts/uat-g1.sh</automated>
  </verify>
  <acceptance_criteria>
    - File scripts/uat-g1.sh exists with mode 0755 (executable)
    - `bash -n scripts/uat-g1.sh` exits 0
    - `scripts/uat-g1.sh -h` exits 0 and prints usage
    - scripts/uat-g1.sh starts with `#!/usr/bin/env bash`
    - scripts/uat-g1.sh contains `set -euo pipefail`
    - scripts/uat-g1.sh contains `command -v jq >/dev/null` early-exit check (INFO 9 fix — jq is hard-required, no python3 fallback)
    - scripts/uat-g1.sh defines `RANDOM_PEPPER="uat-g1-$(...)"`
    - scripts/uat-g1.sh defines `LITELLM_PORT="${LITELLM_PORT:-14000}"`
    - scripts/uat-g1.sh defines `COMPOSE="docker-compose --profile litellm"`
    - scripts/uat-g1.sh defines `cleanup()` function and registers `trap cleanup EXIT INT TERM`
    - scripts/uat-g1.sh defines `dev_run()` helper
    - scripts/uat-g1.sh seeds `pkid_uat_g1` row with `litellm_user_id='u-uat-g1'` and `litellm_token='<placeholder>'`
    - scripts/uat-g1.sh executes `POST /key/generate` against `http://localhost:${LITELLM_PORT}/key/generate`
    - scripts/uat-g1.sh issues `UPDATE "LiteLLM_VerificationToken" SET created_at = now() - interval '11 minutes' WHERE token = '$TOKEN';`
    - scripts/uat-g1.sh launches operator with `ACH_ORPHAN_CLEANUP_INTERVAL=5m`
    - scripts/uat-g1.sh's terminal assertion uses the JSON-form regex `"audit":true.*"outcome":"revoked".*"user_id":"u-uat-g1"` (BLOCKER 1 fix)
    - scripts/uat-g1.sh contains the order-independent `awk` fallback `/"audit":true/ && /"outcome":"revoked"/ && /"user_id":"u-uat-g1"/`
    - scripts/uat-g1.sh does NOT contain the legacy key=value regex `outcome=revoked` (BLOCKER 1: would never match against JSON emitter)
    - scripts/uat-g1.sh does NOT contain strict exactly-one-event parsing (no `grep -c`, no `[ $count -eq 1 ]` against the audit log)
    - scripts/uat-g1.sh does NOT issue `GET /key/info?key=` for post-revocation verification (D-11)
  </acceptance_criteria>
  <done>scripts/uat-g1.sh exists as a single-mode UAT runner mirroring uat-sc5.sh structure, exercises the full Gap G1 cycle (jq-presence-check → seed → /key/generate → UPDATE litellm_token → backdate → operator tick → JSON-form grep with awk fallback), and respects the D-11 deferrals (tolerant grep only).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer host → docker-compose stack | local untrusted-output surface; LiteLLM master key is the literal `sk-spike-mk` (already in repo) |
| developer host → LiteLLM host port :14000 | local-only; not published to LAN by default but bound to 0.0.0.0:14000 on the host |
| operator process → ACH Postgres + LiteLLM | trusted in-process binding via env vars |
| operator audit log → file in $LOG_DIR (mktemp) | trusted developer-only sink |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.2-02-01 | Information Disclosure | LiteLLM master key in docker-compose.yml env block | accept | Master key value `sk-spike-mk` is already in `docker-compose.spike.yaml` and `scripts/spike-litellm-config.yaml`; this plan moves the same value into root compose. The key is a UAT-only credential and is documented as such. No new exposure surface — same secret in the same git repo. Production never uses this compose file. |
| T-02.2-02-02 | Tampering | Docker image pin `ghcr.io/berriai/litellm-database:v1.83.10-stable` | mitigate | Image pin verbatim from sister project `../ach_litellm/docker-compose.spike.yaml` per [[feedback_litellm_version_control]] — no version-probe, no `:latest`. SHA digest pinning is a v2 enhancement not in scope. |
| T-02.2-02-03 | Denial of Service | Published host port `:14000` exposed to LAN | accept | Compose default binding `0.0.0.0:14000` reachable from any host on the local network. Local-dev UAT only; developer is responsible for not running uat-g1.sh on a multi-tenant host. Aligns with existing uat-sc5.sh disposition. |
| T-02.2-02-04 | Tampering | Operator command-line env var leak in log file ($LOG_DIR/operator.log) | mitigate | `ACH_CREDENTIAL_HASH_PEPPER` and `ACH_LITELLM_MASTER_KEY` are passed via `-e` to docker; they appear in `dev_run` echo if the shell echoes commands. uat-g1.sh DOES NOT `set -x`; the echo lines in the script narrate user-visible state only (no env-dump). The pepper is a random per-run value (`RANDOM_PEPPER`), so even if the log file leaks, the value is single-use. |
| T-02.2-02-05 | Spoofing | `--profile litellm` toggle confusion | mitigate | Both new service blocks carry `profiles: ["litellm"]`. Running `docker-compose config` (no profile) prints ONLY postgres + redis; running `docker-compose --profile litellm config` adds the two new services. Acceptance criterion `docker-compose config -q >/dev/null 2>&1` (no profile) gates against accidental always-on exposure with silent stdout (WARNING 5). |
| T-02.2-02-06 | Information Disclosure | `$OPERATOR_LOG` file under `/tmp` not cleaned up | accept | The `cleanup()` trap prints the path to stderr for diagnostic re-inspection; `mktemp -d` puts the file under a per-run dir. Developer is responsible for sweeping `/tmp/ach-uat-g1-*` periodically. Matches existing uat-sc5.sh posture. |
| T-02.2-02-SC | Tampering | npm/pip/cargo installs | mitigate | n/a — this plan adds zero new package-manager installs. The new LiteLLM image is pulled from ghcr.io (D-06 pin); the operator binary builds from the existing go.mod via `go run ./cmd/operator`. `jq` is a host prereq (hard-checked at script start per INFO 9); no new package installs in this plan. |
</threat_model>

<verification>
After all three tasks land (and Plan 02.2-01 has merged):
- `docker-compose config -q >/dev/null 2>&1` (no profile) exits 0; output lists ONLY `postgres` and `redis` services
- `docker-compose --profile litellm config -q >/dev/null 2>&1` exits 0; output adds `litellm-db` and `litellm` services
- `bash -n scripts/uat-sc5.sh` exits 0 and first two lines start with `#!`
- `bash -n scripts/uat-g1.sh` exits 0 and `scripts/uat-g1.sh -h` exits 0
- `grep -rn "spike-" docker-compose.yml scripts/uat-sc5.sh scripts/uat-g1.sh scripts/litellm-config.yaml` returns no matches (deliberate residual `sk-spike-mk` master-key value is OK; check excludes the value)
- `make dev-up` still brings up only postgres + redis (no surprise LiteLLM)
- Optional live smoke (developer's choice — not required for plan completion): `scripts/uat-g1.sh` executes end-to-end and prints `uat-g1: PASS — revocation audit event observed` on stdout.
</verification>

<success_criteria>
- Root `docker-compose.yml` ships the unified compose stack: postgres + redis always-on; litellm-db + litellm under `profiles: [litellm]` (off by default).
- `docker-compose.spike.yaml` is gone from the repo.
- `scripts/spike-litellm-config.yaml` is renamed to `scripts/litellm-config.yaml`; `LITELLM_SPIKE_PORT` env var is renamed to `LITELLM_PORT` everywhere (default 14000 preserved).
- `scripts/uat-sc5.sh` is updated in lockstep so it never has a broken-UAT window.
- `scripts/uat-g1.sh` exists and exercises the full Gap G1 revocation cycle against real LiteLLM v1.83.10, asserting `"outcome":"revoked"` + `"user_id":"u-uat-g1"` (JSON-form) in the operator audit stream via grep + awk fallback (BLOCKER 1 fix — emitter is JSONHandler).
- `jq` is a hard prerequisite for `uat-g1.sh` (INFO 9 fix — early `command -v jq` exit-2 check; no python3 fallback).
- D-11 honored: no strict exactly-one-event audit-stream parsing; no post-revocation `/key/info?key=<token>` 404 verify.
- LiteLLM image pin `ghcr.io/berriai/litellm-database:v1.83.10-stable` preserved verbatim (D-06).
- Separate `litellm-db` service exists per D-07 (avoiding the Prisma vs golang-migrate shared-table collision).
</success_criteria>

<out_of_scope>
- GitHub Actions / CI workflow that runs `scripts/uat-g1.sh` on PR — captured as deferred in CONTEXT D-11 and 02.1-01-SUMMARY follow-up items.
- Strict exactly-one-event audit-stream parsing in uat-g1.sh (D-11).
- Post-revocation `GET /key/info?key=<token>` 404 verification (D-11).
- Adding a real LiteLLM Pod to the kind e2e overlay (`config/e2e/kustomization.yaml`) — Phase 1 SC#3 SKIP under kind stays as-is; reinstatement tracks with Phase 3+.
- Makefile sugar target `make dev-up-litellm` — CONTEXT specifics flags as planner discretion; deferred to follow-up.
- Sweeping references to `spike-` elsewhere in the repo (e.g. `..spike.sh` or any doc strings) — CONTEXT discretion; the planner reviewed and found no other repo callers beyond the files this plan already touches.
</out_of_scope>

<output>
Create `.planning/phases/02.2-phase-02-cleanup-gap-g1-fix-litellm-real-uat-path-invariant-/02.2-02-SUMMARY.md` when done. Include: the diff summary for docker-compose.yml (services added, profiles gate), the deleted file, the renamed file, the uat-sc5.sh edits (line-by-line), the new scripts/uat-g1.sh structure (steps 1-9 including the JSON-form grep + awk fallback + jq prereq check), and either the live smoke result (PASS message) or a note that the smoke was run manually outside this commit cycle.
</output>
</content>
</invoke>