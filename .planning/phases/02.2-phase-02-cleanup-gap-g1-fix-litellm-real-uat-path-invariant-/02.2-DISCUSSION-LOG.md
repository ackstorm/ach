# Phase 02.2: Phase 02 cleanup — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-18
**Phase:** 02.2 — Phase 02 cleanup — Gap G1 fix + LiteLLM-real UAT path + invariant-relaxation docs
**Areas discussed:** G1 namespace fix shape, LiteLLM compose shape, UAT runner shape, Docs lift strategy

---

## G1 namespace fix shape

### Sub-question 1: resolution strategy

| Option | Description | Selected |
|--------|-------------|----------|
| A — DB column (migration 000003) | Add nullable `litellm_token TEXT` to personal_keys + environment_keys. Orphan loop joins by this column. Phase 3 SSO write path becomes responsible for populating it (carved out as a follow-up note). | ✓ |
| B — Adapter map in orphan loop | No DB change. Orphan loop calls /key/list per litellm_user_id, builds token→user_id map per tick, cross-references against personal_keys.litellm_user_id (already exists from migration 000002). | |
| C — Both: column now, map as fallback | Ship the column AND the in-memory cross-ref. Belt-and-suspenders — useful if Phase 3 SSO writes are delayed. | |

**User's choice:** A — DB column
**Notes:** Aligns with §18.4 spec wording (cross-reference by stable identifier). Greenfield = no backfill needed. Phase 3 SSO is the documented write path.

### Sub-question 2: wire-format check location

| Option | Description | Selected |
|--------|-------------|----------|
| Plan 1 — httptest.Server unit test | Plan 1 adds a unit test in `internal/litellm/keyinfo_test.go` that spins up an httptest.Server returning the v1.83.10 /key/list JSON shape verbatim and asserts the struct decodes. | |
| Plan 2 only — real LiteLLM is the wire oracle | Plan 1 only updates the struct + endpoint string. The wire format is validated end-to-end by Plan 2's docker-compose UAT (which talks to real LiteLLM v1.83.10). | ✓ |
| Both — fake-server unit + real-LiteLLM UAT | Belt-and-suspenders. Fake-server catches struct/endpoint regressions in the unit-test loop; real-LiteLLM catches anything the fake server lies about. | |

**User's choice:** Plan 2 only — real LiteLLM is the wire oracle
**Notes:** Fake server would duplicate signal and could drift from upstream silently.

---

## LiteLLM compose shape

### Sub-question 1: docker-compose.spike.yaml disposition

| Option | Description | Selected |
|--------|-------------|----------|
| Retire it | Delete `docker-compose.spike.yaml`. `scripts/uat-sc5.sh` drops its `-f docker-compose.spike.yaml` arg. One source of truth (root `docker-compose.yml`) for all compose services. | ✓ |
| Keep as deprecation stub | Leave the file with a one-line deprecation header pointing at Phase 02.2 Plan 2. | |
| Keep for opt-in only | Leave intact; move LiteLLM into root only behind a `profiles: [litellm]` gate. The spike file becomes the *eager* way to bring LiteLLM up. | |

**User's choice:** Retire it
**Notes:** Combined with profile-gating, the spike file adds nothing — one source of truth.

### Sub-question 2: always-on vs profile-gated

| Option | Description | Selected |
|--------|-------------|----------|
| Profile-gated: profiles: [litellm] | `make dev-up` brings up only Postgres + Redis. `docker compose --profile litellm up -d` adds litellm + litellm-db. UAT scripts pass `--profile litellm` explicitly. | ✓ |
| Always-on | All four services come up on every `make dev-up`. | |

**User's choice:** Profile-gated
**Notes:** Keeps day-to-day Phase 1+ dev cycle light; LiteLLM image (~500 MB) only pulled when needed.

---

## UAT runner shape

### Sub-question 1: script layout

| Option | Description | Selected |
|--------|-------------|----------|
| New scripts/uat-g1.sh (per ROADMAP) | Standalone script, mirrors uat-sc5.sh structure. Two scripts side-by-side. | ✓ |
| Extend uat-sc5.sh with --gap-g1 mode | Add a third mode alongside --smoke / --full. Shares compose lifecycle, migrations, helpers, cleanup trap. | |
| Rename uat-sc5.sh → uat-phase02.sh with subcommands | Promote SC#5 runner into a phase-wide UAT entrypoint with subcommands. | |

**User's choice:** New scripts/uat-g1.sh (per ROADMAP)
**Notes:** Matches ROADMAP text verbatim. Keeps the two concerns separate.

### Sub-question 2: scenario shape and assertion strictness

| Option | Description | Selected |
|--------|-------------|----------|
| Mirror uat-sc5.sh --full: backdate + grep | INSERT row, /key/generate, backdate created_at by 11 min, wait ≤5 min for tick, grep stdout for revocation audit line. Tolerant. | ✓ |
| Strict: assert exactly one revocation event | Same scenario, but parses JSON-line audit stream and asserts exactly one event with the expected shape. | |
| Strict + LiteLLM side-effect verify | Strict audit assertion AND follow-up GET /key/info?key=<token> to confirm LiteLLM-side 404. | |

**User's choice:** Mirror uat-sc5.sh --full: backdate + grep
**Notes:** Easy to debug, easy to ship. Strict modes captured as deferred.

---

## Docs lift strategy

### Sub-question 1: T-02-02-03 rewrite pattern

| Option | Description | Selected |
|--------|-------------|----------|
| In-place rewrite | Edit T-02-02-03 entries in Phase 02 docs to read `LIFTED 2026-05-18 (Phase 02.1 commit 45b7558)`. History is rewritten; current state matches reality. | ✓ |
| Postscript section per file | Leave originals intact; append a postscript pointing to 02.1 commit. Preserves what Phase 02 believed alongside what 02.1 changed. | |
| Single CONCESSIONS.md + backlinks | Create `02-CONCESSIONS.md` capturing both relaxations; each affected file gets a one-line header note pointing at it. | |

**User's choice:** In-place rewrite
**Notes:** Every reader sees current truth. Single edit pass.

### Sub-question 2: PROJECT.md table + CLAUDE.md scope

| Option | Description | Selected |
|--------|-------------|----------|
| Both: PROJECT.md row(s) + CLAUDE.md clarification | Add 2 rows to PROJECT.md Key Decisions table. Edit CLAUDE.md `Security:` bullet to qualify HTTPS-only to Platform API ingress + add SCM auth-optional note. | ✓ |
| CLAUDE.md only — PROJECT.md table stays Phase-level | Edit CLAUDE.md only. PROJECT.md table tracks high-level project decisions, not phase-level concessions. | |
| PROJECT.md only — CLAUDE.md says what it always said | Add table rows; leave CLAUDE.md intact (arguably already only mentions ACH_BASE_URL = Platform API ingress). | |

**User's choice:** Both: PROJECT.md row(s) + CLAUDE.md clarification
**Notes:** Both lifts are architectural concessions at the same load-bearing tier as existing Key Decisions rows.

---

## Claude's Discretion

- Plan numbering (`02.2-01-PLAN.md` etc.) — standard convention.
- Wave layout — Wave 1 = {Plan 1 G1 fix, Plan 3 docs} (independent); Wave 2 = {Plan 2 UAT} (depends on migration 000003). Planner can split further.
- Test discipline for Plan 1 — `internal/orphan/runnable_test.go` mock updates at planner's discretion; new mock fixtures in `internal/litellm/types_test.go` similarly.
- `make dev-up-litellm` sugar Makefile target — planner's call.
- Whether to sweep `grep -rn "spike-" .` for any other rename candidates beyond `LITELLM_SPIKE_PORT` and `spike-litellm-config.yaml`.

## Deferred Ideas

- Strict audit-event parsing in `uat-g1.sh` (exactly-one event with full shape).
- Post-revocation LiteLLM-side 404 verify via `GET /key/info?key=<token>`.
- `httptest.Server` wire-format unit test in `internal/litellm/keyinfo_test.go`.
- CI integration for `uat-g1.sh` (GitHub Actions PR workflow).
- Phase 1 SC#3 reinstatement under kind (needs a real LiteLLM Pod in the e2e overlay).
- Phase 3 SSO write path for `litellm_token` (carve-out captured for Phase 3 context).
- `02-CONCESSIONS.md` consolidated doc — revisit if lifts grow to 3+.
