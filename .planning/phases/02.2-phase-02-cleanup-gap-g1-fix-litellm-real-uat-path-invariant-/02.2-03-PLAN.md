---
phase: 02.2-phase-02-cleanup-gap-g1-fix-litellm-real-uat-path-invariant-
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .planning/PROJECT.md
  - .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-02-PLAN.md
  - .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-02-SUMMARY.md
  - .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-REVIEW.md
  - .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-VERIFICATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "CLAUDE.md `Security:` bullet (line 16) qualifies `HTTPS-only via ACH_BASE_URL` as applying to Platform API ingress only, and explicitly notes upstream HTTPSource admits http:// (Phase 02.1 commit 45b7558) (D-13)"
    - "CLAUDE.md adds an `SCM source-types` bullet noting authSecretRef is optional for github/gitlab/bitbucket; S3/GCS still require credentials (D-13, Phase 02.1 commit 94f24b5)"
    - ".planning/PROJECT.md `Key Decisions` table gains 2 new rows: one for HTTPS-only relaxation (Outcome: Lifted Phase 02.1, commit 45b7558), one for SCM authSecretRef relaxation (Outcome: Lifted Phase 02.1, commit 94f24b5) (D-14)"
    - "14 of the 16 T-02-02-03 / HTTPS-only occurrences across .planning/phases/02-* docs are annotated with the canonical short-form string `LIFTED 2026-05-18 (Phase 02.1 commit 45b7558)` — two specific lines are excluded (see WARNING 7 / BLOCKER 3 disposition); no sidecar 02-CONCESSIONS.md file (D-12)"
    - "Every AuthSecretRef.*Required / authSecretRef.*required claim across .planning/phases/02-* docs is parallel-swept with the commit 94f24b5 cross-reference (D-12 final sentence)"
    - "Discovery-first methodology: re-grep at execution time before rewriting (pattern-mapper finding #3) — CONTEXT D-12 line numbers are approximate signals, not literal targets"
    - "WARNING 7 canonical annotation form: short string `LIFTED 2026-05-18 (Phase 02.1 commit 45b7558)` used uniformly across threat-model Disposition cells AND narrative parentheticals — grep-friendly in both contexts"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Updated `Security:` bullet + new `SCM source-types` bullet"
      contains: "HTTPS-only via deployment-configured `ACH_BASE_URL` for Platform API ingress"
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions table with 2 appended rows on lifted invariants"
      contains: "Lifted Phase 02.1 (commit 45b7558)"
    - path: ".planning/phases/02-external-refs-marketplace-operator-reconciliation/02-02-PLAN.md"
      provides: "T-02-02-03 row rewritten in-place"
      contains: "LIFTED 2026-05-18 (Phase 02.1 commit 45b7558)"
    - path: ".planning/phases/02-external-refs-marketplace-operator-reconciliation/02-02-SUMMARY.md"
      provides: "HTTPS-only narrative occurrences qualified in-place"
      contains: "LIFTED 2026-05-18 (Phase 02.1 commit 45b7558)"
    - path: ".planning/phases/02-external-refs-marketplace-operator-reconciliation/02-REVIEW.md"
      provides: "T-02-02-03 / HTTPS-only review notes qualified in-place"
      contains: "LIFTED 2026-05-18 (Phase 02.1 commit 45b7558)"
    - path: ".planning/phases/02-external-refs-marketplace-operator-reconciliation/02-VERIFICATION.md"
      provides: "T-02-02-03 verification note qualified in-place"
      contains: "LIFTED 2026-05-18 (Phase 02.1 commit 45b7558)"
  key_links:
    - from: "CLAUDE.md `Security:` bullet"
      to: ".planning/PROJECT.md `Key Decisions` table"
      via: "Both surface the same architectural concession; CLAUDE.md is the developer-facing constraint, PROJECT.md is the decision log"
      pattern: "Lifted Phase 02.1"
    - from: ".planning/phases/02-*/* HTTPS-only and T-02-02-03 references"
      to: "Phase 02.1 commit 45b7558 (HTTPSource pattern lift) and 94f24b5 (SCM anonymous fetch)"
      via: "Every occurrence cites the commit sha inline so future readers can locate the source change"
      pattern: "commit 45b7558|commit 94f24b5"
---

<objective>
Synchronize the doc surface with Phase 02.1 reality. Phase 02.1 permanently lifted two Phase 02 invariants:
1. **HTTPS-only on HTTPSource** (commit `45b7558`) — `api/ach/v1alpha1/external_ref_types.go` dropped `+kubebuilder:validation:Pattern=^https://`, `internal/sources/http/fetcher.go` dropped its runtime guard, and `internal/sources/http/fetcher_test.go` renamed `TestNew_RejectsHTTP` → `TestNew_AcceptsHTTPAndHTTPS`. The HTTPS-only invariant survives only as a deployment-time convention.
2. **SCM authSecretRef requirement** (commit `94f24b5`) — github/gitlab/bitbucket fetchers now admit anonymous fetches; CRD field marker dropped `+kubebuilder:validation:Required`; SC1 e2e fixture uses `octocat/Hello-World @ master` with NO `authSecretRef`. S3/GCS retain mandatory auth.

The Phase 02 plan/SUMMARY/REVIEW/VERIFICATION docs still narrate the original invariants as load-bearing — every future reader who consults those docs without also reading `.planning/phases/02.1-.../02.1-01-SUMMARY.md` will be misinformed. Plan 02.2-03 fixes this with an in-place rewrite sweep across all four phase-02 doc files plus `CLAUDE.md` and `.planning/PROJECT.md`.

Purpose: Single source of truth. D-12 explicitly rejects a sidecar `02-CONCESSIONS.md` file in favor of in-place annotation — every reader sees current truth without needing to chase a postscript.

Output: One edit to `CLAUDE.md` (line 16 bullet qualification + new SCM bullet), one edit to `.planning/PROJECT.md` (append 2 rows to `Key Decisions` table), and one re-grep-then-rewrite sweep across 4 phase-02 doc files. No source code touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/02.2-phase-02-cleanup-gap-g1-fix-litellm-real-uat-path-invariant-/02.2-CONTEXT.md
@.planning/phases/02.2-phase-02-cleanup-gap-g1-fix-litellm-real-uat-path-invariant-/02.2-PATTERNS.md
@.planning/phases/02.1-kind-e2e-overlay-config-dev-postgres-config-e2e-scripts-e2e-/02.1-01-SUMMARY.md

@CLAUDE.md
@.planning/phases/02-external-refs-marketplace-operator-reconciliation/02-02-PLAN.md
@.planning/phases/02-external-refs-marketplace-operator-reconciliation/02-02-SUMMARY.md
@.planning/phases/02-external-refs-marketplace-operator-reconciliation/02-REVIEW.md
@.planning/phases/02-external-refs-marketplace-operator-reconciliation/02-VERIFICATION.md

<interfaces>
<!-- Existing doc surfaces this plan modifies. No code interfaces — pure docs. -->

CLAUDE.md current `Security:` bullet (line 16):
```
- **Security**: HMAC-SHA-256 with server-side pepper for credential hashes; pepper held outside Postgres. Plaintext key values MUST NOT be persisted anywhere (DB, Redis, logs, audit, metrics, traces). HTTPS-only via deployment-configured `ACH_BASE_URL` — no HTTP escape hatch.
```

`.planning/PROJECT.md` Key Decisions table column structure:
```
| Decision | Rationale | Outcome |
|----------|-----------|---------|
| ... | ... | ✓ Good (per [[link]]) |
| ... | ... | — Pending |
```

Phase 02.1 commit shas (canonical refs):
- `45b7558` — Task 5a: lift HTTPS-only invariant on HTTPSource (CRD Pattern + runtime fetcher guard + fetcher_test rename)
- `94f24b5` — Task 5b: anonymous SCM fetches (github/gitlab/bitbucket) + SC1 fixture rewrite

CONTEXT D-12 affected files (approximate line numbers — pattern-mapper finding #3: re-grep at execution time):
- `.planning/phases/02-external-refs-marketplace-operator-reconciliation/02-02-PLAN.md` (lines 43, 338, 542, plus T-02-02-08 SSRF row at line 547 which is DO-NOT-ANNOTATE per BLOCKER 3 below)
- `.planning/phases/02-external-refs-marketplace-operator-reconciliation/02-02-SUMMARY.md` (lines 17, 37, 59, 87, 109, 118, 144)
- `.planning/phases/02-external-refs-marketplace-operator-reconciliation/02-REVIEW.md` (lines 129, 137, plus line 254 which is DO-NOT-ANNOTATE per BLOCKER 3 below)
- `.planning/phases/02-external-refs-marketplace-operator-reconciliation/02-VERIFICATION.md` (line 65)

Total per re-grep at planning time (file-by-file `grep -nE "HTTPS-only|T-02-02-03"`): **16 occurrences across 4 files** (verified via execution-time grep on 2026-05-18).

Of those 16:
- **2 are explicitly DO-NOT-ANNOTATE** (see BLOCKER 3 disposition in Task 1 action):
  1. `02-02-PLAN.md:547` — inside the T-02-02-**08** SSRF threat-model row, where "HTTPS-only invariant" appears as background context for an SSRF mitigation. The SSRF threat is unrelated to the HTTPSource scheme lift — annotating it would suggest the SSRF mitigation was lifted, which is false.
  2. `02-REVIEW.md:254` — reviews S3 endpoint behavior and quotes CLAUDE.md's HTTPS-only sentence to argue S3/GCS still require HTTPS per D-13. That assertion is still true (D-13 explicitly preserves S3/GCS HTTPS requirement); annotating would create a false impression that S3/GCS-side HTTPS is also lifted.
- **14 ARE annotated** with the canonical short form (WARNING 7: uniform application — see below).

WARNING 7 — single canonical annotation form (D-12 + WARNING 7 reconciliation):
```
LIFTED 2026-05-18 (Phase 02.1 commit 45b7558)
```
- In threat-model rows: this exact string goes in the Disposition column.
- In narrative paragraphs: append `(LIFTED 2026-05-18 (Phase 02.1 commit 45b7558))` as a parenthetical immediately after the first `HTTPS-only` mention in each unique paragraph.
- The long-form sentence (`— HTTPSource now admits http:// for hermetic in-cluster fixtures; production deployments use https:// by convention`) is NO LONGER used as part of the canonical annotation — it appears once in the CONTEXT D-12 record and may be appended as additional explanatory prose to a threat-model row's Mitigation Plan cell, but the **grep-anchor string is the short form only**.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Discover-then-rewrite — re-grep T-02-02-03 / HTTPS-only / authSecretRef-required occurrences across phase-02 docs</name>
  <files>.planning/phases/02-external-refs-marketplace-operator-reconciliation/02-02-PLAN.md, .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-02-SUMMARY.md, .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-REVIEW.md, .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-VERIFICATION.md</files>
  <read_first>
    .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-02-PLAN.md,
    .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-02-SUMMARY.md,
    .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-REVIEW.md,
    .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-VERIFICATION.md,
    .planning/phases/02.1-kind-e2e-overlay-config-dev-postgres-config-e2e-scripts-e2e-/02.1-01-SUMMARY.md,
    .planning/phases/02.2-phase-02-cleanup-gap-g1-fix-litellm-real-uat-path-invariant-/02.2-CONTEXT.md
  </read_first>
  <action>Two-phase work: discover, then rewrite. Per pattern-mapper finding #3, CONTEXT D-12 line numbers are approximate — re-grep at execution time.

PHASE A — DISCOVERY: Run `grep -nE "HTTPS-only|T-02-02-03" .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-02-PLAN.md .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-02-SUMMARY.md .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-REVIEW.md .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-VERIFICATION.md` and capture the exact line + content of every match. Verify the total count is exactly 16. If the count is different (file content has drifted since planning), STOP and surface the discrepancy in the SUMMARY before continuing — do not silently re-baseline.

Also run `grep -rnE "AuthSecretRef.*Required|authSecretRef.*required" .planning/phases/02-external-refs-marketplace-operator-reconciliation/` to enumerate the SCM-auth parallel sweep targets (CONTEXT D-12 final sentence). Persist the full discovery output (every `grep -n` line) to the SUMMARY at the end (no temporary files).

PHASE B — DO-NOT-ANNOTATE LIST (BLOCKER 3 fix): The following 2 occurrences MUST NOT be annotated. Pass them through unchanged:
1. `.planning/phases/02-external-refs-marketplace-operator-reconciliation/02-02-PLAN.md:547` — the T-02-02-**08** SSRF threat-model row references "HTTPS-only invariant" as background for an SSRF mitigation. The SSRF threat is unrelated to the HTTPSource scheme lift. Per-line disposition: **skip-with-reason: T-02-02-08 SSRF row, HTTPS-only mention is background framing for an unrelated mitigation (SSRF), not a load-bearing scheme assertion**.
2. `.planning/phases/02-external-refs-marketplace-operator-reconciliation/02-REVIEW.md:254` — reviews S3 endpoint behavior and quotes CLAUDE.md's HTTPS-only sentence to assert S3/GCS still require HTTPS per D-13. That assertion remains true. Per-line disposition: **skip-with-reason: S3/GCS endpoint review, D-13 explicitly preserves S3/GCS HTTPS requirement**.

PHASE C — REWRITE the remaining 14 occurrences using the WARNING 7 canonical short-form string `LIFTED 2026-05-18 (Phase 02.1 commit 45b7558)`:
- For each `T-02-02-03` threat-model row found (the load-bearing rows, NOT the T-02-02-08 SSRF row), rewrite the `Disposition` column from `mitigate` to `LIFTED 2026-05-18 (Phase 02.1 commit 45b7558)` (exact string, no trailing prose). The `Mitigation Plan` cell may retain or be replaced by the long-form explanatory prose `HTTPSource now admits http:// for hermetic in-cluster fixtures; production deployments use https:// by convention. (Three-layer defense removed: CRD Pattern marker dropped; Fetcher.New http:// rejection lifted; CLAUDE.md HTTPS-only qualifier added.)` — but the grep-anchor is the Disposition column's short string. Preserve the threat-model table's 5-column structure (Threat ID / Category / Component / Disposition / Mitigation Plan).
- For each narrative occurrence of `HTTPS-only` in flowing prose, append the inline parenthetical `(LIFTED 2026-05-18 (Phase 02.1 commit 45b7558))` immediately after the `HTTPS-only` phrase the first time it appears in each unique narrative paragraph. If the same paragraph repeats `HTTPS-only` multiple times, qualify only the first mention to avoid clutter. Do NOT rewrite history — preserve the original assertion intact and append the qualification (a future reader needs to see what was claimed and how it has been amended).

PHASE D — SCM-auth parallel sweep: For each `AuthSecretRef.*Required` or `authSecretRef.*required` claim discovered, append inline `(LIFTED 2026-05-18 — Phase 02.1 commit 94f24b5; github/gitlab/bitbucket fetchers now admit anonymous fetches; S3/GCS retain mandatory auth)`. (This annotation may use the longer explanatory form because the SCM lift has a different scope qualifier than the HTTPS-only lift.)

PHASE E — Constraints + verification:
- Do NOT create `02-CONCESSIONS.md` (D-12 explicit prohibition).
- After every rewrite, run `grep -c "LIFTED 2026-05-18 (Phase 02.1 commit 45b7558)" .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-02-PLAN.md .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-02-SUMMARY.md .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-REVIEW.md .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-VERIFICATION.md | awk -F: '{s+=$2} END {print s}'` and confirm the sum is **exactly 14** (BLOCKER 3 fix: 16 total occurrences minus the 2 explicit DO-NOT-ANNOTATE lines).
- The SUMMARY (the Phase 02.2 close-out artifact `02.2-03-SUMMARY.md`) MUST enumerate every one of the 16 occurrences with a per-line disposition tag from the set `{rewrite, qualify-inline, skip-with-reason}`. Each `skip-with-reason` entry MUST include the reason string verbatim from the DO-NOT-ANNOTATE list above.</action>
  <verify>
    <automated>S=$(grep -c "LIFTED 2026-05-18 (Phase 02.1 commit 45b7558)" .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-02-PLAN.md .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-02-SUMMARY.md .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-REVIEW.md .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-VERIFICATION.md | awk -F: '{s+=$2} END {print s}') && [ "$S" -eq 14 ] && ! test -f .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-CONCESSIONS.md && ! grep -q "LIFTED 2026-05-18" <(sed -n '547p' .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-02-PLAN.md) && ! grep -q "LIFTED 2026-05-18" <(sed -n '254p' .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-REVIEW.md)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "LIFTED 2026-05-18 (Phase 02.1 commit 45b7558)"` summed across the 4 phase-02 docs equals **exactly 14** (BLOCKER 3 fix — 16 total minus 2 DO-NOT-ANNOTATE)
    - File .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-CONCESSIONS.md does NOT exist (D-12 prohibition)
    - Line 547 of `02-02-PLAN.md` (T-02-02-08 SSRF row) is NOT annotated with the `LIFTED` string (BLOCKER 3 DO-NOT-ANNOTATE #1)
    - Line 254 of `02-REVIEW.md` (S3 endpoint review) is NOT annotated with the `LIFTED` string (BLOCKER 3 DO-NOT-ANNOTATE #2)
    - Each `T-02-02-03` threat-model row's Disposition column reads exactly `LIFTED 2026-05-18 (Phase 02.1 commit 45b7558)` (no trailing prose in the Disposition cell — WARNING 7 short-form canonical)
    - The 5-column threat-model table structure (Threat ID / Category / Component / Disposition / Mitigation Plan) is preserved (no broken pipes; `grep -E "^\|.*\|.*\|.*\|.*\|" file` shows no row got truncated to 4 columns)
    - Each narrative HTTPS-only mention (first occurrence per unique paragraph) has the inline parenthetical `(LIFTED 2026-05-18 (Phase 02.1 commit 45b7558))` appended (WARNING 7 — same canonical short form, parenthetical wrapped)
    - At least one occurrence of `commit 94f24b5` appears across the 4 phase-02 docs (SCM-auth parallel sweep landed; only if discovery found `AuthSecretRef.*Required` or `authSecretRef.*required` matches — if none found, the SUMMARY records "0 SCM-required claims discovered in phase-02 docs" and this criterion is vacuously satisfied)
    - The 02.2-03-SUMMARY.md enumerates all 16 occurrences with a per-line disposition (`rewrite | qualify-inline | skip-with-reason`); each `skip-with-reason` entry includes the reason verbatim from the DO-NOT-ANNOTATE list
    - The discovery output (full `grep -n` line listings) is appended to the SUMMARY for audit
  </acceptance_criteria>
  <done>14 of the 16 HTTPS-only / T-02-02-03 occurrences across the four Phase-02 doc files are qualified in-place with the canonical short-form annotation. The 2 DO-NOT-ANNOTATE occurrences (PLAN.md:547 SSRF row + REVIEW.md:254 S3/GCS review) are preserved unchanged with rationale recorded in the SUMMARY. No sidecar concessions file.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update CLAUDE.md Security bullet + add SCM source-types bullet; append 2 rows to .planning/PROJECT.md Key Decisions table</name>
  <files>CLAUDE.md, .planning/PROJECT.md</files>
  <read_first>
    CLAUDE.md,
    .planning/PROJECT.md,
    .planning/phases/02.1-kind-e2e-overlay-config-dev-postgres-config-e2e-scripts-e2e-/02.1-01-SUMMARY.md,
    .planning/phases/02.2-phase-02-cleanup-gap-g1-fix-litellm-real-uat-path-invariant-/02.2-CONTEXT.md
  </read_first>
  <action>Two file edits, both append/qualify-only (no deletions of project-level invariants).

EDIT 1 — CLAUDE.md: locate the `Security:` bullet under `### Constraints` (currently single line at file-position ~line 16, but re-find via `grep -n '\*\*Security\*\*' CLAUDE.md` at execution time because line numbers may drift). Replace the line verbatim from `- **Security**: HMAC-SHA-256 with server-side pepper for credential hashes; pepper held outside Postgres. Plaintext key values MUST NOT be persisted anywhere (DB, Redis, logs, audit, metrics, traces). HTTPS-only via deployment-configured \`ACH_BASE_URL\` — no HTTP escape hatch.` to `- **Security**: HMAC-SHA-256 with server-side pepper for credential hashes; pepper held outside Postgres. Plaintext key values MUST NOT be persisted anywhere (DB, Redis, logs, audit, metrics, traces). HTTPS-only via deployment-configured \`ACH_BASE_URL\` for Platform API ingress — no HTTP escape hatch on the user-facing surface. Upstream \`HTTPSource\` admits \`http://\` (Phase 02.1 lifted the strict-https CRD pattern for hermetic fixture-server / private-network paths).` Insert a NEW bullet immediately after the Security bullet: `- **SCM source-types** (github/gitlab/bitbucket): accept anonymous fetches — \`authSecretRef\` is optional. S3/GCS still require credentials.` Preserve the 2-space bullet indentation under `### Constraints`. Preserve every other bullet in the Constraints section verbatim. Preserve the HMAC-SHA-256 first sentence (CR-03/DB-04 invariant) and the `Plaintext key values MUST NOT be persisted` sentence — those are NOT relaxed.

EDIT 2 — .planning/PROJECT.md: locate the `## Key Decisions` table (file-position ~line 91 per pattern-mapper; re-find via `grep -n '## Key Decisions' .planning/PROJECT.md`). Append exactly two new rows AFTER the last current row of the table and BEFORE the next heading (typically `## Evolution`): `| \`HTTPS-only\` invariant relaxed for upstream \`HTTPSource\` | Hermetic e2e fixture-server (in-cluster nginx) requires \`http://\` during Phase 02.1; production deployments still use \`https://\` by convention | Lifted Phase 02.1 (commit 45b7558) |` then `| SCM \`authSecretRef\` made optional | Anonymous public-repo fetches needed for SC1 fixture (\`octocat/Hello-World @ master\`); S3/GCS retain mandatory auth | Lifted Phase 02.1 (commit 94f24b5) |`. Preserve the 3-column table structure (Decision / Rationale / Outcome). Preserve every existing row verbatim. The new `Outcome` values use the third variant `Lifted Phase 02.1 (commit <sha>)` — the existing two variants (`✓ Good (per [[link]])` and `— Pending`) remain unchanged for other rows. Do NOT add `[[feedback_...]]` wiki-link brackets to the new rows (the commit shas are the canonical reference).</action>
  <verify>
    <automated>grep -q '\*\*Security\*\*.*Platform API ingress.*no HTTP escape hatch on the user-facing surface' CLAUDE.md && grep -q 'Upstream `HTTPSource` admits `http://`' CLAUDE.md && grep -q '\*\*SCM source-types\*\*.*authSecretRef.*optional' CLAUDE.md && grep -q '`HTTPS-only` invariant relaxed for upstream `HTTPSource`' .planning/PROJECT.md && grep -q 'SCM `authSecretRef` made optional' .planning/PROJECT.md && grep -q 'Lifted Phase 02.1 (commit 45b7558)' .planning/PROJECT.md && grep -q 'Lifted Phase 02.1 (commit 94f24b5)' .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md Security bullet contains exactly one occurrence of the literal substring `Platform API ingress`
    - CLAUDE.md Security bullet contains exactly one occurrence of `Upstream `HTTPSource` admits `http://``
    - CLAUDE.md Security bullet retains the `HMAC-SHA-256 with server-side pepper` opening sentence verbatim
    - CLAUDE.md Security bullet retains the `Plaintext key values MUST NOT be persisted` sentence verbatim
    - CLAUDE.md contains a new bullet starting with `- **SCM source-types**` immediately after the Security bullet (under `### Constraints`)
    - CLAUDE.md SCM bullet mentions `github/gitlab/bitbucket` and `S3/GCS still require credentials`
    - .planning/PROJECT.md `## Key Decisions` table contains a new row matching `| `HTTPS-only` invariant relaxed for upstream `HTTPSource` | … | Lifted Phase 02.1 (commit 45b7558) |`
    - .planning/PROJECT.md `## Key Decisions` table contains a new row matching `| SCM `authSecretRef` made optional | … | Lifted Phase 02.1 (commit 94f24b5) |`
    - The PROJECT.md table retains the 3-column structure: `grep -E "^\| .* \| .* \| .* \|$" .planning/PROJECT.md | head -1` finds the header still well-formed
    - No `## Evolution` heading or any other heading was clobbered by the table appends
    - `grep -c "Lifted Phase 02.1" .planning/PROJECT.md` returns >= 2 (one per new row)
  </acceptance_criteria>
  <done>CLAUDE.md surfaces the qualified HTTPS-only scope and the SCM authSecretRef relaxation as a new constraint bullet; PROJECT.md Key Decisions table gains two rows recording the same concessions at architectural-decision tier, all with commit-sha cross-refs.</done>
</task>

</tasks>

<threat_model>
None — documentation-only plan; no executable surface introduced. No HTTP endpoints, no SQL surface, no shell scripts, no Go code paths created or modified. The only attack surface affected is the documentation itself, which carries no integrity or confidentiality requirements beyond accurate narrative.

For completeness, the canonical security narrative is being QUALIFIED (not removed): the `HTTPS-only via deployment-configured ACH_BASE_URL` invariant survives at the Platform API ingress boundary; only the upstream `HTTPSource` CRD pattern is relaxed. The supply-chain consideration (T-{phase}-SC) does not apply — zero new package-manager installs in this plan.
</threat_model>

<verification>
After both tasks land:
- `grep -c "LIFTED 2026-05-18 (Phase 02.1 commit 45b7558)" .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-* | awk -F: '{s+=$2} END {print s}'` equals exactly 14 (BLOCKER 3 fix)
- `grep -c "Lifted Phase 02.1 (commit 45b7558)" .planning/PROJECT.md` returns 1
- `grep -c "Lifted Phase 02.1 (commit 94f24b5)" .planning/PROJECT.md` returns 1
- `grep -q 'Platform API ingress' CLAUDE.md` and `grep -q 'SCM source-types' CLAUDE.md` both exit 0
- File `.planning/phases/02-external-refs-marketplace-operator-reconciliation/02-CONCESSIONS.md` does NOT exist
- `sed -n '547p' .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-02-PLAN.md | grep -q "LIFTED 2026-05-18"` returns NON-ZERO (the DO-NOT-ANNOTATE line is unchanged)
- `sed -n '254p' .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-REVIEW.md | grep -q "LIFTED 2026-05-18"` returns NON-ZERO (the DO-NOT-ANNOTATE line is unchanged)
- `markdownlint .planning/PROJECT.md CLAUDE.md` exits 0 if installed (skip if not)
- Visual inspection of the PROJECT.md table renders cleanly in a markdown viewer (3 columns; new rows align with prior rows)
</verification>

<success_criteria>
- CLAUDE.md `Security:` bullet correctly qualifies HTTPS-only as Platform API ingress only and notes the HTTPSource lift.
- CLAUDE.md gains a new SCM bullet documenting the authSecretRef relaxation.
- .planning/PROJECT.md `Key Decisions` table records both lifted invariants at the same architectural-decision tier as the project-level `Scope: Hub + CLI as one coordinated project` decisions.
- Exactly 14 of the 16 load-bearing T-02-02-03 / narrative HTTPS-only mentions across the 4 phase-02 doc files are qualified in-place with the canonical short-form `LIFTED 2026-05-18 (Phase 02.1 commit 45b7558)` (BLOCKER 3 + WARNING 7 reconciled).
- The 2 DO-NOT-ANNOTATE occurrences (PLAN.md:547 SSRF row + REVIEW.md:254 S3/GCS review) are explicitly preserved with reason recorded in the SUMMARY (BLOCKER 3).
- WARNING 7: same canonical short form used in both threat-model Disposition cells and narrative parentheticals — uniform grep target.
- Every authSecretRef-required claim across the 4 phase-02 doc files is qualified in-place with the commit-94f24b5 cross-reference (or the SUMMARY documents that discovery found zero such claims to sweep).
- No sidecar `02-CONCESSIONS.md` file exists (D-12 prohibition).
- Discovery output (full `grep -n` listings) preserved in the eventual SUMMARY for audit, with per-line disposition for all 16 occurrences.
</success_criteria>

<out_of_scope>
- Editing source files (CRD types, fetchers, fetcher_test.go) — Phase 02.1 already shipped those changes; this plan documents them.
- Editing 02-CONTEXT.md, 02-RESEARCH.md, or any Phase 02 SUMMARY beyond the explicitly-listed four doc files.
- Editing Phase 02.1 docs — they are authoritative as-is and reference these lifts in their own SUMMARY.
- Writing a separate `02-CONCESSIONS.md` consolidated doc — explicitly rejected by D-12. If future lifts grow to 3+, revisit per CONTEXT deferred items.
- Editing the v1alpha1 spec markdown (`ach_hub_spec_v20260515_FINALv4.md`) — the spec narrates abstract architectural invariants; this plan only edits internal planning docs. Spec-level changes are deferred to a future doc-cycle.
- Updating CONTEXT D-12's prose to record the WARNING-7-canonical short form — CONTEXT artifacts are immutable user decisions; the per-decision short form is recorded in this plan's must_haves and is the new canonical going forward without rewriting D-12 itself.
</out_of_scope>

<output>
Create `.planning/phases/02.2-phase-02-cleanup-gap-g1-fix-litellm-real-uat-path-invariant-/02.2-03-SUMMARY.md` when done. The SUMMARY MUST include:
1. The full Task 1 discovery output: every `grep -n "HTTPS-only\|T-02-02-03" …` line with file:line:content (so a future reviewer can audit completeness). Total = 16.
2. **Per-line disposition table** for all 16 occurrences, with columns `file:line | match content | disposition (rewrite | qualify-inline | skip-with-reason) | reason (only for skip)` — BLOCKER 3 enumeration requirement.
3. Per-file occurrence counts before / after the rewrite.
4. The diff of CLAUDE.md (single bullet replaced + one new bullet added).
5. The two new rows appended to .planning/PROJECT.md.
6. Per-file authSecretRef-required occurrence count discovered (may be 0).
7. Explicit confirmation that `02-CONCESSIONS.md` was NOT created.
8. Final grep-anchor sum confirmation: `grep -c "LIFTED 2026-05-18 (Phase 02.1 commit 45b7558)"` summed across the 4 docs = exactly 14.
</output>
</content>
</invoke>