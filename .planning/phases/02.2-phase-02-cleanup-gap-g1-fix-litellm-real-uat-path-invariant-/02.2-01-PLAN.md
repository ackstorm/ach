---
phase: 02.2-phase-02-cleanup-gap-g1-fix-litellm-real-uat-path-invariant-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/litellm/keyinfo.go
  - internal/litellm/types.go
  - internal/litellm/client.go
  - internal/orphan/runnable.go
  - internal/orphan/runnable_test.go
  - internal/db/litellm_users.go
  - db/migrations/000003_litellm_token.up.sql
  - db/migrations/000003_litellm_token.down.sql
autonomous: true
requirements: [OP-13]
must_haves:
  truths:
    - "GET /key/list?user_id=<uid>&return_full_object=true&include_team_keys=false is the only LiteLLM endpoint the operator calls to enumerate per-user keys (D-01, supersedes /key/info)"
    - "internal/litellm.UserKeyInfo exposes a Token string field (json:\"token\") replacing the prior KeyID field (D-01, ROADMAP G1 description)"
    - "The orphan runnable's set-difference key compares LiteLLM Token against active ACH tokens (not against pkid_/ekid_ prefixes), via the new litellm_token DB column (D-01)"
    - "internal/orphan/runnable.go defines OutcomeRevoked = \"revoked\" for success-path audit events; uat-g1.sh greps JSON-form \"outcome\":\"revoked\" (D-10, pattern-mapper finding #2)"
    - "internal/orphan/runnable.go has NO OutcomeSuccess constant — greenfield/no-compat-shims discipline (WARNING 6: one-shot rename, no dead constants)"
    - "db/migrations/000003_litellm_token.up.sql adds nullable litellm_token TEXT to personal_keys and environment_keys with a UNIQUE partial index per table (D-01)"
    - "Phase 3 SSO is the documented write path for litellm_token — Phase 02.2 ships schema + reader only; orphan loop returns empty until Phase 3 populates the column (D-02)"
    - "Plan 1 ships NO httptest.Server fake-server wire-format test under internal/litellm/keyinfo_test.go — Plan 02's scripts/uat-g1.sh against real LiteLLM v1.83.10 is the wire oracle (D-03)"
  artifacts:
    - path: "internal/litellm/keyinfo.go"
      provides: "Pass-through fetcher calling /key/list with the new query string"
      contains: "/key/list?user_id="
    - path: "internal/litellm/types.go"
      provides: "UserKeyInfo struct with Token field; ListUserKeysResponse envelope"
      contains: "Token     string"
    - path: "internal/orphan/runnable.go"
      provides: "Orphan loop comparing k.Token against achKeySet; OutcomeRevoked constant; OutcomeSuccess removed"
      contains: "OutcomeRevoked"
    - path: "db/migrations/000003_litellm_token.up.sql"
      provides: "ALTER TABLE … ADD COLUMN litellm_token TEXT on both key tables + UNIQUE partial indexes"
      contains: "ADD COLUMN IF NOT EXISTS litellm_token"
    - path: "db/migrations/000003_litellm_token.down.sql"
      provides: "Reverse migration dropping indexes + columns"
      contains: "DROP COLUMN IF EXISTS litellm_token"
  key_links:
    - from: "internal/litellm/keyinfo.go"
      to: "/key/list endpoint on LiteLLM v1.83.10"
      via: "GET with return_full_object=true&include_team_keys=false query params"
      pattern: "/key/list\\?user_id="
    - from: "internal/orphan/runnable.go"
      to: "internal/litellm.UserKeyInfo.Token"
      via: "k.Token cross-referenced against active ACH litellm_token set"
      pattern: "k\\.Token"
    - from: "internal/orphan/runnable.go"
      to: "audit.Logger outcome enum"
      via: "OutcomeRevoked constant emitted on successful revocation"
      pattern: "OutcomeRevoked\\s*=\\s*\"revoked\""
---

<objective>
Close Gap G1 from `02-HUMAN-UAT.md` by swapping the LiteLLM endpoint the orphan-cleanup Runnable uses to enumerate per-user keys, reshaping the JSON envelope to consume the v1.83.10 `/key/list` response, renaming the success-path outcome enum from `success` to `revoked` (one-shot rename — `OutcomeSuccess` deleted outright per greenfield discipline), and adding the `litellm_token` schema column that lets the orphan loop perform its set-difference against LiteLLM-internal opaque hex tokens instead of ACH's `pkid_*`/`ekid_*` prefixes.

Purpose: Phase 2 declared OP-13 (orphan-cleanup audit event per revocation) `passed` against the unit-test mock but the wire-format call against real LiteLLM 1.83.10 returns `404 not_found_error` — the orphan loop mis-classifies that as `litellm_unreachable` and aborts. Until this plan lands, OP-13 cannot be verified live. After this plan, Plan 02 (`uat-g1.sh`) can exercise the end-to-end revocation path against real LiteLLM.

Output: One Go endpoint + struct change in `internal/litellm/`, one orphan-runnable rename (`KeyID`→`Token`) + new outcome constant + `OutcomeSuccess` deletion in `internal/orphan/`, one updated unit-test sweep in `runnable_test.go`, one minor doc update in `internal/db/litellm_users.go`, and one migration pair `000003_litellm_token.{up,down}.sql`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/02.2-phase-02-cleanup-gap-g1-fix-litellm-real-uat-path-invariant-/02.2-CONTEXT.md
@.planning/phases/02.2-phase-02-cleanup-gap-g1-fix-litellm-real-uat-path-invariant-/02.2-PATTERNS.md
@.planning/phases/02-external-refs-marketplace-operator-reconciliation/02-HUMAN-UAT.md

@internal/litellm/keyinfo.go
@internal/litellm/types.go
@internal/litellm/client.go
@internal/orphan/runnable.go
@internal/orphan/runnable_test.go
@internal/db/litellm_users.go
@internal/audit/handler.go
@db/migrations/000002_phase2.up.sql
@db/migrations/000002_phase2.down.sql

<interfaces>
<!-- Key types and contracts the executor needs. No codebase exploration required. -->

From internal/litellm/types.go (current shape, lines 270-284):
```go
type UserKeyInfo struct {
    KeyID     string    `json:"key_id"`
    UserID    string    `json:"user_id"`
    CreatedAt time.Time `json:"created_at"`
    KeyAlias  string    `json:"key_alias,omitempty"`
}

type ListUserKeysResponse struct {
    Keys []UserKeyInfo `json:"keys"`
}
```

From internal/litellm/client.go (line 93 — Client interface entry, signature unchanged):
```go
ListUserKeys(ctx context.Context, userID string) ([]UserKeyInfo, error)
```

From internal/orphan/runnable.go (lines 42-46 — current outcome enum):
```go
const (
    OutcomeSuccess            = "success"
    OutcomeLiteLLMUnreachable = "litellm_unreachable"
    OutcomeRevokeFailed       = "revoke_failed"
)
```

From internal/orphan/runnable.go (lines 211, 215, 221, 225, 230, 234 — every `k.KeyID` reference and the `"key_id"` slog attr on line 234 must rename in lockstep with the struct change):
```go
if _, active := achKeySet[k.KeyID]; active {       // line 211
if err := r.Client.RevokeKey(ctx, k.KeyID); err != nil {   // line 215
    "target.name", k.KeyID,                                // line 221
"key_id", k.KeyID, "err", err)                             // line 225
    "target.name", k.KeyID,                                // line 230
"key_id", k.KeyID, "user_id", uid, "key_alias", k.KeyAlias) // line 234
```

From internal/audit/handler.go (line 45 — JSON handler is the canonical Phase 2 emitter):
```go
h := slog.NewJSONHandler(w, &slog.HandlerOptions{Level: slog.LevelInfo})
return slog.New(h).With(slog.Bool("audit", true))
```
JSON output shape (verified): `{"time":"…","level":"INFO","msg":"operator.orphan-cleanup","audit":true,"target.kind":"litellm_key","target.name":"<token>","outcome":"revoked","user_id":"u-uat-g1"}`. The With-attr `audit:true` is emitted first; call-site attrs follow in declaration order (`target.kind`, `target.name`, `outcome`, `user_id`). The Plan 2 grep regex MUST therefore match the JSON form (`"outcome":"revoked"` not `outcome=revoked`).

Gap G1 wire-format diff (per 02-HUMAN-UAT.md §Gaps G1, lines 62-71):
- Wrong endpoint (current): `GET /key/info?user_id=<id>` → LiteLLM v1.83.10 returns `404 not_found_error "Key not found in database"`.
- Correct endpoint: `GET /key/list?user_id=<id>&return_full_object=true&include_team_keys=false`.
- Correct response shape: `{"keys":[{...token, created_at, key_alias, user_id, ...}], "total_count":N, "current_page":1, "total_pages":1}`. The orphan-loop's comparison field on each entry is `token` (LiteLLM-internal opaque hex), NOT `key_id`.

DB-column path (D-01, pattern: db/migrations/000002_phase2.up.sql):
- New columns: `personal_keys.litellm_token text` (nullable) + `environment_keys.litellm_token text` (nullable).
- New indexes: `personal_keys_litellm_token_uniq` and `environment_keys_litellm_token_uniq`, both `UNIQUE … WHERE litellm_token IS NOT NULL` (partial unique index — tokens are globally unique within a LiteLLM instance per CONTEXT specifics).
- NO CHECK constraint on `litellm_token` (LiteLLM hex tokens have no fixed prefix; only `key_id` carries the `pkid_*`/`ekid_*` CHECK from migration 000001).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add migration 000003_litellm_token.{up,down}.sql</name>
  <files>db/migrations/000003_litellm_token.up.sql, db/migrations/000003_litellm_token.down.sql</files>
  <read_first>
    db/migrations/000002_phase2.up.sql,
    db/migrations/000002_phase2.down.sql,
    .planning/phases/02.2-phase-02-cleanup-gap-g1-fix-litellm-real-uat-path-invariant-/02.2-CONTEXT.md,
    .planning/phases/02.2-phase-02-cleanup-gap-g1-fix-litellm-real-uat-path-invariant-/02.2-PATTERNS.md
  </read_first>
  <action>Create `db/migrations/000003_litellm_token.up.sql` containing exactly two `ALTER TABLE personal_keys ADD COLUMN IF NOT EXISTS litellm_token text;` + `ALTER TABLE environment_keys ADD COLUMN IF NOT EXISTS litellm_token text;` statements followed by two `CREATE UNIQUE INDEX IF NOT EXISTS personal_keys_litellm_token_uniq ON personal_keys (litellm_token) WHERE litellm_token IS NOT NULL;` and the symmetric `environment_keys_litellm_token_uniq` partial unique index (per D-01 and CONTEXT specifics — tokens are globally unique within a LiteLLM instance; NO CHECK constraint because LiteLLM hex tokens have no fixed prefix). Lead the file with a header comment block mirroring 000002 shape: phase reference `Phase 02.2 schema addition per Plan 02.2-01 (Gap G1 fix)`, single concern (`litellm_token`), explanation that the orphan loop's set-difference key changes from `litellm_user_id` (000002) to `litellm_token` (this migration) because LiteLLM `/key/list` returns `token` not `key_id`, and the closing invariant `No new tables. No removed columns. No row writes.`. Create the matching `000003_litellm_token.down.sql` that DROPs the two indexes FIRST, then DROPs the two columns via `ALTER TABLE … DROP COLUMN IF EXISTS litellm_token;`. Use `IF NOT EXISTS` / `IF EXISTS` on every statement (re-apply safety, per 000002 pattern). Per D-02, document in the up.sql header that Phase 3 SSO populates the column on `/key/generate`; Phase 02.2 ships the schema + reader-side only.</action>
  <verify>
    <automated>test -f db/migrations/000003_litellm_token.up.sql && test -f db/migrations/000003_litellm_token.down.sql && grep -q "ADD COLUMN IF NOT EXISTS litellm_token text" db/migrations/000003_litellm_token.up.sql && grep -q "CREATE UNIQUE INDEX IF NOT EXISTS personal_keys_litellm_token_uniq" db/migrations/000003_litellm_token.up.sql && grep -q "CREATE UNIQUE INDEX IF NOT EXISTS environment_keys_litellm_token_uniq" db/migrations/000003_litellm_token.up.sql && grep -q "WHERE litellm_token IS NOT NULL" db/migrations/000003_litellm_token.up.sql && grep -q "DROP COLUMN IF EXISTS litellm_token" db/migrations/000003_litellm_token.down.sql && grep -q "DROP INDEX IF EXISTS personal_keys_litellm_token_uniq" db/migrations/000003_litellm_token.down.sql && grep -q "DROP INDEX IF EXISTS environment_keys_litellm_token_uniq" db/migrations/000003_litellm_token.down.sql</automated>
  </verify>
  <acceptance_criteria>
    - File db/migrations/000003_litellm_token.up.sql exists
    - File db/migrations/000003_litellm_token.down.sql exists
    - up.sql contains `ALTER TABLE personal_keys       ADD COLUMN IF NOT EXISTS litellm_token text;`
    - up.sql contains `ALTER TABLE environment_keys    ADD COLUMN IF NOT EXISTS litellm_token text;`
    - up.sql contains `CREATE UNIQUE INDEX IF NOT EXISTS personal_keys_litellm_token_uniq ON personal_keys (litellm_token) WHERE litellm_token IS NOT NULL;`
    - up.sql contains `CREATE UNIQUE INDEX IF NOT EXISTS environment_keys_litellm_token_uniq ON environment_keys (litellm_token) WHERE litellm_token IS NOT NULL;`
    - up.sql header comment references `Phase 02.2`, `Plan 02.2-01`, `Gap G1`, and Phase 3 SSO write path
    - up.sql contains the closing invariant string `No new tables. No removed columns. No row writes.`
    - down.sql DROPs the two indexes BEFORE dropping the two columns
    - No CHECK constraint on litellm_token anywhere in the up.sql
    - `grep -c "CHECK" db/migrations/000003_litellm_token.up.sql` returns 0
  </acceptance_criteria>
  <done>Two new migration files exist under db/migrations/, follow the 000002 column-add pattern (header comment + IF NOT EXISTS / IF EXISTS defense), add nullable `litellm_token text` to both key tables, create partial UNIQUE indexes, and the down.sql reverses cleanly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2a: Swap /key/info → /key/list endpoint + rename UserKeyInfo.KeyID → Token (internal/litellm/* only)</name>
  <files>internal/litellm/keyinfo.go, internal/litellm/types.go, internal/litellm/client.go</files>
  <read_first>
    internal/litellm/keyinfo.go,
    internal/litellm/types.go,
    internal/litellm/client.go,
    .planning/phases/02.2-phase-02-cleanup-gap-g1-fix-litellm-real-uat-path-invariant-/02.2-PATTERNS.md,
    .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-HUMAN-UAT.md
  </read_first>
  <action>Three coordinated edits inside `internal/litellm/`. NOTE: this commit will leave the tree non-building until Task 2b lands (orphan runnable still references `k.KeyID`); land Task 2a and Task 2b in the same merge — independent commits are OK for review auditability, but DO NOT push between them without 2b. (1) `internal/litellm/keyinfo.go`: in `ListUserKeys`, replace the path string `"/key/info?user_id=" + url.QueryEscape(userID)` with `"/key/list?user_id=" + url.QueryEscape(userID) + "&return_full_object=true&include_team_keys=false"` and update the `fmt.Errorf` wrap text from `litellm: decode GET /key/info: %w` to `litellm: decode GET /key/list: %w`. Update the function docstring (lines 39-50) to reference `/key/list?user_id=<id>&return_full_object=true&include_team_keys=false` and to note the response `token` field is the LiteLLM-internal opaque hex token (not ACH's `pkid_*`/`ekid_*`). Preserve the `§9.1: only the user_id and status code are logged — no response body.` comment verbatim. Preserve function signature and the `url.QueryEscape(userID)` call. (2) `internal/litellm/types.go`: rename the `KeyID string `json:"key_id"`` field on `UserKeyInfo` (line 275) to `Token string `json:"token"``. Update the struct's doc comment (lines 270-273) to reference `/key/list?user_id=<id>&return_full_object=true&include_team_keys=false` and call out that `Token` is the LiteLLM-internal opaque hex (not `pkid_*`/`ekid_*`). Update `ListUserKeysResponse` doc comment to reference `/key/list`; keep the `Keys []UserKeyInfo `json:"keys"`` field unchanged (D-03 — Plan 02 will validate the envelope key against real LiteLLM; CONTEXT does not pre-resolve a rename). Preserve `UserID`, `CreatedAt`, `KeyAlias` fields untouched, the Apache-2.0 license header, and the `omitempty` tag on `KeyAlias`. (3) `internal/litellm/client.go`: update the `ListUserKeys` method docstring above line 93 to reference `/key/list?user_id=<id>&return_full_object=true&include_team_keys=false` instead of `/key/info?user_id=<id>`. Method signature itself stays bit-identical (`ListUserKeys(ctx context.Context, userID string) ([]UserKeyInfo, error)`). D-03: do NOT add a `httptest.Server` wire-format test under `internal/litellm/keyinfo_test.go` — Plan 02's `scripts/uat-g1.sh` is the wire oracle.</action>
  <verify>
    <automated>grep -q 'path := "/key/list?user_id="' internal/litellm/keyinfo.go && grep -q 'return_full_object=true&include_team_keys=false' internal/litellm/keyinfo.go && ! grep -q '/key/info?user_id=' internal/litellm/keyinfo.go && grep -q 'Token     string    `json:"token"`' internal/litellm/types.go && ! grep -q '`json:"key_id"`' internal/litellm/types.go</automated>
  </verify>
  <acceptance_criteria>
    - internal/litellm/keyinfo.go contains the literal substring `/key/list?user_id=`
    - internal/litellm/keyinfo.go contains the literal substring `return_full_object=true&include_team_keys=false`
    - internal/litellm/keyinfo.go does NOT contain `/key/info?user_id=`
    - internal/litellm/keyinfo.go contains the literal substring `litellm: decode GET /key/list:`
    - internal/litellm/keyinfo.go retains the comment `§9.1: only the user_id and status code are logged — no response body.`
    - internal/litellm/types.go contains the field declaration `Token     string    \`json:"token"\``
    - internal/litellm/types.go does NOT contain any json tag `\`json:"key_id"\`` on UserKeyInfo
    - internal/litellm/types.go UserKeyInfo doc comment references `/key/list?user_id=<id>&return_full_object=true&include_team_keys=false`
    - internal/litellm/client.go method docstring for ListUserKeys references `/key/list` (not `/key/info`)
    - No new file under internal/litellm/ ending in `_test.go` was created (D-03 — no httptest fake-server)
  </acceptance_criteria>
  <done>The `/key/list` endpoint string + `UserKeyInfo.Token` field rename ship together inside `internal/litellm/`. Tree compiles only after Task 2b lands the orphan-runnable rename.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2b: Propagate KeyID→Token rename + OutcomeSuccess deletion + OutcomeRevoked add through internal/orphan/*</name>
  <files>internal/orphan/runnable.go, internal/orphan/runnable_test.go</files>
  <read_first>
    internal/orphan/runnable.go,
    internal/orphan/runnable_test.go,
    .planning/phases/02.2-phase-02-cleanup-gap-g1-fix-litellm-real-uat-path-invariant-/02.2-PATTERNS.md
  </read_first>
  <action>Two coordinated edits in `internal/orphan/`. (1) `internal/orphan/runnable.go`: REPLACE the constant `OutcomeSuccess = "success"` (line 43) with `OutcomeRevoked = "revoked"` — this is a one-shot rename per greenfield/no-compat-shims discipline. Do NOT keep `OutcomeSuccess` defined alongside; delete it outright (WARNING 6). Update the success-path `r.Audit.Info` call at line 231 from `"outcome", OutcomeSuccess` to `"outcome", OutcomeRevoked`. Replace every `k.KeyID` reference at lines 211, 215, 221, 230 with `k.Token`. On lines 225 and 234 (operational `r.Log.Info` calls with slog attr `"key_id", k.KeyID`), rename the attribute key to `"token"` and the value to `k.Token` — these are operational logs not audit events, but the rename keeps semantics aligned and matches pattern-mapper finding #1. CR-03 audit-discipline: `target.name` continues to carry the token string (the wire identifier) since it is what `RevokeKey` consumes. Do NOT add `token` to any audit attrs (`r.Audit.Info`) — the audit event already carries `target.name` which is the same value; doubling it would violate the no-leak discipline. (2) `internal/orphan/runnable_test.go`: sweep every `KeyID:` literal inside `fakeLiteLLM.userKeysByUser` maps (TestRunnable_TickOnce_OneOrphan + every other test in the file that builds a `litellm.UserKeyInfo` literal) and rename `KeyID:` → `Token:`. Test assertion strings (`"target.name" == "sk-orphan"`, etc.) stay verbatim — the wire string is what gets passed to `RevokeKey` so `target.name` continues to equal the token. Update any test referencing `OutcomeSuccess` to use `OutcomeRevoked` (grep `internal/orphan/runnable_test.go` for the literal `OutcomeSuccess` and rename to `OutcomeRevoked` — if the test asserts the audit outcome value, the assertion now checks for `"revoked"`). Preserve `assertNoForbiddenAttrs` enforcement, the test seam pattern (`r.ListUsers = func(...)`), and `key_alias` leak guard. Run `go build ./...` and `go test ./internal/orphan/... -count=1` to confirm the rename propagates cleanly.</action>
  <verify>
    <automated>go build ./... && go test ./internal/orphan/... -count=1 && grep -qE 'OutcomeRevoked[[:space:]]+=[[:space:]]+"revoked"' internal/orphan/runnable.go && [ "$(grep -c 'OutcomeSuccess' internal/orphan/ -r)" -eq 0 ] && ! grep -n 'k\.KeyID' internal/orphan/runnable.go && grep -q 'Token:' internal/orphan/runnable_test.go && ! grep -q 'KeyID:.*"sk-orphan"' internal/orphan/runnable_test.go</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` exits 0
    - `go test ./internal/orphan/... -count=1` exits 0
    - internal/orphan/runnable.go contains `OutcomeRevoked            = "revoked"` in the const block
    - `grep -rE "OutcomeSuccess" internal/orphan/` returns no matches (WARNING 6: dead constant deleted outright)
    - internal/orphan/runnable.go retains `OutcomeLiteLLMUnreachable` and `OutcomeRevokeFailed` in the const block
    - internal/orphan/runnable.go does NOT contain any reference to `k.KeyID` (grep -c returns 0)
    - internal/orphan/runnable.go success-path audit.Info call uses `OutcomeRevoked` (not `OutcomeSuccess`)
    - internal/orphan/runnable.go operational logs use slog attr `"token", k.Token` (not `"key_id", k.KeyID`)
    - internal/orphan/runnable_test.go renames every `KeyID:` literal inside `litellm.UserKeyInfo` structs to `Token:`
    - internal/orphan/runnable_test.go does NOT reference `OutcomeSuccess` anywhere
    - internal/orphan/runnable_test.go retains `assertNoForbiddenAttrs` invocation in every test that builds an audit event
  </acceptance_criteria>
  <done>Tree builds; orphan tests pass; `OutcomeSuccess` is gone codebase-wide; `OutcomeRevoked` is the only success-path enum.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2c: Update internal/db/litellm_users.go doc paragraph for Phase 02.2 invariant</name>
  <files>internal/db/litellm_users.go</files>
  <read_first>
    internal/db/litellm_users.go,
    .planning/phases/02.2-phase-02-cleanup-gap-g1-fix-litellm-real-uat-path-invariant-/02.2-CONTEXT.md
  </read_first>
  <action>Single edit. Update the doc paragraph at lines 45-49 from `Phase 2 invariant: the litellm_user_id column is freshly added, nullable, and never written by Phase 2 code (Phase 3 lands the SSO write path).` to `Phase 02.2 invariant: BOTH the litellm_user_id column (added in migration 000002) AND the litellm_token column (added in migration 000003) are nullable and never written by Phase 2 / 02.2 code (Phase 3 lands the SSO write path on /key/generate). Phase 02.2's orphan loop therefore returns an empty slice on every tick until Phase 3 ships — that is the expected steady-state.` SQL body untouched; the existing query is correct for Phase 02.2 steady-state. Run `go build ./...` and `go test ./internal/db/... -count=1` to confirm clean compile + tests still pass.</action>
  <verify>
    <automated>go build ./... && go test ./internal/db/... -count=1 && grep -q 'Phase 02.2 invariant' internal/db/litellm_users.go && grep -q 'litellm_token column' internal/db/litellm_users.go</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` exits 0
    - `go test ./internal/db/... -count=1` exits 0
    - internal/db/litellm_users.go doc comment contains the string `Phase 02.2 invariant`
    - internal/db/litellm_users.go doc comment references both `litellm_user_id` and `litellm_token`
    - No SQL query body changed (the existing reader query is correct for Phase 02.2 steady-state)
  </acceptance_criteria>
  <done>internal/db/litellm_users.go doc paragraph reflects Phase 02.2's parallel-column invariant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → LiteLLM HTTP API | untrusted upstream response crosses here; only user_id query param leaves; response bytes flow back through `c.makeRequest` and are JSON-decoded |
| operator → Postgres | trusted in-cluster; new `litellm_token` column accepts arbitrary-shape TEXT from Phase 3 SSO write path |
| operator audit log → stdout sink | structured JSON; CR-03 forbids plaintext / body / alias / err in audit attrs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.2-01-01 | Tampering | `/key/list` query string construction in keyinfo.go | mitigate | `url.QueryEscape(userID)` already wraps the user-controlled value; the appended literal `&return_full_object=true&include_team_keys=false` is constant — no second user-controlled segment. Keep the existing url.QueryEscape call (do not remove). |
| T-02.2-01-02 | Information Disclosure | Operational log at runnable.go line 225/234 | mitigate | Rename `"key_id", k.KeyID` → `"token", k.Token` in operational `r.Log.Info` only. Do NOT add `"token"` to any `r.Audit.Info` attrs — `target.name` already carries the same value and doubling would violate the no-leak discipline (CR-03). |
| T-02.2-01-03 | Information Disclosure | New `litellm_token` DB column | mitigate | Column nullable + UNIQUE partial index. NO CHECK constraint (LiteLLM hex tokens have no fixed prefix). The token is the LiteLLM-internal opaque key id (NOT the plaintext bearer `pk_`/`ek_` value) — schema-level DB-04 invariant is preserved because the bearer plaintext stays out of the column. |
| T-02.2-01-04 | Repudiation | Outcome enum rename | mitigate | One-shot rename: `OutcomeSuccess = "success"` is DELETED outright and replaced by `OutcomeRevoked = "revoked"` (greenfield discipline; no dead constants). Audit-event vocabulary aligns with Hub §18.2 outcome enum. Plan 2 grep matches `"outcome":"revoked"` against the JSON emitter (internal/audit/handler.go line 45 — `slog.NewJSONHandler`). |
| T-02.2-01-05 | Tampering | Migration 000003 down.sql | accept | down.sql is dev/disaster-recovery only (Plan 01-10 init container runs migrate up). DROP INDEX before DROP COLUMN (Postgres enforces this order natively, but explicit ordering documents intent). |
| T-02.2-01-SC | Tampering | npm/pip/cargo installs | mitigate | n/a — this plan adds zero new go.mod / package.json / Cargo.toml entries (pure-stdlib change). No package legitimacy gate required. |
</threat_model>

<verification>
After all four tasks land:
- `go build ./...` exits 0
- `go test ./internal/litellm/... ./internal/orphan/... ./internal/db/... -count=1` exits 0
- `golangci-lint run ./internal/litellm/... ./internal/orphan/... ./internal/db/...` exits 0 (if installed; skip if not)
- `make dev-up` + apply migration 000003 via `docker run --rm --network=host -e ACH_DB_URL=… ach-devtools:latest go run ./cmd/migrate` produces both new columns + both new partial unique indexes in the developer Postgres
- `make dev-down` cleans state
- `grep -rn "k\.KeyID" internal/orphan/` returns no matches
- `grep -rn "OutcomeSuccess" internal/` returns no matches
- `grep -rn "/key/info?user_id=" internal/` returns no matches
</verification>

<success_criteria>
- Gap G1 is reader-side closed: the only LiteLLM endpoint the orphan runnable hits to enumerate per-user keys is `/key/list?user_id=<id>&return_full_object=true&include_team_keys=false`.
- The struct field `UserKeyInfo.Token` (json tag `token`) replaces `KeyID` / `key_id`; downstream `orphan.Runnable.TickOnce` consumes `k.Token` for both the set-difference check (against `achKeySet`) and the `RevokeKey` call argument.
- The migration 000003 pair adds nullable `litellm_token text` to `personal_keys` and `environment_keys`, with partial unique indexes; no CHECK constraint.
- The audit-event outcome enum is one-shot-renamed: `OutcomeRevoked = "revoked"` is the only success constant; `OutcomeSuccess` is deleted codebase-wide. uat-g1.sh (Plan 02) greps the JSON form `"outcome":"revoked"`.
- The operational-log slog attribute on the success-path log line is `"token", k.Token` (not `"key_id", k.KeyID`).
- `internal/db/litellm_users.go` doc paragraph captures Phase 02.2 invariant: both `litellm_user_id` AND `litellm_token` are nullable until Phase 3 SSO writes them.
- D-03 honored: no httptest fake-server test added in `internal/litellm/keyinfo_test.go`. Plan 02 (`uat-g1.sh`) is the wire oracle.
</success_criteria>

<out_of_scope>
- Phase 3 SSO write path that populates `litellm_token` on `/key/generate` — captured as a downstream prerequisite in `must_haves.truths[6]` and reiterated in `internal/db/litellm_users.go` doc comment, but NOT shipped here.
- Wire-format unit test in `internal/litellm/keyinfo_test.go` via `httptest.Server` — D-03 rejects; Plan 02's live LiteLLM is the oracle.
- Any change to `internal/litellm/noop.go` — `NoopClient.ListUserKeys` returns `(nil, nil)` and is signature-stable per the interface (read-only verification only; pattern-mapper finding for client.go and noop.go marked self/read-only).
- Kind overlay changes — Phase 02.1 SC#3 SKIP discipline stays as-is; SC#3 reinstatement under kind needs a real LiteLLM Pod and tracks with Phase 3+.
</out_of_scope>

<output>
Create `.planning/phases/02.2-phase-02-cleanup-gap-g1-fix-litellm-real-uat-path-invariant-/02.2-01-SUMMARY.md` when done. Include the commit map (Tasks 2a / 2b / 2c may commit independently for review auditability but must land in the same merge — tree does not compile between 2a and 2b), the exact endpoint diff, the struct-field rename, the new outcome constant + the `OutcomeSuccess` deletion, the migration columns + indexes, and a note that Plan 02 is the wire oracle for end-to-end validation.
</output>
</content>
</invoke>