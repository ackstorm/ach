---
phase: 02.2-phase-02-cleanup-gap-g1-fix-litellm-real-uat-path-invariant-
plan: 03
subsystem: docs
tags: [docs, invariant-lift, https-only, scm-anonymous, project-decisions]

requires:
  - phase: 02.1-kind-e2e-overlay-config-dev-postgres-config-e2e-scripts-e2e-
    provides: Phase 02.1 commit 45b7558 (HTTPSource HTTPS-only lift) + commit 94f24b5 (SCM anonymous fetches) — the source events this plan documents
provides:
  - CLAUDE.md Security bullet scoped to Platform API ingress with HTTPSource lift note
  - CLAUDE.md new SCM source-types bullet (anonymous fetches optional for github/gitlab/bitbucket; S3/GCS retain mandatory auth)
  - .planning/PROJECT.md Key Decisions table: 2 new rows recording the invariant relaxations at architectural-decision tier
  - 14 in-place annotations across .planning/phases/02-*/ docs with canonical short form `LIFTED 2026-05-18 (Phase 02.1 commit 45b7558)`
affects:
  - Future readers of Phase 02 plan/SUMMARY/REVIEW/VERIFICATION docs: now see current truth in-place, no need to chase a separate concessions doc
  - Phase 3 planning: PROJECT.md Key Decisions table is the canonical decision log; future architectural concessions follow the same `Lifted Phase XX.Y (commit <sha>)` Outcome variant

tech-stack:
  added: []
  patterns:
    - "In-place doc rewrite with canonical short-form annotation (D-12, WARNING 7) — single grep-anchor string used uniformly in threat-model Disposition cells AND narrative parentheticals"
    - "Architectural-concession recording in PROJECT.md Key Decisions table with `Lifted Phase XX.Y (commit <sha>)` Outcome variant (D-14) — alongside the existing `✓ Good (per [[link]])` and `— Pending` variants"

key-files:
  created:
    - .planning/phases/02.2-phase-02-cleanup-gap-g1-fix-litellm-real-uat-path-invariant-/02.2-03-SUMMARY.md
  modified:
    - CLAUDE.md (Security bullet qualification + new SCM source-types bullet)
    - .planning/PROJECT.md (Key Decisions table: 2 new rows)
    - .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-02-PLAN.md (3 annotations: lines 43, 338, 542)
    - .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-02-SUMMARY.md (8 annotations: lines 9, 17, 37, 59, 87, 109, 118, 144)
    - .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-REVIEW.md (2 annotations: lines 129, 137)
    - .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-VERIFICATION.md (1 annotation: line 65)

key-decisions:
  - "In-place rewrite preserves original assertions and appends qualifying parenthetical — future readers see what was claimed AND how it has been amended (D-12)"
  - "Threat-model row Disposition column uses exact short-form string only; long-form prose stays in Mitigation Plan cell (WARNING 7 grep-anchor discipline)"
  - "DO-NOT-ANNOTATE list applied verbatim: PLAN.md:547 (T-02-02-08 SSRF row, unrelated mitigation) + REVIEW.md:254 (S3/GCS endpoint review, D-13 preserves HTTPS) (BLOCKER 3)"
  - "PROJECT.md Outcome column gains a third variant `Lifted Phase 02.1 (commit <sha>)` — distinct from `✓ Good (per [[link]])` and `— Pending` (D-14)"

requirements-completed: []

duration: 12min
completed: 2026-05-18
---

# Phase 02.2 Plan 03: HTTPS-only / SCM-auth invariant-lift in-place doc sweep Summary

**14 of 16 HTTPS-only / T-02-02-03 occurrences across 4 phase-02 docs annotated in-place with canonical short form `LIFTED 2026-05-18 (Phase 02.1 commit 45b7558)`; CLAUDE.md Security bullet scoped to Platform API ingress with new SCM source-types bullet; PROJECT.md Key Decisions table records both lifts at architectural-decision tier.**

## Performance

- **Duration:** ~12 minutes
- **Tasks:** 2 (Task 1 = 14-occurrence sweep, Task 2 = CLAUDE.md + PROJECT.md edits)
- **Files modified:** 6 (4 phase-02 docs + CLAUDE.md + .planning/PROJECT.md)
- **Files created:** 1 (this SUMMARY)
- **Commits:** 2

## Commit Map

| Task | Description | Commit |
|------|-------------|--------|
| 1 | In-place lift T-02-02-03 / HTTPS-only references in phase-02 docs (14 occurrences across 4 files) | `fa55997` |
| 2 | CLAUDE.md HTTPS-only qualification + SCM source-types bullet + PROJECT.md 2 new rows | `3747da0` |

## Task 1 Discovery Output (execution-time `grep -n`)

Pre-rewrite grep `grep -nE "HTTPS-only|T-02-02-03" <4 phase-02 docs>` — total **16 occurrences** (matches planning-time count):

```
02-02-PLAN.md:43:    - "HTTPSource fetcher refuses any URL whose scheme is not https:// — defense in depth above the CRD CEL Pattern marker (CLAUDE.md HTTPS-only invariant)"
02-02-PLAN.md:338:    - `func New(spec *achv1alpha1.HTTPSource) (*Fetcher, error)` — ... (defense in depth above the CRD CEL Pattern marker per CLAUDE.md HTTPS-only invariant; ...)
02-02-PLAN.md:542:| T-02-02-03 | Spoofing | HTTPSource: deployer typoing `http://` instead of `https://` | mitigate | Three-layer defense: ...
02-02-PLAN.md:547:| T-02-02-08 | Spoofing | SSRF via HTTPSource pointing at internal RFC1918 addresses | accept | ... HTTPS-only invariant + namespace-scoped operator + RBAC ...
02-02-SUMMARY.md:9:    provides: api/ach/v1alpha1 source subtypes (...); SourceAuthSecretRef; HTTPS-only Pattern marker
02-02-SUMMARY.md:17:  - HTTPS-only invariant defense at construction time (defense in depth above CRD CEL Pattern marker)
02-02-SUMMARY.md:37:    - "HTTPS-only defense in depth at Fetcher.New() construction (above CRD CEL Pattern marker)"
02-02-SUMMARY.md:59:    - internal/sources/http/fetcher.go (216 lines — conditional-GET + HTTPS-only)
02-02-SUMMARY.md:87:**Six per-source-type fetcher subpackages (...) with a uniform Fetcher interface, ..., and HTTPS-only defense in depth — consumed by Plans 02-05 and 02-06 reconcilers.**
02-02-SUMMARY.md:109:  - **http** — stdlib `net/http` with conditional-GET (...); HTTPS-only refusal at `New()` construction (defense in depth above CRD CEL Pattern marker).
02-02-SUMMARY.md:118:3. **Task 3: HTTP fetcher (conditional-GET, HTTPS-only)** — `e3a6dff` (feat)
02-02-SUMMARY.md:144:- `internal/sources/http/fetcher.go` — HTTP fetcher (conditional-GET, HTTPS-only)
02-REVIEW.md:129:The implementation generally honors the load-bearing invariants — parameterized SQL throughout, HMAC-pepper fail-fast at startup, HTTPS-only enforcement on the generic HTTP source, ...
02-REVIEW.md:137:Beyond the blockers, the review surfaced 9 warnings (notably: S3 endpoint override bypasses the HTTPS-only invariant; ...) and 6 info-level quality items.
02-REVIEW.md:254:This contradicts CLAUDE.md's "HTTPS-only via deployment-configured `ACH_BASE_URL` — no HTTP escape hatch." ...
02-VERIFICATION.md:65:| `internal/sources/http/fetcher.go` | HTTPS-only conditional-GET fetcher | VERIFIED | Exists; refuses non-https:// at construction |
```

Note: live grep finds **8** matches in `02-02-SUMMARY.md` (line 9 included), one more than CONTEXT D-12's enumeration of 7 (which omitted line 9). The plan's BLOCKER-3 budget of "16 total / 14 rewrites" remains exactly correct because the DO-NOT-ANNOTATE count is 2 (not 3). Re-grep methodology (pattern-mapper finding #3) is authoritative.

### SCM-auth parallel sweep discovery (D-12 final sentence)

`grep -rnE "AuthSecretRef.*Required|authSecretRef.*required" .planning/phases/02-external-refs-marketplace-operator-reconciliation/` — **0 matches** discovered. The SCM-required claim sweep is vacuously satisfied: there are no `authSecretRef.*required` assertions remaining in the phase-02 docs to qualify. The lift surfaces only via CLAUDE.md (Task 2) and PROJECT.md (Task 2).

## Per-Line Disposition Table (all 16 occurrences)

| file:line | match content (truncated) | disposition | reason (skip only) |
|-----------|---------------------------|-------------|---------------------|
| 02-02-PLAN.md:43 | `must_haves.truths` — HTTPSource fetcher refuses non-https:// ... (CLAUDE.md HTTPS-only invariant) | qualify-inline | — |
| 02-02-PLAN.md:338 | Task 3 action — Fetcher.New rejects non-https:// per CLAUDE.md HTTPS-only invariant | qualify-inline | — |
| 02-02-PLAN.md:542 | T-02-02-03 threat-model row Disposition column | rewrite | — |
| 02-02-PLAN.md:547 | T-02-02-08 SSRF row — HTTPS-only invariant + namespace-scoped ... | skip-with-reason | T-02-02-08 SSRF row, HTTPS-only mention is background framing for an unrelated mitigation (SSRF), not a load-bearing scheme assertion |
| 02-02-SUMMARY.md:9 | requires frontmatter — Phase 1 provided HTTPS-only Pattern marker | qualify-inline | — |
| 02-02-SUMMARY.md:17 | provides frontmatter — HTTPS-only invariant defense at construction | qualify-inline | — |
| 02-02-SUMMARY.md:37 | patterns frontmatter — HTTPS-only defense in depth at Fetcher.New() | qualify-inline | — |
| 02-02-SUMMARY.md:59 | key-files frontmatter — internal/sources/http/fetcher.go conditional-GET + HTTPS-only | qualify-inline | — |
| 02-02-SUMMARY.md:87 | one-liner body — uniform Fetcher interface ... HTTPS-only defense in depth | qualify-inline | — |
| 02-02-SUMMARY.md:109 | accomplishments body — http fetcher HTTPS-only refusal at New() | qualify-inline | — |
| 02-02-SUMMARY.md:118 | task commits — Task 3: HTTP fetcher (conditional-GET, HTTPS-only) | qualify-inline | — |
| 02-02-SUMMARY.md:144 | files-created — internal/sources/http/fetcher.go conditional-GET, HTTPS-only | qualify-inline | — |
| 02-REVIEW.md:129 | review summary — HMAC-pepper fail-fast at startup, HTTPS-only enforcement | qualify-inline | — |
| 02-REVIEW.md:137 | warnings summary — S3 endpoint override bypasses the HTTPS-only invariant | qualify-inline | — |
| 02-REVIEW.md:254 | WR-01 S3 endpoint review — quoting CLAUDE.md HTTPS-only sentence to argue S3/GCS still require HTTPS | skip-with-reason | S3/GCS endpoint review, D-13 explicitly preserves S3/GCS HTTPS requirement |
| 02-VERIFICATION.md:65 | file-table row — HTTPS-only conditional-GET fetcher | qualify-inline | — |

**Totals:** 14 rewrite/qualify-inline + 2 skip-with-reason = 16 ✓

## Per-File Occurrence Counts

| File | Pre-rewrite matches | Post-rewrite annotations (canonical short string) | Notes |
|------|---------------------|----------------------------------------------------|-------|
| 02-02-PLAN.md | 4 (incl. 1 DO-NOT-ANNOTATE) | 3 | Line 547 (T-02-02-08 SSRF) preserved unchanged |
| 02-02-SUMMARY.md | 8 | 8 | All eight qualify-inline |
| 02-REVIEW.md | 3 (incl. 1 DO-NOT-ANNOTATE) | 2 | Line 254 (S3 review) preserved unchanged |
| 02-VERIFICATION.md | 1 | 1 | Single VERIFIED-row annotation |
| **TOTAL** | **16** | **14** | BLOCKER 3 budget exact |

## CLAUDE.md Diff Snippet (Security bullet + new SCM bullet)

**Before:**
```
- **Security**: HMAC-SHA-256 with server-side pepper for credential hashes; pepper held outside Postgres. Plaintext key values MUST NOT be persisted anywhere (DB, Redis, logs, audit, metrics, traces). HTTPS-only via deployment-configured `ACH_BASE_URL` — no HTTP escape hatch.
```

**After:**
```
- **Security**: HMAC-SHA-256 with server-side pepper for credential hashes; pepper held outside Postgres. Plaintext key values MUST NOT be persisted anywhere (DB, Redis, logs, audit, metrics, traces). HTTPS-only via deployment-configured `ACH_BASE_URL` for Platform API ingress — no HTTP escape hatch on the user-facing surface. Upstream `HTTPSource` admits `http://` (Phase 02.1 lifted the strict-https CRD pattern for hermetic fixture-server / private-network paths).
- **SCM source-types** (github/gitlab/bitbucket): accept anonymous fetches — `authSecretRef` is optional. S3/GCS still require credentials.
```

Preserved verbatim: HMAC-SHA-256 opening sentence (CR-03/DB-04 invariant); `Plaintext key values MUST NOT be persisted` sentence; 2-space bullet indentation; all other Constraints-section bullets.

## PROJECT.md Key Decisions Table — 2 New Rows Appended

```markdown
| `HTTPS-only` invariant relaxed for upstream `HTTPSource` | Hermetic e2e fixture-server (in-cluster nginx) requires `http://` during Phase 02.1; production deployments still use `https://` by convention | Lifted Phase 02.1 (commit 45b7558) |
| SCM `authSecretRef` made optional | Anonymous public-repo fetches needed for SC1 fixture (`octocat/Hello-World @ master`); S3/GCS retain mandatory auth | Lifted Phase 02.1 (commit 94f24b5) |
```

Appended after the existing last row (`ACH spec source of truth is local .md, not gist`) and before the `## Evolution` heading. 3-column structure preserved (Decision / Rationale / Outcome). Outcome column uses the new `Lifted Phase 02.1 (commit <sha>)` variant alongside the existing `✓ Good (per [[link]])` and `— Pending` variants.

## Confirmation: No Sidecar Concessions File

```
$ test -f .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-CONCESSIONS.md && echo PRESENT || echo ABSENT
ABSENT
```

D-12 explicit prohibition honored: no `02-CONCESSIONS.md` file created. In-place rewrite is the single source of truth.

## Final Grep-Anchor Sum (acceptance gate)

```
$ grep -c "LIFTED 2026-05-18 (Phase 02.1 commit 45b7558)" \
    .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-02-PLAN.md \
    .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-02-SUMMARY.md \
    .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-REVIEW.md \
    .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-VERIFICATION.md \
    | awk -F: '{s+=$2} END {print s}'
14
```

Exactly 14, matching BLOCKER 3 budget (16 total occurrences − 2 DO-NOT-ANNOTATE = 14).

## Deviations from Plan

None — plan executed exactly as written. The plan's WARNING 7 / pattern-mapper finding #3 (re-grep at execution time) correctly anticipated that CONTEXT D-12's per-file line numbers were approximate. Live grep found one extra match in `02-02-SUMMARY.md` (line 9, `HTTPS-only Pattern marker`) that CONTEXT D-12's enumeration omitted; the BLOCKER 3 budget of "16 total / 14 rewrites" remained exact because the DO-NOT-ANNOTATE count is 2 not 3.

The merge of `main` into the worktree branch (`worktree-agent-a441a558dff08fb32`) at the start of execution brought in the Phase 02.1 commits (45b7558 + 94f24b5) and the 02.2 planning artifacts; this is a standard worktree-rebase step and is not a deviation from the plan.

## Issues Encountered

None — pure docs work, no build/test surface affected.

## User Setup Required

None — documentation-only changes.

## Next Phase Readiness

- **Phase 02.2 closure:** This plan + Plans 02.2-01 (G1 fix) + 02.2-02 (LiteLLM-real UAT path) close Phase 02.2.
- **Phase 3 (SSO):** PROJECT.md Key Decisions table is now the canonical decision log for lifted invariants; future architectural concessions follow the same `Lifted Phase XX.Y (commit <sha>)` Outcome variant. The SCM-anonymous bullet in CLAUDE.md telegraphs to future contributors that S3/GCS retain mandatory auth (no further relaxation in Phase 3).
- **No blockers** for downstream phases. Pure-docs synchronization with Phase 02.1 reality.

## Self-Check: PASSED

Commits exist:
- `fa55997` (Task 1, in-place sweep): FOUND
- `3747da0` (Task 2, CLAUDE.md + PROJECT.md): FOUND

Key files exist:
- `CLAUDE.md`: FOUND (modified)
- `.planning/PROJECT.md`: FOUND (modified)
- `.planning/phases/02-external-refs-marketplace-operator-reconciliation/02-02-PLAN.md`: FOUND (modified)
- `.planning/phases/02-external-refs-marketplace-operator-reconciliation/02-02-SUMMARY.md`: FOUND (modified)
- `.planning/phases/02-external-refs-marketplace-operator-reconciliation/02-REVIEW.md`: FOUND (modified)
- `.planning/phases/02-external-refs-marketplace-operator-reconciliation/02-VERIFICATION.md`: FOUND (modified)
- `.planning/phases/02.2-.../02.2-03-SUMMARY.md`: FOUND (this file)

Acceptance gates:
- `grep -c "LIFTED 2026-05-18 (Phase 02.1 commit 45b7558)"` summed across 4 phase-02 docs = **14** ✓
- `02-CONCESSIONS.md` ABSENT ✓
- PLAN.md:547 (T-02-02-08 SSRF) UNANNOTATED ✓
- REVIEW.md:254 (S3/GCS review) UNANNOTATED ✓
- CLAUDE.md `Platform API ingress` present (1 occurrence) ✓
- CLAUDE.md `SCM source-types` present (1 occurrence) ✓
- PROJECT.md `Lifted Phase 02.1 (commit 45b7558)` present (1 occurrence) ✓
- PROJECT.md `Lifted Phase 02.1 (commit 94f24b5)` present (1 occurrence) ✓
- PROJECT.md `## Evolution` heading preserved (1 occurrence) ✓
- No `02-external-repos` typo regression in 02.2-*/ ✓

---

*Phase: 02.2-phase-02-cleanup-gap-g1-fix-litellm-real-uat-path-invariant-*
*Completed: 2026-05-18*
