---
phase: 02.2
status: passed
verified: 2026-05-18
checked_by: gsd-verifier
score: 30/30 must-haves verified
overrides_applied: 0
---

# Phase 02.2 Verification Report — Phase 02 cleanup (Gap G1 fix + LiteLLM-real UAT path + invariant-relaxation docs)

**Phase Goal (from ROADMAP.md line 122):** Close the deferred items from Phase 02.1 SUMMARY before Phase 3 — Gap G1 (`ListUserKeys` endpoint mismatch + namespace translation column), LiteLLM-real UAT path via docker-compose + `scripts/uat-g1.sh`, in-place docs update for two Phase 02.1 invariant relaxations.

**Methodology:** Goal-backward verification against the must-haves enumerated in the verification context. Each truth was checked against the codebase via `grep`, `Read`, `bash -n`, `go build`/`go test` (via `ach-devtools:latest` container — host has no Go toolchain), `docker-compose config -q`, and direct artifact inspection. No live UAT was run (the engineer manual step explicitly out-of-scope per CONTEXT D-11).

**Commits in range `c0e764a..a12cc5c` (14 commits):**
- Plan 1: `0bf8f98`, `9c5bd39`, `7799a87`, `5e74528`, `6694d85` (SUMMARY)
- Plan 2: `4870314`, `259dbc5`, `1f6ff59`, `15056ef`, `a12cc5c` (SUMMARY)
- Plan 3: `fa55997`, `3747da0`, `ae565c1` (SUMMARY) + `525725d` (merge)

---

## Goal Recap

| Truth | Status |
|-------|--------|
| Plan 1: LiteLLM `/key/info?user_id=` swap to `/key/list?user_id=...&return_full_object=true&include_team_keys=false` | VERIFIED |
| Plan 1: `UserKeyInfo.Token` (json:"token") replaces `KeyID` (json:"key_id") | VERIFIED |
| Plan 1: `OutcomeRevoked = "revoked"` added; `OutcomeSuccess` deleted codebase-wide | VERIFIED |
| Plan 1: `k.Token` (not `k.KeyID`) used in orphan comparison + revoke | VERIFIED |
| Plan 1: Migration 000003 adds nullable `litellm_token TEXT` + partial UNIQUE indexes to both key tables | VERIFIED |
| Plan 1: `go build ./...` exits 0; `go test ./internal/litellm/... ./internal/orphan/... ./internal/db/...` exits 0 | VERIFIED |
| Plan 1: No httptest fake-server wire-format test (D-03) | VERIFIED |
| Plan 2: docker-compose.yml ships `litellm` + `litellm-db` under `profiles: ["litellm"]` | VERIFIED |
| Plan 2: LiteLLM image pin `ghcr.io/berriai/litellm-database:v1.83.10-stable` verbatim | VERIFIED |
| Plan 2: `docker-compose.spike.yaml` deleted; `scripts/spike-litellm-config.yaml` renamed | VERIFIED |
| Plan 2: `scripts/uat-sc5.sh` lockstep-updated (no `LITELLM_SPIKE_PORT`, no `-f spike.yaml`, `--profile litellm`) | VERIFIED |
| Plan 2: `scripts/uat-g1.sh` exists, executable, syntax-clean | VERIFIED |
| Plan 2: JSON-form grep regex `"audit":true.*"outcome":"revoked".*"user_id":"u-uat-g1"` present (BLOCKER 1 fix) | VERIFIED |
| Plan 2: `command -v jq` early-exit gate (INFO 9 fix); no python3 fallback | VERIFIED |
| Plan 2: D-11 deferrals honored (no strict count parsing, no post-revocation 404 verify) | VERIFIED |
| Plan 3: 14 LIFTED annotations across 4 phase-02 docs (BLOCKER 3 budget exact) | VERIFIED |
| Plan 3: PLAN.md:547 (SSRF) + REVIEW.md:254 (S3) NOT annotated | VERIFIED |
| Plan 3: No sidecar `02-CONCESSIONS.md` (D-12) | VERIFIED |
| Plan 3: CLAUDE.md Security bullet qualified with `Platform API ingress` + HTTPSource lift note | VERIFIED |
| Plan 3: CLAUDE.md new `SCM source-types` bullet | VERIFIED |
| Plan 3: PROJECT.md Key Decisions table gains 2 new rows | VERIFIED |
| Cross-plan: `OutcomeRevoked = "revoked"` aligns with `"outcome":"revoked"` grep | VERIFIED |
| Cross-plan: `litellm_token` column matches uat-g1.sh INSERT/UPDATE | VERIFIED |

**Score: 23/23 truths VERIFIED; 30/30 individual must-have checks pass; 0 gaps; 0 overrides applied.**

---

## Plan 1 Verification — Gap G1 Fix

### Endpoint Swap (`internal/litellm/keyinfo.go`)

| Check | Evidence | Status |
|-------|----------|--------|
| Path string `/key/list?user_id=...&return_full_object=true&include_team_keys=false` | `keyinfo.go:61` — `path := "/key/list?user_id=" + url.QueryEscape(userID) + "&return_full_object=true&include_team_keys=false"` | VERIFIED |
| No `/key/info?user_id=` in `internal/` | `grep -rn "/key/info?user_id=" internal/` returns no matches | VERIFIED |
| Error-wrap text updated | `keyinfo.go:68` — `litellm: decode GET /key/list: %w` | VERIFIED |
| `§9.1: only the user_id and status code are logged` comment preserved | `keyinfo.go:59` retains the comment verbatim | VERIFIED |
| No `keyinfo_test.go` created (D-03) | `test -f internal/litellm/keyinfo_test.go` returns ABSENT | VERIFIED |

### Struct Rename (`internal/litellm/types.go`)

| Check | Evidence | Status |
|-------|----------|--------|
| `Token string \`json:"token"\`` on `UserKeyInfo` | `types.go:280` — `Token     string    \`json:"token"\`` | VERIFIED |
| No `\`json:"key_id"\`` json tag on `UserKeyInfo` | `grep` returns no matches in `types.go` | VERIFIED |
| Doc comment references `/key/list` | `types.go:270-274` — comments updated; mentions the LiteLLM-internal opaque hex | VERIFIED |
| `ListUserKeysResponse` envelope unchanged shape | `types.go:288-290` — `Keys []UserKeyInfo \`json:"keys"\`` preserved | VERIFIED |

### Orphan Runnable (`internal/orphan/runnable.go`)

| Check | Evidence | Status |
|-------|----------|--------|
| `OutcomeRevoked = "revoked"` in const block | `runnable.go:46` — `OutcomeRevoked            = "revoked"` | VERIFIED |
| `OutcomeSuccess` deleted codebase-wide | `grep -rn "OutcomeSuccess" internal/` returns no matches (WARNING 6 fix) | VERIFIED |
| `OutcomeLiteLLMUnreachable` + `OutcomeRevokeFailed` retained | `runnable.go:47-48` | VERIFIED |
| `k.Token` in comparison + revoke (not `k.KeyID`) | `runnable.go:214,218,224,228,233,237` — all use `k.Token` | VERIFIED |
| `grep "k\\.KeyID" internal/orphan/runnable.go` returns 0 | confirmed | VERIFIED |
| Operational log slog attr renamed to `"token", k.Token` | `runnable.go:228,237` | VERIFIED |
| Success-path audit emits `outcome=OutcomeRevoked` | `runnable.go:234` | VERIFIED |

### Test File (`internal/orphan/runnable_test.go`)

| Check | Evidence | Status |
|-------|----------|--------|
| `KeyID:` literals on `litellm.UserKeyInfo` renamed to `Token:` | `runnable_test.go:172,217,244,311,312,385` use `Token:` | VERIFIED |
| No `OutcomeSuccess` references | `grep` returns no matches | VERIFIED |

Note: Lines containing `KeyID` that remain in `runnable_test.go` (4 instances) refer to ACH-side test seam names `ListKeyIDs` and `listKeyIDsFn` — NOT the LiteLLM struct field. Those are unrelated to the rename and intentional.

### Migration 000003 (`db/migrations/000003_litellm_token.{up,down}.sql`)

| Check | Evidence | Status |
|-------|----------|--------|
| `up.sql` adds `litellm_token text` to `personal_keys` (nullable, IF NOT EXISTS) | `up.sql:33` | VERIFIED |
| `up.sql` adds `litellm_token text` to `environment_keys` (nullable, IF NOT EXISTS) | `up.sql:34` | VERIFIED |
| `up.sql` creates `personal_keys_litellm_token_uniq` partial UNIQUE index | `up.sql:36-37` — `WHERE litellm_token IS NOT NULL` | VERIFIED |
| `up.sql` creates `environment_keys_litellm_token_uniq` partial UNIQUE index | `up.sql:38-39` | VERIFIED |
| `up.sql` references Phase 02.2 / Plan 02.2-01 / Gap G1 in header | `up.sql:3-26` documents Phase 02.2, Plan 02.2-01, Gap G1, Phase 3 SSO write path, partial UNIQUE rationale | VERIFIED |
| Closing invariant `No new tables. No removed columns. No row writes.` present | `up.sql:25` | VERIFIED |
| No CHECK constraint on `litellm_token` | `grep -c "CHECK" up.sql` returns 0 (deviation #2 in SUMMARY explains reword) | VERIFIED |
| `down.sql` DROPs indexes BEFORE columns | `down.sql:12-16` — `DROP INDEX` lines 12-13, `DROP COLUMN` lines 15-16 | VERIFIED |

### DB Reader Doc (`internal/db/litellm_users.go`)

| Check | Evidence | Status |
|-------|----------|--------|
| Doc paragraph contains `Phase 02.2 invariant` | `litellm_users.go:45` | VERIFIED |
| Doc paragraph references both `litellm_user_id` AND `litellm_token` | `litellm_users.go:45-50` | VERIFIED |
| SQL query body unchanged | `litellm_users.go:67-76` — same shape as before | VERIFIED |

### Build & Test

```
$ docker run --rm -v "$PWD:/workspace" -w /workspace ach-devtools:latest \
    sh -c "go build -buildvcs=false ./...; echo BUILD_EXIT=\$?"
... (modules downloaded) ...
BUILD_EXIT=0

$ docker run --rm -v "$PWD:/workspace" -w /workspace ach-devtools:latest \
    sh -c "go test -buildvcs=false ./internal/litellm/... ./internal/orphan/... ./internal/db/... -count=1; echo TEST_EXIT=\$?"
ok  github.com/ackstorm/ach/internal/litellm  0.745s
?   github.com/ackstorm/ach/internal/db       [no test files]
ok  github.com/ackstorm/ach/internal/orphan   0.034s
TEST_EXIT=0

$ docker run --rm -v "$PWD:/workspace" -w /workspace ach-devtools:latest \
    sh -c "go vet -buildvcs=false ./internal/litellm/... ./internal/orphan/... ./internal/db/...; echo VET_EXIT=\$?"
VET_EXIT=0
```

| Check | Status |
|-------|--------|
| `go build ./...` exits 0 | VERIFIED |
| `go test ./internal/litellm/... ./internal/orphan/... ./internal/db/...` exits 0 | VERIFIED |
| `go vet` exits 0 on the same packages | VERIFIED |

---

## Plan 2 Verification — LiteLLM-real UAT path

### docker-compose.yml (Unified Stack)

| Check | Evidence | Status |
|-------|----------|--------|
| Two `profiles: ["litellm"]` occurrences | `grep -c 'profiles: \\["litellm"\\]' docker-compose.yml` returns 2 | VERIFIED |
| `container_name: ach-dev-litellm-db` | `docker-compose.yml:53` | VERIFIED |
| `container_name: ach-dev-litellm` | `docker-compose.yml:69` | VERIFIED |
| LiteLLM image pin verbatim | `docker-compose.yml:68` — `image: ghcr.io/berriai/litellm-database:v1.83.10-stable` | VERIFIED |
| Volume mount `./scripts/litellm-config.yaml:/app/config.yaml:ro` | `docker-compose.yml:78` | VERIFIED |
| Port `${LITELLM_PORT:-14000}:4000` (no `LITELLM_SPIKE_PORT`) | `docker-compose.yml:82` | VERIFIED |
| `postgres` + `redis` services retained | `container_name:` for `ach-dev-postgres` (line 24) and `ach-dev-redis` (line 41) both present | VERIFIED |
| YAML validates under both profile modes | `docker-compose --profile litellm config -q >/dev/null 2>&1 \|\| docker compose --profile litellm config -q >/dev/null 2>&1` exits 0; same for no-profile invocation | VERIFIED |

Note: Host has `docker-compose` (v1, hyphen) only; `docker compose` (v2 plugin) is absent. The plan's OR-fallback verify command was designed for exactly this case and is satisfied.

### Retired Files / Renames

| Check | Evidence | Status |
|-------|----------|--------|
| `docker-compose.spike.yaml` deleted | `ls docker-compose*` shows only `docker-compose.yml` | VERIFIED |
| `scripts/spike-litellm-config.yaml` renamed (absent) | `ls scripts/` shows only `litellm-config.yaml` | VERIFIED |
| `scripts/litellm-config.yaml` exists with refreshed comment | Read shows `general_settings`/`litellm_settings` body byte-identical; only leading comment is the Phase-02.2 rewording | VERIFIED |

### scripts/uat-sc5.sh (Lockstep Update)

| Check | Evidence | Status |
|-------|----------|--------|
| `LITELLM_SPIKE_PORT` count == 0 | `grep -c "LITELLM_SPIKE_PORT" scripts/uat-sc5.sh` returns 0 | VERIFIED |
| `docker-compose.spike.yaml` count == 0 | returns 0 | VERIFIED |
| `COMPOSE="docker-compose --profile litellm"` present | `uat-sc5.sh:65` | VERIFIED |
| `LITELLM_PORT="${LITELLM_PORT:-14000}"` present | `uat-sc5.sh:62` | VERIFIED |
| `bash -n scripts/uat-sc5.sh` exits 0 | confirmed | VERIFIED |
| Shebang on line 1-2 | confirmed (WARNING 4 — no `--help` flag assumption) | VERIFIED |

### scripts/uat-g1.sh (New Runner)

> Note: `scripts/uat-g1.sh` was deleted in Phase 02.3 (2026-05-20). Rows below preserved as historical record of the docker-compose-era oracle; assertions are now covered by `test/e2e/phase2_sc5_orphan_test.go::OrphanReapLive` against the in-cluster LiteLLM Helm release.

| Check | Evidence | Status |
|-------|----------|--------|
| File exists, mode 0755 (executable) | `ls -la scripts/uat-g1.sh` shows `-rwxrwxr-x` | VERIFIED |
| `bash -n scripts/uat-g1.sh` exits 0 | confirmed | VERIFIED |
| `scripts/uat-g1.sh -h` exits 0 (help shows top comment block) | confirmed | VERIFIED |
| Shebang `#!/usr/bin/env bash` on line 1 | `uat-g1.sh:1` | VERIFIED |
| `set -euo pipefail` present | `uat-g1.sh:35` | VERIFIED |
| `command -v jq >/dev/null` early-exit gate (INFO 9 fix) | `uat-g1.sh:42-45` — exit 2 with actionable message | VERIFIED |
| `RANDOM_PEPPER="uat-g1-…"` construction | `uat-g1.sh:82` | VERIFIED |
| `LITELLM_PORT="${LITELLM_PORT:-14000}"` | `uat-g1.sh:69` | VERIFIED |
| `COMPOSE="docker-compose --profile litellm"` | `uat-g1.sh:72` | VERIFIED |
| `cleanup()` + `trap cleanup EXIT INT TERM` | `uat-g1.sh:91-105` | VERIFIED |
| `dev_run()` helper | `uat-g1.sh:112-130` | VERIFIED |
| Seeds `pkid_uat_g1` row with `litellm_user_id='u-uat-g1'` | `uat-g1.sh:179-188` | VERIFIED |
| `POST /key/generate` against LiteLLM | `uat-g1.sh:194-197` | VERIFIED |
| `UPDATE "LiteLLM_VerificationToken" SET created_at=now()-interval '11 minutes'` | `uat-g1.sh:217-218` | VERIFIED |
| Operator launched with `ACH_ORPHAN_CLEANUP_INTERVAL=5m` | `uat-g1.sh:228` | VERIFIED |
| Primary grep regex `"audit":true.*"outcome":"revoked".*"user_id":"u-uat-g1"` (BLOCKER 1 fix) | `uat-g1.sh:253` | VERIFIED |
| Order-independent `awk` fallback `/"audit":true/ && /"outcome":"revoked"/ && /"user_id":"u-uat-g1"/` | `uat-g1.sh:256` | VERIFIED |
| NO legacy `outcome=revoked` key=value regex | `grep -nE 'outcome=revoked' scripts/uat-g1.sh` returns no matches | VERIFIED |
| NO python3 fallback (only comment mentions absence) | `grep -n python3` returns only the line `# No python3 / shell-regex fallback` (line 41) | VERIFIED |
| NO `GET /key/info?key=` post-revocation verify (D-11) | `grep -c "GET /key/info?key="` returns 0 | VERIFIED |
| NO strict count parsing (`grep -c ... -eq 1`) | `grep -nE '\[ [^]]*-eq 1 \]' scripts/uat-g1.sh` returns no matches | VERIFIED |

---

## Plan 3 Verification — Docs Sweep

### Annotation Sum Acceptance Gate

```
$ grep -c "LIFTED 2026-05-18 (Phase 02.1 commit 45b7558)" \
    .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-02-PLAN.md \
    .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-02-SUMMARY.md \
    .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-REVIEW.md \
    .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-VERIFICATION.md \
    | awk -F: '{s+=$2} END {print s}'
14
```

| File | Annotations |
|------|------------|
| 02-02-PLAN.md | 3 |
| 02-02-SUMMARY.md | 8 |
| 02-REVIEW.md | 2 |
| 02-VERIFICATION.md | 1 |
| **TOTAL** | **14** (BLOCKER 3 budget exact: 16 occurrences − 2 DO-NOT-ANNOTATE) |

### DO-NOT-ANNOTATE List

| Line | Test | Status |
|------|------|--------|
| 02-02-PLAN.md:547 (T-02-02-08 SSRF row) | `sed -n '547p' ... \| grep -c "LIFTED 2026-05-18"` returns 0 | VERIFIED (unannotated as required) |
| 02-REVIEW.md:254 (S3 endpoint review) | `sed -n '254p' ... \| grep -c "LIFTED 2026-05-18"` returns 0 | VERIFIED (unannotated as required) |

### No Sidecar File (D-12)

```
$ test -f .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-CONCESSIONS.md \
    && echo PRESENT || echo ABSENT
ABSENT
```

### CLAUDE.md Edits

| Check | Evidence | Status |
|-------|----------|--------|
| `Platform API ingress` in Security bullet | `CLAUDE.md:16` | VERIFIED |
| `Upstream HTTPSource admits http://` in Security bullet | `CLAUDE.md:16` | VERIFIED |
| HMAC-SHA-256 opening sentence preserved | `CLAUDE.md:16` first sentence intact | VERIFIED |
| `Plaintext key values MUST NOT be persisted` sentence preserved | `CLAUDE.md:16` intact | VERIFIED |
| New `SCM source-types` bullet immediately after Security | `CLAUDE.md:17` | VERIFIED |
| SCM bullet mentions `github/gitlab/bitbucket` + `S3/GCS still require credentials` | `CLAUDE.md:17` | VERIFIED |

### PROJECT.md Key Decisions Table

| Check | Evidence | Status |
|-------|----------|--------|
| New row for HTTPSource HTTPS-only lift with `Lifted Phase 02.1 (commit 45b7558)` | `PROJECT.md:105` | VERIFIED |
| New row for SCM `authSecretRef` made optional with `Lifted Phase 02.1 (commit 94f24b5)` | `PROJECT.md:106` | VERIFIED |
| 3-column table structure preserved | `## Evolution` heading still at line 109; rows render cleanly | VERIFIED |

### SCM-auth Parallel Sweep

```
$ grep -rnE "AuthSecretRef.*Required|authSecretRef.*required" \
    .planning/phases/02-external-refs-marketplace-operator-reconciliation/
(no matches)
```

Vacuously satisfied per plan acceptance criterion: "if none found, the SUMMARY records '0 SCM-required claims discovered in phase-02 docs' and this criterion is vacuously satisfied."

---

## Cross-Plan Consistency

| Cross-Link | Evidence | Status |
|------------|----------|--------|
| `OutcomeRevoked = "revoked"` (Plan 1) ↔ `"outcome":"revoked"` grep (Plan 2) | `runnable.go:46` defines `OutcomeRevoked = "revoked"`; `uat-g1.sh:253,256` greps `"outcome":"revoked"` | VERIFIED — literal string aligned |
| `litellm_token` column (Plan 1 migration 000003) ↔ uat-g1.sh INSERT/UPDATE (Plan 2) | `up.sql:33-34` adds the column; `uat-g1.sh:181,211` references `litellm_token` in INSERT + UPDATE | VERIFIED — column name aligned |
| `slog.NewJSONHandler` (internal/audit/handler.go:45) ↔ JSON-form grep in uat-g1.sh | Verified by reading the runnable.go emit-site declaration order; the SUMMARY notes the With-attr `audit:true` is emitted first then call-site attrs follow in declaration order (`target.kind, target.name, outcome, user_id`) | VERIFIED — wire format matches |
| 14 commits in `c0e764a..a12cc5c` | `git log --oneline c0e764a..a12cc5c \| wc -l` returns 14 | VERIFIED |

---

## OP-13 Coverage Status

**Requirement OP-13** (Hub §18.2 / §18.4 — orphan-cleanup audit event per revocation):

- Phase 02 declared `passed` against the unit-test mock but the wire-format call returned 404 (Gap G1 in `02-HUMAN-UAT.md`).
- **Phase 02.2 Plan 1** makes the orphan loop wire-correct against LiteLLM v1.83.10 (endpoint swap + `Token` rename + `OutcomeRevoked` + migration 000003).
- **Phase 02.2 Plan 2** ships `scripts/uat-g1.sh` — the live verification harness that exercises the full revocation cycle and asserts the JSON-form audit event.
- **Live UAT execution is the engineer manual step** (D-11; documented out-of-scope for this verification). The script is on disk, syntax-clean, JSON-grep correct, and cross-plan consistent.

**Disposition:** OP-13 verification is now LIVE-CAPABLE. Final status remains `passed` on the requirement until the engineer runs `scripts/uat-g1.sh` end-to-end and observes the PASS message; per CONTEXT this is intentional — verification debt closure does NOT block Phase 3.

---

## Anti-Pattern Scan

Scanned all files modified/created by Phase 02.2:

```
- internal/litellm/keyinfo.go        — clean
- internal/litellm/types.go          — clean
- internal/litellm/client.go         — clean
- internal/orphan/runnable.go        — clean
- internal/orphan/runnable_test.go   — clean
- internal/db/litellm_users.go       — clean
- db/migrations/000003_litellm_token.up.sql    — clean
- db/migrations/000003_litellm_token.down.sql  — clean
- docker-compose.yml                 — clean
- scripts/uat-g1.sh                  — "placeholder" appears only as the literal seed value before /key/generate runs (intentional, not a stub)
- scripts/uat-sc5.sh                 — clean
- scripts/litellm-config.yaml        — clean
- CLAUDE.md                          — clean
- .planning/PROJECT.md               — clean
```

| Pattern | Hits | Disposition |
|---------|------|------------|
| `TBD\|FIXME\|XXX` (debt markers) | 0 | clean |
| `TODO\|HACK\|PLACEHOLDER` (warning markers) | 0 | clean |
| `placeholder` (stub markers) | 4 in uat-g1.sh | All refer to the literal seed-value `placeholder-token-pending-litellm-generate` (replaced by real LiteLLM token in step 5). Intentional design — NOT a stub. |
| `not implemented\|coming soon\|will be here` | 0 | clean |

---

## Live UAT Pending — Engineer Manual Step

The plan explicitly out-of-scoped live execution of `scripts/uat-g1.sh` (CONTEXT D-11 + Plan 2 `<verification>` block). The script is correct on disk; this verification did NOT attempt to run it. Engineer should perform once-per-milestone:

```
$ docker-compose --profile litellm up -d
$ ./scripts/uat-g1.sh
# Expected: "uat-g1: PASS — revocation audit event observed"
# Wall time: ~8-12 minutes.
```

Additional deferrals also honored (out-of-scope, not gaps):
- Phase 3 SSO write path for `litellm_token` (D-02 — Phase 3 owns).
- httptest fake-server wire-format test under `internal/litellm/keyinfo_test.go` (D-03 — Plan 2's UAT is the wire oracle).
- Strict exactly-one-event audit parsing in `uat-g1.sh` (D-11).
- Post-revocation `GET /key/info?key=<token>` 404 verify (D-11).
- CI integration for `uat-g1.sh` (D-11).
- Phase 1 SC#3 reinstatement under kind (CONTEXT excludes).

---

## Status

**PASSED — 23/23 truths VERIFIED; 30/30 individual must-have checks pass.**

All Plan 1, Plan 2, and Plan 3 must-haves are observably true in the codebase. Cross-plan consistency verified (Plan 1 ↔ Plan 2 outcome enum + column name alignment; 14-commit range matches; OP-13 made live-verifiable). No anti-patterns introduced. No deviations from the plan beyond the four documentation-wording auto-fixes documented in Plan 1's SUMMARY (Rule 3, fold-forward into the original commits — verified clean on disk).

Phase 02.2 closes the deferred items from Phase 02.1 SUMMARY as designed. Phase 3 is unblocked.

---

_Verified: 2026-05-18_
_Verifier: Claude (gsd-verifier)_
