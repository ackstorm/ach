# Phase 02.3: Test framework pivot — Discussion Log

**Session date:** 2026-05-19
**Mode:** Default (multiple AskUserQuestion turns; user-driven scope evolution)
**Trigger:** `/gsd-execute-phase 3 --auto` blocked on Phase 02.2 UAT gate; user redirected mid-checkpoint TWICE — first from docker-compose to kind+Helm UAT scripts, then from "kind+Helm UAT scripts" to "full tier-1/2/3 framework".

This log is human-reference only — downstream agents read CONTEXT.md.

## Timeline

### Turn 0 — UAT gate failure

User invoked `/gsd-execute-phase 3 --auto` at session start. STATE.md `stopped_at` recorded a deliberate pause requiring `scripts/uat-g1.sh` to pass before Phase 3 could begin (commit `083cb95`).

User ran `bash scripts/uat-g1.sh` interactively. Two failures:
1. Docker images for postgres/redis/litellm/litellm-db pulled fine.
2. `docker run ach-devtools:latest` failed: `pull access denied for ach-devtools` — the image is built locally lazily by `scripts/dev.sh` but `uat-g1.sh` invokes `docker run` directly without auto-build.

User surfaced the broader question: "why are we using docker-compose? we should use kind, and install litellm from helm, install redis, postgres, etc."

### Turn 1 — First pivot: docker-compose → kind+Helm UAT scripts

Pre-context Q&A:

| Question | Options offered | User chose |
|---|---|---|
| LiteLLM chart source | (a) Upstream BerriAI litellm-helm (Rec) / (b) Private chart / (c) Hand-rolled minimal chart | (a) Upstream BerriAI litellm-helm |
| Install mechanism in kind | (a) Direct `helm install` (Rec) / (b) Install Flux + apply HelmReleases | (a) Direct `helm install` |
| GSD scoping | (a) New Phase 02.3 (Rec) / (b) Gap-closure on 02.2 / (c) Inline via /gsd-quick | (a) New Phase 02.3 |

Phase 02.3 was created via `gsd-sdk query phase.insert` (SDK auto-numbered as 02.2.1; manually renumbered to 02.3 to match project decimal-sibling convention).

Memory updated: `feedback_local_uat_kind_helm` (new active); `feedback_local_uat_docker_compose` marked superseded.

Discuss-phase Q&A (first version, scoped to "kind+Helm UAT scripts"):

| # | Area | Options offered | User chose |
|---|---|---|---|
| 1 | Cluster lifecycle | (a) Reuse `ach-operator-e2e` (Rec) / (b) Separate `ach-uat` cluster / (c) Reuse e2e overlay + add charts on top | (b) Separate `ach-uat` cluster |
| 2 | Operator runtime | (a) In-cluster Deployment via config/e2e overlay (Rec) / (b) Host subprocess + port-forward | (a) In-cluster Deployment |
| 3 | LiteLLM config delivery | (a) ConfigMap + extraVolumes/extraVolumeMounts (Rec) / (b) Inline via `proxy_config:` values / (c) Wrapper chart | (a) ConfigMap + extraVolumes/extraVolumeMounts |
| 4 | Script topology | (a) Shared `uat-kind.sh` + dispatchers (Rec) / (b) Single uat-kind.sh with --scenario flag / (c) G1-only first | (a) Shared base + dispatchers |

First version of `02.3-CONTEXT.md` written and committed (commit `1685953`) with 13 decisions (D-01..D-13) scoped to UAT scripts only. STATE.md session recorded (commit `5f10ba4`).

### Turn 2 — Second pivot: UAT scripts → full tier-1/2/3 framework

User shared `../ach_litellm/spec/tier2-test-env.md` — sister project's tier-2 test framework design (currently "Draft for Phase 8 ingestion" in sister). User said: "Quiero cambiar la manera de como hacemos los tests, he estado diseñando el sistema de test para nuestro proyecto hermano... quiero que movamos hacia estandarizar esa metodologia, no sé si necesitamos las tres fases nosotros, pero me gusta la estructura, la parte de toolhive también me interesa porque así podré poner un litellm con varios objetos ya creados, varios MCPs, algún modelo ya puesto en la config, etc."

Claude's analysis distinguished what translates 1:1, what doesn't fit ACH, and what ACH needs that sister's design doesn't cover (audit JSON assertions, DB row assertions, multi-component coordination, `pk_`/`ek_` helpers).

User clarifications on each "doesn't fit" item:
- **Tier 3 dogfood**: REJECTED Claude's framing. User clarified Tier 3 for ACH = "validar Environment/PluginMarketplace/etc. manifests" via scripted gen+apply+verify (different shape from sister's "manual against production LiteLLM").
- **Mock providers (openai-mock / kubeai-mock)**: agreed — drop.
- **AC-M3 wholesale-replace**: agreed — drop.
- **Operator Helm chart**: agreed — ACH stays kustomize-only. User noted ACH can work with or without sister's litellm-operator in test cluster.
- **ToolHive**: kept and clarified. User wants real MCPs (e.g., fetch, context7) running via ToolHive, but LiteLLM config references them statically (YAML, not API-side registration). The MCP fixtures are test data, not operator-side CRD contract.

User verdict: **Full pivot.**

Discuss-phase Q&A (second version, scoped to full tier framework):

| # | Area | Options offered | User chose |
|---|---|---|---|
| 1 | uat-g1.sh / uat-sc5.sh fate | (a) Delete — absorb into Ginkgo (Rec) / (b) Bash wrappers calling ginkgo / (c) Archive to scripts/legacy/ | (a) Delete |
| 2 | Sister's litellm-operator in tier-2 by default | (a) Not install (Rec) / (b) Opt-in fixture / (c) Always install | (a) Not install |
| 3 | Tier 3 shape | (a) Scripted `make tier3-validate` (Rec) / (b) Manual checklist / (c) Hybrid | (a) Scripted |
| 4 | ToolHive MCP fixtures starting set | (a) Minimal: fetch + context7 (Rec) / (b) Broad: 5 MCPs / (c) Only fetch | (a) fetch + context7 |

### Turn 3 — Dev-iteration as first-class principle

User interrupted Claude's request to proceed with re-writing CONTEXT.md with: **"Piensa que este entorno debe service como entorno de iteración durante desarrollo, no solo gate de CI."**

This was a principle redirection, not a feature request. It changed how every subsequent design decision was filtered. Claude recomputed the design surface adding:
- `hydrate` is the daily-driver verb, not `up`; hydrate must be incremental (seconds, not minutes)
- Per-component reload affordances as Makefile targets (not bash cluster.sh sub-verbs)
- Inspection helpers (logs-X, psql, audit-tail, pf-X, watch-crs) as first-class targets
- Test scenarios self-cleaning with unique IDs per `It()` instead of cluster-recreate-between-runs
- Conservative resource sizing for laptop daily-driver
- `test/tier2/README.md` documenting CI gate + dev loop side-by-side (plan deliverable)
- `test-tier2-focus` and `test-tier2-label` for single-scenario iteration

### Turn 4 — Sister's dev-iteration proposal validated against ACH

User shared sister's proposed dev-iteration set (5 items):
1. cluster-up + cluster-keep
2. cluster-hydrate must be incremental
3. Helper targets: operator-redeploy, logs-X, watch-crs, pf-litellm, mock-mode
4. README documenting both workflows
5. test-tier2-focus

Claude's adaptation:
- Items 1, 2, 4, 5: adopt verbatim for ACH
- Item 3 partially: drop `mock-mode` (no mocks in ACH), keep `operator-redeploy` / `logs-*` / `watch-crs` / `pf-*`, replace `mock-mode` with ACH-specific `operator-set-env` (G1 testing) and `litellm-rotate-master-key` (future analogous fast-path test)
- Add ACH-specific: `psql`, `db-reset`, `audit-tail`, `litellm-reload-config`, `load-fixtures`

Discuss-phase Q&A (third version):

| # | Area | Options offered | User chose |
|---|---|---|---|
| 1 | Test cleanup strategy | (a) Self-cleaning + unique IDs (Rec) / (b) BeforeEach drops DB / (c) BeforeSuite drops DB | (a) Self-cleaning |
| 2 | ToolHive MCPs fixtures | (a) Plain YAML (Rec) / (b) Helm chart for fixtures / (c) Templated via cluster.sh | (a) Plain YAML |

(Earlier-asked Q1 "cluster.sh reload granularity" and Q3 "README timing" were implicitly answered by sister's proposal — answer: Makefile targets per component, README as plan deliverable.)

## Scope evolution summary

| Stage | Scope | Estimate | Status |
|---|---|---|---|
| Phase 02.3 original | rewrite uat-g1.sh / uat-sc5.sh against kind+Helm | 3-5 days | Obsolete |
| Turn 1 outcome | full kind+Helm UAT pivot (uat-kind.sh base + dispatchers + config/uat overlay) | 1-2 weeks | Obsolete after Turn 2 |
| Turn 2 outcome | full tier-1/2/3 framework adopting sister's design | 3-4 weeks | Obsolete after Turn 3 |
| Turn 3+4 outcome (current) | tier-1/2/3 framework with first-class dev iteration + audit-tail/psql/db-reset/forensics helpers | **4-5 weeks** | Current |

## Notable decisions diverging from sister's design

- **No openai-mock / kubeai-mock** — ACH has no `ModelDiscovery` CRD.
- **No `AC-M3 wholesale-replace`** — sister spec, not ACH.
- **No `charts/ach-operator/` Helm chart** — kustomize overlay sufficient.
- **No sister's `litellm-operator` in tier-2** — LiteLLM via static config + ToolHive-provided MCPs.
- **Self-cleaning tests + unique IDs** — sister recreates cluster between runs; ACH lives in cluster for days.
- **Conservative resource sizing** — daily-driver laptop, not CI-grade.
- **Tier 3 = scripted ACH-CRD manifest validation** (not "manual against production LiteLLM" — ACH has no team-prod-LiteLLM yet).
- **`psql` / `db-reset` / `audit-tail`** — ACH-specific (Postgres-as-SoT + audit JSON handler).

## Open items for next steps

CONTEXT.md `<open_items>` section enumerates 12 research questions for `gsd-phase-researcher` to resolve before planning. Highest-priority: ToolHive Service DNS pattern, chart version pins, LiteLLM `mcp_servers:` config schema, ToolHive MCPServer CR `spec` shape.

## Memory updates this session

- Wrote `feedback_local_uat_kind_helm.md` (Turn 1). Active guidance for UAT direction.
- Edited `feedback_local_uat_docker_compose.md` to mark superseded 2026-05-19 (Turn 1).
- Updated `MEMORY.md` index (Turn 1).
- No memory updates from Turns 2-4 — those captured in CONTEXT.md as project facts, not user-preference facts.

A new memory may be appropriate post-execute: a `reference_ach_tier2_framework.md` documenting the tier model + dev loop, for future sessions to discover without re-reading this 500-line context.

---

### Turn 5 — Third pivot (2026-05-20): tier-1/2/3 framework REJECTED

**Trigger:** user invoked `/gsd-stats`, audited Phase 02.3's 11-PLAN status, and asked "why we need phase 02.3 we have full e2e."

Claude clarified the gap: commit `e1b943c` ("feat(test): adopt sister project test infrastructure") had ALREADY shipped the kind+Helm substrate (`scripts/cluster.sh`, Helm pins, CI workflow, `test/e2e/CHART_PINS.md`). What `02.3-CONTEXT.md` from Turn 4 still wanted on top was the **Ginkgo migration + `test/tier2/` tree + ~25 dev-loop Makefile targets + scripted Tier 3 + `config/uat/` overlay** — ~4-5 weeks of work.

User verdict: **"my intuition says that we should plan 02.3 with our new test suite and not ship as planned. we should adopt our new suite, build over it, ensure full e2e, and remove all old test files and plans."**

Translation:
- Keep stdlib `test/e2e/` as-is; do NOT migrate to Ginkgo
- Build over the sister-adopted substrate; do NOT build a parallel tier-2 tree
- Fill SC#5 stdlib coverage gap (uat-g1/uat-sc5 assertions); delete the docker-compose-era files
- Delete the 11 PLAN docs that assumed Ginkgo migration

Re-pivot Q&A (2026-05-20):

| # | Area | Options offered | User chose |
|---|---|---|---|
| 1 | `docker-compose.yml` (root Postgres+Redis stack) | (a) Delete entirely / (b) Keep root services only (Rec) / (c) Decide later | (a) Delete entirely |
| 2 | 11 existing 02.3 PLAN docs (assume Ginkgo migration) | (a) Delete (Rec) / (b) Archive in `.planning/archive/` / (c) Strike-through in place | (a) Delete |
| 3 | Approve overall re-scope plan | (a) Approve, execute step by step / (b) Approve, write new PLANs first / (c) Hold | (a) Approve, execute step by step |
| 4 | Step 2 design (`phase2_sc5_orphan_test.go`) | (a) New file with 3 sub-T (Rec) / (b) Split into 2 files / (c) Inline | (a) Approved as proposed |
| 5 | Step 3 deletion list (revised: config/dev-postgres/ stays) | (a) Approve, proceed (Rec) / (b) Show Makefile hunks / (c) Hold | (a) Approve, proceed |
| 6 | Step 4 replan style | (a) Full `/gsd-plan-phase` rerun (Rec) / (b) Manual / (c) Commit first | (a) Full `/gsd-plan-phase` rerun |

### Execution log (2026-05-20)

| Step | Action | Result |
|---|---|---|
| 1 | Gap analysis — uat-g1/uat-sc5 vs stdlib e2e | Identified 5 missing assertions; SC#5 wholesale missing |
| 2 | Wrote `test/e2e/phase2_sc5_orphan_test.go` | ~330 LOC; `go vet`/`go build` clean |
| 2b | Updated `phase2_invariants_test.go` header | dropped stale "SC#5 NOT replicated here" note |
| 3 | Deleted 6 files + stripped Makefile + comment cleanups | git rm clean; build green |
| 4a | Deleted 11 PLAN docs + RESEARCH/PATTERNS/VALIDATION | git rm forced (RESEARCH had local mods) |
| 4b | Rewrote CONTEXT.md for narrower scope | this file |
| 4c | Appended Turn 5 to DISCUSSION-LOG | this entry |
| 4d | `/gsd-plan-phase` to regenerate 2-3 PLAN docs | pending |
| 5 | Memory: `feedback_023_tier_framework_rejected` | pending |

## Scope evolution summary (final)

| Stage | Scope | Estimate | Status |
|---|---|---|---|
| Phase 02.3 original (2026-05-19 Turn 0) | rewrite uat-g1.sh/uat-sc5.sh vs kind+Helm | 3-5 days | Obsolete |
| Turn 1 outcome | full kind+Helm UAT pivot | 1-2 weeks | Obsolete |
| Turn 2 outcome | full tier-1/2/3 framework adopting sister design | 3-4 weeks | Obsolete |
| Turn 3+4 outcome | tier framework + dev-loop affordances | 4-5 weeks | Obsolete |
| **Turn 5 outcome (2026-05-20)** | **stdlib e2e SC#5 port + docker-compose deletion + replan** | **1-2 days** | **Current** |

## Memory updates this re-pivot

- `feedback_023_tier_framework_rejected.md` — new (pending). User rejected tier-2/Ginkgo for ACH on 2026-05-20; do not re-propose. Stdlib `test/e2e/` is the canonical e2e surface.
- `feedback_local_uat_kind_helm.md` — still active (no change).
- `feedback_local_uat_docker_compose.md` — still superseded (no change).
