---
phase: 02.3-uat-pivot-to-kind-helm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/e2e/phase2_sc5_orphan_test.go
  - test/e2e/phase2_invariants_test.go
autonomous: true
requirements:
  - "OP-15-live: Phase 02 SC#5 (orphan-cleanup interval floor + audit event per revocation) is verifiable-live against an in-cluster LiteLLM Helm release via the stdlib e2e suite — no docker-compose required."
mode: retrospective
gap_closure: false

must_haves:
  truths:
    - "test/e2e/phase2_sc5_orphan_test.go exists with build tag `e2e` and stdlib `testing` (no Ginkgo)."
    - "TestPhase2SC5Orphan declares exactly two sub-T: IntervalFloor and OrphanReapLive — replicated from the deleted scripts/uat-sc5.sh assertions plus the deleted scripts/uat-g1.sh audit-event grep."
    - "IntervalFloor patches deployment/ach-operator env to ACH_ORPHAN_CLEANUP_INTERVAL=1m and asserts the new Pod logs the §18.4 / D-15 below-minimum error within 120s; restores 5m on cleanup."
    - "OrphanReapLive scales operator to 0 (per D-05-v3 race avoidance), port-forwards svc/litellm / svc/ach-postgres / svc/litellm-postgresql, seeds personal_keys row, POSTs /key/generate against in-cluster LiteLLM, backdates LiteLLM_VerificationToken.created_at by 11m, calls orphan.NewRunnable(...).TickOnce() in-process (per D-06-v3) with a bytes.Buffer-backed audit logger, asserts one emitted JSON line carries audit:true + outcome:revoked + user_id:u-e2e-sc5."
    - "phase2_invariants_test.go header references phase2_sc5_orphan_test.go as the SC#5 home (the obsolete \"SC#5 deliberately NOT replicated here\" note has been removed)."
    - "`go vet -tags=e2e ./test/e2e/...` exits 0; `go build ./...` exits 0 — the file compiles in isolation before the executor's live run."
  artifacts:
    - path: "test/e2e/phase2_sc5_orphan_test.go"
      provides: "Phase 02 SC#5 e2e coverage (interval floor + orphan revocation + audit JSON wire form)"
      contains: "TestPhase2SC5Orphan"
      min_lines: 200
    - path: "test/e2e/phase2_invariants_test.go"
      provides: "Phase 02 SC#1-4 e2e coverage; header points readers to phase2_sc5_orphan_test.go"
      contains: "phase2_sc5_orphan_test.go"
  key_links:
    - from: "test/e2e/phase2_sc5_orphan_test.go"
      to: "internal/orphan.NewRunnable"
      via: "in-process TickOnce() invocation (D-06-v3)"
      pattern: "orphan\\.NewRunnable"
    - from: "test/e2e/phase2_sc5_orphan_test.go"
      to: "internal/audit.NewLogger"
      via: "bytes.Buffer-backed audit logger for line-by-line assertion"
      pattern: "audit\\.NewLogger\\(auditBuf\\)"
    - from: "test/e2e/phase2_sc5_orphan_test.go"
      to: "internal/litellm.NewRESTClient"
      via: "real LiteLLM REST client pointed at port-forwarded svc/litellm:4000"
      pattern: "litellm\\.NewRESTClient"
    - from: "test/e2e/phase2_sc5_orphan_test.go (constants)"
      to: "test/e2e/values/litellm.values.yaml (Helm release name)"
      via: "sc5LiteLLMSvc = svc/litellm; sc5MasterKey = sk-test-master-key"
      pattern: "sc5LiteLLMSvc|sc5MasterKey"
---

<objective>
**Status: RETROSPECTIVE.** The file `test/e2e/phase2_sc5_orphan_test.go` and the header tweak to `test/e2e/phase2_invariants_test.go` were already written on 2026-05-20 by the orchestrator before this planner spawn. This plan captures the design rationale and acceptance criteria so the executor (Plan 03) knows what the live-validation gate must demonstrate and so future readers understand why the new file exists.

Purpose: Document the SC#5 stdlib port — what assertions it replaces (deleted `scripts/uat-sc5.sh` for the interval floor + revocation path; deleted `scripts/uat-g1.sh` for the audit JSON wire form), what design trade-offs it embodies (D-05-v3 scale-to-0; D-06-v3 in-process TickOnce), and what shape the live cluster must satisfy for it to pass.

Output: This PLAN.md (retrospective). No new code artifacts; the file already exists on disk per orchestrator step 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.3-uat-pivot-to-kind-helm/02.3-CONTEXT.md

# Source file under retrospective documentation
@test/e2e/phase2_sc5_orphan_test.go
@test/e2e/phase2_invariants_test.go
@test/e2e/e2e_suite_test.go

# Live cluster contract (Service names + master key are encoded in these files)
@test/e2e/values/litellm.values.yaml
@scripts/cluster.sh

# Phase 02 SC#5 origin — the now-deleted assertions ported into the new file
# (Read the Phase 02 ROADMAP entry SC#5 bullet; bash scripts are deleted but their assertions
#  are reflected in phase2_sc5_orphan_test.go sub-T bodies.)

<interfaces>
<!-- Key Go contracts the new test depends on. Extracted from internal/* packages. -->

From internal/orphan (used by OrphanReapLive sub-T):
```go
// NewRunnable wires the orphan-cleanup loop. Phase 02 plan 02-08 ships
// this constructor; phase2_sc5_orphan_test.go calls TickOnce() in-process
// instead of letting the in-cluster operator tick (D-06-v3).
func NewRunnable(
    client litellm.Client,
    pool *pgxpool.Pool,
    auditLog audit.Logger,
    interval time.Duration,
    log logr.Logger,
) *Runnable

func (r *Runnable) TickOnce(ctx context.Context)
```

From internal/audit (used to capture wire-form events):
```go
// NewLogger returns an audit.Logger that writes JSON events to the given io.Writer.
// Phase 02 plan 02-04 ships slog.NewJSONHandler under the hood — wire form is
// {"audit":true,"outcome":"...","user_id":"..."} per §18.2.
func NewLogger(w io.Writer) Logger
```

From internal/litellm (used to talk to in-cluster LiteLLM):
```go
// NewRESTClient — Phase 02 plan 02-01 / 02-09 ship the REST client.
// phase2_sc5_orphan_test.go points it at the port-forwarded svc/litellm:4000.
func NewRESTClient(baseURL, masterKey string, log logr.Logger) *RESTClient
```

From test/e2e/e2e_suite_test.go (helpers reused by the new file):
- `runCmd(name string, args ...string) (string, error)` — 60s-bounded shell-out
- `runCmdLonger(timeout time.Duration, ...) (string, error)` — caller-bounded shell-out
- Package-globals `namespace` (= "ach-system" by default), `kindCluster`, `operatorImage`
</interfaces>

# Note: Plan 02 covers the deletion set that makes this file the canonical SC#5 home.
# Note: Plan 03 is the live-validation + commit plan that gates this work as "done".
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document acceptance criteria for the SC#5 stdlib port (retrospective)</name>
  <files>(retrospective — no file mutation; documents already-shipped test/e2e/phase2_sc5_orphan_test.go and test/e2e/phase2_invariants_test.go header)</files>
  <read_first>
    - test/e2e/phase2_sc5_orphan_test.go (fully — sub-T bodies, helpers, constants)
    - test/e2e/phase2_invariants_test.go lines 1-50 (header forward-pointer)
    - test/e2e/e2e_suite_test.go (shared runCmd / namespace globals)
    - test/e2e/values/litellm.values.yaml (Service name + masterkey verification)
    - .planning/phases/02.3-uat-pivot-to-kind-helm/02.3-CONTEXT.md `<decisions>` D-02-v3, D-05-v3, D-06-v3
    - .planning/ROADMAP.md Phase 02 Success Criterion 5 (line ~83)
  </read_first>
  <action>
    No file mutation. This task documents the acceptance criteria for the already-shipped `test/e2e/phase2_sc5_orphan_test.go` so Plan 03's executor knows what "live SC#5 verified" means.

    Verify by inspection that the on-disk file at `test/e2e/phase2_sc5_orphan_test.go` satisfies (no edits — observation only):

    1. **Build tag and package** (per D-02-v3): line 1 is `//go:build e2e`; package declaration is `package e2e`. The file lives in the same package as `phase2_invariants_test.go` so it shares the suite's package-globals (`namespace`, `runCmd`, `runCmdLonger`).

    2. **Top-level Test entrypoint**: `TestPhase2SC5Orphan(t *testing.T)` declares exactly two sequential sub-T via `t.Run`:
       - `IntervalFloor` → `testSC5IntervalFloor`
       - `OrphanReapLive` → `testSC5OrphanReapLive`
       Subtests run sequentially (per the file's docstring). No parallelism inside this Test because OrphanReapLive scales the operator to 0 and would race IntervalFloor's log-grep otherwise.

    3. **IntervalFloor assertions** (replaces `scripts/uat-sc5.sh --smoke`):
       - `kubectl set env -n ach-system deployment/ach-operator -c manager ACH_ORPHAN_CLEANUP_INTERVAL=1m`
       - Poll new Pod logs for `ACH_ORPHAN_CLEANUP_INTERVAL=1m0s is below minimum 5m0s` with a 120s deadline and 3s polling interval (no naked polling loops; deadline is a wall-clock bound per global CLAUDE.md anti-pattern rule).
       - `t.Cleanup` restores `ACH_ORPHAN_CLEANUP_INTERVAL=5m` and waits up to 120s for the rollout — even on test failure.

    4. **OrphanReapLive assertions** (replaces `scripts/uat-sc5.sh --full` + the `scripts/uat-g1.sh` audit-event JSON grep):
       - Scale `deployment/ach-operator` to 0 replicas (per D-05-v3 — eliminate race against the in-cluster ticker).
       - `kubectl wait --for=delete pods -l app.kubernetes.io/name=ach-operator --timeout=60s` (best-effort log, not fatal).
       - Three concurrent `startPortForward` invocations: `svc/litellm.litellm-system:4000`, `svc/ach-postgres.ach-system:5432`, `svc/litellm-postgresql.litellm-system:5432`. Each picks a free local port via `pickFreePort` (net.Listen :0) and waits up to 30s for TCP reachability via `net.DialTimeout`. Each registers its own kill-on-cleanup.
       - `seedPersonalKey(t, ctx, achDBURL, "pkid_e2e_sc5", "u-e2e-sc5")` INSERTs into `personal_keys` with `litellm_user_id=u-e2e-sc5` so the user becomes ACH-managed and ON CONFLICT (key_id) DO NOTHING so re-runs after a partial cleanup do not fail.
       - `createLiteLLMKey(t, ctx, litellmURL, "sk-test-master-key", "u-e2e-sc5")` POSTs `/key/generate` with `{"user_id":"u-e2e-sc5","duration":"24h","metadata":{"created_by":"e2e-sc5"}}`. Returns LiteLLM's opaque token.
       - `backdateLiteLLMKey(t, ctx, llDBURL, token, 11)` UPDATEs `"LiteLLM_VerificationToken"` SET `created_at = now() - interval '11 minutes'` so the token reads older than the 10m `OrphanAgeFloor`.
       - `r := orphan.NewRunnable(client, pool, auditLog, 5*time.Minute, logr.Discard()); r.TickOnce(ctx)` runs the orphan path in-process (D-06-v3) with `auditLog = audit.NewLogger(bytes.Buffer)`.
       - For each non-empty line in `auditBuf.String()`: `json.Unmarshal` to `map[string]any`; the test PASSES when one line satisfies `ev["audit"] == true && ev["outcome"] == "revoked" && ev["user_id"] == "u-e2e-sc5"`.
       - Cleanup deletes the LiteLLM key (`/key/delete`), deletes the `personal_keys` row, and restores `deployment/ach-operator` to `--replicas=1`.

    5. **Helpers in the file** (replicated from deleted bash; now Go):
       - `startPortForward(t, ns, target, remotePort) int` — spawns kubectl port-forward, picks free local port, DialTimeout polls for reachability with 30s deadline, kills on cleanup.
       - `pickFreePort(t) int` — net.Listen("tcp","127.0.0.1:0") and read the assigned port.
       - `seedPersonalKey` / `cleanupPersonalKey` — pgxpool against ACH Postgres.
       - `createLiteLLMKey` / `deleteLiteLLMKey` — net/http against LiteLLM REST.
       - `backdateLiteLLMKey` — pgxpool against LiteLLM Postgres; minutes is `int` so the rendered `interval '%d minutes'` syntax is trivial (fractional offsets are not needed for SC#5).

    6. **Constants** (the live-cluster contract surface):
       - `sc5LiteLLMSvc = "svc/litellm"` (litellm-helm release name = "litellm")
       - `sc5LiteLLMDBSvc = "svc/litellm-postgresql"` (bitnami postgresql subchart convention; db.deployStandalone=true)
       - `sc5ACHPgSvc = "svc/ach-postgres"` (config/dev-postgres/postgres_service.yaml)
       - `sc5LiteLLMNS = "litellm-system"` (namespace created by scripts/cluster.sh)
       - `sc5MasterKey = "sk-test-master-key"` (matches test/e2e/values/litellm.values.yaml `masterkey:` and the operator Secret in `config/e2e/litellm_connection.yaml`)
       - `sc5UserID = "u-e2e-sc5"` and `sc5KeyID = "pkid_e2e_sc5"` — disjoint from any other test's IDs so concurrent test runs do not collide.

    7. **`phase2_invariants_test.go` header forward-pointer** (already shipped): lines 19-23 of that file mention `phase2_sc5_orphan_test.go` as the SC#5 home and explain SC#5 is split out because it scales the operator to 0 and uses port-forwards. No "SC#5 deliberately NOT replicated here" text remains.

    If any of these observations does NOT match the on-disk file, this plan FAILS — Plan 03 must not proceed. In that case, surface the diff to the user; do not silently rewrite the file.
  </action>
  <verify>
    <automated>
    test -f test/e2e/phase2_sc5_orphan_test.go && \
    head -1 test/e2e/phase2_sc5_orphan_test.go | grep -q '^//go:build e2e' && \
    grep -q 'func TestPhase2SC5Orphan' test/e2e/phase2_sc5_orphan_test.go && \
    grep -q 't.Run("IntervalFloor"' test/e2e/phase2_sc5_orphan_test.go && \
    grep -q 't.Run("OrphanReapLive"' test/e2e/phase2_sc5_orphan_test.go && \
    grep -q 'orphan.NewRunnable' test/e2e/phase2_sc5_orphan_test.go && \
    grep -q 'audit.NewLogger' test/e2e/phase2_sc5_orphan_test.go && \
    grep -q 'litellm.NewRESTClient' test/e2e/phase2_sc5_orphan_test.go && \
    grep -q 'phase2_sc5_orphan_test.go' test/e2e/phase2_invariants_test.go && \
    ! grep -q 'SC#5 deliberately NOT replicated here' test/e2e/phase2_invariants_test.go && \
    go vet -tags=e2e ./test/e2e/... && \
    go build ./...
    </automated>
  </verify>
  <acceptance_criteria>
    - `test/e2e/phase2_sc5_orphan_test.go` exists on disk with the shape documented above (build tag, TestPhase2SC5Orphan top-level, two sub-T).
    - `go vet -tags=e2e ./test/e2e/...` exits 0.
    - `go build ./...` exits 0.
    - `test/e2e/phase2_invariants_test.go` header references the new SC#5 file and no longer carries the "SC#5 deliberately NOT replicated here" note.
    - No other file is modified by this plan.
  </acceptance_criteria>
  <done>
    The SC#5 stdlib port is documented as RETROSPECTIVE. The on-disk file matches the contract above; Plan 03's executor can rely on the documented behaviour when running `make e2e-focus FOCUS=TestPhase2SC5Orphan` against a real kind+Helm cluster.
  </done>
</task>

</tasks>

<verification>
This plan documents already-shipped work. The verification command in Task 1 is a structural check (the file exists, compiles, has the expected shape). The end-to-end live verification — the runtime gate — is owned by Plan 03, which runs `make e2e-focus FOCUS=TestPhase2SC5Orphan` against a real kind+Helm cluster.
</verification>

<success_criteria>
- `test/e2e/phase2_sc5_orphan_test.go` matches the contract documented in Task 1.
- `test/e2e/phase2_invariants_test.go` header references the new file.
- `go vet -tags=e2e ./test/e2e/...` and `go build ./...` both exit 0.
- This PLAN.md exists at `.planning/phases/02.3-uat-pivot-to-kind-helm/02.3-01-PLAN.md`.
</success_criteria>

<output>
Create `.planning/phases/02.3-uat-pivot-to-kind-helm/02.3-01-SUMMARY.md` when done. The SUMMARY records that this plan is RETROSPECTIVE (no new code shipped), references the orchestrator's 2026-05-20 step-2 file write, and surfaces the constant-set the live cluster must satisfy (sc5LiteLLMSvc, sc5LiteLLMDBSvc, sc5ACHPgSvc, sc5MasterKey) as a Plan-03 hand-off.
</output>
</content>
</invoke>