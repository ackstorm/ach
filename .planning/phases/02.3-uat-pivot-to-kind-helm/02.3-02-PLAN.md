---
phase: 02.3-uat-pivot-to-kind-helm
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/uat-g1.sh
  - scripts/uat-sc5.sh
  - scripts/litellm-config.yaml
  - scripts/e2e-kind.sh
  - docker-compose.yml
  - internal/orphan/uat_test.go
  - Makefile
  - test/e2e/e2e_suite_test.go
  - config/dev-postgres/postgres_deployment.yaml
  - scripts/dev.sh
autonomous: true
requirements:
  - "Single substrate (D-07-v3): kind+Helm is the only local-UAT substrate; docker-compose-era artifacts removed. No new REQ-IDs — this is verification-infrastructure cleanup."
mode: retrospective
gap_closure: false

must_haves:
  truths:
    - "No file matching scripts/uat-g1.sh, scripts/uat-sc5.sh, scripts/litellm-config.yaml, scripts/e2e-kind.sh, docker-compose.yml, or internal/orphan/uat_test.go exists in the working tree."
    - "Makefile contains no target named dev-up, dev-down, uat-sc5, e2e-kind, or e2e-kind-cleanup; no DOCKER_COMPOSE variable; no Phase 1 Plan 10 local-dev comment block."
    - "Makefile retains the `test-e2e` alias (per D-10-v3 — Kubebuilder convention)."
    - "Makefile `.PHONY: e2e e2e-focus test-e2e e2e-full e2e-keep` line is exactly that (no e2e-kind* tokens)."
    - "test/e2e/e2e_suite_test.go header docstring (line 73-80 region) references scripts/cluster.sh — no scripts/e2e-kind.sh mention."
    - "config/dev-postgres/postgres_deployment.yaml header (lines 4-5) makes no reference to docker-compose.yml or docker-compose.spike.yaml."
    - "scripts/dev.sh header (line 12) makes no reference to docker-compose."
    - "`go vet ./...` and `go vet -tags=e2e ./test/e2e/...` and `go build ./...` all exit 0 — the deletions have not broken any non-test reference path."
    - "config/dev-postgres/ directory STAYS (still consumed by scripts/cluster.sh:119-121 via config/e2e/kustomization.yaml — bitnami migration is out of scope per CONTEXT.md)."
  artifacts:
    - path: "Makefile"
      provides: "Build/test target surface after docker-compose-era target removal"
      contains: "test-e2e: e2e-full"
    - path: "test/e2e/e2e_suite_test.go"
      provides: "e2e suite bootstrap with scripts/cluster.sh as the canonical orchestrator reference"
      contains: "cluster.sh"
    - path: "config/dev-postgres/postgres_deployment.yaml"
      provides: "In-cluster ACH Postgres for e2e (still consumed by config/e2e)"
  key_links:
    - from: "scripts/cluster.sh:119-121"
      to: "config/e2e/kustomization.yaml → config/dev-postgres"
      via: "kustomize build of the e2e overlay"
      pattern: "kustomize build config/e2e"
    - from: "Makefile e2e-full target"
      to: "scripts/cluster.sh up/down"
      via: "cluster-up → e2e → cluster-down"
      pattern: "cluster-up.*e2e.*cluster-down"
---

<objective>
**Status: RETROSPECTIVE.** The orchestrator step-3 commit on 2026-05-20 has already executed every deletion and Makefile edit listed in this plan (uncommitted at planner-spawn; visible in `git status` as `D` entries and `M` entries on Makefile/e2e_suite_test.go/postgres_deployment.yaml/scripts/dev.sh). This plan captures *what* was deleted and *what was* stripped from the Makefile, so the executor (Plan 03) and future readers have a single source of truth for the docker-compose-era removal.

Purpose: Document the single-substrate decision (D-07-v3) — kind+Helm is the only local-UAT path going forward. The docker-compose stack is gone in full; the wrapper script `scripts/e2e-kind.sh` is gone (it was a 5-line shim to `make e2e-full` per D-08-v3); the `internal/orphan/uat_test.go` Go test (build-tag `uat`) is gone because its assertions are now wholly subsumed by `test/e2e/phase2_sc5_orphan_test.go`'s `OrphanReapLive` sub-T (per D-09-v3).

Output: This PLAN.md (retrospective). No working-tree mutations; all of them happened in the orchestrator's step-3 work-product before this planner ran.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02.3-uat-pivot-to-kind-helm/02.3-CONTEXT.md

# Live files post-deletion (already on disk)
@Makefile
@test/e2e/e2e_suite_test.go
@config/dev-postgres/postgres_deployment.yaml
@scripts/dev.sh
@scripts/cluster.sh

# Note: the deleted files are no longer Readable; the deletion set is authoritative
# in CONTEXT.md `<domain>` deliverable #2.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the deletion set + Makefile diff (retrospective)</name>
  <files>(retrospective — no file mutation; documents the orchestrator's step-3 work product)</files>
  <read_first>
    - Makefile (verify the strip is complete — no stray references)
    - test/e2e/e2e_suite_test.go lines 70-90 (verify cluster.sh reference is in place)
    - config/dev-postgres/postgres_deployment.yaml lines 1-10 (verify no docker-compose mention)
    - scripts/dev.sh lines 1-20 (verify no docker-compose mention)
    - scripts/cluster.sh lines 119-121 (verify config/e2e overlay still routes to config/dev-postgres)
    - .planning/phases/02.3-uat-pivot-to-kind-helm/02.3-CONTEXT.md `<domain>` deliverable #2 + decisions D-07-v3, D-08-v3, D-09-v3
  </read_first>
  <action>
    No working-tree mutation. This task documents the deletion set already executed by the orchestrator's step-3 commit boundary.

    The authoritative deletion list (6 files removed via `git rm`):

    | File | Reason | Replaced by |
    |---|---|---|
    | `scripts/uat-g1.sh` | Bash UAT script for Gap G1 audit JSON wire form (docker-compose stack). | `test/e2e/phase2_sc5_orphan_test.go::OrphanReapLive` (asserts the same JSON wire form via `bytes.Buffer`-backed audit logger). |
    | `scripts/uat-sc5.sh` | Bash UAT script for Phase 02 SC#5 orphan cleanup (docker-compose `--profile litellm`). | `test/e2e/phase2_sc5_orphan_test.go::IntervalFloor` (operator log grep against in-cluster Deployment) + `OrphanReapLive` (in-process TickOnce). |
    | `scripts/litellm-config.yaml` | LiteLLM proxy config for the docker-compose stack. | `test/e2e/values/litellm.values.yaml` (Helm values; `cluster.sh install_litellm`). |
    | `scripts/e2e-kind.sh` | 5-line wrapper that called `make e2e-full` (D-08-v3 — no external callers; CI uses `make e2e` directly). | Direct `make e2e-full` invocation. |
    | `docker-compose.yml` | Root Postgres+Redis+litellm+litellm-db stack with `profiles: ["litellm"]`. | `scripts/cluster.sh up` (kind cluster + Helm releases for LiteLLM + ToolHive + ACH via `config/e2e` kustomize overlay). |
    | `internal/orphan/uat_test.go` | Go test with `//go:build uat` exercising `TestSC5LiveOrphanRevocation` against the docker-compose LiteLLM (D-09-v3 — logically equivalent to `OrphanReapLive` sub-T; keeping both would split SC#5 coverage across two build-tag-isolated suites). | `test/e2e/phase2_sc5_orphan_test.go::OrphanReapLive`. |

    The authoritative Makefile diff (already applied):

    Removed targets / variables:
    - `dev-up`, `dev-down` (docker-compose verbs)
    - `DOCKER_COMPOSE` variable
    - Phase 1 Plan 10 local-dev comment block (the prose explaining `docker-compose` hyphen-vs-plugin)
    - `uat-sc5` target (invoked `scripts/uat-sc5.sh`)
    - `e2e-kind` target (invoked `scripts/e2e-kind.sh`)
    - `e2e-kind-cleanup` target (invoked `scripts/e2e-kind.sh --cleanup`)

    Retained / rewritten:
    - `.PHONY: e2e e2e-focus test-e2e e2e-full e2e-keep` — the line is now exactly these five tokens; no `e2e-kind*` left.
    - `test-e2e: e2e-full` alias kept per D-10-v3 (Kubebuilder convention).
    - `cluster-up`, `cluster-down`, `cluster-hydrate`, `cluster-keep`, `cluster-status` — sister-adopted from commit `e1b943c`; unchanged.
    - `operator-redeploy`, `logs-operator`, `logs-litellm`, `logs-toolhive`, `watch-crs`, `pf-litellm`, `wait-operator`, `wait-litellm`, `wait-toolhive` — sister-adopted; the few dev-loop affordances that survived the 2026-05-20 re-pivot (the broader ~25-target bouquet from the original 02.3-10 plan was rejected per CONTEXT.md `<domain>` "What is OUT of scope").

    The authoritative comment-cleanup diff (already applied):
    - `test/e2e/e2e_suite_test.go:73-80` — block comment rewritten so it references `scripts/cluster.sh` (the orchestrator's `E2E_SKIP_SETUP=1` path). No `scripts/e2e-kind.sh` mention remains.
    - `config/dev-postgres/postgres_deployment.yaml:4-5` — header drops the `docker-compose.yml` / `docker-compose.spike.yaml` mention; the file still ships an in-cluster Postgres consumed by `config/e2e/kustomization.yaml`.
    - `scripts/dev.sh:12` — header drops `docker-compose` mention; the devtools image is still the recommended Go toolchain (just no compose dependency).

    Survival list (the "still on disk" decision, per CONTEXT.md `<domain>` deliverable #7):
    - `config/dev-postgres/` directory STAYS. It is still consumed by `scripts/cluster.sh:119-121` via `kustomize build config/e2e | kubectl apply -f -`. Migrating ACH Postgres to bitnami Helm is its own future phase, NOT 02.3.

    Verify by inspection (no edits — read-only assertion):

    1. `git ls-files scripts/uat-g1.sh scripts/uat-sc5.sh scripts/litellm-config.yaml scripts/e2e-kind.sh docker-compose.yml internal/orphan/uat_test.go` returns 0 lines (or the equivalent `find` shows none exist).
    2. `grep -nE '^(dev-up|dev-down|uat-sc5|e2e-kind|e2e-kind-cleanup):' Makefile` returns 0 lines.
    3. `grep -nE 'DOCKER_COMPOSE' Makefile` returns 0 lines.
    4. `grep -n '^.PHONY: e2e e2e-focus test-e2e e2e-full e2e-keep$' Makefile` returns exactly 1 line (the rewritten phony line).
    5. `grep -n '^test-e2e:' Makefile` returns 1 line (the alias survives).
    6. `grep -n 'scripts/e2e-kind.sh' test/e2e/e2e_suite_test.go` returns 0 lines; `grep -n 'cluster.sh' test/e2e/e2e_suite_test.go` returns ≥ 1 line.
    7. `grep -n 'docker-compose' config/dev-postgres/postgres_deployment.yaml scripts/dev.sh` returns 0 lines.
    8. `kustomize build config/e2e` exits 0 (the dev-postgres reference path still resolves).
    9. `go vet ./...`, `go vet -tags=e2e ./test/e2e/...`, `go build ./...` all exit 0.

    If any of these checks FAILS, this plan FAILS — Plan 03 must not proceed. In that case, surface the diff to the user; do not silently restore deleted files.
  </action>
  <verify>
    <automated>
    set -e; \
    test ! -f scripts/uat-g1.sh && \
    test ! -f scripts/uat-sc5.sh && \
    test ! -f scripts/litellm-config.yaml && \
    test ! -f scripts/e2e-kind.sh && \
    test ! -f docker-compose.yml && \
    test ! -f internal/orphan/uat_test.go && \
    [ "$(grep -cE '^(dev-up|dev-down|uat-sc5|e2e-kind|e2e-kind-cleanup):' Makefile)" = "0" ] && \
    [ "$(grep -c 'DOCKER_COMPOSE' Makefile)" = "0" ] && \
    grep -qE '^\.PHONY: e2e e2e-focus test-e2e e2e-full e2e-keep$' Makefile && \
    grep -qE '^test-e2e:' Makefile && \
    [ "$(grep -c 'scripts/e2e-kind.sh' test/e2e/e2e_suite_test.go)" = "0" ] && \
    grep -q 'cluster.sh' test/e2e/e2e_suite_test.go && \
    [ "$(grep -c 'docker-compose' config/dev-postgres/postgres_deployment.yaml)" = "0" ] && \
    [ "$(grep -c 'docker-compose' scripts/dev.sh)" = "0" ] && \
    test -f config/dev-postgres/postgres_deployment.yaml && \
    test -f config/dev-postgres/postgres_service.yaml && \
    go vet ./... && \
    go vet -tags=e2e ./test/e2e/... && \
    go build ./...
    </automated>
  </verify>
  <acceptance_criteria>
    - All 6 deletion-set files are absent from the working tree.
    - Makefile carries no docker-compose-era target/variable; the `.PHONY: e2e...` line matches the post-strip form; `test-e2e` alias survives.
    - Stale header references to docker-compose or scripts/e2e-kind.sh are absent from the four touched comment files.
    - `kustomize build config/e2e` still resolves (config/dev-postgres survived).
    - All Go build/vet commands exit 0.
    - This plan does NOT modify any file — it asserts state established by orchestrator step 3.
  </acceptance_criteria>
  <done>
    The docker-compose-era artifact removal is documented and the working-tree state matches the contract above. Plan 03 can rely on this baseline when authoring the atomic commit.
  </done>
</task>

</tasks>

<verification>
This plan documents already-applied deletions and Makefile edits. The verification gate is a structural read-only audit of the working tree. The actual commit boundary that ratifies these deletions is owned by Plan 03 (`git commit` task), which bundles the deletions, the new SC#5 file (Plan 01's subject), the planning docs, and the forward-pointer touch-ups into a single atomic commit OR splits into 2-3 commits per the executor's granularity judgment.
</verification>

<success_criteria>
- All 6 deletion-set files are absent.
- Makefile + 3 comment-cleanup files match the post-strip form documented in Task 1.
- `kustomize build config/e2e` + `go build ./...` + `go vet ./...` + `go vet -tags=e2e ./test/e2e/...` all exit 0.
- This PLAN.md exists at `.planning/phases/02.3-uat-pivot-to-kind-helm/02.3-02-PLAN.md`.
</success_criteria>

<output>
Create `.planning/phases/02.3-uat-pivot-to-kind-helm/02.3-02-SUMMARY.md` when done. The SUMMARY records that this plan is RETROSPECTIVE (no new mutations), enumerates the 6 deleted files with their rationale, captures the Makefile diff summary (5 removals + 1 phony rewrite + test-e2e survival), and hands off to Plan 03 the live-validation + atomic-commit responsibility.
</output>
</content>
</invoke>