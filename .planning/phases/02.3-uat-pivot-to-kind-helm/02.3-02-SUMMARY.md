---
phase: 02.3-uat-pivot-to-kind-helm
plan: 02
status: complete
mode: retrospective
executed: 2026-05-20
files_deleted:
  - scripts/uat-g1.sh
  - scripts/uat-sc5.sh
  - scripts/litellm-config.yaml
  - scripts/e2e-kind.sh
  - docker-compose.yml
  - internal/orphan/uat_test.go
files_modified:
  - Makefile
  - test/e2e/e2e_suite_test.go
  - config/dev-postgres/postgres_deployment.yaml
  - scripts/dev.sh
files_retained:
  - config/dev-postgres/postgres_deployment.yaml
  - config/dev-postgres/postgres_service.yaml
  - config/dev-postgres/kustomization.yaml
---

# Plan 02.3-02: Docker-compose-era artifact deletion + Makefile strip — SUMMARY

**Status:** Complete (RETROSPECTIVE — work shipped 2026-05-20 by the orchestrator before this plan was authored)

## Deletion set (6 files)

| File | Rationale (D-XX-v3) | Replacement |
|---|---|---|
| `scripts/uat-g1.sh` | D-07-v3 (single substrate) | `test/e2e/phase2_sc5_orphan_test.go::OrphanReapLive` audit JSON assertion |
| `scripts/uat-sc5.sh` | D-07-v3 (single substrate) | `test/e2e/phase2_sc5_orphan_test.go::IntervalFloor` + `OrphanReapLive` |
| `scripts/litellm-config.yaml` | D-07-v3 (single substrate) | `test/e2e/values/litellm.values.yaml` (Helm) |
| `scripts/e2e-kind.sh` | D-08-v3 (no external callers) | direct `make e2e-full` |
| `docker-compose.yml` | D-07-v3 (kind+Helm is the only local substrate) | `scripts/cluster.sh` |
| `internal/orphan/uat_test.go` | D-09-v3 (overlapping coverage) | `test/e2e/phase2_sc5_orphan_test.go::OrphanReapLive` (subsumes `TestSC5LiveOrphanRevocation`) |

## Makefile diff

**Removed:**
- `dev-up` target + `dev-down` target (docker-compose verbs)
- `DOCKER_COMPOSE ?= docker-compose` variable
- Phase 1 Plan 10 local-dev comment block (~10 lines of prose)
- `uat-sc5` target (invoked `scripts/uat-sc5.sh`)
- `e2e-kind` target (invoked `scripts/e2e-kind.sh`)
- `e2e-kind-cleanup` target (invoked `scripts/e2e-kind.sh --cleanup`)

**Rewritten:**
- `.PHONY: e2e e2e-focus test-e2e e2e-full e2e-keep` — no more `e2e-kind`/`e2e-kind-cleanup` tokens

**Retained (D-10-v3 Kubebuilder convention):**
- `test-e2e: e2e-full` alias

**Sister-adopted post-`e1b943c`, unchanged:**
- `cluster-up`, `cluster-down`, `cluster-hydrate`, `cluster-keep`, `cluster-status`
- `operator-redeploy`, `logs-operator`, `logs-litellm`, `logs-toolhive`, `watch-crs`, `pf-litellm`, `wait-operator`, `wait-litellm`, `wait-toolhive`

(The broader ~25-target dev-loop bouquet from the original 02.3-10 plan was rejected on the 2026-05-20 re-pivot — see CONTEXT.md `<domain>` "What is OUT of scope" and `[[feedback_023_tier_framework_rejected]]`.)

## Comment cleanups (3 files)

| File | Line(s) | Change |
|---|---|---|
| `test/e2e/e2e_suite_test.go` | 73-80 | Block comment rewrites `scripts/e2e-kind.sh` reference → `scripts/cluster.sh` |
| `config/dev-postgres/postgres_deployment.yaml` | 4-5 | Header drops `docker-compose.yml` / `docker-compose.spike.yaml` mention |
| `scripts/dev.sh` | 12 | Header drops `docker-compose` mention; devtools still recommended Go toolchain |

## Survival list (intentional — NOT deleted)

| Path | Reason |
|---|---|
| `config/dev-postgres/` | Still consumed by `scripts/cluster.sh:119-121` via `kustomize build config/e2e \| kubectl apply -f -`. Migration to bitnami/postgresql Helm is out of scope per CONTEXT.md `<domain>` deliverable #7. |
| Sister-adopted scripts: `scripts/cluster.sh`, `scripts/collect-forensics.sh`, `scripts/dev.sh`, `scripts/kind-config.yaml` | Canonical e2e substrate. |
| `test/e2e/` (entire dir minus deletions): `e2e_suite_test.go`, `phase{1,2}_invariants_test.go`, `phase2_sc5_orphan_test.go`, `fixtures/`, `values/`, `README.md`, `CHART_PINS.md` | Canonical stdlib e2e suite. |
| Makefile `test-e2e` alias | D-10-v3 Kubebuilder convention. |

## Acceptance criteria verified

All structural gates in PLAN.md `<verify>` block pass:

| Gate | Result |
|---|---|
| All 6 deletion-set files absent | ✅ |
| Makefile has no `dev-up\|dev-down\|uat-sc5\|e2e-kind\|e2e-kind-cleanup:` target | ✅ |
| Makefile has no `DOCKER_COMPOSE` variable | ✅ |
| Makefile `.PHONY: e2e e2e-focus test-e2e e2e-full e2e-keep` line exact | ✅ |
| Makefile retains `test-e2e:` alias | ✅ |
| `test/e2e/e2e_suite_test.go` references `cluster.sh`, not `e2e-kind.sh` | ✅ |
| `config/dev-postgres/postgres_deployment.yaml` no `docker-compose` mention | ✅ |
| `scripts/dev.sh` no `docker-compose` mention | ✅ |
| `config/dev-postgres/{deployment,service}.yaml` survive | ✅ |
| `go vet ./...` exits 0 | ✅ |
| `go vet -tags=e2e ./test/e2e/...` exits 0 | ✅ |
| `go build ./...` exits 0 | ✅ |
| `kustomize build config/e2e` exits 0 | ✅ |

## Out of scope (intentional)

- Live cluster verification — owned by Plan 03.
- bitnami/postgresql Helm migration for ACH Postgres — future phase, not 02.3.
- Atomic commit boundary — owned by Plan 03 (single commit recommended; 2 commits if executor judgment favors).

## Verification trail

- 6 `D` entries in `git status -s` for the deletion set
- 4 `M` entries for Makefile + 3 comment-cleanup files
- All Go build/vet/kustomize gates exit 0
