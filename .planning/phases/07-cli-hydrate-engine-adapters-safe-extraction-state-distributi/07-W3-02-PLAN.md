---
phase: 07-cli-hydrate-engine-adapters-safe-extraction-state-distributi
plan: 07-W3-02
type: execute
wave: 3
depends_on:
  - 07-W3-01
files_modified:
  - internal/cli/adapter/codex/codex.go
  - internal/cli/adapter/codex/codex_test.go
autonomous: true
requirements_addressed:
  - ADAPT-01
  - ADAPT-03
  - ADAPT-04
  - ADAPT-05
  - ADAPT-06
  - ADAPT-07
must_haves:
  truths:
    - "internal/cli/adapter/codex compiles standalone and registers via init()."
    - "codex adapter implements all 7 Adapter interface methods from 07-W3-01."
    - "ID() = 'codex'; Aliases() = ['codex-cli'] case-folded (ADAPT-01)."
    - "Detect(root) scans for .codex/ directory + .codex/config.toml + ~/.codex/ in global mode; Confidence high/medium/low accumulated per ADAPT-02 / spec §7.5."
    - "RenderRuntime emits FileWrite for .codex/config.toml — TOML-encoded with [mcp_servers.<id>] tables for each manifest.Runtime.MCPServers entry (per spec §7.4 codex) + same for a2a_agents; entry.Endpoint is the URL source per ADAPT-03 + 07-W1-05 ContentRef.Endpoint; Merge = MergeDeep; Keys = []string{<each mcp_servers.id>, <each a2a_agents.id>} (ADAPT-05)."
    - "TransformPlugin distributes Claude-format plugin pieces into .codex/{prompts,agents,skills}/<name>/ (per CONTEXT.md D-05 + spec §7.4) — prompts/ trees copied verbatim; agents/ frontmatter rewritten to match codex schema (model/tools/permissions; spec §7.4)."
    - "ADAPT-07 silent-drop per D-08: codex SILENTLY DROPS hook/ + command/ components — adapter accumulates dropped names into PluginWrite.Dropped (field declared in 07-W3-01 adapter.go; this plan consumes it, never declares it); orchestrator emits a single stderr warning at end of hydration; exit code unchanged."
    - "Credential obtained via adapter.CredentialFromContext(ctx) (declared in 07-W3-01) — never read from env vars directly."
    - "ResolveOutputContent for target '.codex/config.toml' returns the bytes RenderRuntime would emit (SAFE-04 Tier 2)."
    - "ADAPT-06 scope rule: codex touches ONLY .codex/ — never .claude/, .gemini/, or .opencode/."
  artifacts:
    - path: "internal/cli/adapter/codex/codex.go"
      provides: "codex adapter — TOML merge + plugin distribution"
      contains: "package codex"
  key_links:
    - from: "internal/cli/adapter/codex/codex.go"
      to: "internal/cli/adapter"
      via: "init() Register"
      pattern: "adapter\\.Register"
---

<objective>
Ship the codex platform adapter — second of four per ADAPT-01. Parallel to 07-W3-03 (gemini-cli) and 07-W3-04 (opencode); all three reference the claudecode shape from 07-W3-01.

Per CLI spec §7.4 (codex): runtime-config rendering targets `.codex/config.toml` (TOML); MCP/A2A entries become `[mcp_servers.<id>]` tables; plugins distribute Claude-format pieces into `.codex/{prompts,agents,skills}/<name>/`; agent frontmatter rewrites the Claude `model/tools/permissions` keys to codex schema; `hooks/` + `commands/` components are silently dropped per ADAPT-07.

Per CONTEXT.md `<specifics>` "Adapter parallelism in W3": codex/gemini/opencode can land in parallel after 07-W3-01 lands the interface + PluginWrite.Dropped + WithCredential helpers + claudecode reference; each is a separate plan with independent file set. This plan touches ONLY internal/cli/adapter/codex/ — no edits to adapter.go (PluginWrite.Dropped is already declared upstream in W3-01).

Output: 2 files (1 source + 1 test) under internal/cli/adapter/codex/. Reuses adapter package types + manifest + state. New direct dep: github.com/BurntSushi/toml (TOML encoder); verify at task time whether already in go.mod (likely not — add if absent).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-CONTEXT.md
@.planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-PATTERNS.md
@internal/cli/adapter/adapter.go
@internal/cli/adapter/claudecode/claudecode.go
@internal/cli/manifest/manifest.go
@internal/cli/state/state.go
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: codex adapter — TOML merge + plugin distribution + frontmatter rewrite</name>
  <files>internal/cli/adapter/codex/codex.go, internal/cli/adapter/codex/codex_test.go</files>
  <read_first>
    internal/cli/adapter/adapter.go (07-W3-01 — Adapter interface + MergeKind + FileWrite + PluginWrite (incl. Dropped []string) + Match types + WithCredential/CredentialFromContext helpers)
    internal/cli/adapter/claudecode/claudecode.go (07-W3-01 — reference Adapter impl; codex mirrors its shape but with TOML + .codex/ target)
    spec/ach_cli_spec_v20260515_FINALv4.md §7.2 (codex detection paths + aliases)
    spec/ach_cli_spec_v20260515_FINALv4.md §7.4 (codex behavior — TOML target, plugin distribution layout, frontmatter rewrite)
    .planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-CONTEXT.md (D-05 codex transformation rules; D-08 silent-drop semantics)
    internal/cli/manifest/manifest.go (07-W1-05 — RuntimeBlock.MCPServers/A2AAgents shapes including ContentRef.Endpoint)
    go.mod (check if github.com/BurntSushi/toml is already present — if not, this plan adds it)
  </read_first>
  <behavior>
    - Package codex under internal/cli/adapter/codex/
    - Adapter struct (empty value — no state held)
    - ID() returns "codex"
    - Aliases() returns []string{"codex-cli"}
    - Detect(root string) scans for: .codex/ directory; .codex/config.toml; .codex/agents/ directory; ~/.codex/config.toml in global-mode workflows (Detect can return Low confidence even when local cwd has no codex artifacts, IF $HOME/.codex/ is present — global mode discovery). Confidence accumulation: 3+ signals → High, 2 → Medium, 1 → Low. Reasons accumulate "found .codex/config.toml" etc.
    - RenderRuntime(ctx, m, s) emits a single FileWrite: Path=".codex/config.toml"; Content=TOML-encoded structure with `[mcp_servers.<id>]` tables (url + headers + transport) for each m.Runtime.MCPServers entry and `[a2a_agents.<id>]` tables for each m.Runtime.A2AAgents entry. The `url` field is taken from entry.Endpoint (per 07-W1-05 ContentRef.Endpoint). The Bearer credential for headers["x-ach-key"] is obtained via adapter.CredentialFromContext(ctx). Merge=MergeDeep; Keys=[]string{<each mcp_servers.id with the "mcp_servers." prefix>, <each a2a_agents.id with "a2a_agents." prefix>}. The Keys prefix discipline lets STATE-05 inverse-merge target the right TOML subtable.
    - For TOML encoding, use github.com/BurntSushi/toml (the standard Go TOML library). If go.mod doesn't already have it, this plan adds it via `./scripts/dev.sh go get github.com/BurntSushi/toml@latest`.
    - TransformPlugin(ctx, src, dst string) walks the src tree (Claude Code plugin layout: `agents/`, `commands/`, `prompts/`, `skills/`, `hooks/`, `.mcp.json`); maps each to codex destinations:
      - src/prompts/<name>.md → dst/<name>/prompts/<name>.md (verbatim copy)
      - src/agents/<name>.md → dst/<name>/agents/<name>.md AFTER frontmatter rewrite (replace Claude `model:`, `tools:`, `permissions:` keys with codex-schema equivalents per §7.4 — `model:` stays, `tools:` becomes `allowed_tools:`, `permissions:` becomes `permissions:` with codex's permission grammar; the exact mapping is documented in spec §7.4 codex section)
      - src/skills/<name>.md → dst/<name>/skills/<name>.md (verbatim)
      - src/commands/ → SILENT DROP per ADAPT-07 (codex has no commands concept) — accumulate "commands" into the Dropped list
      - src/hooks/ → SILENT DROP per ADAPT-07 — accumulate "hooks"
      - src/.mcp.json → merged into .codex/config.toml's [mcp_servers] tables (handled by RenderRuntime path, NOT TransformPlugin's per-file output) — Dropped list does NOT include .mcp.json since it IS consumed, just at a different layer
    - PluginWrite returned: adapter.PluginWrite{ExtractedFiles: <list of dst files written>, Dropped: <silently-dropped names — e.g. ["commands", "hooks"] when both subdirs were present in src>}. The Dropped field is declared in adapter.go by 07-W3-01; this plan ONLY populates it.
    - MergeStrategies() returns map[string]MergeKind{".codex/config.toml": MergeDeep}
    - ResolveOutputContent(ctx, m, target) — for target ".codex/config.toml", recompute the same Content RenderRuntime would emit; for other targets (per-plugin per-file destinations like ".codex/<plugin>/agents/<name>.md") return source bytes verbatim (which is what TransformPlugin already produces — pass-through tier for non-merged files)
    - init() registers
    - Tests: TestCodex_ID; TestCodex_Aliases; TestCodex_Detect_NoCodexDir_Low; TestCodex_Detect_FullArtifacts_High; TestRenderRuntime_TomlShape (build a manifest with 1 MCP + 1 A2A each carrying Endpoint, decode the emitted TOML via BurntSushi/toml.Decode into a map, assert keys [mcp_servers.<id>] and [a2a_agents.<id>] exist with url == entry.Endpoint); TestRenderRuntime_CredentialPropagation (wrap ctx via adapter.WithCredential and assert the rendered TOML headers carry "x-ach-key": "<credential>"); TestTransformPlugin_DistributesPrompts (src dir with agents/x.md, prompts/y.md, hooks/z.md, commands/q.md → assert dst/<plugin>/{agents,prompts}/ have the right files AND PluginWrite.Dropped contains both "hooks" AND "commands"); TestTransformPlugin_FrontmatterRewrite_AgentsKeys (an input agent file with Claude `tools: [...]` becomes codex `allowed_tools: [...]`); TestResolveOutputContent_TomlRoundTrips (bytes returned equal Content from RenderRuntime FileWrite); TestRegistry_RegistersOnImport (blank-import codex, adapter.Lookup("codex") returns true)
  </behavior>
  <action>
    First check whether github.com/BurntSushi/toml is in go.mod (./scripts/dev.sh grep BurntSushi go.mod || echo missing). If missing, run `./scripts/dev.sh go get github.com/BurntSushi/toml@latest` and `./scripts/dev.sh go mod tidy`. NOTE: do NOT modify internal/cli/adapter/adapter.go — PluginWrite.Dropped is declared upstream in 07-W3-01 Task 1; this plan only consumes that field. Create internal/cli/adapter/codex/codex.go with SPDX header, package codex, imports (bytes, context, errors, io, os, path/filepath, regexp, strings, github.com/BurntSushi/toml, github.com/ackstorm/ach/internal/cli/adapter, github.com/ackstorm/ach/internal/cli/manifest, github.com/ackstorm/ach/internal/cli/state). Implement Adapter struct + all 7 methods per behavior. For RenderRuntime, build the TOML structure as a map[string]any with nested map["mcp_servers"][<id>] = {"url": entry.Endpoint, "headers": {"x-ach-key": adapter.CredentialFromContext(ctx)}, "transport": "http"} per spec §7.4. Encode via toml.NewEncoder(&buf).Encode(root). For frontmatter rewrite in TransformPlugin's agents path: use a regexp or manual line-by-line parse of the YAML frontmatter block (between `---` markers at file start); replace `tools:` lines with `allowed_tools:`; preserve other lines. Document the exact key mapping table in a code comment citing spec §7.4. init() func calls adapter.Register(&Adapter{}). Create internal/cli/adapter/codex/codex_test.go with stdlib testing + httptest where needed. Use BurntSushi/toml's Decode for round-trip assertions in TestRenderRuntime_TomlShape. NO testify.
  </action>
  <verify>
    <automated>./scripts/dev.sh make unit-pkg PKG=./internal/cli/adapter/...</automated>
  </verify>
  <acceptance_criteria>
    - `./scripts/dev.sh make unit-pkg PKG=./internal/cli/adapter/...` exits 0 (regression-safe — claudecode tests still pass)
    - `grep -q "package codex" internal/cli/adapter/codex/codex.go`
    - `grep -q "\"codex\"" internal/cli/adapter/codex/codex.go` (canonical ID)
    - `grep -q "\"codex-cli\"" internal/cli/adapter/codex/codex.go` (alias)
    - `grep -q "adapter.Register" internal/cli/adapter/codex/codex.go` (init registration)
    - `grep -q "adapter.CredentialFromContext" internal/cli/adapter/codex/codex.go` (credential via ctx, never env)
    - `grep -q "BurntSushi/toml" internal/cli/adapter/codex/codex.go`
    - `grep -q "\\.codex/config\\.toml\\|.codex/config.toml" internal/cli/adapter/codex/codex.go`
    - `grep -q "Dropped" internal/cli/adapter/codex/codex.go` (PluginWrite.Dropped populated — consumed, not declared)
    - `! grep -q "internal/cli/adapter/adapter\\.go" <(git status --porcelain)` (post-execution, adapter.go in adapter package root is NOT modified by this plan — declaration ownership stays in W3-01)
    - go.mod contains `github.com/BurntSushi/toml` (`grep -q "BurntSushi/toml" go.mod`)
    - codex_test.go contains TestRenderRuntime_TomlShape AND TestTransformPlugin_DistributesPrompts AND TestTransformPlugin_FrontmatterRewrite_AgentsKeys AND TestRenderRuntime_CredentialPropagation
    - `./scripts/dev.sh make lint-changed` exits 0
  </acceptance_criteria>
  <done>codex adapter ships; registry now has 2 of 4 adapters; gemini-cli + opencode can land in parallel after this completes. adapter.go in root package untouched — W3-01 declaration ownership preserved.</done>
</task>

</tasks>

<verification>
Wave 3 plan 07-W3-02 closes when:
- `./scripts/dev.sh make unit-pkg PKG=./internal/cli/adapter/...` exits 0 with codex + claudecode tests green
- TOML encoding round-trips correctly with Endpoint URL + credential headers
- TransformPlugin's silent-drop discipline (ADAPT-07) holds — hooks/ + commands/ are dropped, accumulated, surfaced via PluginWrite.Dropped
- files_modified frontmatter contains ONLY codex/* — adapter.go in root unchanged
</verification>

<success_criteria>
- 2 of 4 adapters shipped (claudecode + codex)
- §7.4 codex transformation contract held: TOML target, plugin distribution, frontmatter rewrite, ADAPT-07 silent drop
- BurntSushi/toml dep ships at the codex plan boundary (not the foundation)
- W3-02/03/04 parallel-safety preserved: this plan never touches adapter.go
</success_criteria>

<output>
Create `.planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-W3-02-SUMMARY.md` when done.
</output>
