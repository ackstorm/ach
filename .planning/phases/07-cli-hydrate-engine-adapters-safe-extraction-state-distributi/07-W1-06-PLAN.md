---
phase: 07-cli-hydrate-engine-adapters-safe-extraction-state-distributi
plan: 07-W1-06
type: execute
wave: 1
depends_on:
  - 07-W1-01
  - 07-W1-02
  - 07-W1-03
  - 07-W1-04
  - 07-W1-05
files_modified:
  - internal/cli/hydrate/doc.go
  - internal/cli/hydrate/flags.go
  - internal/cli/hydrate/result.go
  - internal/cli/hydrate/commit.go
  - internal/cli/hydrate/commit_test.go
  - internal/cli/hydrate/drift.go
  - internal/cli/hydrate/drift_test.go
autonomous: true
requirements_addressed:
  - STATE-01
  - STATE-03
  - STATE-04
  - STATE-07
  - STATE-08
  - STATE-09
  - STATE-10
  - STATE-11
must_haves:
  truths:
    - "internal/cli/hydrate package compiles standalone (W2 extract + W3 adapter consumed via interfaces — concrete impls supplied by orchestrator caller; stub fakes used in unit tests)."
    - "hydrate.Run(ctx, opts) (Result, error) is the SINGLE public engine entry point per CONTEXT.md `<code_context>` Integration Points #2."
    - "Opts struct carries every engine flag from D-03: Environment, Platform, Global, IncludeRuntime, OnlyRuntime, Sync, Force, DryRun, AllowSymlinks, Output, Wait bool, LockTimeout time.Duration, Verbose bool."
    - "commit.Run executes §6.7 14 steps in order: (1) lock → (2) sweep tmp → (3) read state + GuardEnvironment → (4) reconcile vs disk (silent prune of missing-but-recorded files via STATE-04 truth table) → (5) POST manifest → (6) diff scope-aware (STATE-10) → (7) GET content unconditionally (STATE-11) — W1 stubs these as 'TODO 07-W2' → (8) safe extract (07-W2) → (9) hash + classify (uses hash pkg) → (10) adapter run (07-W3) → (11) optional --sync → (12) atomic state write (STATE-07) → (13) cleanup tmp → (14) exit."
    - "Stages 7/8/9/10/11 are interface-shaped stubs in W1 (Extractor / AdapterDispatcher / Differ interfaces) — concrete impls land in W2/W3. The commit struct holds these as injectable fields per 07-PATTERNS.md test-seam discipline."
    - "drift.go implements the §8.4 four-outcome truth table: NoOp / UpstreamOnlyOverwrite / LocalEditPreserve / ConflictPreserve. LocalEditPreserve + ConflictPreserve both map to exit.Drift (code 2 per 07-W1-01) unless --force is set (STATE-04 + D-14)."
    - "Result struct surfaces counts (filesWritten, filesPreserved, filesPruned, droppedComponents []string per ADAPT-07) consumed by --verbose stderr output (CONTEXT.md Integration Points)."
    - "STATE-11 unconditional fetch is structurally enforced: the orchestrator does NOT consult state when deciding whether to GET — it ALWAYS GETs (the disk-write short-circuit happens at extract/stage time via the sha256 compare; that compare lives in 07-W2-02, not here)."
    - "STATE-10 scope filter: when OnlyRuntime is true, context iteration is skipped entirely; when IncludeRuntime is false (default), runtime iteration is skipped entirely; out-of-scope state slices remain untouched (per spec §6.3)."
    - "TEST-ONLY env-var seam ACH_E2E_PHASE7_INJECT_SIGKILL_AFTER_STEP=<N> (read once at commit.run entry); when N matches a step number, commit.go calls syscall.Kill(os.Getpid(), syscall.SIGKILL) immediately after stepN returns AND before stepN+1 begins — gives W4-01 sc2_commit_sequence_sigkill a deterministic kill point between any pair of steps. The env-var is documented as TEST-ONLY with a TODO removing it post-Phase-7-close; never read in non-test code paths beyond the single ReadEnv site; unset/empty value disables the seam (no syscall, no overhead)."
  artifacts:
    - path: "internal/cli/hydrate/commit.go"
      provides: "Run + 14-step orchestrator + Opts/Result/commit types + ACH_E2E_PHASE7_INJECT_SIGKILL_AFTER_STEP seam"
      contains: "func Run"
    - path: "internal/cli/hydrate/flags.go"
      provides: "Opts struct with all engine flags"
      contains: "type Opts struct"
    - path: "internal/cli/hydrate/drift.go"
      provides: "Drift four-outcome truth table per STATE-04"
      contains: "DriftOutcome"
  key_links:
    - from: "internal/cli/hydrate/commit.go"
      to: "internal/cli/lock"
      via: "Acquire at step 1"
      pattern: "lock\\.Acquire|Locker"
    - from: "internal/cli/hydrate/commit.go"
      to: "internal/cli/state"
      via: "Load + GuardEnvironment + Save"
      pattern: "state\\.Load|state\\.GuardEnvironment|state\\.WriteAtomic"
    - from: "internal/cli/hydrate/commit.go"
      to: "internal/cli/manifest"
      via: "Fetch at step 5"
      pattern: "manifest\\.Fetch"
    - from: "internal/cli/hydrate/commit.go"
      to: "internal/cli/exit"
      via: "raises Drift/EnvironmentMismatch/SchemaMismatch CodedError"
      pattern: "exit\\.(Drift|EnvironmentMismatch|SchemaMismatch)"
    - from: "internal/cli/hydrate/drift.go"
      to: "internal/cli/hash"
      via: "hash comparisons feed the truth table"
      pattern: "hash\\.Hash"
    - from: "internal/cli/hydrate/commit.go"
      to: "test/e2e/cli_hydrate_engine_test.go"
      via: "ACH_E2E_PHASE7_INJECT_SIGKILL_AFTER_STEP env-var consumed by W4-01 sc2_commit_sequence_sigkill"
      pattern: "ACH_E2E_PHASE7_INJECT_SIGKILL_AFTER_STEP"
---

<objective>
Ship `internal/cli/hydrate/` — the 14-step commit-sequence orchestrator skeleton (D-01 4-wave structure + D-02 W1 atomic boundary), the public `Run` entry point, the `Opts`/`Result` types, the §8.4 drift four-outcome truth table, AND the test-only SIGKILL injection seam consumed by 07-W4-01 sc2.

Per D-02: W1 atomic boundary covers state + lock + atomic-write + the orchestrator skeleton. NO content fetch (07-W2 extract), NO adapter dispatch (07-W3). Stages 7/8/9/10/11 are interface-shaped stubs that the orchestrator calls; concrete impls are injected by the caller layer in 07-W3-05 (cobra wiring). Unit tests use fakes.

Purpose: This is the engine's load-bearing nucleus. Every Phase 7 invariant the spec calls 'commit sequence' lives here. Stages 1-6 (lock, sweep, read state, reconcile, manifest, diff) are FULLY implemented in W1 — they have no W2/W3 dependencies. Stages 7-10 are interface-stubbed (real impls land in W2/W3, dropped into commit.go via the injectable interface fields). Stage 12 (atomic state write) reuses state.WriteAtomic. Stage 13 (cleanup tmp) reuses state.SweepTmp. STATE-04 drift truth table is fully implemented in W1 because drift logic is pure xxh3 string comparison — no dep on extract or adapter.

The TEST-ONLY env-var ACH_E2E_PHASE7_INJECT_SIGKILL_AFTER_STEP lets the W4-01 sc2_commit_sequence_sigkill subtest induce a deterministic SIGKILL between any pair of commit-sequence steps without timing flakiness. Without this seam the e2e would have to use `timeout --signal=KILL 0.5s` and accept a flaky race — D-22 close criterion requires the test to land at exit 0 reliably. Documented inline as test-only + TODO post-Phase-7-close removal. Unset/empty value short-circuits before any syscall; no production-path overhead beyond the env-var read at orchestrator entry.

Output: 7 files (4 source + 2 test + doc.go) under internal/cli/hydrate/. Unit-tested with fakes covering: end-to-end commit happy path (with stub Extractor + stub AdapterDispatcher), the SIGKILL-injection seam is reachable + dispatchable for known step numbers, GuardEnvironment exit-4 path, ErrSchemaMismatch exit-5 path, drift truth table exit-2 path, --force overrides.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-CONTEXT.md
@.planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-PATTERNS.md
@internal/cli/state/state.go
@internal/cli/lock/lock.go
@internal/cli/manifest/manifest.go
@internal/cli/hash/xxh3.go
@internal/cli/exit/exit.go
@cmd/ach-cli/cmd/hydrate.go
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Opts + Result + interface stubs (Extractor, AdapterDispatcher) + doc.go</name>
  <files>internal/cli/hydrate/doc.go, internal/cli/hydrate/flags.go, internal/cli/hydrate/result.go</files>
  <read_first>
    .planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-CONTEXT.md (D-02 W1 atomic boundary; D-03 engine flag list; Integration Points "Engine entry point" — hydrate.Run signature)
    .planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-PATTERNS.md ("internal/cli/hydrate/commit.go" + "internal/cli/hydrate/flags.go" + "internal/cli/hydrate/result.go" sections)
    cmd/ach-cli/cmd/hydrate.go (Phase 6 hydrateInputs struct lines 167-180 — flag bag idiom; runHydrate flow lines 192-267)
    spec/ach_cli_spec_v20260515_FINALv4.md §6.7 (14-step commit sequence — names the stages whose interfaces this task defines)
    spec/ach_cli_spec_v20260515_FINALv4.md §6.3 (diff computation + scope-selection table for STATE-10)
  </read_first>
  <behavior>
    - Opts struct fields (all engine flags from D-03): Environment string, Platform string, Global bool, IncludeRuntime bool, OnlyRuntime bool, Sync bool, Force bool, DryRun bool, AllowSymlinks bool, Output string, Wait bool, LockTimeout time.Duration, Verbose bool, BaseURL string, Bearer string, Stderr io.Writer (test seam), Stdout io.Writer (test seam)
    - Result struct fields: FilesWritten int, FilesPreserved int, FilesPruned int, DroppedComponents []string (per ADAPT-07 — surfaced to --verbose), PlatformID string (detected or supplied)
    - Extractor interface (stubbed in W1, impl in W2): ExtractContent(ctx, manifest.ContentRef, achDir string) (ExtractResult, error) where ExtractResult is a tuple of (writtenFiles []FileWrite, sourceHash string, error)
    - AdapterDispatcher interface (stubbed in W1, impl in W3): Render(ctx, *manifest.Manifest, *state.File, achDir string) (RenderResult, error) where RenderResult carries dropped-component names + written files
    - Differ interface (fully implemented in W1 — drift.go defines it): Compare(stateEntry *state.FileEntry, freshOnDisk string, freshSource string) DriftOutcome
    - Locker interface comes from 07-W1-03 (no re-declaration here — just import)
    - StateStore interface (W1 — wraps state.Load + state.Save + state.GuardEnvironment for test injection): Load(path) (*state.File, error); Save(path, *state.File) error; GuardEnvironment(*state.File, string, bool) error
    - doc.go documents the W1 atomic boundary (D-02) + the 14-step sequence + the interface stubs that W2/W3 replace; cites every STATE-* REQ-ID this plan addresses; cites the TEST-ONLY ACH_E2E_PHASE7_INJECT_SIGKILL_AFTER_STEP env-var seam consumed in Task 2 (commit.go) with explicit TODO marker for post-Phase-7-close removal
  </behavior>
  <action>
    Create internal/cli/hydrate/doc.go: SPDX header, package hydrate doc body. State the package role (14-step commit orchestrator per CLI spec §6.7); cite D-02 W1 atomic boundary (no extract, no adapter dispatch in W1 — interface-stubbed); cite STATE-01..STATE-11 closed set; cite D-09 layout (siblings: state, lock, hash, manifest, extract, adapter — this package imports each). Add a Test Seam discipline bullet: all stage-7-onward stages reach through interface fields on the commit struct so W1 unit tests can use fakes. Add a TEST-ONLY section calling out ACH_E2E_PHASE7_INJECT_SIGKILL_AFTER_STEP: documented as test-only, consumed by W4-01 sc2; TODO post-Phase-7-close removal. Create internal/cli/hydrate/flags.go: SPDX header, package hydrate, define Opts struct with the fields listed in behavior. Add field comments citing the originating decision (e.g. `Force bool // STATE-04 --force overrides drift; STATE-03 --force overrides Environment guard`). Create internal/cli/hydrate/result.go: SPDX header, Result struct per behavior. Add interface declarations for Extractor, AdapterDispatcher, Differ, StateStore — each as a single-method or small-method interface. Each interface declaration carries a TODO comment naming the plan that supplies the concrete impl (e.g. `// TODO 07-W2-01 supplies the concrete Extractor`). The interfaces use the io.Reader / *state.File / *manifest.Manifest types from their respective packages — manifest+state imports are needed in result.go (introduces hash, lock implicitly via Opts but those import where used).
  </action>
  <verify>
    <automated>./scripts/dev.sh go build ./internal/cli/hydrate/...</automated>
  </verify>
  <acceptance_criteria>
    - `./scripts/dev.sh go build ./internal/cli/hydrate/...` exits 0 (compiles standalone with interface stubs)
    - `grep -q "type Opts struct" internal/cli/hydrate/flags.go`
    - `grep -q "type Result struct" internal/cli/hydrate/result.go`
    - `grep -q "type Extractor interface" internal/cli/hydrate/result.go`
    - `grep -q "type AdapterDispatcher interface" internal/cli/hydrate/result.go`
    - `grep -q "type Differ interface" internal/cli/hydrate/result.go`
    - `grep -q "type StateStore interface" internal/cli/hydrate/result.go`
    - `grep -c "TODO 07-W[23]" internal/cli/hydrate/result.go` is >= 2 (Extractor + AdapterDispatcher both flagged as W2/W3 impl)
    - `grep -q "ACH_E2E_PHASE7_INJECT_SIGKILL_AFTER_STEP" internal/cli/hydrate/doc.go` (seam documented in doc.go)
    - Opts struct field count >= 12 (matches D-03 flag list)
    - `./scripts/dev.sh make lint-changed` exits 0
  </acceptance_criteria>
  <done>Opts/Result/interface stubs compile and are ready for Task 2 (commit.go orchestrator) and Task 3 (drift truth table) to consume.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: commit.go 14-step orchestrator skeleton + Run entry point + TEST-ONLY SIGKILL injection seam</name>
  <files>internal/cli/hydrate/commit.go, internal/cli/hydrate/commit_test.go</files>
  <read_first>
    internal/cli/hydrate/flags.go (Task 1 — Opts struct)
    internal/cli/hydrate/result.go (Task 1 — Result + interfaces)
    internal/cli/state/state.go (Load + Save signatures)
    internal/cli/state/guard.go (GuardEnvironment signature; ErrEnvironmentGuard sentinel)
    internal/cli/state/sweep.go (SweepTmp signature)
    internal/cli/state/path.go (ResolvePath signature)
    internal/cli/lock/lock.go (Locker interface + AcquireMode constants + ErrLockContended + ErrLockTimeout)
    internal/cli/manifest/manifest.go (Fetch signature + ErrSchemaMismatch)
    internal/cli/exit/exit.go (Drift / EnvironmentMismatch / SchemaMismatch / CollisionRefuse Code constants + CodedError type)
    spec/ach_cli_spec_v20260515_FINALv4.md §6.7 (14-step sequence — name each step in code comments verbatim)
    cmd/ach-cli/cmd/hydrate.go (runHydrate flow analog — flat sequential steps, return on each error, lines 192-267)
  </read_first>
  <behavior>
    - Run(ctx, opts Opts) (Result, error) is the package's single public entry point
    - Internally Run constructs a commit struct holding: opts Opts, stateStore StateStore, locker Locker, extractor Extractor, adapter AdapterDispatcher, differ Differ — defaults are wired in Run via package-private newDefaultStateStore() / lock.NewLocker(path) / drift.NewDiffer() / nil extractor + nil adapter in W1 (W3-05 wiring supplies real instances)
    - In W1 unit tests, every field is overridable via a `*commit` returned by an unexported `newCommit(opts, deps)` constructor (test seam)
    - commit.Run executes the 14 steps in order (each as an unexported method on *commit). The method names are step1Lock, step2SweepTmp, step3ReadState, step4ReconcileVsDisk, step5Manifest, step6Diff, step7Fetch, step8Extract, step9Hash, step10Adapter, step11Sync, step12WriteState, step13Cleanup. Step 14 is implicit return.
    - TEST-ONLY SIGKILL injection seam: at commit construction time, read os.Getenv("ACH_E2E_PHASE7_INJECT_SIGKILL_AFTER_STEP") once; strconv.Atoi into commit.injectSigkillAfterStep (default 0 = disabled). After each stepN returns (inside the dispatch loop in c.run), if c.injectSigkillAfterStep == N, call syscall.Kill(os.Getpid(), syscall.SIGKILL) — process dies before stepN+1 executes; no recovery, no defer-fired cleanups beyond what the kernel does on SIGKILL. Document inline above the field declaration with: `// injectSigkillAfterStep — TEST-ONLY seam consumed by W4-01 sc2_commit_sequence_sigkill. Read from ACH_E2E_PHASE7_INJECT_SIGKILL_AFTER_STEP. Zero/unset disables. TODO(post-Phase-7-close): remove this seam once sc2 stabilizes via a less invasive mechanism.`. The env-var read happens exactly once at newCommit() entry; production callers never set it; unit tests verify the seam is reachable (TestCommit_SigkillSeam_ReachableForKnownStep) by injecting a stub that records the would-be-killed step number into a *int instead of actually calling syscall.Kill — this seam testability requires factoring the "kill" action behind a function-typed field on commit (e.g. commit.killFn func(int) — default to func(step int) { syscall.Kill(os.Getpid(), syscall.SIGKILL) }; test override sets it to a recorder).
    - Step 1 (Lock): resolve <ach-dir> via state.ResolvePath(opts.Output or cwd, opts.Environment, opts.Global); ensure parent dir exists via os.MkdirAll(achDir, 0755); call locker.Acquire(ctx, mode, timeout) where mode = opts.Wait ? AcquireWait : (opts.LockTimeout > 0 ? AcquireWithTimeout : AcquireFailFast); on ErrLockContended return &exit.CodedError{Code: exit.General, ...}; on ErrLockTimeout return &exit.CodedError{Code: exit.General, ...}; defer lease.Release()
    - Step 2 (SweepTmp): call state.SweepTmp(achDir); errors swallowed silently per spec §6.7 step 2
    - Step 3 (ReadState + Guard): stateStore.Load(filepath.Join(achDir, "state.json")); on errors.Is(err, state.ErrSchemaMismatch) AND !opts.Force return &exit.CodedError{Code: exit.SchemaMismatch, ...}; if opts.Force log a warning to opts.Stderr and proceed with a fresh File; call stateStore.GuardEnvironment(loaded, opts.Environment, opts.Force); on errors.Is(err, state.ErrEnvironmentGuard) return &exit.CodedError{Code: exit.EnvironmentMismatch, ...}
    - Step 4 (ReconcileVsDisk): for each FileEntry in loaded.{Prompts,Plugins,Artifacts,RuntimeFiles,Adapter.Files}, stat the target; if missing on disk, silently drop from the in-memory state slice (STATE-04 "Tracked file missing on disk → silently pruned"); record count in result.FilesPruned
    - Step 5 (Manifest): build httpclient.Client via Phase 6 helpers (or via a manifestFetcher Differ field for test seam — recommend a manifestFetcher interface defaulting to manifest.Fetch); call fetcher(ctx, client, opts.Environment); on errors.Is(err, manifest.ErrSchemaMismatch) return &exit.CodedError{Code: exit.SchemaMismatch, ...}; the package may declare its own httpclient field of interface type to inject test fakes
    - Step 6 (Diff scope-aware): apply STATE-10 — when opts.OnlyRuntime, skip context block iteration; when !opts.IncludeRuntime && !opts.OnlyRuntime, skip runtime block iteration; emit a slice of diff targets for stages 7-10
    - Steps 7-10 (Fetch / Extract / Hash / Adapter): W1 STUB — if commit.extractor == nil OR commit.adapter == nil, return early with a Result reflecting steps 1-6 only AND a sentinel error sentinel (or nil if --dry-run). When concrete extractor/adapter are present (W3-05 wiring), invoke them per CLI spec §6.7 steps 7-10. W1 unit tests assert behavior with fake Extractor + AdapterDispatcher injected.
    - Step 11 (Sync): if opts.Sync, iterate state entries for missing-from-manifest files and call deepest-first deletion (STATE-05 helper — recommend a sync.go file in W2 or W3; W1 stubs this as an interface method on Differ)
    - Step 12 (WriteState): if !opts.DryRun, marshal result-derived state.File and call stateStore.Save(filepath.Join(achDir, "state.json"), state). This is the load-bearing barrier — last write before successful return.
    - Step 13 (Cleanup): state.SweepTmp(achDir) again; errors swallowed
    - Result.FilesWritten / FilesPreserved / FilesPruned populated per step accounting
    - Tests use fakes: TestCommit_HappyPath uses stub extractor returning no files + stub adapter returning no files + a manifest fixture with empty runtime+context; asserts Run returns a Result with all counts zero and no error; TestCommit_GuardEnvironmentMismatch_ExitCode4 seeds a state.File with Environment="prod" and Opts{Environment: "dev"}, asserts the returned error is *exit.CodedError with Code == exit.EnvironmentMismatch; TestCommit_GuardEnvironmentMismatch_ForceBypasses asserts opts.Force = true bypasses to next stage; TestCommit_SchemaMismatch_ExitCode5 seeds state.json with schemaVersion="v1", asserts Code == exit.SchemaMismatch; TestCommit_ManifestSchemaMismatch_ExitCode5 injects a fake fetcher returning manifest.ErrSchemaMismatch; TestCommit_LockContended_ExitCode1 injects a fake Locker returning ErrLockContended; TestCommit_DryRun_NoStateWrite asserts opts.DryRun = true skips step 12; TestCommit_SigkillSeam_ReachableForKnownStep injects a stub killFn that records the step number into a *int, sets c.injectSigkillAfterStep = 11, runs c.run, asserts the recorder captured exactly 11 (the seam fired at step 11 boundary); TestCommit_SigkillSeam_DisabledByDefault asserts that when injectSigkillAfterStep == 0 the killFn is never invoked (recorder still nil after Run completes)
  </behavior>
  <action>
    Create internal/cli/hydrate/commit.go: SPDX header, package hydrate, imports per behavior (including os, strconv, syscall for the TEST-ONLY seam). Define unexported commit struct with all DI fields PLUS injectSigkillAfterStep int + killFn func(int) (defaults to func(step int) { _ = syscall.Kill(os.Getpid(), syscall.SIGKILL) }). The killFn indirection exists ONLY so unit tests can verify the seam is reachable without crashing the test runner; comment above the field calls this out + the post-Phase-7-close TODO. In newCommit(opts), read os.Getenv("ACH_E2E_PHASE7_INJECT_SIGKILL_AFTER_STEP") once; if non-empty, strconv.Atoi into injectSigkillAfterStep (silently ignore parse errors → leave zero). After each stepN method returns inside c.run's dispatch, check `if c.injectSigkillAfterStep == N { c.killFn(N) }` — production killFn calls syscall.Kill so the process dies; the next-step return code path is unreachable in production but tests stub killFn to a recorder. Implement Run(ctx, opts) (Result, error) as a thin wrapper that constructs a *commit via newCommit(opts) and calls c.run(ctx). c.run() invokes step1..step13 in order with early-return on error AND the inline killFn check after each. For step 1 use defer lease.Release() inline (must be inside the method that acquired the lease, OR return the lease from step1 and defer in run() — prefer the latter so SIGKILL during step 12 still releases via the deferred close, but note that production SIGKILL skips defers; the defer matters only for production paths NOT exercising the seam). For step 4 ReconcileVsDisk define a helper that walks each FileEntry slice and rebuilds the slice with surviving entries (os.Stat tolerance to fs.ErrNotExist). For step 5 use a fetcher interface field so tests inject a fake; default to a closure calling manifest.Fetch. For step 12 use stateStore.Save with the freshly computed *state.File. Define newCommit(opts) *commit setting default DI values: stateStore = packageDefaultStateStore (wrapping state.Load + Save + GuardEnvironment), locker = lock.NewLocker(path) (constructed when achDir resolves), extractor = nil (W1 stub — caller wires real in W3-05), adapter = nil (same), differ = drift.NewDiffer() (concrete from Task 3). Create internal/cli/hydrate/commit_test.go: SPDX header, table-driven tests per behavior. Use fake StateStore + Locker + Extractor + AdapterDispatcher + Differ implementations defined locally in commit_test.go (no separate test-helpers file). Each fake is the simplest possible impl satisfying the interface (e.g. fakeLocker holds an error to return on Acquire and a noop lease). Use t.TempDir() for the achDir. For TestCommit_SigkillSeam_ReachableForKnownStep: construct *commit via newCommit; override killFn to func(step int) { recorded = step }; set c.injectSigkillAfterStep = 11; run c.run; assert recorded == 11. NO testify imports.
  </action>
  <verify>
    <automated>./scripts/dev.sh make unit-pkg PKG=./internal/cli/hydrate/...</automated>
  </verify>
  <acceptance_criteria>
    - `./scripts/dev.sh make unit-pkg PKG=./internal/cli/hydrate/...` exits 0
    - `grep -q "func Run" internal/cli/hydrate/commit.go`
    - `grep -qE "step[1-9]|step1[0-3]" internal/cli/hydrate/commit.go` (numbered step methods present)
    - `grep -q "lease.Release\|defer.*Release" internal/cli/hydrate/commit.go` (lock release deferred)
    - `grep -q "exit.EnvironmentMismatch" internal/cli/hydrate/commit.go`
    - `grep -q "exit.SchemaMismatch" internal/cli/hydrate/commit.go`
    - `grep -q "ACH_E2E_PHASE7_INJECT_SIGKILL_AFTER_STEP" internal/cli/hydrate/commit.go` (env-var read site present)
    - `grep -q "TEST-ONLY\\|TODO.*Phase-7-close\\|TODO(post-Phase-7-close)" internal/cli/hydrate/commit.go` (seam removal TODO marker)
    - `grep -q "injectSigkillAfterStep" internal/cli/hydrate/commit.go`
    - `grep -q "killFn" internal/cli/hydrate/commit.go` (testable indirection)
    - `grep -q "TestCommit_GuardEnvironmentMismatch_ExitCode4" internal/cli/hydrate/commit_test.go`
    - `grep -q "TestCommit_SchemaMismatch_ExitCode5" internal/cli/hydrate/commit_test.go`
    - `grep -q "TestCommit_HappyPath" internal/cli/hydrate/commit_test.go`
    - `grep -q "TestCommit_SigkillSeam_ReachableForKnownStep" internal/cli/hydrate/commit_test.go`
    - `./scripts/dev.sh make lint-changed` exits 0
  </acceptance_criteria>
  <done>14-step orchestrator runs end-to-end against fakes; the SIGKILL seam is reachable via injected killFn (test) and lethal in production (real syscall); W3-05 wiring will replace stub extractor + adapter with real impls without touching commit.go.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: drift.go — STATE-04 four-outcome truth table</name>
  <files>internal/cli/hydrate/drift.go, internal/cli/hydrate/drift_test.go</files>
  <read_first>
    spec/ach_cli_spec_v20260515_FINALv4.md §8.4 (drift four-outcome truth table — this is the authoritative spec; the table maps state.hash + state.sourceHash + onDiskHash + freshSourceHash → outcome)
    .planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-CONTEXT.md (D-14 dual-hash discipline + D-16 --sync inverse-merge — drift wins; user edits sacred)
    internal/cli/hydrate/result.go (Task 1 — Differ interface declaration)
    internal/cli/state/state.go (FileEntry struct — Hash + SourceHash fields)
    internal/cli/hash/xxh3.go (Hash + HashBytes signatures — drift consumes pre-computed string digests, not raw bytes)
  </read_first>
  <behavior>
    - DriftOutcome typed int: NoOp (iota+1), UpstreamOnlyOverwrite, LocalEditPreserve, ConflictPreserve
    - Compare(stateEntry *state.FileEntry, onDiskHash string, freshSourceHash string) DriftOutcome
    - Truth table per spec §8.4 (computed at Compare time from the four digest values; stateEntry.Hash + stateEntry.SourceHash + onDiskHash + freshSourceHash):
      - onDiskHash == stateEntry.Hash && freshSourceHash == stateEntry.SourceHash → NoOp (nothing changed upstream or locally)
      - onDiskHash == stateEntry.Hash && freshSourceHash != stateEntry.SourceHash → UpstreamOnlyOverwrite (upstream moved; no local edit; safe to overwrite without warning)
      - onDiskHash != stateEntry.Hash && freshSourceHash == stateEntry.SourceHash → LocalEditPreserve (local edit exists; upstream unchanged; preserve file + exit 2 in caller)
      - onDiskHash != stateEntry.Hash && freshSourceHash != stateEntry.SourceHash → ConflictPreserve (both moved; preserve file + exit 2 in caller; --force overrides)
    - NewDiffer() returns the concrete Differ impl (a value-type, no state held)
    - ShouldExit2(outcome DriftOutcome) bool helper: returns true for LocalEditPreserve OR ConflictPreserve
    - WrapDriftError(outcome DriftOutcome, target string) error returns nil for NoOp / UpstreamOnlyOverwrite; returns &exit.CodedError{Code: exit.Drift, Msg: "drift detected on <target> (outcome=<name>)"} for LocalEditPreserve / ConflictPreserve
    - Tests: TestDrift_TruthTable iterates a table of {stateHash, stateSourceHash, onDiskHash, freshSourceHash} input quadruples and asserts each maps to the correct DriftOutcome (cover all 4 outcomes plus the empty-state edge case where stateEntry == nil → NoOp because there's nothing to compare); TestShouldExit2_LocalEdit_True; TestShouldExit2_NoOp_False; TestWrapDriftError_LocalEdit_HasExitCode2 asserts the returned CodedError.Code == exit.Drift
  </behavior>
  <action>
    Create internal/cli/hydrate/drift.go: SPDX header, package hydrate. Define `type DriftOutcome int` with the const block. Define `type Differ struct {}` (zero-state) implementing the Differ interface from result.go via a Compare method. Implement Compare with the truth table — write four if-branches in the order listed in behavior (NoOp first, ConflictPreserve last); each branch returns the corresponding constant. Handle stateEntry == nil by returning NoOp (no prior state means there's nothing to compare against — fresh-extract paths bypass drift). Implement ShouldExit2 + WrapDriftError as package-level functions taking a DriftOutcome. Add an outcomeString helper for human-readable error messages (NoOp/UpstreamOnlyOverwrite/LocalEditPreserve/ConflictPreserve). NewDiffer() returns *Differ. Create internal/cli/hydrate/drift_test.go: SPDX header. TestDrift_TruthTable as a table-driven test with 4+ cases — each case is a struct of {name, stateHash, stateSourceHash, onDiskHash, freshSourceHash, want DriftOutcome}. Use literal "xxh3:aaaa...0001" / "xxh3:bbbb...0002" / etc. as the digest strings (the actual byte values don't matter — string equality is what drives the truth table). NO testify; NO assertion library. Add TestWrapDriftError_LocalEdit_HasExitCode2 with errors.As + asserting the CodedError.Code == exit.Drift.
  </action>
  <verify>
    <automated>./scripts/dev.sh make unit-pkg PKG=./internal/cli/hydrate/...</automated>
  </verify>
  <acceptance_criteria>
    - `./scripts/dev.sh make unit-pkg PKG=./internal/cli/hydrate/...` exits 0
    - `grep -q "type DriftOutcome int" internal/cli/hydrate/drift.go`
    - `grep -E "NoOp|UpstreamOnlyOverwrite|LocalEditPreserve|ConflictPreserve" internal/cli/hydrate/drift.go | wc -l | grep -q "^4$"` (4 constants present)
    - `grep -q "exit.Drift" internal/cli/hydrate/drift.go` (WrapDriftError uses code 2)
    - `grep -q "TestDrift_TruthTable" internal/cli/hydrate/drift_test.go`
    - `grep -q "TestWrapDriftError_LocalEdit_HasExitCode2" internal/cli/hydrate/drift_test.go`
    - Truth table test contains at least 4 sub-cases (one per DriftOutcome variant)
    - `./scripts/dev.sh make lint-changed` exits 0
  </acceptance_criteria>
  <done>STATE-04 §8.4 truth table is computable end-to-end via Differ.Compare; caller (commit.go step 9) wires it into the engine flow.</done>
</task>

</tasks>

<verification>
Wave 1 plan 07-W1-06 closes when:
- `./scripts/dev.sh make unit-pkg PKG=./internal/cli/hydrate/...` exits 0 with all subtests green
- `./scripts/dev.sh make unit-pkg PKG=./internal/cli/state/...` AND `./scripts/dev.sh make unit-pkg PKG=./internal/cli/lock/...` AND `./scripts/dev.sh make unit-pkg PKG=./internal/cli/hash/...` AND `./scripts/dev.sh make unit-pkg PKG=./internal/cli/manifest/...` AND `./scripts/dev.sh make unit-pkg PKG=./internal/cli/exit/...` all still exit 0 (regression-safe)
- Run(ctx, opts) is the single public engine entry point; 14-step orchestrator + drift truth table are the structural enforcers for STATE-01/03/04/07/08/09/10/11
- W2/W3 plans can land Extractor + AdapterDispatcher concrete impls by setting fields on a *commit value without modifying commit.go's step methods
- TEST-ONLY ACH_E2E_PHASE7_INJECT_SIGKILL_AFTER_STEP seam is reachable, unit-tested via killFn indirection, and inline-documented with a TODO post-Phase-7-close marker
</verification>

<success_criteria>
- Phase 7 engine has a working skeleton: lock → sweep → state read → manifest → diff → (W2 extract stub) → (W3 adapter stub) → atomic write → cleanup
- STATE-04 four-outcome truth table is fully implemented; caller (07-W3-05 cobra wiring) uses it to enforce exit 2 on drift
- All STATE-* exit-code paths (4/5) raise *exit.CodedError with the correct Code constant
- Foundation closed for W2/W3 to slot concrete extractor + adapter impls without touching the orchestrator
- W4-01 sc2_commit_sequence_sigkill has a deterministic kill point (env-var seam) instead of a flaky timeout race
</success_criteria>

<output>
Create `.planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-W1-06-SUMMARY.md` when done.
</output>
