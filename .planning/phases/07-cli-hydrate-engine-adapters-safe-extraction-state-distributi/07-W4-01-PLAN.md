---
phase: 07-cli-hydrate-engine-adapters-safe-extraction-state-distributi
plan: 07-W4-01
type: execute
wave: 4
depends_on:
  - 07-W3-05
files_modified:
  - test/e2e/cli_hydrate_engine_test.go
  - test/e2e/phase7_helpers_test.go
autonomous: false
requirements_addressed:
  - STATE-01
  - STATE-02
  - STATE-03
  - STATE-04
  - STATE-05
  - STATE-06
  - STATE-07
  - STATE-08
  - STATE-09
  - STATE-10
  - STATE-11
  - ADAPT-01
  - ADAPT-02
  - ADAPT-03
  - ADAPT-04
  - ADAPT-05
  - ADAPT-06
  - ADAPT-07
  - SAFE-01
  - SAFE-02
  - SAFE-03
  - SAFE-04
  - SAFE-05
  - SAFE-06
must_haves:
  truths:
    - "test/e2e/cli_hydrate_engine_test.go drives 4-platform × {pk_, ek_} = 8 SC#1 subtests against the kept kind cluster."
    - "TestPhase7CLIEngine umbrella's `sc2_commit_sequence_sigkill` subtest induces a SIGKILL between §6.7 steps 11 and 12 via ACH_E2E_PHASE7_INJECT_SIGKILL_AFTER_STEP=11 (env-var seam landed by 07-W1-06 Task 2) and asserts prior state.json intact + <ach-dir>/tmp swept on resume (SC#2 / D-20). Deterministic — no timeout-race fallback."
    - "`sc3_drift_four_outcomes` subtest exercises the §8.4 truth table: no-op, upstream-only-overwrite, local-edit-preserve (exit 2), conflict-preserve (exit 2), --force overrides (STATE-04 / SC#3)."
    - "`sc4_safe_extract_malicious` subtest iterates the test/fixtures/malicious-archives/ fixture set (from 07-W2-01) and asserts every fixture rejects with exit non-zero + no files written under output (SAFE-01)."
    - "`sc4_safe_extract_bomb` subtest sets ACH_MAX_EXTRACTED_PLUGIN_MIB=1 + uses a 10MiB artificial bomb-tarball; asserts exit non-zero + partial output discarded (SAFE-03)."
    - "`sc4_autoclaim_three_tier` subtest exercises the auto-claim cascade via a final-rename collision: seed final path with bytes that match adapter output → auto-claim succeeds; seed with differing bytes → exit 7; with --force → bytes overwritten (SAFE-04)."
    - "`w1_baseline_no_op` subtest runs hydrate twice against an empty workspace and asserts the second invocation produces zero new writes + same state.json hash (D-20 baseline)."
    - "Test file has //go:build e2e tag; ACH_E2E_PHASE7=1 env-var gate; skip-on-cluster-missing pattern from phase6_helpers_test.go."
    - "Subtests pass on linux-amd64 against `make e2e-keep` kind+Helm fixture (D-22 close criterion)."
  artifacts:
    - path: "test/e2e/cli_hydrate_engine_test.go"
      provides: "Phase 7 e2e suite — 4 platforms × {pk,ek} + SC#2/3/4 + baseline"
      contains: "func TestPhase7CLIEngine"
    - path: "test/e2e/phase7_helpers_test.go"
      provides: "Phase 7 helpers — suite guard + binary path + temp XDG seeder + manifest fetcher"
      contains: "func phase7SuiteGuard"
  key_links:
    - from: "test/e2e/cli_hydrate_engine_test.go"
      to: "./bin/ach-cli"
      via: "exec.CommandContext with ACH_E2E_PHASE7 gates"
      pattern: "phase7BinaryPath\\|ach-cli"
    - from: "test/e2e/cli_hydrate_engine_test.go"
      to: "test/fixtures/malicious-archives/"
      via: "BuildAll fixture seeder + per-fixture extract attempt"
      pattern: "maliciousfixtures\\.BuildAll"
    - from: "test/e2e/cli_hydrate_engine_test.go"
      to: "internal/cli/hydrate/commit.go"
      via: "ACH_E2E_PHASE7_INJECT_SIGKILL_AFTER_STEP env-var seam consumed by sc2_commit_sequence_sigkill (declared in 07-W1-06)"
      pattern: "ACH_E2E_PHASE7_INJECT_SIGKILL_AFTER_STEP"
---

<objective>
Ship the Phase 7 e2e verifier — `test/e2e/cli_hydrate_engine_test.go` driving the Core Value path end-to-end on the kept kind cluster for all 4 platforms × {pk_, ek_} plus SC#2 (commit-sequence SIGKILL recovery, deterministic via env-var seam), SC#3 (drift truth table), SC#4 (malicious-archive + bomb + auto-claim cascade), and the W1 baseline no-op test.

Per D-20 + D-22: this is the closeout verifier — Phase 7 closes when this test green on linux-amd64 against `make e2e-keep` cluster.

Per CLAUDE.md "E2E debug loop": the kept-cluster iteration loop is the dev pattern: `./scripts/dev.sh make e2e-keep` once, then `./scripts/dev.sh make e2e-focus FOCUS=TestPhase7CLIEngine` iteratively. The `make e2e-full` final-gate path replays this from clean.

Output: 2 files (test + helpers) under test/e2e/. Builds via //go:build e2e tag — skipped unless ACH_E2E_PHASE7=1 set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-CONTEXT.md
@.planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-PATTERNS.md
@test/e2e/cli_login_hydrate_test.go
@test/e2e/phase6_helpers_test.go
@examples/04-environment-demo.yaml
@examples/hydrate.json
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: phase7_helpers_test.go — suite guard + binary path + XDG seeder + per-subtest harness</name>
  <files>test/e2e/phase7_helpers_test.go</files>
  <read_first>
    test/e2e/phase6_helpers_test.go (analog — phase6SuiteGuard lines 90-144 + phase6RunAchCli lines 243-254 + temp-XDG seeder)
    test/e2e/cli_login_hydrate_test.go (analog umbrella test + subtest harness; lines 51-260)
    .planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-PATTERNS.md ("test/e2e/phase7_helpers_test.go" section lines 503-538)
    CLAUDE.md ("E2E debug loop" — kept-cluster + e2e-focus pattern; "Waiting for state" — make wait-* targets)
    examples/04-environment-demo.yaml (the demo Environment fixture the e2e uses)
  </read_first>
  <behavior>
    - Build tag: //go:build e2e + SPDX header + package e2e
    - phase7BinaryPath = "../../bin/ach-cli" const matching phase6 pattern
    - phase7SuiteGuard(t *testing.T) — early-skip when ACH_E2E_PHASE7 != "1"; early-skip when binary absent; kubectl probe against ach-platform-api Deployment + Service (mirrors phase6SuiteGuard logic; uses runCmd helper)
    - phase7RunAchCli(t, xdgHome string, args ...string) ([]byte, []byte, error) — mirrors phase6RunAchCli; sets XDG_CONFIG_HOME=xdgHome; 60s timeout; captures stdout + stderr
    - phase7RunAchCliEnv(t, xdgHome string, extraEnv []string, args ...string) ([]byte, []byte, error) — variant that accepts additional env vars (used by sc2 to set ACH_E2E_PHASE7_INJECT_SIGKILL_AFTER_STEP and by sc4 bomb to set ACH_MAX_EXTRACTED_PLUGIN_MIB)
    - phase7SeedXdgConfig(t, baseURL string) (xdgHome, pk string) — runs `ach-cli login` (or equivalent test-helper SSO bypass) against the platform-api to obtain a fresh pk_ and write it to ~/.config/ach/config.yaml in xdgHome; returns the xdgHome path + the raw pk_ plaintext (test-only use — never logs it)
    - phase7CreateEkKey(t, xdgHome, environment string) (ekPlaintext string) — runs `ach-cli env-keys create demo --name e2e-phase7` against the seeded xdgHome; parses the plaintext output; returns the ek_ for subtests that exercise the ek_ path
    - phase7DemoEnvironmentReady(t) — wraps `make wait-cr-ready KIND=Environment NAME=demo NS=ach-system` (kubectl-equivalent) so the Environment is Available=True before any hydrate runs
    - phase7Workspace(t) string — returns a t.TempDir + os.MkdirAll(.claude) helper used by sc1 subtests
    - phase7BaseURL() string — reads ACH_E2E_PHASE7_BASEURL env var with fallback "http://localhost:8080" (matches the kind+Helm ach-local-gateway pattern in CLAUDE.md `<phase6_normalize>` failure mode)
    - Each helper logs failure context to t.Logf BEFORE t.Fatalf so CI logs are useful — no opaque "command failed" messages
  </behavior>
  <action>
    Create test/e2e/phase7_helpers_test.go with //go:build e2e tag + SPDX header + package e2e. Implement phase7SuiteGuard mirroring phase6SuiteGuard: read ACH_E2E_PHASE7; os.Stat the binary path; runCmd kubectl get deploy ach-platform-api -n ach-system; t.Skipf on any miss with a descriptive message ("Phase 7 e2e gated behind ACH_E2E_PHASE7=1; got ..."). Implement phase7RunAchCli per behavior with exec.CommandContext + context.WithTimeout(60s). Implement phase7RunAchCliEnv as the env-augmented variant — append the extraEnv slice to exec.Cmd.Env before running. Implement phase7SeedXdgConfig by writing a config.yaml directly to filepath.Join(xdgHome, "ach", "config.yaml") with the demo deployment URL + a fresh pk_ obtained via a direct httpclient.Client POST against /platform/auth/cli/init + /platform/auth/cli/token (matches the Phase 6 device-code flow; OR use a test-only direct-pk-mint endpoint if available; OR use kubectl exec on the platform-api Pod to mint a pk_ via the admin path — pick whichever is cheapest to wire). PREFERRED: mirror Phase 6 e2e's existing pk-mint helper from test/e2e/phase6_helpers_test.go (it already does this; find the function name via grep "func phase6.*Login\|func phase6.*Pk" test/e2e/phase6_helpers_test.go and re-use OR copy-paste its body with the phase6 prefix changed to phase7). Implement phase7CreateEkKey via phase7RunAchCli(xdgHome, "env-keys", "create", "demo", "--name", "e2e-phase7-"+rand). Implement phase7DemoEnvironmentReady wrapping make wait-cr-ready via exec. Implement phase7Workspace + phase7BaseURL per behavior. NO testify. NO assertion library.
  </action>
  <verify>
    <automated>./scripts/dev.sh go vet -tags=e2e ./test/e2e/</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 test/e2e/phase7_helpers_test.go | grep -q "^//go:build e2e$"`
    - `grep -q "func phase7SuiteGuard" test/e2e/phase7_helpers_test.go`
    - `grep -q "func phase7RunAchCli" test/e2e/phase7_helpers_test.go`
    - `grep -q "func phase7RunAchCliEnv" test/e2e/phase7_helpers_test.go` (env-var variant for sc2 + sc4_bomb)
    - `grep -q "func phase7SeedXdgConfig" test/e2e/phase7_helpers_test.go`
    - `grep -q "func phase7CreateEkKey" test/e2e/phase7_helpers_test.go`
    - `grep -q "ACH_E2E_PHASE7" test/e2e/phase7_helpers_test.go`
    - `grep -q "ach-platform-api" test/e2e/phase7_helpers_test.go`
    - `./scripts/dev.sh go vet -tags=e2e ./test/e2e/` exits 0
    - `./scripts/dev.sh go build -tags=e2e ./test/e2e/...` exits 0
  </acceptance_criteria>
  <done>Helpers compile under -tags=e2e; downstream Task 2 reuses them.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: cli_hydrate_engine_test.go — 8 SC#1 subtests + SC#2/3/4 subtests + W1 baseline + LIVE VERIFICATION</name>
  <files>test/e2e/cli_hydrate_engine_test.go</files>
  <read_first>
    test/e2e/phase7_helpers_test.go (Task 1 — helpers this test consumes)
    test/e2e/cli_login_hydrate_test.go (analog umbrella + subtest harness)
    .planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-CONTEXT.md (D-20 subtest list + D-22 close criteria)
    .planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-PATTERNS.md ("test/e2e/cli_hydrate_engine_test.go" section lines 460-499)
    examples/04-environment-demo.yaml (demo Environment fixture)
    spec/ach_cli_spec_v20260515_FINALv4.md §6.7 (14-step commit sequence — sc2 SIGKILL test asserts the spec contract)
    spec/ach_cli_spec_v20260515_FINALv4.md §8.4 (drift truth table — sc3 subtest follows verbatim)
    .planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-W1-06-PLAN.md (TEST-ONLY ACH_E2E_PHASE7_INJECT_SIGKILL_AFTER_STEP env-var seam consumed by sc2)
    CLAUDE.md ("E2E debug loop" — kept-cluster pattern for iteration; partial re-run via `make e2e-focus FOCUS=TestPhase7CLIEngine/<sub>`)
  </read_first>
  <what-built>
    A 14-subtest e2e suite at test/e2e/cli_hydrate_engine_test.go covering:
    - w1_baseline_no_op
    - sc1_claudecode_pk + sc1_claudecode_ek
    - sc1_codex_pk + sc1_codex_ek
    - sc1_gemini_pk + sc1_gemini_ek
    - sc1_opencode_pk + sc1_opencode_ek
    - sc2_commit_sequence_sigkill
    - sc3_drift_no_op + sc3_drift_upstream_only + sc3_drift_local_edit_preserve + sc3_drift_conflict_preserve
    - sc3_drift_force_overrides
    - sc4_safe_extract_malicious (loops fixture set)
    - sc4_safe_extract_bomb
    - sc4_autoclaim_three_tier_match + sc4_autoclaim_three_tier_differ
    
    Each sc1_*_pk subtest: phase7SeedXdgConfig → phase7DemoEnvironmentReady → phase7RunAchCli with `hydrate --environment demo --platform <id> --output <tmpdir>` → assert exit 0 + assert the adapter's canonical runtime-config file exists in tmpdir + assert state.json present with schemaVersion="2".
    Each sc1_*_ek subtest: same but uses ek_ from phase7CreateEkKey AND omits --environment (ek_ binds it).
    sc2_commit_sequence_sigkill: run hydrate once cleanly to seed a known state.json snapshot (read its bytes; hash them). Then run hydrate via phase7RunAchCliEnv with extraEnv = ["ACH_E2E_PHASE7_INJECT_SIGKILL_AFTER_STEP=11"] — the seam declared in 07-W1-06 Task 2 fires syscall.Kill(SIGKILL) after step 11 returns and before step 12 (atomic state write). The process exit code reflects SIGKILL termination (-1 / signal-killed). Assert (a) the on-disk state.json bytes equal the prior snapshot (step 12 never ran), (b) <ach-dir>/tmp/ contains at least one orphan staging dir from the killed run. Run hydrate a third time WITHOUT the env-var; assert it completes successfully + the orphan tmp/ is swept (per spec §6.7 step 2). NO TIMEOUT FALLBACK — the env-var seam is deterministic per D-22 close criterion; the previously-considered `timeout --signal=KILL 0.5s` retry-3-times fallback is REMOVED because it cannot guarantee landing between specific step boundaries.
    sc3_drift_*: seed state.json with known {hash, sourceHash} pairs; modify on-disk / upstream state to produce each of the four §8.4 outcomes; assert the right exit code (0/0/2/2) and the right stderr message. Use a fake content server OR mutate the cached file directly on disk.
    sc3_drift_force_overrides: from the conflict-preserve setup, re-run with --force; assert exit 0 + bytes overwritten.
    sc4_safe_extract_malicious: import test/fixtures/malicious-archives.BuildAll (from 07-W2-01); for each fixture, push it to the cluster's content cache (via kubectl cp OR via a fake content-service substitute) THEN run hydrate; assert exit != 0 + no files extracted under output. Per-fixture loop with t.Run subtest naming each fixture.
    sc4_safe_extract_bomb: phase7RunAchCliEnv with extraEnv = ["ACH_MAX_EXTRACTED_PLUGIN_MIB=1"]; supply a 10MiB synthetic tar.gz via httptest content server; hydrate; assert exit != 0 + partial output discarded.
    sc4_autoclaim_three_tier_match: seed final-path bytes equal to adapter's RenderRuntime output; hydrate; assert exit 0 + bytes unchanged + state.json now claims the file (auto-claim).
    sc4_autoclaim_three_tier_differ: seed final-path bytes differing; hydrate without --force; assert exit 7 + bytes preserved. Then with --force; assert exit 0 + bytes overwritten.
    
    Re-run guidance: this subtest list is bundled under one umbrella for atomic verification; a partial failure does NOT require re-running the whole suite. Per CLAUDE.md "E2E debug loop", a single failing subtest can be replayed via:
      `./scripts/dev.sh make e2e-focus FOCUS=TestPhase7CLIEngine/<failing-subtest-name>`
    e.g. `FOCUS=TestPhase7CLIEngine/sc2_commit_sequence_sigkill`. This is the standard kept-cluster iteration loop and avoids burning a full ~6-minute e2e cycle on a single regression.
  </what-built>
  <how-to-verify>
    Run the full Phase 7 verifier:
    
    ```
    # Bring up the kept cluster:
    ./scripts/dev.sh make e2e-keep
    
    # Run the Phase 7 suite:
    ACH_E2E_PHASE7=1 ./scripts/dev.sh make e2e-focus FOCUS=TestPhase7CLIEngine
    ```
    
    Expected: exit 0 with all 14+ subtests green.
    
    Partial re-run after a single subtest failure (per CLAUDE.md E2E debug loop):
    
    ```
    ACH_E2E_PHASE7=1 ./scripts/dev.sh make e2e-focus FOCUS=TestPhase7CLIEngine/<failing-subtest>
    # e.g. FOCUS=TestPhase7CLIEngine/sc2_commit_sequence_sigkill
    #      FOCUS=TestPhase7CLIEngine/sc4_safe_extract_bomb
    ```
    
    Common-failure-mode debugging steps (per CLAUDE.md):
    
    1. If `ACH_E2E_PHASE7 != "1"` — suite skips silently. Set the env var.
    2. If `./bin/ach-cli` missing — run `./scripts/dev.sh make build` first.
    3. If a sc1_* subtest fails with HTTP 503 — the Environment may not be Available=True yet. `./scripts/dev.sh kubectl -n ach-system describe environment demo` to inspect conditions. Common cause: AccessGroupSynced=False (see CLAUDE.md "Environment stuck in AccessGroupSynced=False").
    4. If sc4_safe_extract_malicious subtest reports a fixture extracted bytes — the SAFE-01 contract regressed in 07-W2-01. Check `grep "Typeflag" internal/cli/extract/tar.go`.
    5. If sc2_commit_sequence_sigkill never kills mid-sequence — the env-var seam in commit.go may have been removed or its handling regressed. Re-check 07-W1-06 Task 2: `grep "ACH_E2E_PHASE7_INJECT_SIGKILL_AFTER_STEP" internal/cli/hydrate/commit.go` AND that injectSigkillAfterStep is checked after stepN returns AND that killFn defaults to syscall.Kill in production paths.
  </how-to-verify>
  <action>
    Create test/e2e/cli_hydrate_engine_test.go: //go:build e2e + SPDX header + package e2e + imports per phase6 pattern + maliciousfixtures import. Implement TestPhase7CLIEngine umbrella with all subtests per behavior using t.Run sub-naming. Each subtest body: phase7SuiteGuard → phase7DemoEnvironmentReady → seed XDG → invoke phase7RunAchCli → assertions on stdout/stderr/exit/disk state. For sc2, use phase7RunAchCliEnv with extraEnv=["ACH_E2E_PHASE7_INJECT_SIGKILL_AFTER_STEP=11"] — this is the deterministic seam from 07-W1-06; the previously-flaky `timeout --signal=KILL 0.5s` retry-3-times fallback is REMOVED. After the killed run, re-read the state.json bytes and assert byte-for-byte equality with the pre-kill snapshot (step 12 never ran). For sc4_safe_extract_malicious, seed each fixture via inline tar generation if cluster-side content-cache mutation is too complex — alternative simpler implementation: invoke a local-mode hydrate against a httptest.Server returning the malicious tar (the engine path is the same whether the content server is the kind cluster or a httptest server). PREFER the httptest approach for sc4 subtests since it avoids cluster-side fixture seeding and exercises the same internal/cli/extract code path. For sc4_safe_extract_bomb use phase7RunAchCliEnv with extraEnv=["ACH_MAX_EXTRACTED_PLUGIN_MIB=1"]. Sc1 subtests stay against the real cluster since the value of e2e is exercising the kind+Helm + Postgres + LiteLLM-stub path end-to-end. Add per-platform assertion helpers: assertClaudeCodeWorkspace(t, dir) asserts .claude/.mcp.json with mcpServers map present; assertCodexWorkspace(t, dir) asserts .codex/config.toml; same for gemini + opencode. Each helper unmarshals the runtime-config file and asserts at least one MCP server entry from the demo Environment + the x-ach-key header attachment per ADAPT-03. NO testify. NO ginkgo.
    
    Once written, RUN the verification command per <how-to-verify>. Each failing subtest gets a t.Logf with enough context to debug. After the run, present the user with:
    - Total subtests run, passed, failed
    - Output of `./scripts/dev.sh make e2e-focus FOCUS=TestPhase7CLIEngine` tail
    - If all green: signal Phase 7 verifier passed
    - If any failed: list which subtests failed + which Phase 7 plan owns the failing concern (e.g., sc4_safe_extract_malicious failures point at 07-W2-01) AND the focused re-run command `./scripts/dev.sh make e2e-focus FOCUS=TestPhase7CLIEngine/<failing-subtest>` so the user can iterate on the kept cluster without burning a full ~6-minute e2e cycle
  </action>
  <resume-signal>
    Type "approved" if all 14+ subtests pass and the Phase 7 e2e verifier is green. If subtests fail, describe which ones and we'll route fixes to the responsible W1/W2/W3 plan. For a single-subtest re-run, use `./scripts/dev.sh make e2e-focus FOCUS=TestPhase7CLIEngine/<sub>` against the kept cluster.
  </resume-signal>
</task>

</tasks>

<verification>
Wave 4 plan 07-W4-01 closes when:
- `./scripts/dev.sh go build -tags=e2e ./test/e2e/...` exits 0
- ACH_E2E_PHASE7=1 ./scripts/dev.sh make e2e-focus FOCUS=TestPhase7CLIEngine exits 0 with all subtests green against the kept kind cluster
- D-22 close criteria met:
  - All 8 sc1_*_{pk,ek} subtests green
  - sc2_commit_sequence_sigkill green (deterministic — env-var seam, no timeout-race fallback)
  - sc3_drift_* (4 outcomes + force) green
  - sc4_safe_extract_malicious + sc4_safe_extract_bomb + sc4_autoclaim_three_tier_* green
- Human verification gate signs off (checkpoint:human-verify task above)
</verification>

<success_criteria>
- Core Value path verified end-to-end for all 4 platforms × {pk_, ek_}
- §6.7 commit-sequence crash-recovery semantics observable deterministically in sc2 subtest via ACH_E2E_PHASE7_INJECT_SIGKILL_AFTER_STEP=11 seam
- §8.4 drift truth table mechanically exercised in sc3 subtests
- SAFE-01..06 malicious-archive + bomb + cascade contracts upheld in sc4 subtests
- Phase 7 ready for ROADMAP refresh + CLAUDE.md failure-mode additions (07-W4-02)
- Iteration loop documented: `make e2e-focus FOCUS=TestPhase7CLIEngine/<sub>` for single-subtest re-runs against the kept cluster
</success_criteria>

<output>
Create `.planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-W4-01-SUMMARY.md` when done.
</output>
