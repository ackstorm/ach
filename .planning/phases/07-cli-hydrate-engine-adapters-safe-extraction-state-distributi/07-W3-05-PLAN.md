---
phase: 07-cli-hydrate-engine-adapters-safe-extraction-state-distributi
plan: 07-W3-05
type: execute
wave: 3
depends_on:
  - 07-W1-06
  - 07-W2-01
  - 07-W2-02
  - 07-W2-03
  - 07-W3-01
  - 07-W3-02
  - 07-W3-03
  - 07-W3-04
files_modified:
  - cmd/ach-cli/cmd/hydrate.go
  - cmd/ach-cli/cmd/hydrate_test.go
  - internal/cli/hydrate/autodetect.go
  - internal/cli/hydrate/autodetect_test.go
  - internal/cli/hydrate/wiring.go
  - internal/cli/hydrate/wiring_test.go
autonomous: true
requirements_addressed:
  - ADAPT-01
  - ADAPT-02
  - ADAPT-03
  - STATE-01
  - STATE-05
  - STATE-08
  - STATE-11
  - SAFE-04
must_haves:
  truths:
    - "cmd/ach-cli/cmd/hydrate.go refactored in place per D-03: adds engine flags --include-runtime / --only-runtime / --sync / --force / --dry-run / --wait / --lock-timeout / --output / --allow-symlinks / --platform / --global + hidden --raw."
    - "runHydrate's body dispatches: if flagRaw → runHydrateRaw (the existing Phase 6 POST+stream body unchanged); else hydrate.Run(cmd.Context(), opts) — per D-04 the --raw flag short-circuits before any engine call so the W3-P3 golden-diff anchor (D-21) survives byte-for-byte."
    - "--raw flag is hidden via cmd.Flags().MarkHidden('raw') — kept out of --help but functional (D-04)."
    - "internal/cli/hydrate/autodetect.go implements ADAPT-02 + D-06 verbatim: scan cwd (or $HOME under --global) via each registered adapter.Detect; zero matches → exit 1 with prompt; one match → emit 'Detected platform: <id>' to stderr and use it; multi-match → exit 1 listing matches + prompt to pass --platform. NO silent priority ordering — user confronts the ambiguity."
    - "STATE-05 --sync per D-16: deepest-first deletion; on-disk-hash mismatch preserves the file with stderr warning (drift wins); merge+keys[] entries get inverse-merge — remove only listed top-level keys for JSON/TOML deep-merge inverse OR replace <!-- ach:begin -->...<!-- ach:end --> block content for composite markdown; empty-dir pruning via rmdir(2) honoring ENOTEMPTY silently — CLI NEVER recursively deletes a directory."
    - "internal/cli/hydrate/wiring.go provides the default Extractor + AdapterDispatcher impls that the orchestrator (07-W1-06) consumes via the interface fields: extractorImpl wraps internal/cli/extract.{FetchContent,StageAndPublish}; adapterDispatcherImpl wraps adapter.Lookup + RenderRuntime + TransformPlugin + the SAFE-04 cascade from 07-W2-03 autoclaim."
    - "Adapter dispatcher invokes Cascade at final-rename time with adapter.ResolveOutputContent as the ContentResolver — closing the loop on D-07 + D-17."
    - "STATE-05 --sync deepest-first deletion + inverse-merge implementation lives in wiring.go's sync handler (or a sibling sync.go) since it bridges extract + adapter + state."
    - "All 4 adapter subpackages blank-imported by hydrate package OR by cmd/ach-cli root so their init() fires (CLOSED SET registration)."
  artifacts:
    - path: "cmd/ach-cli/cmd/hydrate.go"
      provides: "engine flags + --raw short-circuit + dispatch to hydrate.Run"
      contains: "hydrate.Run"
    - path: "internal/cli/hydrate/autodetect.go"
      provides: "ADAPT-02 platform autodetection"
      contains: "func Autodetect"
    - path: "internal/cli/hydrate/wiring.go"
      provides: "default Extractor + AdapterDispatcher impls + sync handler"
      contains: "extractorImpl"
  key_links:
    - from: "cmd/ach-cli/cmd/hydrate.go"
      to: "internal/cli/hydrate.Run"
      via: "engine dispatch"
      pattern: "hydrate\\.Run"
    - from: "cmd/ach-cli/cmd/hydrate.go"
      to: "internal/cli/adapter/{claudecode,codex,gemini,opencode}"
      via: "blank-import (_ \"...\") to fire init() registrations"
      pattern: "_ \"github.com/ackstorm/ach/internal/cli/adapter/(claudecode|codex|gemini|opencode)\""
---

<objective>
Wire the engine end-to-end. Refactor cmd/ach-cli/cmd/hydrate.go in place per D-03 + D-04 (—raw fallback to Phase 6 behavior). Implement ADAPT-02 autodetection. Implement the default Extractor + AdapterDispatcher impls that satisfy the interfaces declared in 07-W1-06 — these are the missing pieces that turn the stub-fed orchestrator into a working engine. Implement STATE-05 --sync deepest-first deletion.

Per CONTEXT.md `<code_context>` Integration Points #1: hydrate.go gets engine-flag wiring + the --raw short-circuit at top of runHydrate (before assertMutexCreds). Per D-03 the cobra command name + Short + RunE shape preserved.

Per `<specifics>` "Auto-claim three-tier cascade adapter coupling": the dispatcher invokes Cascade with adapter.ResolveOutputContent as the ContentResolver. Per `<specifics>` "--raw golden-diff preservation": W3-P3 e2e test (test/e2e/cli_login_hydrate_test.go) is updated in 07-W4-02 — not here — to pass --raw.

Output: 6 files (3 source + 3 test) — 1 modified cobra file + 2 new hydrate sibling files for autodetect + wiring. NO new go.mod entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-CONTEXT.md
@.planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-PATTERNS.md
@cmd/ach-cli/cmd/hydrate.go
@cmd/ach-cli/cmd/hydrate_test.go
@internal/cli/hydrate/commit.go
@internal/cli/hydrate/flags.go
@internal/cli/hydrate/result.go
@internal/cli/extract/stage.go
@internal/cli/extract/fetch.go
@internal/cli/extract/autoclaim.go
@internal/cli/adapter/registry.go
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: autodetect.go — ADAPT-02 platform autodetection</name>
  <files>internal/cli/hydrate/autodetect.go, internal/cli/hydrate/autodetect_test.go</files>
  <read_first>
    spec/ach_cli_spec_v20260515_FINALv4.md §7.5 (autodetection algorithm — zero/one/multi match outcomes)
    .planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-CONTEXT.md (D-06 — multi-match exits 1 with list; NO silent priority ordering)
    internal/cli/adapter/registry.go (07-W3-01 — Iter + Lookup signatures)
    internal/cli/adapter/adapter.go (07-W3-01 — Detect + Match shapes)
    internal/cli/exit/exit.go (General Code = 1 — autodetect failure path)
  </read_first>
  <behavior>
    - Autodetect(root string, stderr io.Writer) (string, error) returns the canonical platform ID on success; on zero matches returns ("", &exit.CodedError{Code: exit.General, Msg: "no platform detected at <root>; pass --platform <id> (one of: claude-code, codex, gemini-cli, opencode)"}); on multi-match returns ("", &exit.CodedError{Code: exit.General, Msg: "multiple platforms detected at <root>: <list>; pass --platform <id> to disambiguate"})
    - Iteration: for each adapter in adapter.Iter(), call a.Detect(root) — collect (id, confidence, reasons) tuples for adapters returning Confidence >= ConfidenceLow
    - One-match success path emits "Detected platform: <id>" to stderr per ADAPT-02
    - Multi-match list format: comma-separated canonical IDs in sort.Strings(ids) order (deterministic output for tests)
    - Tests: TestAutodetect_Zero_ExitsOne_Stderr (use a fake registry seeded via blank-importing claudecode but in a TempDir with no .claude/ — assert error.Code==General + stderr empty); TestAutodetect_One_EmitsDetectedLine (seed a t.TempDir with .claude/.mcp.json — assert returned id == "claude-code" + stderr contains "Detected platform: claude-code"); TestAutodetect_Multi_ListsBoth_ExitsOne (seed both .claude/ AND .codex/ in TempDir; assert error mentions both ids)
    - Resolve(id string) (string, error) helper does the alias lookup + case-fold against adapter.Lookup; used by both --platform flag and Autodetect's result
  </behavior>
  <action>
    Create internal/cli/hydrate/autodetect.go: SPDX header, package hydrate, imports (errors, fmt, io, sort, github.com/ackstorm/ach/internal/cli/adapter, github.com/ackstorm/ach/internal/cli/exit). Implement Autodetect per behavior — iterate adapter.Iter, accumulate matches, dispatch on count. Implement ResolvePlatform(id string) (string, error) wrapping adapter.Lookup — returns canonical ID or wrapped exit.CodedError{Code: exit.General, Msg: "unknown platform: <id>; one of: ..."}. Create internal/cli/hydrate/autodetect_test.go with stdlib testing. For tests, blank-import all 4 adapters at the top of the test file (`_ "github.com/ackstorm/ach/internal/cli/adapter/{claudecode,codex,gemini,opencode}"`) so init() fires; tests then seed t.TempDir contents matching each adapter's Detect signals. NO testify.
  </action>
  <verify>
    <automated>./scripts/dev.sh make unit-pkg PKG=./internal/cli/hydrate/...</automated>
  </verify>
  <acceptance_criteria>
    - `./scripts/dev.sh make unit-pkg PKG=./internal/cli/hydrate/...` exits 0
    - `grep -q "func Autodetect" internal/cli/hydrate/autodetect.go`
    - `grep -q "func ResolvePlatform\|func Resolve" internal/cli/hydrate/autodetect.go`
    - `grep -q "Detected platform:" internal/cli/hydrate/autodetect.go` (ADAPT-02 stderr emission)
    - autodetect_test.go contains TestAutodetect_Zero AND TestAutodetect_One AND TestAutodetect_Multi
    - autodetect_test.go has 4 blank-imports for all 4 adapter subpackages
    - `./scripts/dev.sh make lint-changed` exits 0
  </acceptance_criteria>
  <done>ADAPT-02 closed-set autodetection ships; cobra layer consumes via hydrate.Autodetect / hydrate.ResolvePlatform.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: wiring.go — default Extractor + AdapterDispatcher impls + STATE-05 sync handler</name>
  <files>internal/cli/hydrate/wiring.go, internal/cli/hydrate/wiring_test.go</files>
  <read_first>
    internal/cli/hydrate/result.go (07-W1-06 — Extractor + AdapterDispatcher interface declarations)
    internal/cli/hydrate/commit.go (07-W1-06 — commit struct + step methods)
    internal/cli/extract/fetch.go (07-W2-02 — FetchContent signature)
    internal/cli/extract/stage.go (07-W2-02 — StageAndPublish + PublishResult)
    internal/cli/extract/autoclaim.go (07-W2-03 — Classify + Cascade + ContentResolver)
    internal/cli/adapter/registry.go (07-W3-01 — Lookup + Iter)
    internal/cli/adapter/adapter.go (07-W3-01 — RenderRuntime + TransformPlugin + ResolveOutputContent signatures)
    spec/ach_cli_spec_v20260515_FINALv4.md §8.5 (--sync inverse-merge for merge+keys entries)
    .planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-CONTEXT.md (D-16 --sync deepest-first + drift wins; STATE-05)
  </read_first>
  <behavior>
    - extractorImpl struct holds: client *httpclient.Client, limits extract.Limits, allowSymlinks bool. Satisfies the Extractor interface from 07-W1-06.
    - extractorImpl.ExtractContent(ctx, ref manifest.ContentRef, achDir string) (ExtractResult, error) — call extract.FetchContent → extract.StageAndPublish — return tuple of WrittenFiles + SourceHash + sha256-skipped flag
    - adapterDispatcherImpl struct holds: platformID string. Satisfies AdapterDispatcher from 07-W1-06.
    - adapterDispatcherImpl.Render(ctx, m, s, achDir string) (RenderResult, error): lookup adapter via adapter.Lookup(d.platformID); call ad.RenderRuntime(ctx, m, s); for each FileWrite, run extract.Classify on the final path; if CollisionExistsUnowned, build Cascade params using ad.ResolveOutputContent as the resolver + the staged source as the sourceFn; on Cascade Identical=false return wrapDriftError-but-collision form; on Identical=true auto-claim (let it be added to state); write the FileWrite content to its target path via extract.WriteAtomic-equivalent helper (or via state.WriteAtomic since the helper is general).
    - For TransformPlugin path (plugins under context block), invoke ad.TransformPlugin(ctx, srcStagingDir, finalDstDir) and accumulate any Dropped components.
    - DroppedComponents accumulator surfaces via RenderResult.Dropped []string — fed into Result.DroppedComponents in commit.go step 10.
    - Sync(ctx, prev *state.File, new *state.File, achDir string, force bool) error implements STATE-05:
      - Collect targets in prev but missing from new — these are the to-delete set
      - Sort deepest-first by path-depth (longest path first) so parent dirs aren't touched before child files
      - For each missing-target: compute on-disk hash; compare to prev's recorded hash; on mismatch, preserve with stderr warning + skip (drift wins per D-16 — UNLESS --force, in which case proceed); on match (or --force), os.Remove the file; for entries with Merge=MergeDeep and Keys != nil, INVERSE-MERGE: read the on-disk JSON/TOML file, remove the listed top-level keys (or the deeply-nested ones if Keys carry dot-separated paths), and re-write atomically. For Merge=MergeComposite, replace the marked block content via regexp `<!-- ach:begin -->.*?<!-- ach:end -->` (DOTALL); if no marker found, preserve file with warning.
      - After per-file deletion/inverse-merge, walk parent dirs deepest-first calling os.Remove (which honors ENOTEMPTY silently — empty dirs prune, non-empty preserved). CLI NEVER recursively deletes.
    - Tests: TestExtractorImpl_DispatchesToStage uses a httptest fake content server returning a known body; assert StageAndPublish was invoked + WrittenFiles non-empty; TestAdapterDispatcherImpl_InvokesRender_ForPlatform uses a fake adapter registered under a test-only ID (or uses claudecode via blank-import + a tiny manifest); assert FileWrite list returned; TestAdapterDispatcherImpl_CollisionCascade_Identical (seed final-path with bytes that match RenderRuntime output → auto-claim, no error); TestAdapterDispatcherImpl_CollisionCascade_Differ_Force (seed with differing bytes + force=true → write proceeds without exit 7); TestAdapterDispatcherImpl_CollisionCascade_Differ_NoForce (assert exit.CodedError.Code == exit.CollisionRefuse); TestSync_DeepestFirst_Order (seed prev with nested entries + new empty → assert deletion order is deepest first); TestSync_LocalEdit_PreservesAndWarns (on-disk hash differs from prev.hash + !force → file preserved + stderr warning emitted); TestSync_InverseMerge_RemovesContributedKeys (a state entry with Merge=MergeDeep + Keys=["mcpServers.foo"] + an on-disk .claude/.mcp.json containing mcpServers.foo and mcpServers.bar → after Sync the file contains only mcpServers.bar); TestSync_CompositeBlock_RemovesMarkedRegion (file with `<!-- ach:begin -->X<!-- ach:end -->Y` → after Sync the file contains only Y)
  </behavior>
  <action>
    Create internal/cli/hydrate/wiring.go: SPDX header, package hydrate, imports (context, encoding/json, errors, fmt, io, os, path/filepath, regexp, sort, strings, github.com/ackstorm/ach/internal/cli/adapter, github.com/ackstorm/ach/internal/cli/extract, github.com/ackstorm/ach/internal/cli/exit, github.com/ackstorm/ach/internal/cli/httpclient, github.com/ackstorm/ach/internal/cli/manifest, github.com/ackstorm/ach/internal/cli/state, github.com/BurntSushi/toml). Define extractorImpl + adapterDispatcherImpl structs satisfying the orchestrator interfaces from result.go. Implement extractorImpl.ExtractContent: call extract.FetchContent(ctx, e.client, ref.Kind, ref.Name) → defer resp.Body.Close() → extract.StageAndPublish(...). Implement adapterDispatcherImpl.Render: adapter.Lookup(d.platformID); call ad.RenderRuntime; for each FileWrite, compute final path = filepath.Join(achDir, fw.Path); call extract.Classify(finalPath, currentState); on CollisionExistsUnowned, run extract.Cascade with eagerBytes=fw.Content + resolver=ad (since ad satisfies extract.ContentResolver via its ResolveOutputContent method — confirm the type-satisfaction in result.go's interface declaration; if not, add an adapter shim type that wraps adapter.Adapter and satisfies extract.ContentResolver). Implement Sync per behavior — deepest-first sort key is strings.Count(target, string(os.PathSeparator)). For JSON inverse-merge use a recursive map[string]any decode-modify-encode; for TOML use BurntSushi/toml.Decode + Encode; for composite markdown use regexp.MustCompile(`(?s)<!-- ach:begin -->.*?<!-- ach:end -->`). Define NewWiring(client *httpclient.Client, platformID string, limits extract.Limits, allowSymlinks bool) (Extractor, AdapterDispatcher) constructor that returns the impl pair — commit.go's newCommit will call this to populate the DI fields. Create internal/cli/hydrate/wiring_test.go with stdlib testing + httptest + t.TempDir + blank-imports for all 4 adapters. Tests per behavior. NO testify.
  </action>
  <verify>
    <automated>./scripts/dev.sh make unit-pkg PKG=./internal/cli/hydrate/...</automated>
  </verify>
  <acceptance_criteria>
    - `./scripts/dev.sh make unit-pkg PKG=./internal/cli/hydrate/...` exits 0
    - `grep -q "type extractorImpl struct" internal/cli/hydrate/wiring.go`
    - `grep -q "type adapterDispatcherImpl struct" internal/cli/hydrate/wiring.go`
    - `grep -q "extract.Cascade\|extract.Classify" internal/cli/hydrate/wiring.go` (SAFE-04 cascade integration)
    - `grep -q "func Sync\|func.*sync" internal/cli/hydrate/wiring.go` (STATE-05 deepest-first deletion)
    - `grep -q "func NewWiring\|func New" internal/cli/hydrate/wiring.go` (DI constructor)
    - wiring_test.go contains TestSync_DeepestFirst_Order AND TestSync_LocalEdit_PreservesAndWarns AND TestSync_InverseMerge_RemovesContributedKeys AND TestSync_CompositeBlock_RemovesMarkedRegion AND TestAdapterDispatcherImpl_CollisionCascade_Differ_NoForce
    - `./scripts/dev.sh make lint-changed` exits 0
  </acceptance_criteria>
  <done>Default Extractor + AdapterDispatcher impls + STATE-05 sync handler ship; cmd/ach-cli/cmd/hydrate.go can dispatch hydrate.Run with NewWiring-supplied DI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: cmd/ach-cli/cmd/hydrate.go refactor — engine flags + --raw short-circuit + dispatch</name>
  <files>cmd/ach-cli/cmd/hydrate.go, cmd/ach-cli/cmd/hydrate_test.go</files>
  <read_first>
    cmd/ach-cli/cmd/hydrate.go (current Phase 6 form — newHydrateCmd lines 86-153, runHydrate lines 192-267, hydrateInputs struct lines 167-180)
    internal/cli/hydrate/flags.go (07-W1-06 — Opts struct with all engine flags)
    internal/cli/hydrate/commit.go (07-W1-06 — Run signature)
    internal/cli/hydrate/autodetect.go (Task 1 — Autodetect + ResolvePlatform)
    internal/cli/hydrate/wiring.go (Task 2 — NewWiring constructor)
    .planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-CONTEXT.md (D-03 + D-04: engine-flag list + --raw hidden short-circuit)
    .planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-PATTERNS.md ("cmd/ach-cli/cmd/hydrate.go" section — exact adaptation steps lines 250-321)
    cmd/ach-cli/cmd/hydrate_test.go (test discipline + executeCommand helper pattern)
  </read_first>
  <behavior>
    - newHydrateCmd adds the engine flag block per D-03: --include-runtime / --only-runtime / --sync / --force / --dry-run / --wait / --lock-timeout <dur> / --output <dir> / --allow-symlinks / --platform <id> / --global (all bool except --lock-timeout / --output / --platform); plus hidden --raw bool
    - cmd.Flags().MarkHidden("raw") called after flag declaration to keep it out of --help
    - runHydrate body restructured: snapshot inputs (existing pattern preserved); --raw short-circuit at top → if flagRaw return runHydrateRaw(...) which IS the current Phase 6 body (extracted as a private function); else fall through to engine path
    - Engine path: assertMutexCreds + synthetic.GuardCommand (preserved); resolveBearer (preserved — engine reuses for credential lookup); classify pk_/ek_ (preserved); --environment gate (preserved for pk_); pk_ stderr warning (preserved with --no-warnings); plaintext-transport warning (preserved); then dispatch to hydrate.Run(cmd.Context(), opts) where opts is built from the flag bag.
    - opts.Platform resolution: if --platform set → hydrate.ResolvePlatform(--platform value) → opts.Platform = canonical ID; else → hydrate.Autodetect(cwd, cmd.ErrOrStderr()) → opts.Platform = detected ID
    - For --global, autodetect against os.UserHomeDir() instead of cwd per ADAPT-02 / spec §7.3
    - opts construction: BaseURL=baseURL, Bearer=bearer, Environment=effectiveEnv, Platform=resolvedID, Global=flagGlobal, IncludeRuntime=flagIncludeRuntime, OnlyRuntime=flagOnlyRuntime, Sync=flagSync, Force=flagForce, DryRun=flagDryRun, AllowSymlinks=flagAllowSymlinks, Output=flagOutput, Wait=flagWait, LockTimeout=flagLockTimeout, Verbose=flagVerbose, Stderr=cmd.ErrOrStderr(), Stdout=cmd.OutOrStdout()
    - Validation: --include-runtime + --only-runtime mutually exclusive (per spec §6.3 scope-selection table — both → exit 1); validate via assertScopeFlags helper
    - The 4 adapter subpackages MUST be blank-imported somewhere reachable from the binary so init() fires. Recommend adding the 4 blank-imports to cmd/ach-cli/cmd/hydrate.go top-of-file imports — they're never used in this file's code but the side-effect register call makes them needed at runtime. Alternative location: cmd/ach-cli/main.go or a sibling cmd/ach-cli/cmd/adapters_register.go. PREFER the dedicated adapters_register.go file with only the 4 blank imports — keeps hydrate.go clean.
    - Tests: TestNewHydrateCmd_FlagsRegistered (assert all engine flags present via cmd.Flags().Lookup(name) != nil); TestNewHydrateCmd_RawFlag_Hidden (assert cmd.Flags().Lookup("raw").Hidden == true); TestRunHydrate_RawDispatchesToLegacy (set --raw + httptest server returning a known body; assert stdout equals the body bytewise — preserves Phase 6 D-09 contract); TestRunHydrate_EngineDispatch (no --raw + a fake hydrate.Run injectable seam OR a high-level integration test that asserts hydrate.Run was invoked with the right opts); TestRunHydrate_IncludeAndOnlyRuntime_MutuallyExclusive (--include-runtime + --only-runtime → exit 1)
    - The hydrate.Run seam: cmd/ach-cli/cmd/hydrate.go defines a package-level `var hydrateRunFn = hydrate.Run` so unit tests can swap it (matches Phase 6's swapHydrateHTTPClientForTest pattern lines 73-81)
  </behavior>
  <action>
    Create cmd/ach-cli/cmd/adapters_register.go: SPDX header, package cmd, blank-imports for all 4 adapters: `_ "github.com/ackstorm/ach/internal/cli/adapter/claudecode" / codex / gemini / opencode`. Top-of-file comment explains the side-effect registration discipline + cites ADAPT-01. Refactor cmd/ach-cli/cmd/hydrate.go in place: rename existing runHydrate body to runHydrateRaw (extract the Phase 6 POST+stream logic into a named function); add engine flags to newHydrateCmd; add flagRaw bool with cmd.Flags().BoolVar + cmd.Flags().MarkHidden("raw"); restructure runHydrate as a thin dispatcher: snapshot inputs (the existing hydrateInputs struct stays as the raw-path bag; engine path uses an opts builder), then if flagRaw call runHydrateRaw, else build hydrate.Opts and call hydrateRunFn(cmd.Context(), opts). Add `var hydrateRunFn = hydrate.Run` package-level. Add an assertScopeFlags helper that rejects --include-runtime + --only-runtime together with &exit.CodedError{Code: exit.General, Msg: "--include-runtime and --only-runtime are mutually exclusive"}. Add platform resolution helper: if flagPlatform != "" use hydrate.ResolvePlatform; else use hydrate.Autodetect on cwd (or HOME when flagGlobal). Update cmd/ach-cli/cmd/hydrate_test.go: add test scenarios per behavior; preserve all existing Phase 6 tests (they exercise --raw path now); existing tests that called runHydrate directly should call runHydrateRaw OR set --raw=true. NO testify.
  </action>
  <verify>
    <automated>./scripts/dev.sh make unit-pkg PKG=./cmd/ach-cli/cmd/...</automated>
  </verify>
  <acceptance_criteria>
    - `./scripts/dev.sh make unit-pkg PKG=./cmd/ach-cli/cmd/...` exits 0 (preserves Phase 6 hydrate_test.go suite via runHydrateRaw path)
    - `grep -q "hydrate.Run\|hydrateRunFn" cmd/ach-cli/cmd/hydrate.go`
    - `grep -q "MarkHidden(\"raw\")\|MarkHidden(.raw.)" cmd/ach-cli/cmd/hydrate.go`
    - `grep -q "flagRaw\|--raw" cmd/ach-cli/cmd/hydrate.go`
    - All 4 adapter blank-imports present: `grep -c "_ \"github.com/ackstorm/ach/internal/cli/adapter/" cmd/ach-cli/cmd/adapters_register.go | grep -q "^4$"`
    - `grep -qE "include-runtime|only-runtime|--sync|--force|--dry-run|--wait|--lock-timeout|--output|--allow-symlinks|--platform|--global" cmd/ach-cli/cmd/hydrate.go` (engine flags wired)
    - hydrate_test.go contains TestNewHydrateCmd_FlagsRegistered AND TestRunHydrate_RawDispatchesToLegacy AND TestRunHydrate_IncludeAndOnlyRuntime_MutuallyExclusive
    - `./scripts/dev.sh make lint-changed` exits 0
    - `./scripts/dev.sh go build ./cmd/ach-cli/...` exits 0 (binary builds with all 4 adapters registered)
  </acceptance_criteria>
  <done>Cobra wiring complete; --raw short-circuits to Phase 6 behavior; engine path is the new default user-facing dispatch; 4 adapters are blank-imported and register via init() at process start.</done>
</task>

</tasks>

<verification>
Wave 3 plan 07-W3-05 closes when:
- `./scripts/dev.sh make unit-pkg PKG=./internal/cli/hydrate/...` exits 0
- `./scripts/dev.sh make unit-pkg PKG=./cmd/ach-cli/cmd/...` exits 0
- `./scripts/dev.sh go build ./cmd/ach-cli/...` exits 0
- `./bin/ach-cli hydrate --help 2>&1 | grep -q "include-runtime"` (engine flag visible in help)
- `./bin/ach-cli hydrate --help 2>&1 | grep -qv "raw"` (--raw hidden in help)
- The engine end-to-end path is wired: cobra → opts builder → autodetect (if no --platform) → hydrate.Run → orchestrator with NewWiring-supplied Extractor + AdapterDispatcher → 14-step commit sequence → state.json + adapter outputs
</verification>

<success_criteria>
- Phase 7 engine is dispatchable from the cobra layer
- --raw fallback preserves Phase 6 byte-for-byte stdout contract (golden-diff test in 07-W4-02 updates the e2e test to pass --raw)
- ADAPT-02 autodetection runs when --platform omitted; multi-match aborts with list per D-06
- SAFE-04 cascade is wired via adapter.ResolveOutputContent as ContentResolver Tier 2
- STATE-05 --sync deepest-first + inverse-merge + composite-block replacement all implemented
</success_criteria>

<output>
Create `.planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-W3-05-SUMMARY.md` when done.
</output>
