---
phase: 07-cli-hydrate-engine-adapters-safe-extraction-state-distributi
plan: 07-W6-01
type: execute
wave: 6
depends_on:
  - 07-W5-01
  - 07-W5-02
  - 07-W5-03
  - 07-W5-04
  - 07-W5-06
files_modified:
  - test/e2e/cli_hydrate_engine_test.go
  - test/e2e/phase7_helpers_test.go
  - examples/hydrate.json
gap_closure: true
autonomous: false
requirements:
  - STATE-04
  - STATE-05
  - STATE-08
  - STATE-10
  - STATE-11
  - ADAPT-03
  - ADAPT-04
  - ADAPT-05
  - ADAPT-06
  - ADAPT-07
  - SAFE-04
must_haves:
  truths:
    - "After all five Wave 5 plans land (W5-01 CR-02 wiring, W5-02 CR-01 mode-0o600, W5-03 CR-03 path normalization, W5-04 WR-01 build-tag gate, W5-06 WR-03 schemaVersion ordering — W5-05 is independent and not load-bearing for this gate), the full Phase 7 e2e suite runs green: `make cluster-keep` + `ACH_E2E_PHASE7=1 ACH_E2E_PHASE7_PK=<pk_> ACH_E2E_PHASE7_BASE_URL=http://localhost:8080 ./scripts/dev.sh make e2e-focus FOCUS=TestPhase7CLIEngine` exits 0 across all 14 subtests."
    - "Each of the 8 `sc1_*` platform×credential subtests produces ACTUAL workspace files: for claude-code the test asserts `<ach-dir>/.claude/.mcp.json` exists, contains an `x-ach-key` header entry, and has mode `0o600`; for codex `.codex/config.toml`; for gemini `.gemini/settings.json`; for opencode `.opencode/opencode.json`. The bearer credential type (pk_ vs ek_) is sourced from the test's parameterization; both variants produce non-empty files."
    - "The `sc2_commit_sequence_sigkill` subtest fires the SIGKILL seam (the binary was built with `-tags=e2e` per 07-W5-04) and asserts the prior state.json is intact on resume."
    - "The `sc3_drift_four_outcomes` subtest exercises the §8.4 truth table with exit codes 0/0/2/2 — drift detection now ACTUALLY drives file overwrites (the W5-01 fix makes diffTargets observable, so STATE-04 / STATE-10 / STATE-11 are exercised end-to-end)."
    - "The `sc4_autoclaim_three_tier` subtest succeeds on re-hydrate with rotated credentials: the autoclaim cascade returns CollisionOwnedByCurrent (W5-03 fix), the engine overwrites the prior credential file, exit code is 0 — NOT 7 (CollisionRefuse). Without W5-03 this subtest would fail with exit 7 on every credential rotation."
    - "The `sc4_safe_extract_malicious` and `sc4_safe_extract_bomb` subtests continue to reject malicious archives and bomb tarballs with non-zero exit + no partial output (SAFE-01..03; W5 does not regress these)."
    - "If `examples/hydrate.json` (the Phase 6 W3-P3 golden anchor) needs re-capture due to a legitimate response-shape change introduced by the W5-01 wiring (e.g. Result now carries DroppedComponents that the manifest doesn't), the re-capture happens in this plan with a clear diff audit comment justifying each line of change. If the response shape is unchanged, this is a no-op."
    - "RESULT: Phase 7's three FAILED truths from VERIFICATION.md flip to VERIFIED; the score moves from 2/4 to 4/4."
  artifacts:
    - path: "test/e2e/cli_hydrate_engine_test.go"
      provides: "the Phase 7 e2e suite — unchanged in shape; possibly minor assertion tightening if a subtest needs to assert the new 0o600 mode or the new owned-by-current outcome"
      contains: "TestPhase7CLIEngine"
    - path: "examples/hydrate.json"
      provides: "golden hydrate.json anchor, re-captured if and only if W5-01 wiring legitimately changed the response shape"
      contains: "schemaVersion"
  key_links:
    - from: "test/e2e/cli_hydrate_engine_test.go:TestPhase7CLIEngine"
      to: "the kept kind+Helm cluster"
      via: "ACH_E2E_PHASE7=1 + ACH_E2E_PHASE7_PK + ACH_E2E_PHASE7_BASE_URL"
      pattern: "ACH_E2E_PHASE7"
    - from: "sc1_* subtests"
      to: "engine-path adapter writes"
      via: "WriteAtomic 0o600 (W5-02) + Render Render (W5-01) + Classify owned (W5-03)"
      pattern: "0o600|x-ach-key"
---

> **Revision note (2026-05-29):** Targeted revision addressing checker W6.
> Strengthened Task 2's `rotated_credential_owned_by_current` sub-case:
> the `t.Skip` fallback is removed. The acceptance criteria now require
> EITHER (a) a real credential rotation via the platform-api admin
> endpoint, OR (b) a documented test-seam mutation of the credential +
> state.json hash. If neither is feasible in the current kind fixture,
> the test MUST `t.Fatal` with a "blocker follow-up plan required"
> message rather than silently skipping. This makes the SAFE-04
> closeout gap visible at test-run time and prevents the W5-03 fix
> from being assumed-verified without proof.

<objective>
Close the runtime gate for Phase 7 gap closure: after Wave 5 lands, re-run the full Phase 7 e2e suite against a live kind+Helm cluster and confirm all 14 subtests pass. This is the load-bearing proof that the engine ships real files for all four adapters at the correct mode with the correct re-hydration semantics.

This plan is intentionally THIN — most of the work is the e2e suite itself, which already exists (07-W4-01). The Wave 5 fixes (W5-01..W5-06) close the underlying engine gaps. This plan does FOUR things:

1. Tighten sc1_* subtest assertions to assert MODE 0o600 on every credential-bearing adapter file. W4-01 may have asserted file existence + bearer presence but not the file mode — W5-02 makes the mode contract verifiable end-to-end, and this plan adds the assertion.
2. Tighten sc4_autoclaim_three_tier (or add a new sub-subtest) to exercise the re-hydrate-with-rotated-credentials path: run hydrate twice with two different bearers; assert exit 0 on the second invocation (NOT exit 7). This proves W5-03 closes the SAFE-04 BLOCKER.
3. Re-capture `examples/hydrate.json` IF AND ONLY IF the W5-01 wiring legitimately changes the response shape. The runHydrate `--raw` path is unaffected by W5-01 (it short-circuits to the Phase 6 POST+stream contract per D-04), so examples/hydrate.json SHOULD remain byte-identical. If a diff appears, audit it line-by-line and either (a) accept it as legitimate (e.g., a newly emitted top-level field) and update the golden, or (b) reject it as a regression and file a follow-up.
4. RUN the full suite. Capture pass/fail per subtest. If any subtest fails, attach the failure log to this plan's SUMMARY.md as a blocker requiring a Wave 7 follow-up.

The HUMAN_VERIFICATION column is set because this plan requires a live kind cluster to execute — it cannot be automated end-to-end in a planning agent's environment. The acceptance is the human running the test and observing PASS for all 14 subtests.

Output: 2 e2e source files + (conditionally) 1 golden artifact. Net LOC: ~30 (mode-assertion + autoclaim-rotate test). The PRIMARY artifact of this plan is a PASSING e2e run, captured in SUMMARY.md.

Purpose: prove the Phase 7 core value path works end-to-end. Without this gate, Phase 7 closes with a "we believe the fixes are correct" assertion — and the verifier explicitly flagged "Kind cluster is not running in this session" as the reason the runtime gate was deferred. This plan IS the runtime gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-VERIFICATION.md
@.planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-REVIEW.md
@.planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-CONTEXT.md
@.planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-W4-01-PLAN.md
@.planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-W4-01-SUMMARY.md
@test/e2e/cli_hydrate_engine_test.go
@test/e2e/phase7_helpers_test.go
@examples/hydrate.json
@CLAUDE.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live kind cluster → ach-cli binary | The e2e suite drives a real ach-cli build against a real platform-api + content-service in kind; production trust boundaries are exercised, not mocked. |
| test process → workspace filesystem | The e2e suite writes real files; the mode/permission assertions verify the W5-02 mitigation on real-disk artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-W6-01-01 | Tampering | e2e test asserting 0o600 against a file the engine wrote | mitigate | The sc1_* mode assertion is the FINAL verification step for W5-02; if any sc1 subtest reports mode != 0o600, the W5-02 plan failed and Phase 7 cannot close. |
| T-07-W6-01-02 | Denial of Service | re-hydrate with rotated credential returning exit 7 | mitigate | sc4_autoclaim_three_tier's rotate sub-case is the FINAL verification step for W5-03; if exit != 0 on the second invocation, W5-03 failed. |
| T-07-W6-01-03 | Information Disclosure | golden hydrate.json re-capture potentially embedding a real bearer | accept | The fixture cluster uses test-only credentials documented in scripts/cluster.sh; the golden carries a stable `pk_demo` token that is not a real production credential. Capture is fine. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Tighten sc1_* subtests to assert mode 0o600 on every adapter file</name>
  <files>test/e2e/cli_hydrate_engine_test.go</files>
  <read_first>
    test/e2e/cli_hydrate_engine_test.go (find the sc1_* subtest fixtures — likely 8 subtests parameterized over 4 platforms × {pk, ek}. Look for the file-existence assertions per platform: `.claude/.mcp.json`, `.codex/config.toml`, `.gemini/settings.json`, `.opencode/opencode.json`. These are the targets of the mode assertion.)
    test/e2e/phase7_helpers_test.go (any existing helper for asserting file mode — if absent, this task adds one)
    internal/cli/state/atomic.go (the post-W5-02 WriteAtomic — confirms 0o600 is the contract for adapter files)
    internal/cli/hydrate/wiring.go (the post-W5-02 call sites passing 0o600)
    .planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-W5-02-PLAN.md (the W5-02 plan's success criteria — confirm the mode contract)
  </read_first>
  <action>
    Locate each sc1_* subtest in cli_hydrate_engine_test.go. After each subtest's existing file-existence + bearer-presence assertions, ADD an assertion:

    ```
    info, err := os.Stat(adapterFilePath)
    if err != nil { t.Fatalf("stat %s: %v", adapterFilePath, err) }
    if got := info.Mode().Perm(); got != 0o600 {
        t.Errorf("adapter file %s mode = %o, want %o (W5-02 CR-01 mitigation)", adapterFilePath, got, 0o600)
    }
    ```

    Apply this assertion to ALL 8 sc1_* subtests (4 platforms × 2 credentials). The exact `adapterFilePath` variable name will vary by subtest; use the existing variable that already holds the path being asserted for existence.

    If the existing sc1_* tests share a helper for "assert adapter file present + bearer matches", add the mode check INTO that helper rather than duplicating across all 8 subtests. The helper's signature stays unchanged; the new check is internal.

    Imports if missing: `os` (likely already present). No new imports beyond stdlib.

    Do NOT change sc2_*, sc3_*, or sc4_* assertions in this task. Do NOT add new sc1_* subtests — only tighten the existing ones.
  </action>
  <verify>
    <automated>./scripts/dev.sh go build -tags=e2e ./test/e2e/...</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "0o600" test/e2e/cli_hydrate_engine_test.go` returns at least 1 (the mode assertion is present — at minimum once in the shared helper, or 8 times if duplicated).
    - Source: `grep -n "Mode().Perm()" test/e2e/cli_hydrate_engine_test.go` returns the mode-extraction line.
    - Source: `grep -n "W5-02" test/e2e/cli_hydrate_engine_test.go` returns the rationale citation in the error message.
    - Behavior: `./scripts/dev.sh go build -tags=e2e ./test/e2e/...` exits 0 (the e2e suite compiles with the new assertion).
    - Behavior: `./scripts/dev.sh make lint-changed` exits 0.
    - Behavior under live cluster (HUMAN_VERIFICATION): after Wave 5 lands, `make cluster-keep && ACH_E2E_PHASE7=1 ACH_E2E_PHASE7_PK=<pk> ACH_E2E_PHASE7_BASE_URL=http://localhost:8080 ./scripts/dev.sh make e2e-focus FOCUS=TestPhase7CLIEngine/sc1` passes all 8 sc1_* subtests with the new mode assertion firing AND succeeding (mode == 0o600).
  </acceptance_criteria>
  <done>sc1_* subtests now assert mode 0o600 in addition to file existence + bearer; the W5-02 contract is verifiable end-to-end.</done>
</task>

<task type="auto">
  <name>Task 2: Extend sc4_autoclaim_three_tier with a rotated-credential re-hydrate sub-case (no t.Skip escape)</name>
  <files>test/e2e/cli_hydrate_engine_test.go</files>
  <read_first>
    test/e2e/cli_hydrate_engine_test.go (find the sc4_autoclaim_three_tier subtest — currently exercises auto-claim with a manually-seeded final path; the new sub-case exercises re-hydrate with a rotated credential, which is the canonical scenario for W5-03)
    test/e2e/phase7_helpers_test.go (find any existing test seam for credential rotation — e.g. phase7SeedXdgConfig, phase7RefreshPk, or similar; if no seam exists, identify whether the platform-api exposes `/admin/users/{id}/refresh-pk` or equivalent)
    internal/cli/extract/autoclaim.go (the post-W5-03 Classify — confirms the normalized comparison; the test verifies the integration end-to-end)
    .planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-W5-03-PLAN.md (the W5-03 plan's success criterion 7 — the runtime gate this sub-case exercises)
  </read_first>
  <action>
    In `test/e2e/cli_hydrate_engine_test.go`, locate the `sc4_autoclaim_three_tier` subtest. Add a new sub-subtest (or a step within the existing subtest) `rotated_credential_owned_by_current`.

    Implementation contract:

      - First invocation: run `ach-cli hydrate --environment demo --platform claude-code` with a pk_ that produces some bearer credential X in the headers map of `.claude/.mcp.json`. Assert exit 0 + file exists + state.json carries an `adapter.files` entry for `.claude/.mcp.json` with a non-empty hash.
      - Rotate the credential. Pick ONE of the two real-rotation approaches below — `t.Skip` is NOT acceptable:

        APPROACH A (preferred — real credential rotation): use the platform-api admin endpoint `POST /admin/users/{id}/refresh-pk` (or whichever existing endpoint mints a new pk_ for the test user). Look for an existing helper in `test/e2e/phase7_helpers_test.go` named like `phase7RefreshPk`, `phase7RotatePk`, or `phase7IssueNewPk`. If a helper exists, use it. If no helper exists, ADD ONE in `test/e2e/phase7_helpers_test.go` that POSTs to the admin endpoint with the dev-fixture admin master key (`scripts/cluster.sh hydrate_fixtures` seeds this). Capture the new pk_ from the response and write it to the local `~/.ach/config` via `phase7SeedXdgConfig` or equivalent.

        APPROACH B (acceptable fallback — test-seam mutation): if the platform-api in the kind fixture does NOT expose a `/admin/users/{id}/refresh-pk` endpoint, document this in the test's comment and use a documented test seam: add an exported helper `phase7MutateCredentialAndRehash(t, achDir, oldBearer, newBearer)` in `test/e2e/phase7_helpers_test.go` that (a) reads `.claude/.mcp.json`, (b) replaces `oldBearer` with `newBearer` in the on-disk file, (c) recomputes the xxh3 hash of the new content, (d) reads `<achDir>/state.json`, (e) updates the matching `adapter.files[]` entry's `hash` field to the new value, (f) writes state.json back via `state.WriteAtomic(path, buf, 0o644)`. Document the seam in a godoc comment naming it as a test-only credential-rotation simulator. The test invokes this seam between the two hydrate calls.

      - Second invocation: run `ach-cli hydrate --environment demo --platform claude-code` a SECOND time using the new credential.

      - Assert: exit code is 0 (NOT 7). The file `.claude/.mcp.json` has been overwritten and now contains the new bearer X' (not X). The state.json's adapter.files entry has an updated hash reflecting X' in the rendered file.

      - WITHOUT the W5-03 fix, the second invocation would exit 7 (CollisionRefuse) because `Classify` would have returned `CollisionExistsUnowned` for a file the engine itself wrote, triggering the SAFE-04 cascade and refusing to overwrite a non-matching file.

    Hard rule: if neither Approach A nor Approach B is feasible in the current kind fixture (e.g. neither endpoint exists AND adding a test seam would require deep refactoring not in scope for this plan), the test MUST `t.Fatal("rotated_credential_owned_by_current cannot run in this fixture — blocker follow-up plan required to expose a credential-rotation seam; see SAFE-04 W5-03 closeout requirement")`. A `t.Fatal` makes the gap visible at test-run time and forces a follow-up plan. `t.Skip` is FORBIDDEN here — a silent skip falsely greens the suite and the W5-03 fix would be unverified end-to-end.

    Pick the approach that exercises the production path most faithfully: prefer Approach A over B; prefer B over t.Fatal. Use phase7SeedXdgConfig or equivalent to update the local config between invocations under Approach A.
  </action>
  <verify>
    <automated>./scripts/dev.sh go build -tags=e2e ./test/e2e/...</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -n "rotated_credential_owned_by_current" test/e2e/cli_hydrate_engine_test.go` returns the new sub-case.
    - Source: `grep -n "W5-03" test/e2e/cli_hydrate_engine_test.go` returns the rationale citation in the test comment.
    - Source: the new sub-case has TWO `phase7RunAchCli` invocations (or equivalent helper calls) for hydrate, with assertion on each invocation's exit code.
    - Source: `grep -n "t.Skip" test/e2e/cli_hydrate_engine_test.go` in the lines of the new sub-case returns NO match (skip is forbidden — only t.Fatal acceptable as the no-fixture fallback).
    - Source: either (a) `grep -nE "phase7RefreshPk|phase7RotatePk|phase7IssueNewPk|/admin/users/.*/refresh-pk" test/e2e/` returns a helper invocation (Approach A) OR (b) `grep -n "phase7MutateCredentialAndRehash" test/e2e/` returns the helper definition + invocation (Approach B) OR (c) `grep -n "blocker follow-up plan required" test/e2e/cli_hydrate_engine_test.go` returns the t.Fatal message (genuine-fixture-blocker fallback). One of the three MUST be present.
    - Behavior: `./scripts/dev.sh go build -tags=e2e ./test/e2e/...` exits 0.
    - Behavior: `./scripts/dev.sh make lint-changed` exits 0.
    - Behavior under live cluster (HUMAN_VERIFICATION): the new sub-case passes — exit 0 on both invocations, file content reflects the rotated credential. If the implementation chose the t.Fatal fallback, the HUMAN_VERIFICATION instead documents which follow-up plan opens the fixture-credential-rotation seam.
  </acceptance_criteria>
  <done>sc4 exercises the re-hydrate-rotated-credential path via real platform-api rotation OR a documented state-mutation seam; t.Skip is gone; the W5-03 fix is verifiable end-to-end against a live cluster (or the unverifiable-in-fixture state is loudly surfaced via t.Fatal).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run the full Phase 7 e2e suite on the kept kind cluster</name>
  <what-built>The Wave 5 fixes (W5-01 wiring, W5-02 mode 0o600, W5-03 autoclaim path normalization, W5-04 build-tag gate, W5-06 schemaVersion ordering) plus the e2e suite hardening (Task 1 mode assertion, Task 2 rotated-credential sub-case) are in the tree. The runtime gate is the full e2e run against a live kind+Helm cluster.</what-built>
  <how-to-verify>
    1. From the repository root, bring up the kept kind cluster:
       `make cluster-keep`
       (or, if a cluster is already up, skip — `kubectl get pods -n ach-system` should show platform-api + operator + forwarder Ready)
    2. Provision a test pk_ via the platform-api SSO flow OR by using the dev-fixture pk in `scripts/cluster.sh hydrate_fixtures` output. Capture it as `<pk>`.
    3. Run the focused e2e suite:
       `ACH_E2E_PHASE7=1 ACH_E2E_PHASE7_PK=<pk> ACH_E2E_PHASE7_BASE_URL=http://localhost:8080 ./scripts/dev.sh make e2e-focus FOCUS=TestPhase7CLIEngine`
    4. Observe: all 14 subtests pass (Go's t.Run output shows `--- PASS` for each subtest).
    5. Specifically confirm:
       - `sc1_claude-code_pk`, `sc1_claude-code_ek`, `sc1_codex_pk`, `sc1_codex_ek`, `sc1_gemini_pk`, `sc1_gemini_ek`, `sc1_opencode_pk`, `sc1_opencode_ek` — ALL PASS, including the new mode-0o600 assertion (Task 1).
       - `sc2_commit_sequence_sigkill` — PASS (W5-04 build-tag gate confirmed: the e2e binary contains the seam and SIGKILL fires).
       - `sc3_drift_four_outcomes` — PASS (exits 0/0/2/2 for the four §8.4 cases; W5-01 wiring makes diffTargets observable).
       - `sc4_safe_extract_malicious` — PASS.
       - `sc4_safe_extract_bomb` — PASS.
       - `sc4_autoclaim_three_tier` including the new `rotated_credential_owned_by_current` sub-case (Task 2) — PASS.
       - `w1_baseline_no_op` — PASS.
    6. If ALL pass: capture the test output in 07-W6-01-SUMMARY.md as proof of runtime green; close the gap_closure cycle. The Phase 7 VERIFICATION.md gaps[0..2] flip from FAILED to VERIFIED.
    7. If ANY subtest fails: STOP. Capture the failure log + failing-subtest name + exit code in 07-W6-01-SUMMARY.md. Identify which Wave 5 plan owns the underlying fix that didn't land correctly. Open a follow-up plan in the same phase directory (07-W7-NN-PLAN.md) with the failing subtest as its driving truth.
  </how-to-verify>
  <resume-signal>Type "all 14 passed" with the test output excerpt, OR describe which subtest(s) failed with the captured exit code + log snippet.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Re-capture examples/hydrate.json IF AND ONLY IF the response shape legitimately changed</name>
  <files>examples/hydrate.json</files>
  <read_first>
    examples/hydrate.json (the current golden body — note the top-level fields: `schemaVersion`, `environment`, `runtime`, `context`, etc.)
    cmd/ach-cli/cmd/hydrate.go (the --raw short-circuit path — confirm it bypasses the engine and emits the Phase 6 POST+stream body verbatim; W5-01 changes do NOT affect the --raw path)
    test/e2e/cli_login_hydrate_test.go (the W3-P3 golden-diff test — runs `ach-cli hydrate --raw --environment demo > hydrate.json` and diffs against examples/hydrate.json byte-for-byte)
    .planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-W4-02-PLAN.md (D-21 — the --raw flag is the byte-equal anchor; W5-01 should not regress this)
  </read_first>
  <action>
    From the kept cluster, run a controlled capture:

      `./bin/ach-cli hydrate --raw --environment demo > /tmp/hydrate-post-w5.json`

    Diff against the existing golden:

      `diff -u examples/hydrate.json /tmp/hydrate-post-w5.json`

    EXPECTED OUTCOME 1 (most likely): empty diff. The --raw path is the Phase 6 POST+stream body; W5-01 changes the ENGINE path (steps 7-10 wiring) but does NOT modify the response body. examples/hydrate.json stays byte-identical. NO CHANGE to the golden in this task — the file is untouched.

    EXPECTED OUTCOME 2 (unlikely but possible): a small diff. If the diff shows ONLY legitimate response-shape changes (e.g., the platform-api now emits a new top-level field that wasn't there at Phase 6 close), audit the diff line-by-line:
      - If the change is from upstream platform-api changes unrelated to Phase 7: re-capture is correct; commit the new golden with a CHANGELOG.md entry.
      - If the change is from a Phase 7 modification to the platform-api that wasn't expected: STOP and file a regression follow-up. The --raw path is the load-bearing byte-equal anchor; unexpected shape changes there are a Phase 7 regression.

    EXPECTED OUTCOME 3 (regression): a large diff or wholly different schema. STOP; capture the diff in SUMMARY.md as a blocker; the W5-01 wiring may have inadvertently changed the platform-api side (unlikely — wiring is CLI-side only).

    Document the chosen outcome + rationale in 07-W6-01-SUMMARY.md regardless of which path is taken.

    Do NOT manually edit examples/hydrate.json without a diff-driven justification. The golden's integrity is the W3-P3 anchor's load-bearing assertion.
  </action>
  <verify>
    <automated>diff -u examples/hydrate.json /tmp/hydrate-post-w5.json || true</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `diff -u examples/hydrate.json /tmp/hydrate-post-w5.json` produces empty output (Outcome 1) — preferred.
    - OR: if a diff appears, it has been audited line-by-line and either accepted with a CHANGELOG entry (Outcome 2) or rejected as a regression (Outcome 3 → blocker).
    - Behavior: `test/e2e/cli_login_hydrate_test.go` (the W3-P3 golden-diff test) continues to pass: `./scripts/dev.sh make e2e-focus FOCUS=TestPhase6CLI/hydrate_golden_diff` exits 0.
    - Documentation: 07-W6-01-SUMMARY.md records the outcome with a one-paragraph audit of any diff.
  </acceptance_criteria>
  <done>examples/hydrate.json is unchanged OR re-captured with a documented audit; the W3-P3 byte-equal anchor remains valid against the post-Wave-5 binary.</done>
</task>

</tasks>

<verification>
- Task 3 (HUMAN_VERIFICATION) is the load-bearing gate. The other tasks are preparatory.
- All four tasks combined close the gap_closure cycle: Phase 7 VERIFICATION.md gaps[0..2] flip from FAILED to VERIFIED.
- A second verification pass (gsd-verify-phase 07) after this plan lands should produce a score of 4/4 with no remaining critical findings.
- The Phase 7 close criterion (per D-20 + D-22) is satisfied: linux-amd64 against `make e2e-keep` cluster, all 14 subtests pass.
</verification>

<success_criteria>
1. sc1_* subtests now assert mode 0o600 on every adapter output file.
2. sc4_autoclaim_three_tier has a new rotated-credential sub-case that exercises W5-03's owned-by-current path via real platform-api credential rotation OR a documented state-mutation seam (t.Skip is forbidden).
3. The full Phase 7 e2e suite runs green against the kept kind cluster: all 14 subtests pass.
4. examples/hydrate.json is unchanged (preferred) OR re-captured with a documented audit; the W3-P3 byte-equal anchor remains valid.
5. 07-W6-01-SUMMARY.md captures the test output as proof; Phase 7 VERIFICATION.md gaps[0..2] are eligible to flip to VERIFIED on the next verify pass.
6. NO regression introduced by Wave 5 fixes: sc2_*, sc3_*, sc4_safe_extract_*, w1_baseline_no_op all continue to pass.
</success_criteria>

<output>
Create `.planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-W6-01-SUMMARY.md` when done. Include the full test output (pass/fail per subtest) as a code block.
</output>
