---
phase: 07-cli-hydrate-engine-adapters-safe-extraction-state-distributi
plan: 07-W1-05
type: execute
wave: 1
depends_on:
  - 07-W1-01
files_modified:
  - internal/cli/manifest/doc.go
  - internal/cli/manifest/manifest.go
  - internal/cli/manifest/manifest_test.go
autonomous: true
requirements_addressed:
  - STATE-09
  - STATE-11
must_haves:
  truths:
    - "internal/cli/manifest package compiles standalone."
    - "Decode(r io.Reader) (*Manifest, error) rejects manifest.SchemaVersion != 'v1alpha1' with %w-wrapped ErrSchemaMismatch (STATE-09)."
    - "Decode rejects manifest with nil Runtime or nil Context block via ErrSchemaMismatch (STATE-09 + 07-CONTEXT.md `<canonical_refs>` §6.2)."
    - "Decode uses json.Decoder.DisallowUnknownFields(true) — unknown top-level or nested fields are a decode error wrapped via fmt.Errorf so callers see the JSON path; consistent with W1-02 state.Decode strict-shape posture."
    - "Manifest struct fields match Hub spec §15.2: SchemaVersion string, Runtime *RuntimeBlock, Context *ContextBlock, with runtime.{models,mcpServers,a2aAgents} and context.{prompts,plugins,artifacts} populated with []ContentRef-shaped entries each carrying id + downloadUrl + endpoint + name."
    - "ContentRef carries the optional Endpoint field (json:\"endpoint,omitempty\") — runtime entries (models, mcpServers, a2aAgents) populate it from the Hub response per examples/hydrate.json; context entries (prompts, plugins, artifacts) leave it empty. Single struct, no split — keeps the orchestrator + adapters consuming one type."
    - "Fetch(ctx, client, environment string) (*Manifest, error) wraps httpclient.Client.Do — reuses Phase 6 client (D-09 'Reuses httpclient.Client.DoRaw' — actually Do for typed decode per 07-PATTERNS.md)."
    - "Package boundary stays strict per CONTEXT.md `Claude's Discretion`: POST + decode + version-assert only. NO scope-filter logic — that belongs to hydrate orchestrator (07-W1-06)."
  artifacts:
    - path: "internal/cli/manifest/manifest.go"
      provides: "Manifest struct + Decode + Fetch + ErrSchemaMismatch + ContentRef.Endpoint"
      contains: "type Manifest struct"
  key_links:
    - from: "internal/cli/manifest/manifest.go"
      to: "internal/cli/httpclient"
      via: "Client.Do reuse"
      pattern: "httpclient"
    - from: "internal/cli/manifest/manifest.go"
      to: "internal/cli/exit.SchemaMismatch"
      via: "callers map ErrSchemaMismatch to exit code 5"
      pattern: "ErrSchemaMismatch"
    - from: "internal/cli/manifest/manifest.go"
      to: "internal/cli/adapter"
      via: "ContentRef.Endpoint feeds adapter.RenderRuntime URL construction (ADAPT-03)"
      pattern: "Endpoint"
---

<objective>
Ship `internal/cli/manifest/` — the POST /platform/hydrate decoder + schema-version assertion (`v1alpha1` strict) + runtime/context presence assertion + strict unknown-field rejection. STATE-09 + STATE-11 (partial — STATE-11 unconditional fetch is enforced by the hydrate commit-sequence orchestrator 07-W1-06; this package owns only the decode+assert path).

Purpose: Decoupled package boundary per CONTEXT.md `<decisions>` D-09: this package reuses `httpclient.Client.Do` for POST /platform/hydrate, decodes into a typed `*Manifest`, asserts schemaVersion == "v1alpha1" + runtime + context both non-nil, and returns. Strict scope per `Claude's Discretion` ("recommend strict POST + decode + version assert only; do not bleed scope-filter logic into it"). The hydrate orchestrator (07-W1-06) calls this once per `hydrate.Run` invocation in step 5 of §6.7.

ContentRef carries an optional `Endpoint` field — the Hub response embeds it on runtime entries (see `examples/hydrate.json`: `runtime.models[].endpoint`, `runtime.mcpServers[].endpoint`, `runtime.a2aAgents[].endpoint`). Adapters consume `m.Runtime.MCPServers[i].Endpoint` (ADAPT-03) to construct platform-specific runtime-config URLs. A single ContentRef serves both runtime + context — context entries leave Endpoint empty; the `omitempty` tag keeps the JSON output round-trip clean.

Output: 3 files (1 source + 1 test + doc.go) under internal/cli/manifest/. Reuses existing internal/cli/httpclient.Client; no new go.mod entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-CONTEXT.md
@.planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-PATTERNS.md
@internal/cli/httpclient/client.go
@cmd/ach-cli/cmd/hydrate.go
@examples/hydrate.json
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Manifest type + Decode + Fetch + schema assertion + ContentRef.Endpoint + DisallowUnknownFields</name>
  <files>internal/cli/manifest/doc.go, internal/cli/manifest/manifest.go, internal/cli/manifest/manifest_test.go</files>
  <read_first>
    spec/ach_hub_spec_v20260515_FINALv4.md §15.2 (manifest schema — schemaVersion "v1alpha1", runtime + context blocks always present, runtime entries carry endpoint)
    spec/ach_cli_spec_v20260515_FINALv4.md §6.2 (CLI side manifest fetch contract — strict version check)
    examples/hydrate.json (golden artifact — actual on-disk JSON shape; runtime entries carry "endpoint": "...", context entries carry "downloadUrl": "..."; assert decoder round-trips this)
    internal/cli/httpclient/client.go (Do method — typed decode helper, lines 113-139)
    cmd/ach-cli/cmd/hydrate.go (current httpclient usage pattern lines 354-381 — postAndStream; manifest decoder uses the typed Do path instead of DoRaw)
    .planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-PATTERNS.md ("internal/cli/manifest/manifest.go" section — exact adaptation)
    internal/cli/exit/exit.go (post-07-W1-01: SchemaMismatch Code = 5 now exists)
    internal/cli/state/state.go (07-W1-02 — analog Decode uses DisallowUnknownFields(true); same strict-shape posture)
  </read_first>
  <behavior>
    - Manifest struct: SchemaVersion string `json:"schemaVersion"`, Environment string `json:"environment"`, Runtime *RuntimeBlock `json:"runtime"`, Context *ContextBlock `json:"context"`
    - RuntimeBlock: Models []ContentRef `json:"models"`, MCPServers []ContentRef `json:"mcpServers"`, A2AAgents []ContentRef `json:"a2aAgents"` — all three slices present with `[]` when empty per Hub §15.2 (decode-time tolerance is fine; the orchestrator iterates them)
    - ContextBlock: Prompts []ContentRef `json:"prompts"`, Plugins []ContentRef `json:"plugins"`, Artifacts []ContentRef `json:"artifacts"`
    - ContentRef: ID string `json:"id"`, Name string `json:"name,omitempty"`, DownloadURL string `json:"downloadUrl,omitempty"`, Endpoint string `json:"endpoint,omitempty"`. SINGLE STRUCT serving both runtime (populates Endpoint) and context (populates DownloadURL) entries per examples/hydrate.json. The omitempty tags keep JSON output clean per scope.
    - ErrSchemaMismatch sentinel: errors.New("manifest: schemaVersion != \"v1alpha1\" or runtime/context block missing (STATE-09 / spec §6.2)")
    - Decode(r io.Reader) (*Manifest, error): dec := json.NewDecoder(r); dec.DisallowUnknownFields(); dec.Decode(&m); if a json.UnmarshalTypeError or similar bubble up, return fmt.Errorf wrapping that error (no ErrSchemaMismatch — unknown-field errors are decode-level, distinct from schema-version drift); if m.SchemaVersion != "v1alpha1" return %w-wrap of ErrSchemaMismatch; if m.Runtime == nil || m.Context == nil return %w-wrap of ErrSchemaMismatch; success returns &m
    - Fetch(ctx, client *httpclient.Client, environment string) (*Manifest, error): build POST body {"environment": environment} (or {} when environment == ""); call client.Do(ctx, http.MethodPost, "/platform/hydrate", body, &raw) where raw is a *bytes.Buffer or similar capturing the raw response body; then Decode(raw). Alternative cleaner: client.DoRaw → io.Copy into a *bytes.Buffer → Decode(buf). The 07-PATTERNS.md guidance says "use `Do` (typed decode) instead of `DoRaw` (stream)" — prefer that if httpclient.Client.Do supports passing a non-pointer-to-struct target; otherwise use DoRaw + manual decode.
    - Tests: TestDecode_GoldenHydrate uses examples/hydrate.json via os.ReadFile and asserts roundtrip including that m.Runtime.Models[0].Endpoint, m.Runtime.MCPServers[i].Endpoint, m.Runtime.A2AAgents[i].Endpoint all round-trip non-empty (and equal the literal "http://localhost:8080/..." values from the golden); TestDecode_SchemaV2_ReturnsErrSchemaMismatch with manifest.SchemaVersion="v2"; TestDecode_NilRuntime_ReturnsErrSchemaMismatch; TestDecode_NilContext_ReturnsErrSchemaMismatch; TestDecode_EmptyRuntimeArrays_OK (runtime: {models: [], mcpServers: [], a2aAgents: []} is valid per §15.2 always-present-with-[]); TestDecode_UnknownField_Rejects asserts that decoding `{"schemaVersion":"v1alpha1","runtime":{...},"context":{...},"bogusTopLevelField":1}` returns a non-nil error AND the error is NOT %w-of-ErrSchemaMismatch (it's the json decoder's unknown-field error wrapped via fmt.Errorf); TestFetch_PostShape_BuildsCorrectBody uses httptest.NewServer asserting incoming POST body is {"environment": "<env>"} and a 200 response decodes
    - doc.go cites STATE-09 + §15.2 + D-09 boundary discipline + ContentRef single-struct rationale (runtime entries carry Endpoint; context entries carry DownloadURL; both share ID + Name)
  </behavior>
  <action>
    Create internal/cli/manifest/doc.go: SPDX header, package manifest doc body citing STATE-09 + Hub §15.2 + CLI §6.2 + D-09 boundary (POST + decode + version-assert only; NO scope filtering). Document the ContentRef single-struct discipline: runtime entries populate Endpoint, context entries populate DownloadURL, both share ID+Name; the omitempty tags keep encoded output minimal and round-trip clean against examples/hydrate.json. Create internal/cli/manifest/manifest.go: SPDX header, package manifest, imports (bytes, context, encoding/json, errors, fmt, io, net/http, github.com/ackstorm/ach/internal/cli/httpclient). Define ContentRef + RuntimeBlock + ContextBlock + Manifest structs with the json tags listed in behavior. Declare `var ErrSchemaMismatch = errors.New(...)` per behavior. Implement Decode(r io.Reader) (*Manifest, error) using json.NewDecoder + DisallowUnknownFields() — match the W1-02 state.Decode strict-shape posture. Implement Fetch(ctx, *httpclient.Client, environment string) (*Manifest, error): build {"environment": "<env>"} body if environment != "" else {}; use client.DoRaw(ctx, http.MethodPost, "/platform/hydrate", bytes.NewReader(body)) since the response body is JSON we need to fully buffer then Decode; defer resp.Body.Close(); decode via Decode helper. On non-2xx HTTP, surface via httpclient.ServerError pass-through (caller layer maps to exit codes per Phase 6 D-13). Create internal/cli/manifest/manifest_test.go: SPDX header, package manifest, stdlib testing. Use httptest.NewServer for TestFetch_*; use os.ReadFile("../../../examples/hydrate.json") for the round-trip test. The round-trip test asserts non-empty Endpoint on each runtime slice entry (models, mcpServers, a2aAgents) by re-marshaling and string-comparing to expected literals from the golden. The unknown-field test crafts an inline JSON literal with a bogus top-level field. NO testify imports. NO assertion library.
  </action>
  <verify>
    <automated>./scripts/dev.sh make unit-pkg PKG=./internal/cli/manifest/...</automated>
  </verify>
  <acceptance_criteria>
    - `./scripts/dev.sh make unit-pkg PKG=./internal/cli/manifest/...` exits 0
    - `grep -q "type Manifest struct" internal/cli/manifest/manifest.go`
    - `grep -q "var ErrSchemaMismatch" internal/cli/manifest/manifest.go`
    - `grep -q "func Decode" internal/cli/manifest/manifest.go`
    - `grep -q "func Fetch" internal/cli/manifest/manifest.go`
    - `grep -q "v1alpha1" internal/cli/manifest/manifest.go` (literal version string for assertion)
    - `grep -q "DisallowUnknownFields" internal/cli/manifest/manifest.go` (strict-shape posture)
    - `grep -q "Endpoint string" internal/cli/manifest/manifest.go` (ContentRef.Endpoint field present)
    - `grep -q "json:\"endpoint,omitempty\"" internal/cli/manifest/manifest.go` (json tag with omitempty)
    - `grep -q "examples/hydrate.json" internal/cli/manifest/manifest_test.go` (round-trip test against golden)
    - manifest_test.go contains TestDecode_GoldenHydrate AND TestDecode_SchemaV2_ReturnsErrSchemaMismatch AND TestDecode_NilRuntime_ReturnsErrSchemaMismatch AND TestDecode_UnknownField_Rejects
    - TestDecode_GoldenHydrate body asserts MCPServers[0].Endpoint != "" (Endpoint round-trips from the golden)
    - `./scripts/dev.sh make lint-changed` exits 0
  </acceptance_criteria>
  <done>internal/cli/manifest decodes /platform/hydrate responses; rejects schema drift; rejects unknown fields; ContentRef carries Endpoint so adapters (ADAPT-03) can build runtime-config URLs; ready for orchestrator consumption in 07-W1-06.</done>
</task>

</tasks>

<verification>
Wave 1 plan 07-W1-05 closes when:
- `./scripts/dev.sh make unit-pkg PKG=./internal/cli/manifest/...` exits 0
- examples/hydrate.json round-trips via Decode without loss of any field the orchestrator + adapters read (SchemaVersion, Environment, Runtime.{Models,MCPServers,A2AAgents}, Context.{Prompts,Plugins,Artifacts}, ContentRef.{ID,Name,DownloadURL,Endpoint})
- ErrSchemaMismatch fires on schemaVersion drift AND on nil Runtime AND on nil Context
- Unknown top-level fields rejected at decode time (DisallowUnknownFields)
</verification>

<success_criteria>
- internal/cli/manifest is the single decode point for POST /platform/hydrate in the engine path
- Boundary discipline: NO scope filtering, NO state interaction, NO file I/O — pure HTTP→JSON→struct
- Caller layer (07-W1-06 commit orchestrator) maps ErrSchemaMismatch to exit.SchemaMismatch (code 5 from 07-W1-01)
- ContentRef.Endpoint round-trips for runtime entries — adapters (07-W3-01..04) consume m.Runtime.MCPServers[i].Endpoint without re-fetching
</success_criteria>

<output>
Create `.planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-W1-05-SUMMARY.md` when done.
</output>
