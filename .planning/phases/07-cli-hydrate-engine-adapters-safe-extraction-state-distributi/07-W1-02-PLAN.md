---
phase: 07-cli-hydrate-engine-adapters-safe-extraction-state-distributi
plan: 07-W1-02
type: execute
wave: 1
depends_on:
  - 07-W1-01
files_modified:
  - internal/cli/state/doc.go
  - internal/cli/state/state.go
  - internal/cli/state/state_test.go
  - internal/cli/state/atomic.go
  - internal/cli/state/atomic_test.go
  - internal/cli/state/path.go
  - internal/cli/state/path_test.go
  - internal/cli/state/sweep.go
  - internal/cli/state/sweep_test.go
  - internal/cli/state/guard.go
  - internal/cli/state/guard_test.go
autonomous: true
requirements_addressed:
  - STATE-01
  - STATE-02
  - STATE-03
  - STATE-07
  - STATE-09
must_haves:
  truths:
    - "internal/cli/state package compiles standalone (no engine imports — testable without W2/W3/W4)."
    - "state.File JSON shape implements §8.2 v2 schema verbatim: {schemaVersion: '2', environment, deployment, prompts: [{target, hash, sourceHash, merge?, keys?}], plugins, artifacts, runtimeFiles, adapter} (per D-13)."
    - "Load returns ErrSchemaMismatch sentinel when schemaVersion != '2' (per STATE-02 + D-13 — no v1 reader code shipped)."
    - "Save writes via tmp+fsync(fd)+rename+fsync(parent_dir) — STATE-07 four-step contract verified by a test that asserts a SIGKILL between Write and Rename leaves state.json intact (via process-fork harness)."
    - "ResolvePath returns <workspace>/.ach/state.json for workspace mode and ~/.ach/<environment>/state.json for global mode (STATE-01)."
    - "GuardEnvironment(existingState, requestedEnvironment) returns ErrEnvironmentGuard when existingState.environment != '' && requestedEnvironment != existingState.environment (STATE-03)."
    - "SweepTmp(achDir) removes every subentry under <achDir>/tmp/ unconditionally (no maxAge cutoff per D-02 + spec §6.7 step 2)."
    - "Stdlib-only — only encoding/json, errors, fmt, io, os, path/filepath, sync. NO yaml, NO log, NO log/slog."
  artifacts:
    - path: "internal/cli/state/state.go"
      provides: "File type + Load + Save + ErrSchemaMismatch sentinel"
      contains: "type File struct"
    - path: "internal/cli/state/atomic.go"
      provides: "WriteAtomic helper (tmp+fsync+rename+fsync_parent)"
      contains: "func WriteAtomic"
    - path: "internal/cli/state/sweep.go"
      provides: "SweepTmp(achDir) — unconditional subentry removal"
      contains: "func SweepTmp"
    - path: "internal/cli/state/guard.go"
      provides: "GuardEnvironment + ErrEnvironmentGuard"
      contains: "func GuardEnvironment"
    - path: "internal/cli/state/path.go"
      provides: "ResolvePath(workspace, environment, global bool) (string, error)"
      contains: "func ResolvePath"
  key_links:
    - from: "internal/cli/state/state.go"
      to: "internal/cli/state/atomic.go"
      via: "Save calls WriteAtomic"
      pattern: "WriteAtomic\\("
    - from: "internal/cli/state/state.go"
      to: "internal/cli/exit"
      via: "callers map ErrSchemaMismatch/ErrEnvironmentGuard to exit codes 5/4"
      pattern: "ErrSchemaMismatch|ErrEnvironmentGuard"
---

<objective>
Ship `internal/cli/state/` — the v2 state.json marshaler, atomic-write helper (tmp+fsync+rename+fsync_parent), tmp-sweep, schema-version guard, same-`<ach-dir>` different-Environment guard, and `<ach-dir>` resolver. STATE-01, STATE-02, STATE-03, STATE-07, STATE-09 (in part — STATE-09 also covered by 07-W1-05 manifest decoder). NO drift logic in this plan (STATE-04 lands with 07-W1-06 commit-sequence skeleton). NO lock (07-W1-03). NO hash (07-W1-04).

Purpose: This package is the foundation every other Phase 7 plan reads from. State.json v2 marshaling, atomic publication semantics (STATE-07), schema-mismatch + Environment-mismatch sentinel errors that map to exit codes 4/5, and the tmp-sweep called by every hydrate at step 2 of the §6.7 14-step sequence all live here. Stdlib-only — mirrors `internal/cli/config`, `internal/cachefs`, `internal/credhash` discipline. Per D-13: clean-break v2 — NO v1 reader code.

Output: 11 files (5 source + 5 test + doc.go) under internal/cli/state/. All unit-tested. Zero new go.mod entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-CONTEXT.md
@.planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-PATTERNS.md
@internal/cli/config/config.go
@internal/cli/config/config_test.go
@internal/cachefs/sweep.go
@internal/cachefs/sweep_test.go
@internal/cachefs/doc.go
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: state.File type + Load + Save + ErrSchemaMismatch + path resolver</name>
  <files>internal/cli/state/doc.go, internal/cli/state/state.go, internal/cli/state/state_test.go, internal/cli/state/atomic.go, internal/cli/state/atomic_test.go, internal/cli/state/path.go, internal/cli/state/path_test.go</files>
  <read_first>
    internal/cli/config/config.go (analog — File struct + Load + Save + Path + sentinel errors; lines 41-88, 98-185, 241-252)
    internal/cli/config/config_test.go (analog test discipline — stdlib testing only)
    .planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-PATTERNS.md ("internal/cli/state/state.go" section + "internal/cli/state/atomic.go" section — exact adaptation guidance)
    spec/ach_cli_spec_v20260515_FINALv4.md §8.1 (state file location semantics — workspace vs global)
    spec/ach_cli_spec_v20260515_FINALv4.md §8.2 (schema v2 — {target, hash, sourceHash, merge?, keys?} per entry)
    spec/ach_cli_spec_v20260515_FINALv4.md §8.7 (atomic write — tmp → fsync(fd) → rename(2) → fsync(parent_dir))
    internal/cachefs/bootstrap.go (skip-on-Windows + skip-on-root mode-check pattern, lines 130-148)
  </read_first>
  <behavior>
    - state.File struct fields: SchemaVersion string (json:"schemaVersion"), Environment string (json:"environment"), Deployment string (json:"deployment"), Prompts []FileEntry (json:"prompts,omitempty"), Plugins []FileEntry (json:"plugins,omitempty"), Artifacts []FileEntry (json:"artifacts,omitempty"), RuntimeFiles []FileEntry (json:"runtimeFiles,omitempty"), Adapter AdapterSection (json:"adapter,omitempty")
    - FileEntry struct fields: Target string (json:"target"), Hash string (json:"hash"), SourceHash string (json:"sourceHash"), Merge string (json:"merge,omitempty"), Keys []string (json:"keys,omitempty")
    - AdapterSection struct fields: ID string (json:"id"), Files []FileEntry (json:"files,omitempty")
    - Sentinel errors: ErrSchemaMismatch (schemaVersion != "2"), ErrEnvironmentGuard (handled by guard.go), ErrStateParse (json decode wrap)
    - Load(path string) (*File, error) — returns (nil, nil) on os.IsNotExist (fresh workspace); reads file; json.Decode with DisallowUnknownFields(true); rejects schemaVersion != "2" with %w-wrapped ErrSchemaMismatch unwrapping; wraps decode errors as %w of ErrStateParse
    - Save(path string, f *File) error — delegates to WriteAtomic (atomic.go); rejects nil File with errors.New("state: Save called with nil File"); does NOT enforce a mode tighter than 0644 (state has no secrets unlike config 0600)
    - ResolvePath(workspaceCwd, environment string, global bool) (string, error) — workspace: filepath.Join(workspaceCwd, ".ach", "state.json"); global: filepath.Join(os.UserHomeDir(), ".ach", environment, "state.json"); global with empty environment returns error wrapping ErrSchemaMismatch's sibling ErrInvalidPath
    - Tests: TestLoad_AbsentFile_ReturnsNilNil (fresh workspace), TestLoad_SchemaV1_ReturnsErrSchemaMismatch, TestLoad_SchemaV2_RoundTrip, TestLoad_UnknownField_ReturnsErrStateParse (DisallowUnknownFields gate), TestSave_NilFile_Errors, TestResolvePath_Workspace, TestResolvePath_Global, TestResolvePath_GlobalEmptyEnv_Errors
    - SPDX header on every *.go file
    - doc.go header text matches the per-file package doc pattern from 07-PATTERNS.md lines 99-114 (state spec §8.2 quote + discipline bullets)
  </behavior>
  <action>
    Create internal/cli/state/doc.go with SPDX header and package doc body adapted from 07-PATTERNS.md "SPDX + package doc pattern" lines 98-114 (drop yaml mention; reference encoding/json; cite §8.2 schema and §8.7 atomic-write contract). Create internal/cli/state/state.go: define File / FileEntry / AdapterSection structs with the exact JSON tags listed in behavior; define sentinel errors ErrSchemaMismatch / ErrStateParse / ErrInvalidPath; implement Load via os.ReadFile + json.NewDecoder + DisallowUnknownFields(true) + schemaVersion check; implement Save as a one-line dispatch to WriteAtomic. Create internal/cli/state/atomic.go: implement WriteAtomic(path string, data []byte) error via os.CreateTemp(filepath.Dir(path), ".state-*.json.tmp"), Write, Sync (fsync(fd) — STATE-07 contract addition over config.go which omits this), Close, os.Rename, then open parent dir with os.Open and call Sync on it (fsync(parent_dir) — STATE-07 second fsync). Cleanup closure removes tmp on every error path. On Windows the parent-dir Sync is a no-op (best-effort) — wrap in a runtime.GOOS == "windows" skip to avoid the call (NTFS doesn't honor fsync on dirs). Create internal/cli/state/path.go: implement ResolvePath per behavior. Create matching _test.go files with stdlib testing only (NO testify). Each test file gets SPDX header + a package-level table-driven test where appropriate. For TestSave_AtomicWrite_CrashRecovery, the test pattern: write a known File via Save, then unset_via_truncate the tmp file mid-stream is NOT achievable in a single-process test — instead assert WriteAtomic's three observable invariants in three sub-tests: (a) post-Save the target path exists and json.Unmarshal round-trips to equal File, (b) post-Save no `.state-*.json.tmp` files remain in filepath.Dir(path), (c) on inducing a write error mid-Save by passing a nonexistent parent dir, the target path is untouched (still absent OR still old content). For SIGKILL semantics the actual crash-recovery test lives in the W4 e2e suite (D-20 SC#2 subtest); unit tests only assert the rename ordering invariant. Add file-mode-bit chmod call on the tmp file before Close (0644). The fsync_parent_dir step is best-effort on Windows (skip with `if runtime.GOOS != "windows"` guard).
  </action>
  <verify>
    <automated>./scripts/dev.sh make unit-pkg PKG=./internal/cli/state/...</automated>
  </verify>
  <acceptance_criteria>
    - `./scripts/dev.sh make unit-pkg PKG=./internal/cli/state/...` exits 0
    - `grep -q "type File struct" internal/cli/state/state.go`
    - `grep -q "ErrSchemaMismatch" internal/cli/state/state.go`
    - `grep -q "func WriteAtomic" internal/cli/state/atomic.go`
    - `grep -q "tmp.Sync" internal/cli/state/atomic.go` (fsync(fd) — STATE-07 addition over config.go)
    - `grep -qE "(parent|dir)\.Sync\(\)|fsync.*parent" internal/cli/state/atomic.go` (fsync(parent_dir) — STATE-07 second fsync, may be runtime.GOOS-gated)
    - `grep -q "func ResolvePath" internal/cli/state/path.go`
    - state.go top-of-file imports list contains NEITHER "gopkg.in/yaml.v3" NOR "log" NOR "log/slog" (stdlib-only discipline)
    - `grep -q "DisallowUnknownFields" internal/cli/state/state.go` (Load rejects unknown fields per §8.2 strict-schema discipline)
    - `./scripts/dev.sh make lint-changed` exits 0
  </acceptance_criteria>
  <done>state.File + Load + Save + WriteAtomic + ResolvePath all ship with passing unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SweepTmp + GuardEnvironment</name>
  <files>internal/cli/state/sweep.go, internal/cli/state/sweep_test.go, internal/cli/state/guard.go, internal/cli/state/guard_test.go</files>
  <read_first>
    internal/cachefs/sweep.go (analog — SweepTmp lines 117-157; state version drops maxAge per D-02)
    internal/cachefs/sweep_test.go (analog test discipline — MissingTmpDir + RaceTolerance subtests, lines 227-271)
    .planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-PATTERNS.md ("internal/cli/state/sweep.go" + "internal/cli/state/guard.go" sections)
    spec/ach_cli_spec_v20260515_FINALv4.md §6.7 step 2 (tmp sweep — unconditional at hydrate start)
    spec/ach_cli_spec_v20260515_FINALv4.md §8.3 (Same-<ach-dir> different-Environment guard — workspace scope only)
    internal/cli/state/state.go (from Task 1 — File.Environment field this guard reads)
  </read_first>
  <behavior>
    - SweepTmp(achDir string) error — opens <achDir>/tmp/ via os.ReadDir; if os.IsNotExist returns nil (idempotent — same as cachefs); for each subentry calls os.RemoveAll on filepath.Join(achDir, "tmp", entry.Name()); errors on RemoveAll are swallowed silently per spec §6.7 step 2 ("benign cleanup, never aborts hydrate")
    - NO maxAge parameter — D-02 atomic boundary mandates unconditional sweep
    - subentries are <rand>/ staging subtrees (directories), not files — must use RemoveAll not Remove
    - GuardEnvironment(existing *File, requested string, force bool) error — existing==nil OR existing.Environment=="" returns nil (fresh state — no prior binding); requested != existing.Environment AND !force returns %w-wrapped ErrEnvironmentGuard with "%s != %s" detail; requested == existing.Environment returns nil; force==true bypasses with no error
    - ErrEnvironmentGuard sentinel error declared at package level (not in state.go since state.go owns ErrSchemaMismatch — group with guard.go to keep guard concern co-located)
    - Tests SweepTmp: TestSweepTmp_AbsentTmpDir_ReturnsNil (idempotent), TestSweepTmp_RemovesAllSubentries (no maxAge cutoff — different from cachefs), TestSweepTmp_RemoveFails_SwallowsError, TestSweepTmp_PreservesNonTmpSiblings (only <achDir>/tmp/ is touched, not <achDir>/state.json or <achDir>/lock)
    - Tests GuardEnvironment: TestGuard_FreshState_ReturnsNil, TestGuard_SameEnvironment_ReturnsNil, TestGuard_DifferentEnvironment_ReturnsErrEnvironmentGuard, TestGuard_DifferentEnvironment_WithForce_ReturnsNil
  </behavior>
  <action>
    Create internal/cli/state/sweep.go with SPDX header + package state. Implement SweepTmp per behavior: filepath.Join(achDir, "tmp"), os.ReadDir, errors.Is(err, fs.ErrNotExist) early return nil, iterate entries calling os.RemoveAll(filepath.Join(tmpRoot, entry.Name())) and swallowing per-entry errors (no propagation — spec §6.7 step 2 is benign cleanup). Add a package-level doc comment citing D-02 ("no maxAge — unconditional at hydrate start") and §6.7 step 2. Create internal/cli/state/guard.go with SPDX header. Declare `var ErrEnvironmentGuard = errors.New("state: same <ach-dir> bound to a different Environment (per STATE-03 / spec §8.3)")` at the package level. Implement GuardEnvironment per behavior — handle nil existing first, then existing.Environment=="" early return, then the mismatch arm wrapping with fmt.Errorf("%w: have=%q want=%q", ErrEnvironmentGuard, existing.Environment, requested). For force==true, return nil with no logging (no slog import — caller layer prints the override message). Create matching sweep_test.go and guard_test.go with stdlib testing, t.TempDir() patterns, and the test cases listed in behavior. Mirror cachefs/sweep_test.go's t.TempDir + os.WriteFile seeding pattern.
  </action>
  <verify>
    <automated>./scripts/dev.sh make unit-pkg PKG=./internal/cli/state/...</automated>
  </verify>
  <acceptance_criteria>
    - `./scripts/dev.sh make unit-pkg PKG=./internal/cli/state/...` exits 0 (regression-safe — Task 1 tests still pass)
    - `grep -q "func SweepTmp" internal/cli/state/sweep.go`
    - `grep -q "os.RemoveAll" internal/cli/state/sweep.go` (subentries are directories, not files)
    - `grep -qE "func SweepTmp\\(achDir string\\) error" internal/cli/state/sweep.go` (NO maxAge parameter — D-02 unconditional contract)
    - `grep -q "func GuardEnvironment" internal/cli/state/guard.go`
    - `grep -q "var ErrEnvironmentGuard" internal/cli/state/guard.go`
    - `./scripts/dev.sh make lint-changed` exits 0
  </acceptance_criteria>
  <done>SweepTmp + GuardEnvironment ship with passing unit tests; package surface is complete.</done>
</task>

</tasks>

<verification>
Wave 1 plan 07-W1-02 closes when:
- `./scripts/dev.sh make unit-pkg PKG=./internal/cli/state/...` exits 0 with all subtests green
- Package imports list contains NO yaml, NO log, NO log/slog (stdlib-only discipline grep gate: `grep -vE '^#' internal/cli/state/*.go | grep -cE 'gopkg.in/yaml|"log"' | grep -q '^0$'`)
- Downstream plans (07-W1-06 commit-sequence skeleton, W2 extract, W3 adapter) can import `github.com/ackstorm/ach/internal/cli/state` with full surface (File, Load, Save, WriteAtomic, ResolvePath, SweepTmp, GuardEnvironment, ErrSchemaMismatch, ErrEnvironmentGuard)
</verification>

<success_criteria>
- internal/cli/state ships 11 files (5 source + 5 test + doc.go) — same shape as internal/cli/config
- State.json v2 round-trips via Load/Save — JSON shape matches §8.2 exactly
- Atomic write contract: tmp+fsync(fd)+rename+fsync(parent_dir) per STATE-07
- Schema mismatch surfaces ErrSchemaMismatch — caller layer maps to exit 5
- Environment guard surfaces ErrEnvironmentGuard — caller layer maps to exit 4
- Zero new go.mod entries; stdlib-only mirrors Phase 1/6 discipline
</success_criteria>

<output>
Create `.planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-W1-02-SUMMARY.md` when done.
</output>
