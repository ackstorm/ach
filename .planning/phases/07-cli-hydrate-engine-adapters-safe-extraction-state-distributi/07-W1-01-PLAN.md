---
phase: 07-cli-hydrate-engine-adapters-safe-extraction-state-distributi
plan: 07-W1-01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - internal/cli/exit/exit.go
  - internal/cli/exit/exit_test.go
autonomous: true
requirements_addressed:
  - STATE-02
  - STATE-03
  - STATE-04
  - STATE-09
  - SAFE-04
must_haves:
  truths:
    - "Phase shape per D-01: single Phase 7 organized into 4 waves (W1 foundation, W2 safe-extract, W3 adapters, W4 e2e+docs) with ~12-15 plans; this ROADMAP plan ships first to materialize the wave boundary."
    - "Phase 7 ROADMAP entry no longer lists DIST-01..04 or SC#5 (D-23)."
    - "ROADMAP.md contains a new Phase 7.1 entry with Goal/Depends-on/Requirements DIST-01..04/Success Criteria mirroring the old SC#5/Plans=TBD."
    - "REQUIREMENTS.md Traceability rows for DIST-01..04 list Phase 7.1 (not Phase 7)."
    - "internal/cli/exit/exit.go exports four NEW typed constants: Drift Code = 2, EnvironmentMismatch Code = 4, SchemaMismatch Code = 5, CollisionRefuse Code = 7."
    - "Existing Phase 6 constants (OK=0, General=1, AuthN=3, Network=6, ConfigFile=8) are NOT renamed or renumbered (additive change per exit.go top-of-file comment)."
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 7 entry refresh + Phase 7.1 insert"
      contains: "Phase 7.1:"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability rows DIST-01..04 retargeted to Phase 7.1"
      contains: "| DIST-01 | Phase 7.1 |"
    - path: "internal/cli/exit/exit.go"
      provides: "Drift/EnvironmentMismatch/SchemaMismatch/CollisionRefuse constants"
      contains: "Drift Code = 2"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "DIST-* phase column"
      pattern: "DIST-0[1-4] \\| Phase 7.1"
    - from: "internal/cli/exit/exit.go"
      to: "internal/cli/exit/exit_test.go"
      via: "constant value assertions"
      pattern: "Drift.*== 2"
---

<objective>
Ship the Phase 7.1 carve-out and seed the Phase 7 exit-code surface.

Two concerns bundled because both are pure-docs/pure-additive scaffolding gates that every downstream W1/W2/W3/W4 plan depends on:

1. **ROADMAP / REQUIREMENTS refresh (D-23 + "Specifics" item 6 + CLAUDE.md "Documentation hygiene"):** drop DIST-01..04 and SC#5 from the Phase 7 entry; insert a new "Phase 7.1: Distribution polish" entry immediately after Phase 7 with goal sentence + dependency + Requirements DIST-01..04 + Success Criteria mirroring the old SC#5 + Plans: TBD; retarget REQUIREMENTS.md Traceability rows for DIST-01..04 from Phase 7 to Phase 7.1.

2. **internal/cli/exit Phase 7 constants (D-04/STATE-02/STATE-03/STATE-04/STATE-09/SAFE-04 exit codes):** add `Drift Code = 2`, `EnvironmentMismatch Code = 4`, `SchemaMismatch Code = 5`, `CollisionRefuse Code = 7` as additive constants per the exit.go top-of-file comment lines 6-8. Phase 6 constants (OK=0, General=1, AuthN=3, Network=6, ConfigFile=8) are UNCHANGED.

Purpose: Every Phase 7 plan that raises a coded error references these constants; every Phase 7 commit that touches ROADMAP.md MUST be in a tree where the Phase 7.1 entry already exists (avoiding mid-phase ROADMAP churn). Shipping this first eliminates a cross-cutting rebase hazard.

Output: ROADMAP.md + REQUIREMENTS.md updated; exit.go has 4 new constants + exit_test.go asserts values verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-CONTEXT.md
@.planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-PATTERNS.md
@internal/cli/exit/exit.go
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ROADMAP + REQUIREMENTS refresh — carve out Phase 7.1 (DIST-01..04, SC#5)</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/ROADMAP.md (current Phase 7 entry lines 305-319 — strip DIST-01..04 from Requirements, strip SC#5)
    .planning/REQUIREMENTS.md (Traceability table lines 337-340 — DIST-01..04 rows currently Phase 7)
    .planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-CONTEXT.md (D-23 + Specifics item 6 — exact carve-out scope)
    CLAUDE.md ("Documentation hygiene" section — same-commit rule)
  </read_first>
  <behavior>
    - Phase 7 Requirements line lists only: STATE-01..11, ADAPT-01..07, SAFE-01..06 (24 IDs, no DIST-* per D-23)
    - Phase 7 Success Criteria lists only SC#1..SC#4 (4 of the original 5; SC#5 moves to Phase 7.1)
    - New "Phase 7.1: Distribution polish" entry inserted between Phase 7 and the Progress section
    - Phase 7.1 entry includes: bolded Goal sentence, Depends on: Phase 7, Requirements: DIST-01..04, Success Criteria mirroring old SC#5 wording, Plans: TBD
    - Phase 7.1 added to the Progress table at the bottom of ROADMAP.md as "0/TBD | Not started | -"
    - REQUIREMENTS.md Traceability rows for DIST-01..04 read "Phase 7.1" in the Phase column (was "Phase 7")
    - Coverage line in REQUIREMENTS.md at line 211 still reads "126 of 126 v1alpha1 REQ-IDs mapped" — no IDs lost
  </behavior>
  <action>
    Edit .planning/ROADMAP.md Phase 7 entry per D-23: in the "Requirements:" line strip the four DIST-IDs leaving STATE-01..11 + ADAPT-01..07 + SAFE-01..06; in Success Criteria block delete SC#5 (the "Distribution artifacts are publishable..." paragraph). Insert a new "### Phase 7.1: Distribution polish (windows binary, Homebrew tap, Helm chart polish, K8s InitContainer pattern)" entry immediately after the Phase 7 block and before the "## Progress" section. Phase 7.1 entry body uses the format: bolded "**Goal**:" one sentence summarizing DIST-01..04 + the SC#5 wording recast as the phase goal, "**Depends on**: Phase 7" line, "**Requirements**: DIST-01, DIST-02, DIST-03, DIST-04" line, "**Success Criteria** (what must be TRUE):" numbered list mirroring SC#5 four sub-points (OCI image runs with no preset config + 5 binaries downloadable + brew install ackstorm/tap/ach succeeds + Helm chart deploys Hub end-to-end + InitContainer pattern runs in synthetic mode), "**Plans**: TBD", "**UI hint**: no". Add a row at the end of the Progress table: "| 7.1. Distribution polish (windows binary, Homebrew tap, Helm chart polish, K8s InitContainer pattern) | 0/TBD | Not started | - |". Then edit .planning/REQUIREMENTS.md Traceability table rows DIST-01..04 to read "Phase 7.1" in the Phase column (was "Phase 7"). Do NOT change the Coverage line at line 211 (still 126/126 — no IDs were dropped, only retargeted). Do NOT modify any other Phase 7 success criterion or requirement.
  </action>
  <verify>
    <automated>grep -c "DIST-0[1-4]" .planning/ROADMAP.md | grep -v "^#" | (read n; test "$n" -ge 4) && grep -q "^### Phase 7.1: Distribution polish" .planning/ROADMAP.md && grep -E "^\| DIST-0[1-4] \| Phase 7.1 \|" .planning/REQUIREMENTS.md | wc -l | grep -q "^4$" && grep -q "Coverage:\*\* 126 of 126" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/ROADMAP.md Phase 7 "**Requirements**:" line does NOT contain "DIST-01", "DIST-02", "DIST-03", "DIST-04"
    - .planning/ROADMAP.md contains heading "### Phase 7.1: Distribution polish"
    - .planning/ROADMAP.md Phase 7 Success Criteria list contains exactly 4 numbered items (SC#5 removed)
    - .planning/REQUIREMENTS.md rows matching `^\| DIST-0[1-4] \| Phase 7.1 \|` count = 4
    - .planning/REQUIREMENTS.md Coverage line still reads "126 of 126"
    - `git diff --stat .planning/ROADMAP.md .planning/REQUIREMENTS.md` shows two files modified
  </acceptance_criteria>
  <done>Phase 7.1 entry exists; DIST-01..04 Traceability rows retargeted; Phase 7 entry no longer references DIST-* or SC#5.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Phase 7 exit codes — additive constants in internal/cli/exit/exit.go</name>
  <files>internal/cli/exit/exit.go, internal/cli/exit/exit_test.go</files>
  <read_first>
    internal/cli/exit/exit.go (current Phase 6 constants block lines 23-47; top-of-file comment lines 6-8 explicitly reserves codes 2/4/5/7 for "hydrate-engine territory")
    .planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-PATTERNS.md ("Phase 7 additions to internal/cli/exit/exit.go" — exact wording for new constants, lines 561-577)
    .planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-CONTEXT.md (D-04 = manifest schemaVersion mismatch exit 5; D-13 = state.schemaVersion != "2" exit 5; D-17 = collision exit 7)
    spec/ach_cli_spec_v20260515_FINALv4.md §9.3 (exit-code matrix — Phase 7 adds 2/4/5/7 to Phase 6 set)
  </read_first>
  <behavior>
    - exit.go exports `Drift Code = 2` with godoc citing STATE-04 four-outcome truth table (local-edit-preserve + conflict-preserve both surface as Drift per D-14/D-16)
    - exit.go exports `EnvironmentMismatch Code = 4` with godoc citing STATE-03 (same-<ach-dir> different-Environment guard per spec §8.3)
    - exit.go exports `SchemaMismatch Code = 5` with godoc citing STATE-09 (manifest schemaVersion != "v1alpha1" per §6.2) AND STATE-02 (state.schemaVersion != "2" per §8.2) — same code, two trigger paths
    - exit.go exports `CollisionRefuse Code = 7` with godoc citing SAFE-04 (auto-claim exists-unowned + bytes differ + !--force per §6.4)
    - Phase 6 constants (OK=0, General=1, AuthN=3, Network=6, ConfigFile=8) UNCHANGED — same values, same names, same order in the const block
    - The new constants slot between existing arms in numeric order: OK(0), General(1), Drift(2), AuthN(3), EnvironmentMismatch(4), SchemaMismatch(5), Network(6), CollisionRefuse(7), ConfigFile(8)
    - Top-of-file comment lines 6-8 (which currently says "codes 2 (drift), 4 (state mismatch), 5 (schema mismatch), and 7 (local I/O) are hydrate-engine territory (Phase 7) and stay absent here") is rewritten: drop the "stay absent here" note since these codes are now present; replace with a one-line note that codes 2/4/5/7 are Phase 7 additions per STATE-02/STATE-03/STATE-04/STATE-09/SAFE-04
    - exit_test.go contains `TestPhase7Codes` asserting all four new constant values via `if int(exit.Drift) != 2 { t.Fatalf(...) }` form (no testify, no gomega — Phase 6 discipline)
    - exit_test.go contains `TestPhase6CodesUnchanged` asserting OK=0, General=1, AuthN=3, Network=6, ConfigFile=8 still hold (regression gate against accidental renumbering)
    - `MapServerError` unchanged — Phase 7 codes are raised via `*exit.CodedError`, not via HTTP status mapping; the existing 401/403/503/504 switch arms still cover Phase 7's server-error surface
  </behavior>
  <action>
    Edit internal/cli/exit/exit.go: rewrite the lines 6-8 comment to say "Codes 2 (Drift), 4 (EnvironmentMismatch), 5 (SchemaMismatch), and 7 (CollisionRefuse) are Phase 7 additions per STATE-02/STATE-03/STATE-04/STATE-09/SAFE-04. The Phase 6 set (0/1/3/6/8) is unchanged — every renumber would be a wire-format break." Insert the four new constants into the existing const block in numeric order (Drift between General and AuthN; EnvironmentMismatch between AuthN and the next existing; SchemaMismatch between EnvironmentMismatch and Network; CollisionRefuse between Network and ConfigFile). Each new constant carries a godoc comment naming the originating REQ-ID and spec section (per 07-PATTERNS.md "Phase 7 additions to internal/cli/exit/exit.go"). Do NOT touch MapServerError. Do NOT touch CodedError struct, Error method, Unwrap method, or the Code typed-int. Create internal/cli/exit/exit_test.go with stdlib testing package; add TestPhase7Codes asserting Drift==2, EnvironmentMismatch==4, SchemaMismatch==5, CollisionRefuse==7 via `int(exit.Drift) != 2 { t.Fatalf(...) }`-shape table tests; add TestPhase6CodesUnchanged asserting OK==0, General==1, AuthN==3, Network==6, ConfigFile==8. SPDX header on the test file. NO testify imports. NO assertion library.
  </action>
  <verify>
    <automated>./scripts/dev.sh make unit-pkg PKG=./internal/cli/exit/...</automated>
  </verify>
  <acceptance_criteria>
    - `./scripts/dev.sh make unit-pkg PKG=./internal/cli/exit/...` exits 0
    - `grep -E "^\s+(Drift|EnvironmentMismatch|SchemaMismatch|CollisionRefuse) Code = [0-9]" internal/cli/exit/exit.go` returns 4 matches
    - `grep -E "^\s+(OK|General|AuthN|Network|ConfigFile) Code = [0-9]" internal/cli/exit/exit.go` still returns 5 matches with values 0/1/3/6/8 respectively (Phase 6 regression gate)
    - internal/cli/exit/exit_test.go contains "TestPhase7Codes" and "TestPhase6CodesUnchanged" function declarations
    - SPDX header present on exit_test.go (first line `// SPDX-License-Identifier: Apache-2.0`)
    - `./scripts/dev.sh make lint-changed` exits 0
  </acceptance_criteria>
  <done>Four Phase 7 exit codes ship; Phase 6 codes unchanged; tests assert both invariants.</done>
</task>

</tasks>

<verification>
Wave 1 plan 07-W1-01 closes when:
- `./scripts/dev.sh make unit-pkg PKG=./internal/cli/exit/...` exits 0 (Task 2 gate)
- `grep -q "^### Phase 7.1: Distribution polish" .planning/ROADMAP.md` (Task 1 gate)
- `grep -E "^\| DIST-0[1-4] \| Phase 7.1 \|" .planning/REQUIREMENTS.md` returns 4 matches (Task 1 gate)
- No downstream plan in W1/W2/W3/W4 references `exit.<NewConst>` until this plan lands
</verification>

<success_criteria>
- Phase 7.1 carve-out lives in ROADMAP.md before any other Phase 7 commit (avoids mid-phase ROADMAP churn)
- internal/cli/exit exports the 4 new constants downstream plans depend on
- All other Phase 7 plans can `import "github.com/ackstorm/ach/internal/cli/exit"` and reference Drift/EnvironmentMismatch/SchemaMismatch/CollisionRefuse without further edits to exit.go
</success_criteria>

<output>
Create `.planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-W1-01-SUMMARY.md` when done.
</output>
