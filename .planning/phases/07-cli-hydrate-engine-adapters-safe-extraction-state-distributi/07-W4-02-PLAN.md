---
phase: 07-cli-hydrate-engine-adapters-safe-extraction-state-distributi
plan: 07-W4-02
type: execute
wave: 4
depends_on:
  - 07-W3-05
  - 07-W4-01
files_modified:
  - test/e2e/cli_login_hydrate_test.go
  - CLAUDE.md
autonomous: true
requirements_addressed:
  - STATE-02
  - STATE-03
  - STATE-09
must_haves:
  truths:
    - "test/e2e/cli_login_hydrate_test.go's W3-P3 invocation (`ach-cli hydrate --environment demo > hydrate.json`) now passes --raw so the byte-for-byte golden diff against examples/hydrate.json continues to pass under the new engine default (D-21 / `<specifics>` '--raw golden-diff preservation')."
    - "CLAUDE.md `## Common failure modes` gains TWO new entries: (1) state.json schemaVersion mismatch → exit 5; (2) Same-`<ach-dir>` different-Environment → exit 4; both with --force overrides documented (CONTEXT.md `<code_context>` Integration Points #6)."
    - "CLAUDE.md existing failure-mode entry 'Hydrate output ≠ examples/hydrate.json' updated to make the `--raw` invariant explicit — the entry's normalization fallback now explicitly references the `--raw` short-circuit and the engine-path default."
    - "No production code touched in this plan — purely test + docs."
  artifacts:
    - path: "test/e2e/cli_login_hydrate_test.go"
      provides: "W3-P3 golden-diff anchor preserved via --raw flag"
      contains: "--raw"
    - path: "CLAUDE.md"
      provides: "New schemaVersion + Environment guard failure-mode entries"
      contains: "exit 5"
  key_links:
    - from: "test/e2e/cli_login_hydrate_test.go"
      to: "examples/hydrate.json"
      via: "--raw preserves byte-for-byte stdout"
      pattern: "--raw"
    - from: "CLAUDE.md"
      to: "internal/cli/exit"
      via: "new entries cite exit 4 + exit 5"
      pattern: "exit 4|exit 5"
---

<objective>
Close Phase 7 documentation hygiene. Two concerns:

1. **W3-P3 e2e anchor preservation (D-21):** the existing test/e2e/cli_login_hydrate_test.go runs `ach-cli hydrate --environment demo` and byte-diffs the stdout against `examples/hydrate.json`. Phase 7 changes the engine default to NOT emit the raw response; it materializes content. The `--raw` flag (D-04, hidden) preserves the Phase 6 POST+stream byte-equal behavior. Update the test to pass `--raw`. ONE-LINE CHANGE per 07-PATTERNS.md.

2. **CLAUDE.md failure-mode additions:** add two new entries to the `## Common failure modes` section per CONTEXT.md `<code_context>` Integration Points #6:
   - state.json schemaVersion mismatch — exit 5, no files written, `--force` overrides (STATE-02 / D-13)
   - Same-`<ach-dir>` different-Environment — exit 4, `--force` overrides (STATE-03)
   Plus update the existing `Hydrate output ≠ examples/hydrate.json` entry to make the `--raw` flag explicit.

Per CLAUDE.md "Documentation hygiene": these are same-commit-with-code-change concerns — they MUST land before Phase 7 closes, not as a follow-up.

Output: 2 files modified. No new go.mod entries. No new tests (07-W4-01 already drives the engine path; the schemaVersion + Environment-guard subtests there exercise the same surfaces).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-CONTEXT.md
@test/e2e/cli_login_hydrate_test.go
@examples/hydrate.json
@CLAUDE.md
@internal/cli/exit/exit.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: test/e2e/cli_login_hydrate_test.go — add --raw to W3-P3 hydrate invocation</name>
  <files>test/e2e/cli_login_hydrate_test.go</files>
  <read_first>
    test/e2e/cli_login_hydrate_test.go (find the hydrate invocation lines — likely lines 222-226 per 07-PATTERNS.md analog reference, but confirm by reading the file)
    .planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-CONTEXT.md (D-04 + D-21 — --raw flag is the byte-equal anchor)
    examples/hydrate.json (the golden artifact the test diffs against — its shape MUST be preserved by --raw)
    cmd/ach-cli/cmd/hydrate.go (07-W3-05 modifications — verify --raw flag is registered + hidden)
  </read_first>
  <behavior>
    - Locate the existing hydrate invocation in cli_login_hydrate_test.go (it currently runs `ach-cli hydrate --environment demo` with NO --raw flag)
    - Add `--raw` to the args slice for the W3-P3 subtest (or whichever subtest does the golden-diff byte-equal compare against examples/hydrate.json)
    - The test should otherwise be unchanged — same subtest names, same assertions, same cluster prerequisites
    - The change is exactly ONE argument added; if the test wraps multiple invocations, only the one that does the golden-byte-diff gets --raw (the others may exercise engine-path behavior intentionally — verify before adding)
    - After the change, the test must still pass against the engine-path-default ach-cli binary because --raw short-circuits to the Phase 6 POST+stream body (07-W3-05 Task 3 contract)
  </behavior>
  <action>
    Read test/e2e/cli_login_hydrate_test.go to locate the W3-P3 hydrate invocation. Identify the args slice passed to phase6RunAchCli (or equivalent helper) — likely something like `[]string{"hydrate", "--environment", "demo"}` or args passed as variadic strings. Add `--raw` to that args list. Locate it by searching for the lines that immediately precede the byte-for-byte comparison against examples/hydrate.json (look for `examples/hydrate.json` references, then look upward for the hydrate invocation that produced the bytes being compared). If there are multiple hydrate invocations in the test file, ONLY add --raw to the one that does the golden-diff byte-compare; subtests that intentionally exercise engine-path behavior (if any) stay unmodified. After adding --raw, re-read the changed lines to confirm the modification is minimal and contextually correct.
  </action>
  <verify>
    <automated>./scripts/dev.sh go build -tags=e2e ./test/e2e/...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -B5 "examples/hydrate.json" test/e2e/cli_login_hydrate_test.go | grep -q "raw\\|--raw"` (— the hydrate invocation upstream of the golden-diff compare now references --raw)
    - `./scripts/dev.sh go build -tags=e2e ./test/e2e/...` exits 0
    - The Phase 6 W3-P3 subtest name is unchanged (`grep -q "hydrate_golden_diff\\|w3_p3" test/e2e/cli_login_hydrate_test.go` — depends on existing name)
    - No subtest other than the golden-diff one had --raw added (confirm via `grep -c "\"--raw\"" test/e2e/cli_login_hydrate_test.go | grep -qE "^[1-2]$"` — 1 occurrence in args slice + possibly 1 mention in a comment is acceptable; >2 means accidentally modified other invocations)
  </acceptance_criteria>
  <done>W3-P3 golden-diff test passes --raw; engine default does not regress the byte-for-byte stdout anchor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CLAUDE.md — add schemaVersion mismatch + Environment guard failure-mode entries + update --raw entry</name>
  <files>CLAUDE.md</files>
  <read_first>
    CLAUDE.md (current "Common failure modes" section — locate the existing "Hydrate output ≠ examples/hydrate.json" entry to verify wording before modifying)
    .planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-CONTEXT.md (`<code_context>` Integration Points #6 — three CLAUDE.md modifications listed; this task lands all three)
    internal/cli/exit/exit.go (post-07-W1-01 — Drift=2, EnvironmentMismatch=4, SchemaMismatch=5, CollisionRefuse=7 are the codes the new entries reference)
    spec/ach_cli_spec_v20260515_FINALv4.md §8.2 (state schema v2 — schemaVersion contract for the new entry)
    spec/ach_cli_spec_v20260515_FINALv4.md §8.3 (Same-<ach-dir> different-Environment guard — for the new entry)
  </read_first>
  <behavior>
    - Two new "### ❌ ... ✅ ..." entries added to CLAUDE.md `## Common failure modes` section (matches the existing entry style — heading then ❌ symptom then ✅ remedy then "WHY IT FAILS:" prose)
    - Entry 1: state.json schemaVersion mismatch:
      - ❌ Symptom: ach-cli hydrate emits `state: schemaVersion != "2"` and exits 5; no files written
      - ✅ Remedy: this is the v1alpha1 clean-break per spec §8.2. The state.json on disk was written by an out-of-tree CLI build OR a future v2-incompatible schema. Either (a) delete `<ach-dir>/state.json` (Phase 7 ships only schemaVersion="2"; no v1 reader code exists), OR (b) pass `--force` to overwrite — this discards any state about prior hydrations + ALL drift detection becomes inert. The clean-break is intentional per CONTEXT.md D-13.
      - WHY IT FAILS: STATE-02 forbids silent migration. Crashes on schemaVersion drift are the only safe posture; a v1 reader could mis-interpret v2 dual-hash fields and trigger spurious overwrites.
    - Entry 2: Same-`<ach-dir>` different-Environment guard:
      - ❌ Symptom: ach-cli hydrate emits `state: same <ach-dir> bound to a different Environment (per STATE-03 / spec §8.3)` and exits 4
      - ✅ Remedy: this is the workspace scope guard — the .ach/ directory you ran from previously hydrated against Environment X and you're now requesting Environment Y. Either (a) `cd` into a different workspace (each Environment gets its own <ach-dir>), OR (b) use --global which writes to ~/.ach/<environment>/ instead (each Environment is namespaced by directory), OR (c) pass --force to overwrite. The guard ONLY fires in workspace scope; global scope's namespacing makes the conflict impossible.
      - WHY IT FAILS: STATE-03 is the load-bearing barrier against mixing Environment-bound credentials in the same on-disk runtime config. Without it, a `claude-code` workspace bound to `prod` could silently be relabeled to `staging` mid-session.
    - Existing "Hydrate output ≠ examples/hydrate.json" entry updated: the entry currently documents the normalization-helper approach. Add a paragraph at the top explicitly stating "Phase 7 changed the default `ach-cli hydrate` behavior: the engine path materializes content to disk (NOT to stdout). The byte-for-byte golden-diff anchor lives behind the hidden `--raw` flag — `ach-cli hydrate --raw --environment demo > /tmp/hydrate-test.json` reproduces the Phase 6 stdout contract verbatim." Then the existing normalization paragraphs stay (they remain relevant for the --raw path against custom base URLs).
  </behavior>
  <action>
    Locate the "## Common failure modes" section in CLAUDE.md. Identify the existing "❌ Hydrate output ≠ examples/hydrate.json" entry. Insert TWO new entries in the appropriate location (chronologically — Phase 7 entries go near the end of the section, after the existing hydrate/Forwarder/Environment ones). Use the exact ❌ / ✅ / WHY IT FAILS structure documented in the existing entries (match heading levels, bold/italic conventions, and code-fence usage). Cite the originating REQ-IDs (STATE-02 / STATE-03) + decision IDs (D-13 / D-04) + spec sections (§8.2 / §8.3) in WHY IT FAILS prose. After inserting the new entries, scroll to the existing "Hydrate output ≠ examples/hydrate.json" entry and add the Phase-7-default paragraph at the top of the ✅ remedy block (above the existing two-numbered-remedies list). Do NOT delete or reorder the existing normalization paragraphs — they continue to apply for the --raw path against exotic-host clusters. Re-read the modified section after writing to ensure no formatting breakage (CLAUDE.md is the load-bearing AI-reference file — corrupted formatting blocks everyone downstream).
  </action>
  <verify>
    <automated>grep -c "exit 5\|exit 4" CLAUDE.md | (read n; test "$n" -ge 2)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "schemaVersion" CLAUDE.md` (new entry 1 references the field)
    - `grep -q "same <ach-dir>\\|Same-.*<ach-dir>" CLAUDE.md` (new entry 2 references the guard)
    - `grep -q "exit 5" CLAUDE.md` (schemaVersion entry cites the exit code)
    - `grep -q "exit 4" CLAUDE.md` (Environment guard entry cites the exit code)
    - `grep -q "--raw" CLAUDE.md` (existing hydrate-golden entry updated)
    - `grep -q "STATE-02" CLAUDE.md` (new entry cites REQ-ID)
    - `grep -q "STATE-03" CLAUDE.md`
    - Count of ❌ markers in "Common failure modes" section is at least 2 higher than before (verify via wc -l of the section before/after)
    - CLAUDE.md still renders as valid markdown (no broken code-fence balance: `grep -c '^```' CLAUDE.md | awk '{ if ($1 % 2 != 0) exit 1 }'`)
  </acceptance_criteria>
  <done>CLAUDE.md gains 2 new failure-mode entries + updates existing --raw entry; documentation hygiene rule satisfied for Phase 7 close.</done>
</task>

</tasks>

<verification>
Wave 4 plan 07-W4-02 closes when:
- `./scripts/dev.sh go build -tags=e2e ./test/e2e/...` exits 0 (the --raw addition compiles)
- `grep -q "--raw" test/e2e/cli_login_hydrate_test.go` (Task 1)
- `grep -E "exit 5|exit 4|schemaVersion" CLAUDE.md` shows the new failure-mode entries (Task 2)
- The W3-P3 golden-diff test passes against examples/hydrate.json byte-for-byte under the engine-default binary
</verification>

<success_criteria>
- D-21 W3-P3 e2e golden-diff anchor preserved via --raw
- CLAUDE.md "Common failure modes" reflects Phase 7's three new exit codes (4, 5, plus the existing --raw normalization context)
- Documentation hygiene rule satisfied — same-commit-with-code-change for ROADMAP (07-W1-01) + CLAUDE.md (here) + REQUIREMENTS (07-W1-01) all land within Phase 7 close
</success_criteria>

<output>
Create `.planning/phases/07-cli-hydrate-engine-adapters-safe-extraction-state-distributi/07-W4-02-SUMMARY.md` when done.
</output>
