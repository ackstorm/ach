---
phase: 04-hub-forwarder-jwt-trust-path
plan: 06
plan_id: 04-06
type: execute
wave: 2
depends_on: [04-03]
files_modified:
  - internal/forwarder/precheck/check.go
  - internal/forwarder/precheck/check_test.go
  - internal/forwarder/precheck/doc.go
  - internal/forwarder/precheck/errors.go
autonomous: true
requirements: [FWD-03]
tags: [forwarder, precheck, mcp, a2a, ek-pk-authorization]
must_haves:
  truths:
    - "CheckMCP(ctx, kc, name, deps) returns nil on authorized access; typed sentinel error otherwise"
    - "CheckA2A(ctx, kc, name, deps) mirrors CheckMCP shape but reads spec.runtime.a2aAgents[] (not mcpServers[])"
    - "ek_ path: O(1) informer lookup of bound Environment; check name presence in spec.runtime.{mcpServers|a2aAgents}[]; fail-closed if Environment terminating (DeletionTimestamp != nil) → ErrUnauthorizedResource (D-15 pre-decision: narrow error surface)"
    - "D-16: pk_ path — keystore.TeamsResolver.Resolve gets caller's teams; List ALL Environments in namespace where spec.runtime.{mcpServers|a2aAgents}[] contains name AND spec.authorizedTeams[] intersects caller teams; empty result → ErrUnauthorizedTeam; LiteLLM unreachable → 503 fail-closed."
    - "LiteLLM-unreachable during pk_ teams resolve → ErrLiteLLMUnreachable (Forwarder maps to 503 per FWD-03)"
    - "No JWT signing, no LiteLLM forwarding on any failure return path (caller handles error before invoking proxy)"
  artifacts:
    - path: "internal/forwarder/precheck/check.go"
      provides: "CheckMCP + CheckA2A + Deps struct"
      contains: "func CheckMCP"
    - path: "internal/forwarder/precheck/errors.go"
      provides: "Typed sentinel errors (ErrUnauthorizedResource, ErrUnauthorizedTeam, ErrLiteLLMUnreachable, ErrEnvironmentNotFound, ErrInvalidKeyType)"
      contains: "ErrUnauthorizedResource"
  key_links:
    - from: "internal/forwarder/precheck/check.go"
      to: "internal/keystore.TeamsResolver (Plan 04-03)"
      via: "deps.TeamsResolver.Resolve(ctx, kc.OwnerEmail)"
      pattern: "TeamsResolver\\.Resolve"
    - from: "internal/forwarder/precheck/check.go"
      to: "Environment informer via mgr.GetClient()"
      via: "client.Client.Get or client.Client.List (read-only via cached client)"
      pattern: "client\\.Client"
---

<objective>
Phase 4 Wave 2 §5.1 step-4 pre-check: implement the per-route authorization that determines whether `/mcp/{name}` / `/a2a/{name}` requests proceed to the proxy. Both key-types covered; both routes covered.

Per FWD-03 + SC#2:
- `ek_`: name MUST appear in bound `Environment.spec.runtime.mcpServers[]` (on /mcp/) or `.a2aAgents[]` (on /a2a/). O(1) informer lookup. No LiteLLM round-trip on the ek_ path.
- `pk_`: caller's LiteLLM Teams MUST intersect non-empty with `Environment.spec.authorizedTeams[]` of at least one Environment whose `spec.runtime.{mcpServers|a2aAgents}` contains the route name. LiteLLM teams come from `keystore.TeamsResolver` (Redis-cached, ≤60s TTL).
- Failures: `403 unauthorized_resource` (ek_ name not in bound env), `403 unauthorized_team` (pk_ empty intersection), `503 litellm_unreachable` (teams resolve fail), `404 environment_not_found` (terminating env per D-15 narrowed to ErrUnauthorizedResource).
- No JWT signed, no LiteLLM forward on ANY failure.

Output: 3 new Go files (check.go, errors.go, doc.go) + tests using mocks for keystore.TeamsResolver and the controller-runtime client.

Wave 2 because depends on Plan 04-03's `keystore.TeamsResolver` (the type must exist before this package can type its Deps).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-hub-forwarder-jwt-trust-path/04-CONTEXT.md
@.planning/phases/04-hub-forwarder-jwt-trust-path/04-PATTERNS.md
@.planning/phases/04-hub-forwarder-jwt-trust-path/04-03-PLAN.md
@internal/keystore/teamsresolver.go
@internal/platformapi/store/store.go
@internal/platformapi/teams/lookup.go
@internal/platformapi/middleware/middleware.go
@internal/keys/keys.go
@api/ach/v1alpha1/environment_types.go
@hack/boilerplate.go.txt

<interfaces>
<!-- Existing types this plan composes. -->
From internal/keystore/teamsresolver.go (Plan 04-03):
  - type TeamsResolver interface { Resolve(ctx context.Context, ownerEmail string) ([]string, error) }

From internal/platformapi/middleware/middleware.go (existing Phase 3):
  - type KeyContext struct { KeyID, KeyType, OwnerEmail, Environment string; ... }   // KeyType is one of "pk", "ek", or empty
  - func KeyContextFromCtx(ctx context.Context) (KeyContext, bool)

From internal/keys/keys.go (existing Phase 3 — confirm exact constants):
  - PrefixPk = "pk"  or similar — match the literal string used in middleware.KeyContext.KeyType

From api/ach/v1alpha1/environment_types.go:
  - type EnvironmentSpec struct { Runtime RuntimeSpec; AuthorizedTeams []string; ... }
  - type RuntimeSpec struct { Models []ModelRef; MCPServers []string; A2AAgents []string; ... }

From sigs.k8s.io/controller-runtime/pkg/client:
  - client.Client.Get(ctx, types.NamespacedName, obj) — informer-cached read
  - client.Client.List(ctx, list, ...) — informer-cached list with optional MatchingFields / InNamespace

Plan 04-07 (proxy per-route handlers) will:
  err := precheck.CheckMCP(r.Context(), kc, name, precheck.Deps{...})
  // map err to render.Error(w, 403, "unauthorized_resource", ..., reqID) etc.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: precheck package — typed errors + Deps + CheckMCP + CheckA2A + tests</name>
  <files>internal/forwarder/precheck/check.go, internal/forwarder/precheck/check_test.go, internal/forwarder/precheck/errors.go, internal/forwarder/precheck/doc.go</files>
  <read_first>
    - internal/keystore/teamsresolver.go (after Plan 04-03 lands; confirm exact TeamsResolver interface signature)
    - internal/platformapi/teams/lookup.go (Phase 3 pk_ team-intersection helper — pattern to lift; PATTERNS.md "exact analog")
    - internal/platformapi/store/store.go (cached-client Environment read; GetEnvironment + ListAuthorizedEnvironments helpers — close in shape to what precheck needs)
    - internal/platformapi/middleware/middleware.go (KeyContext struct + KeyContextFromCtx — precheck consumes this from request ctx)
    - internal/keys/keys.go (PrefixPk / PrefixEk constants — confirm exact literal values to switch on KeyType)
    - api/ach/v1alpha1/environment_types.go (EnvironmentSpec.Runtime.MCPServers + .A2AAgents + .AuthorizedTeams field names)
    - .planning/phases/04-hub-forwarder-jwt-trust-path/04-CONTEXT.md sections D-15 (ek_ logic + DeletionTimestamp pre-decision), D-16 (pk_ logic + planner clarification on union-of-environments lookup), D-17 (TeamsResolver shape)
    - .planning/phases/04-hub-forwarder-jwt-trust-path/04-PATTERNS.md section "internal/forwarder/precheck/check.go"
    - hack/boilerplate.go.txt
  </read_first>
  <behavior>
    - Test PC1: CheckMCP with KeyType="" (no key) → ErrInvalidKeyType.
    - Test PC2-ek: CheckMCP with ek_ KeyContext where Environment "demo" exists, runtime.mcpServers=["foo","bar"], request name="foo" → returns nil.
    - Test PC3-ek: same fixture, request name="baz" (not in list) → ErrUnauthorizedResource.
    - Test PC4-ek: Environment lookup returns nil (not found) → ErrUnauthorizedResource (per D-15 narrow error surface pre-decision; NOT ErrEnvironmentNotFound — that's reserved for a future strict variant).
    - Test PC5-ek: Environment has DeletionTimestamp != nil (terminating) → ErrUnauthorizedResource.
    - Test PC6-ek: client.Get returns transient k8s error → propagate as wrapped error (NOT a typed sentinel; caller maps to 500 internal_error).
    - Test PC7-pk: CheckMCP with pk_ KeyContext; TeamsResolver returns ["team-a"]; Environment list contains {Name:"env1", AuthorizedTeams:["team-a"], Runtime.MCPServers:["foo"]} → for request name="foo": intersection non-empty AND "foo" present in runtime.mcpServers → returns nil.
    - Test PC8-pk: same fixture but TeamsResolver returns ["team-x"] (no intersection with env1.AuthorizedTeams) → ErrUnauthorizedTeam.
    - Test PC9-pk: TeamsResolver returns generic error (e.g. connection refused) → ErrLiteLLMUnreachable.
    - Test PC10-pk: TeamsResolver returns []string{} (user has no teams) → ErrUnauthorizedTeam (empty caller teams ∩ anything = ∅).
    - Test PC11-pk: Two Environments — env1 has "foo" in runtime.mcpServers but caller not in authorizedTeams; env2 has "foo" AND caller in authorizedTeams → returns nil (any matching env grants access; union semantics).
    - Test PC12-pk: Two Environments — env1 has "foo" + caller in teams, env2 has "bar" + caller in teams; request name="bar" → returns nil (only env2 matches; correctly resolved).
    - Test PC13-CheckA2A: same scenarios PC2 through PC4 but reading runtime.a2aAgents[] instead of runtime.mcpServers[].
    - Test PC14: terminating Environment in pk_ List path is excluded from the match set (a terminating env does NOT grant access to its mcpServers; an active env with same setup does).
  </behavior>
  <action>
Create `internal/forwarder/precheck/doc.go` — 3-line package doc citing FWD-03 + Hub §5.1 step-4.

Create `internal/forwarder/precheck/errors.go`:
```
package precheck

import "errors"

var (
    ErrInvalidKeyType        = errors.New("precheck: invalid or missing key type")
    ErrUnauthorizedResource  = errors.New("precheck: unauthorized resource (name not in bound environment)")
    ErrUnauthorizedTeam      = errors.New("precheck: unauthorized team (no environment grants caller access to this name)")
    ErrLiteLLMUnreachable    = errors.New("precheck: litellm unreachable during teams resolve")
    ErrEnvironmentNotFound   = errors.New("precheck: environment not found")
)
```

Create `internal/forwarder/precheck/check.go`:

Package `precheck`. Imports: `context`, `errors`, `fmt`, `sigs.k8s.io/controller-runtime/pkg/client`, `k8s.io/apimachinery/pkg/types`, `k8s.io/apimachinery/pkg/api/errors` aliased `apierrors` (for IsNotFound), `github.com/ackstorm/ach/api/ach/v1alpha1` aliased `achv1alpha1`, `github.com/ackstorm/ach/internal/keystore`, `github.com/ackstorm/ach/internal/platformapi/middleware`, `github.com/ackstorm/ach/internal/keys` (for PrefixPk/PrefixEk constants).

```
// Deps wires the precheck package's dependencies — kept narrow so tests
// can substitute mocks. The controller-runtime client is the cached
// client from mgr.GetClient() (Plan 04-08 wires).
type Deps struct {
    K8sClient      client.Client
    TeamsResolver  keystore.TeamsResolver
    Namespace      string
}

// CheckMCP runs the §5.1 step-4 pre-check for /mcp/<name>:
//   - ek_: name MUST appear in Environment.spec.runtime.mcpServers[]
//   - pk_: caller's teams MUST intersect AuthorizedTeams[] of at least one
//          Environment whose runtime.mcpServers[] contains <name>
//   - Failure returns a typed sentinel; caller maps to HTTP outcome.
func CheckMCP(ctx context.Context, kc middleware.KeyContext, name string, deps Deps) error {
    return check(ctx, kc, name, deps, mcpResourceKind)
}

func CheckA2A(ctx context.Context, kc middleware.KeyContext, name string, deps Deps) error {
    return check(ctx, kc, name, deps, a2aResourceKind)
}

type resourceKind int
const (
    mcpResourceKind resourceKind = iota
    a2aResourceKind
)

// runtimeList extracts the right resource slice per route family.
func runtimeList(env *achv1alpha1.Environment, kind resourceKind) []string {
    switch kind {
    case mcpResourceKind:
        return env.Spec.Runtime.MCPServers
    case a2aResourceKind:
        return env.Spec.Runtime.A2AAgents
    }
    return nil
}

func check(ctx context.Context, kc middleware.KeyContext, name string, deps Deps, kind resourceKind) error {
    switch kc.KeyType {
    case keys.PrefixEk:
        return checkEk(ctx, kc, name, deps, kind)
    case keys.PrefixPk:
        return checkPk(ctx, kc, name, deps, kind)
    default:
        return ErrInvalidKeyType
    }
}

func checkEk(ctx context.Context, kc middleware.KeyContext, name string, deps Deps, kind resourceKind) error {
    env := &achv1alpha1.Environment{}
    err := deps.K8sClient.Get(ctx, types.NamespacedName{Namespace: deps.Namespace, Name: kc.Environment}, env)
    if err != nil {
        if apierrors.IsNotFound(err) { return ErrUnauthorizedResource } // D-15 narrow
        return fmt.Errorf("precheck.checkEk: get env %s: %w", kc.Environment, err)
    }
    if !env.DeletionTimestamp.IsZero() { return ErrUnauthorizedResource } // terminating; D-15 pre-decision
    list := runtimeList(env, kind)
    for _, n := range list {
        if n == name { return nil }
    }
    return ErrUnauthorizedResource
}

func checkPk(ctx context.Context, kc middleware.KeyContext, name string, deps Deps, kind resourceKind) error {
    callerTeams, err := deps.TeamsResolver.Resolve(ctx, kc.OwnerEmail)
    if err != nil { return ErrLiteLLMUnreachable }
    if len(callerTeams) == 0 { return ErrUnauthorizedTeam }
    teamSet := make(map[string]struct{}, len(callerTeams))
    for _, t := range callerTeams { teamSet[t] = struct{}{} }

    var envList achv1alpha1.EnvironmentList
    if err := deps.K8sClient.List(ctx, &envList, client.InNamespace(deps.Namespace)); err != nil {
        return fmt.Errorf("precheck.checkPk: list envs: %w", err)
    }
    for i := range envList.Items {
        env := &envList.Items[i]
        if !env.DeletionTimestamp.IsZero() { continue } // PC14: skip terminating
        // (1) runtime.<kind> contains name?
        has := false
        for _, n := range runtimeList(env, kind) { if n == name { has = true; break } }
        if !has { continue }
        // (2) authorizedTeams intersect callerTeams?
        for _, t := range env.Spec.AuthorizedTeams {
            if _, ok := teamSet[t]; ok { return nil }
        }
    }
    return ErrUnauthorizedTeam
}
```

NOTES:
- `keys.PrefixPk` / `keys.PrefixEk`: read the existing `internal/keys/keys.go` to confirm exact literal values. If the existing constants are e.g. `"pk_"` / `"ek_"` (with underscore) and KeyContext.KeyType stores `"pk"` / `"ek"` (without), adjust the switch accordingly — match whatever shape the Phase 3 middleware stores. The contract test (PC2) MUST exercise the literal KeyType string the middleware produces.
- DO NOT add a `client.MatchingFields` query for pk_ — there is no informer index on `spec.runtime.mcpServers[]` (and adding one would force a Plan 04-04 second IndexField call). The simple namespace List is O(N) over Environments — acceptable for typical N (<100 envs/namespace).
- NEVER touch `env.Status` — precheck reads `env.Spec` only.
- NEVER call litellm.Client directly — go through `deps.TeamsResolver` (cache + singleflight is in Plan 04-03).

Create `internal/forwarder/precheck/check_test.go` covering PC1-PC14:
- Use a fake controller-runtime client (`sigs.k8s.io/controller-runtime/pkg/client/fake.NewClientBuilder().WithScheme(s).WithObjects(...).Build()`) for the k8s mock — confirm this import is already in go.mod via Phase 3 envtest usage.
- Use an in-file `mockTeamsResolver` implementing keystore.TeamsResolver with configurable return value + error + call count.
- For PC9 (LiteLLM unreachable): mockTeamsResolver returns `errors.New("connection refused")`.
- For PC13 (CheckA2A): mirror PC2-PC4 fixtures but with runtime.a2aAgents instead of runtime.mcpServers.
- stdlib testing + controller-runtime fake client + project-internal mocks only.

All files start with SPDX header.
  </action>
  <verify>
    <automated>./scripts/dev.sh make unit-pkg PKG=./internal/forwarder/precheck/... exits 0</automated>
  </verify>
  <acceptance_criteria>
    - Source: `internal/forwarder/precheck/check.go` exports exactly `CheckMCP`, `CheckA2A`, `Deps` (verify with `grep -c '^func Check\\|^type Deps' check.go == 3`).
    - Source: `internal/forwarder/precheck/errors.go` exports exactly 5 sentinels matching the must_haves list.
    - Source negative: `grep -v '^//' internal/forwarder/precheck/check.go | grep -c 'env.Status\\|\\.Status\\.' == 0` (precheck reads spec only, never status).
    - Source: `internal/forwarder/precheck/check.go` contains `deps.TeamsResolver.Resolve(ctx, kc.OwnerEmail)`.
    - Test command: `./scripts/dev.sh make unit-pkg PKG=./internal/forwarder/precheck/...` exits 0 with PC1-PC14 PASS.
    - Behavior PC4: missing Environment → ErrUnauthorizedResource (NOT ErrEnvironmentNotFound, per D-15 pre-decision).
    - Behavior PC11: union-of-environments — any matching env (correct runtime+team) grants access; failing envs are skipped.
    - Behavior PC14: terminating Environments are skipped in pk_ List path.
  </acceptance_criteria>
  <done>Precheck package exports CheckMCP + CheckA2A with the §5.1 step-4 logic for both key types; all 14 behavior tests PASS; reads spec only, never status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Forwarder per-route handler → precheck | Trusted internal API; KeyContext already authenticated by Phase 3 keystore. |
| precheck → TeamsResolver | Cached resolver delegates to LiteLLM REST; failures propagate fail-closed. |
| precheck → Environment informer | Read-only cached client; namespace-scoped (no cross-namespace). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Elevation of privilege | ek_ bound env mismatch | mitigate | checkEk reads bound env from KeyContext.Environment (set by Phase 3 keystore.EkResolve); fail-closed on missing, terminating, or name not in spec.runtime.{mcpServers|a2aAgents}. Tests PC3-PC5 cover. |
| T-04-06-02 | Elevation of privilege | pk_ team-intersection bypass | mitigate | checkPk requires non-empty intersection of caller teams ∩ env.AuthorizedTeams for at least one env that ALSO hosts the requested name. Empty caller teams (PC10) → fail-closed. Tests PC7, PC8, PC10 cover. |
| T-04-06-03 | Information disclosure | Error messages | accept | Typed sentinels carry generic strings; per-handler render.Error (Plan 04-07) emits the §15.5 envelope shape with stable outcome codes — no internal state leak. |
| T-04-06-04 | Denial of service | pk_ env-list scan | accept | List is O(N) over namespace Environments. N is operator-bounded; Hub spec is silent on per-namespace cap. Cache-miss-storm bounded by TeamsResolver singleflight (Plan 04-03). |
| T-04-06-05 | Spoofing | Stale TeamsResolver cache | accept | 60s TTL per Hub §5.1; team-removal-during-cache-window is accepted v1alpha1 behavior (auto-revoke ek_ on Team-membership removal is v1beta1 §20). |
| T-04-06-06 | Tampering | Terminating env permits | mitigate | checkEk + checkPk both skip envs with DeletionTimestamp != nil (PC5 + PC14). Terminating env cannot serve as authorization grant. |
| T-04-06-SC | Tampering | go module install | mitigate | No new go.mod entries; uses existing controller-runtime fake client (Phase 3 envtest dep), keystore (Plan 04-03), middleware (Phase 3), keys (Phase 3), achv1alpha1 (Phase 1). |

</threat_model>

<verification>
- `./scripts/dev.sh make unit-pkg PKG=./internal/forwarder/precheck/...` PASS for PC1-PC14
- `./scripts/dev.sh go test -race ./internal/forwarder/precheck/...` PASS
- `grep -c 'env.Status\\|\\.Status\\.' internal/forwarder/precheck/check.go == 0` (spec only)
- `git diff go.mod` shows zero additions
- `./scripts/dev.sh make lint-changed` exit 0
</verification>

<success_criteria>
- CheckMCP + CheckA2A cover both ek_ and pk_ key types with correct fail-closed semantics
- Terminating envs cannot grant access (in either key type path)
- LiteLLM unreachable mapped to typed sentinel (Forwarder will emit 503 per FWD-03)
- precheck reads env.Spec only, never .Status
- 14 unit tests PASS including -race
</success_criteria>

<output>
Create `.planning/phases/04-hub-forwarder-jwt-trust-path/04-06-SUMMARY.md` with PC1-PC14 results and confirmation that the precheck package never reads env.Status.
</output>
