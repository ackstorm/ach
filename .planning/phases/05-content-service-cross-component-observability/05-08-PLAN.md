---
phase: 05-content-service-cross-component-observability
plan: 08
type: execute
wave: 5
depends_on: [05-04, 05-05, 05-06, 05-07]
files_modified:
  - test/e2e/phase5_invariants_test.go
  - test/e2e/phase5_helpers_test.go
  - test/e2e/phase5_fixtures
  - Makefile
autonomous: true
requirements: [CS-01, CS-02, CS-03, CS-04, CS-05, CS-06, CS-07, CS-08, CS-09, CS-10, CS-11, OBS-03, OBS-04, OBS-05, OBS-06]
tags: [e2e, sendfile, strace, prometheus, ginkgo-free, stdlib-testing]

must_haves:
  truths:
    - "test/e2e/phase5_invariants_test.go covers ROADMAP Phase 5 SC#1-#5 — sendfile, error envelopes, §12.3 precedence, in-flight rename + staleness, /metrics endpoints across all 4 services"
    - "SC#1 verification asserts at least one sendfile / sendfile64 syscall in strace output during a 1MB+ content GET"
    - "SC#5 verification curls /metrics on each of operator, platform-api, forwarder, content-service and greps for the §18.5 normative metric names per OBS-04/OBS-06"
    - "Makefile wait-content-service target fixed — co-located Pod means CS readiness piggybacks on operator Deployment rollout, NOT a non-existent ach-content-service Deployment"
    - "phase5SuiteGuard pattern mirrors phase4SuiteGuard — Skipf cleanly when ACH_E2E_PHASE5 env var unset OR Forwarder/CS endpoints unreachable"
    - "Test suite uses stdlib testing only (no Ginkgo) — convention from phase4_invariants_test.go"
  artifacts:
    - path: test/e2e/phase5_invariants_test.go
      provides: "TestPhase5Invariants umbrella test with SC1..SC5 subtests"
      min_lines: 300
    - path: test/e2e/phase5_helpers_test.go
      provides: "phase5SuiteGuard + curl/exec helpers + strace marshalling helper"
      min_lines: 150
    - path: Makefile
      provides: "wait-content-service target updated to reflect co-located Pod topology"
      contains: "wait-content-service"
  key_links:
    - from: test/e2e/phase5_invariants_test.go (SC1)
      to: kubectl exec operator Pod content-service container strace
      via: "exec into the container, run strace -e trace=sendfile,sendfile64 -p 1 -e signal=none for N seconds while curling"
      pattern: "strace.*sendfile"
    - from: test/e2e/phase5_invariants_test.go (SC5)
      to: each service's /metrics endpoint
      via: "kubectl port-forward + http.Get + body grep for normative metric names"
      pattern: "/metrics"
---

<objective>
Land the terminal Phase 5 verification surface: a stdlib-testing-based E2E suite at `test/e2e/phase5_invariants_test.go` covering ROADMAP Phase 5 SC#1..SC#5 against the kind+Helm cluster brought up by `make e2e-keep`. Fix the broken `make wait-content-service` Makefile target (the co-located-Pod topology from Plans 05-06/05-07 means there's no `ach-content-service` Deployment — readiness piggybacks on the operator Deployment).

Purpose: ROADMAP Phase 5 SCs (sendfile, errors, §12.3 precedence, in-flight rename + staleness, /metrics) are normative — without an E2E gate, no claim can be made that Phase 5 is "done" against live infrastructure. The sendfile syscall assertion is the highest-signal test: it proves the D-01 streaming discipline (no http.ServeContent, identity transfer, zero-copy) on real Linux kernel rather than relying on integration-test header-only assertions from Plan 05-05.
Output: Two test files (one umbrella + one helper file) + 1 Makefile target fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-content-service-cross-component-observability/05-CONTEXT.md
@.planning/phases/05-content-service-cross-component-observability/05-PATTERNS.md
@CLAUDE.md
@.planning/ROADMAP.md
@test/e2e/phase4_invariants_test.go
@test/e2e/phase4_helpers_test.go
@test/e2e/e2e_suite_test.go
@test/e2e/README.md
@Makefile

<interfaces>
<!-- Pattern to mirror — phase4_invariants_test.go shape -->
From test/e2e/phase4_invariants_test.go:
- `//go:build e2e` tag
- TestPhase4Invariants umbrella with 6 SC-subtests via `t.Run("SC1_...", testPhase4SC1...)`
- phase4SuiteGuard at the top of TestPhase4Invariants — Skipf cleanly when ACH_E2E_PHASE4=1 env var unset OR Forwarder Deployment not Ready
- Per-SC functions take *testing.T, build HTTP requests via stdlib http.Client, parse JSON via encoding/json
- No Ginkgo, no Gomega, no testify
- Helpers in phase4_helpers_test.go: phase4SuiteGuard, mustAcquirePk, mustAcquireEk
- ACH_FORWARDER_URL / ACH_CONTENT_SERVICE_URL / ACH_PLATFORM_API_URL env vars provide port-forwarded URLs

<!-- Phase 5 ROADMAP SCs (lines 250-260) -->
SC#1: curl pk_ → 200 + Content-Type application/gzip + exact Content-Length + Cache-Control no-store + identity transfer + sendfile(2) (verified with strace); Range/INM/IMS ignored
SC#2: error matrix — pk_ missing env → 400, team-no-intersect → 403, ek_ wrong env → 403, unauthorized_content → 403, content_not_found → 404, litellm_unreachable → 503
SC#3: §12.3 plugin precedence — Plugin CRD wins over marketplace; alphabetically-lowest marketplace fallback; deletion-drain still serves
SC#4: 503 stale_cache_expired when now-lsr > max_staleness; in-flight CS read survives Operator atomic rename mid-request
SC#5: GET /metrics on Platform API, Forwarder, Content Service, Operator returns the §18.5 metric set including forwarder_requests_total, forwarder_jwt_signed_total, content_service_requests_total, content_service_bytes_served_total, litellm_unreachable_total{caller}

<!-- Existing Makefile target (broken in Phase 5 co-located Pod topology) -->
.PHONY: wait-content-service
wait-content-service: ## Wait for content-service Deployment Available
	kubectl rollout status deploy/ach-content-service -n ach-system --timeout=$(WAIT_TIMEOUT)
# ^^ `deploy/ach-content-service` does NOT exist — co-located in operator Pod since Plan 01-08.
# Phase 5 Plans 05-06/05-07 don't change this — they reaffirm the co-location.
# The target needs to poll the operator Deployment instead, AND verify the cs-http container port is open.
</interfaces>

<drift_callouts>
**Drift flag #1, #2, #3, #4, #5 — final E2E gates lock them all:**
- #1 + #4 (single-controller-per-kind + Pod-with-two-metric-ports): SC#5 confirms operator Pod's :8080 AND CS :8082 both serve /metrics with their respective collectors
- #2 (cheaper-first divergence): SC#2 verifies "name not in spec.context AND not in CRD" returns 403 unauthorized_content (NOT 404 content_not_found)
- #3 (Cache-Control no-store): SC#1 asserts header value verbatim
- #5 (marketplace_plugins column name): SC#3 marketplace fallback test only passes if the §12.3 CTE compiles + matches — proves the CTE column reference is correct

**Co-located Pod E2E ramification:** The Phase 5 SC#1 strace test must `kubectl exec` into the `content-service` CONTAINER (NOT the `manager` container) of the operator Pod: `kubectl -n ach-system exec deploy/ach -c content-service -- strace ...`. The strace target PID is the content-service process (PID 1 in the container). The default container annotation on the Pod (set in operator-deployment.yaml line 44: `kubectl.kubernetes.io/default-container: manager`) means `-c content-service` MUST be explicit, or kubectl exec defaults to the wrong container.
</drift_callouts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Makefile wait-content-service target for co-located Pod topology</name>
  <files>Makefile</files>
  <read_first>
    - Makefile (lines 480-540 — the wait-* targets block)
    - deploy/helm/ach/templates/operator-deployment.yaml (confirm operator Deployment name `ach-operator` + the cs-http container port :8082 is part of the same Deployment)
    - deploy/helm/ach/templates/content-service-deployment.yaml (confirm there's no Deployment for content-service, only a Service)
    - CLAUDE.md (Waiting for state section — blessed wait patterns)
  </read_first>
  <behavior>
    The current `wait-content-service` target polls `deploy/ach-content-service` which does not exist (Plan 01-08 co-located both containers in the operator Pod under Deployment `ach-operator`). The Phase 5 E2E suite needs a working target. Fix: rewrite to poll the operator Deployment rollout AND verify the CS container's :8082 health endpoint by curling through `kubectl exec` or via the `ach-content-service` Service.

    New target body (uses one of the 4 blessed patterns from CLAUDE.md "Waiting for state"):

    1. `kubectl rollout status deploy/ach-operator -n ach-system --timeout=$(WAIT_TIMEOUT)` — waits for the Pod's both containers to be Ready (Deployment-level rollout encompasses all containers in the Pod spec).
    2. Then exec a short curl against the content-service container's healthz: `kubectl -n ach-system exec deploy/ach-operator -c content-service -- wget -qO- http://localhost:8082/healthz` — confirms CS container is serving.

    Both steps run sequentially; second is bounded by timeout via `kubectl exec ... --request-timeout=30s`.

    The target's `## Wait for content-service ...` help text should be updated to clarify: "Wait for content-service container (co-located in operator Pod) Ready".
  </behavior>
  <action>
    Edit `Makefile`. Find the `wait-content-service:` target (around line 522). Replace its single-line body with:

    ```make
    .PHONY: wait-content-service
    wait-content-service: ## Wait for content-service container (co-located in operator Pod) Ready (bounded).
    	# Co-located topology: content-service is the second container in the
    	# ach-operator Pod (RWO PVC forces co-location; see Plan 01-08, Plan 05-07).
    	# There is NO ach-content-service Deployment — the rollout status of the
    	# operator Deployment ensures both containers are Ready. We then probe
    	# the cs-http container directly to confirm the CS process is serving.
    	kubectl rollout status deploy/ach-operator -n ach-system --timeout=$(WAIT_TIMEOUT)
    	kubectl -n ach-system exec deploy/ach-operator -c content-service \
    	    --request-timeout=30s -- \
    	    wget -qO- http://localhost:8082/healthz >/dev/null
    ```

    If the operator Pod's Deployment name is NOT `ach-operator` (verify by `helm template test deploy/helm/ach/ | grep -A 2 '^kind: Deployment$' | grep 'name:'`) — substitute the actual Deployment name. The current pattern of plan 01-08 plus operator-deployment.yaml uses `ach-operator` per the standard chart convention.

    If `wget` is not available in the distroless content-service container image (likely true — `gcr.io/distroless/static:nonroot` has no shell or wget), substitute with a `kubectl run --rm` ephemeral test pod OR use a `kubectl debug` ephemeral container, OR drop the second-step probe entirely and rely solely on the rollout status. The rollout status alone is sufficient — readinessProbe in the Pod template at line 137 of operator-deployment.yaml already verifies CS HTTP via `httpGet: /healthz :8082`, so a Ready Deployment IS a healthy CS. Prefer the rollout-only single-step body:

    ```make
    .PHONY: wait-content-service
    wait-content-service: ## Wait for content-service container (co-located in operator Pod) Ready (bounded).
    	# Co-located topology: content-service is the second container in
    	# the ach-operator Pod (RWO PVC; see Plan 01-08 + 05-07).
    	# Operator Deployment rollout encompasses both containers and the
    	# Pod readinessProbe already verifies CS :8082/healthz, so
    	# rollout=Ready ⇒ both containers serving.
    	kubectl rollout status deploy/ach-operator -n ach-system --timeout=$(WAIT_TIMEOUT)
    ```

    Pick the single-step variant unless engineer-time has surfaced flake on the rollout-only path.
  </action>
  <verify>
    <automated>grep -A 8 '^wait-content-service:' Makefile | grep -c 'ach-operator' | head -1 | grep -q '^1$'</automated>
  </verify>
  <done>
    - `wait-content-service` target body references `ach-operator` (not `ach-content-service`).
    - `grep -c 'kubectl rollout status deploy/ach-operator' Makefile` returns ≥ 1.
    - The target's help-line text mentions "co-located".
    - `make wait-content-service` on a running cluster exits 0 (verify on the next e2e-keep iteration; not part of this plan's pre-merge gate).
  </done>
</task>

<task type="auto">
  <name>Task 2: phase5 helpers + suite guard + fixture loading</name>
  <files>test/e2e/phase5_helpers_test.go, test/e2e/phase5_fixtures</files>
  <read_first>
    - test/e2e/phase4_helpers_test.go (full file — phase4SuiteGuard pattern + mustAcquirePk/mustAcquireEk helpers + env-var gating discipline)
    - test/e2e/e2e_suite_test.go (TestMain + cluster-bootstrap discipline + ACH_E2E_* env-var conventions)
    - test/e2e/phase3_helpers_test.go (DB seeding pattern + JSON fixture loader if any)
    - test/e2e/phase3_fixtures (existing fixture YAML/SQL examples)
    - examples/04-environment-demo.yaml (Environment CR fixture for SC#2-SC#4 — projection rows derive from this)
    - .planning/phases/05-content-service-cross-component-observability/05-CONTEXT.md (D-20 E2E plan + Specifics block sendfile gate)
    - CLAUDE.md (E2E debug loop section + Waiting for state section + banned-naked-polling rules)
  </read_first>
  <behavior>
    **test/e2e/phase5_helpers_test.go**:
    - `//go:build e2e` tag.
    - SPDX header.
    - Package `e2e`.
    - `func phase5SuiteGuard(t *testing.T)` mirrors phase4SuiteGuard:
      - Skipf if `os.Getenv("ACH_E2E_PHASE5") != "1"`.
      - Skipf if any of `ACH_CONTENT_SERVICE_URL`, `ACH_PLATFORM_API_URL`, `ACH_FORWARDER_URL`, `ACH_OPERATOR_METRICS_URL` is empty (with a clear "see CLAUDE.md E2E debug loop" message).
      - Skipf if `kubectl -n ach-system get deploy ach-operator -o json | jq '.status.readyReplicas'` is not ≥1.
    - `mustAcquirePkPhase5(t)`, `mustAcquireEkPhase5(t)`: same shape as Phase 4 helpers, returning `pk_<hex>` / `ek_<hex>`. Acquire via the existing Phase 3 platform-api endpoints (`POST /platform/env-keys` etc.) — reuse Phase 4 helpers if their signatures are still applicable.
    - `seedPhase5Fixtures(t, ctx)`:
      - Apply `test/e2e/phase5_fixtures/environment.yaml` (Environment CR with specific authorizedTeams + context.plugins/prompts/artifacts).
      - Apply `test/e2e/phase5_fixtures/plugin-foo.yaml` (Plugin CR with externalRef pointing to an HTTPS fixture URL).
      - Apply `test/e2e/phase5_fixtures/prompt-bar.yaml`, `test/e2e/phase5_fixtures/artifact-baz.yaml`.
      - Wait for each via `make wait-cr-ready KIND=... NAME=... NS=...` (CLAUDE.md blessed pattern — invoke via `exec.CommandContext(ctx, "make", "wait-cr-ready", "KIND=plugin", "NAME=foo", "NS=ach-system")`).
      - On success, return the (pk_, ek_, environment-name) tuple for downstream subtests.
    - `straceCSSendfile(t, ctx, contentPath string) bool`:
      - `kubectl -n ach-system exec deploy/ach-operator -c content-service --request-timeout=30s -- strace -p 1 -e trace=sendfile,sendfile64 -f -e signal=none -o /tmp/strace.out &` (background).
      - Sleep 200ms (allow strace to attach).
      - `curl -sS -H "x-ach-key: <pk>" -H "x-ach-environment: prod" $ACH_CONTENT_SERVICE_URL/content/<kind>/<name> > /dev/null` (foreground).
      - Wait 1s.
      - `kubectl -n ach-system exec deploy/ach-operator -c content-service -- cat /tmp/strace.out > <local-tmp>`.
      - grep -c "sendfile\|sendfile64" against the captured output; return true if ≥ 1.

      ALTERNATIVE if distroless has no strace: deploy a debug Pod alongside via `kubectl debug -n ach-system deploy/ach-operator --image=nicolaka/netshoot --target=content-service -- strace ...`. Documents which path was chosen in the SUMMARY.

    - `getMetricsBody(t, url string) string`: stdlib http.Get returning the body as a string, t.Fatal on non-200.

    **test/e2e/phase5_fixtures/** (NEW directory with 4 YAMLs):
    - `environment.yaml`: Environment CR named `prod` in ach-system, `authorizedTeams: ["team-a"]`, `context: { plugins: ["foo"], prompts: ["bar"], artifacts: ["baz"] }`.
    - `plugin-foo.yaml`: Plugin CR named `foo`, externalRef.HTTPS.url to a publicly-available tarball (e.g., the existing `examples/06-plugin-caveman.yaml`-style fixture, but inlined for E2E reproducibility).
    - `prompt-bar.yaml`: Prompt CR named `bar` with specific contentType.
    - `artifact-baz.yaml`: Artifact CR named `baz` with scope=object (separate test asserts scope=directory).

    Existing examples in `examples/` (04-environment-demo, 06-plugin-caveman, 07-prompt-claudecode-leak, 08-artifact-openclaw-templates) may be reused as templates — copy the relevant fields, ensure the `metadata.name` matches the phase5 test fixture names.

    Reuse Phase 4 helpers where applicable — do NOT duplicate `mustAcquirePk` if it already lives in phase4_helpers_test.go. Verify by reading that file; alias the function if naming-collision-free.
  </behavior>
  <action>
    1. **Read** test/e2e/phase4_helpers_test.go end-to-end to inventory helpers that can be reused (`mustAcquirePk`, `mustAcquireEk`, env-var gates).

    2. **Create test/e2e/phase5_fixtures/** directory + 4 YAML files. Each starts with `# SPDX-License-Identifier: Apache-2.0` (YAML-comment header — non-blocking but consistent with the project's SPDX discipline). Each file is a minimal CR that lands in the projection tables via Plan 05-04's reconcilers.

    3. **Create test/e2e/phase5_helpers_test.go**. SPDX header. `//go:build e2e`. Package `e2e`. Implement:
       - `phase5SuiteGuard(t)` per behavior.
       - `seedPhase5Fixtures(t, ctx) (pk, ek, env string)` per behavior.
       - `straceCSSendfile(t, ctx, contentPath) bool` per behavior. Pick the strace-via-kubectl-debug path if the distroless image lacks strace (default assumption).
       - `getMetricsBody(t, url) string`.
       - Any kubectl-exec wrapper helpers needed (`kubectlExec(ctx, namespace, deployment, container string, cmd ...string) (stdout, stderr string, err error)`).

    4. Reuse Phase 4's `mustAcquirePk`/`mustAcquireEk` directly (same Phase 3 platform-api endpoint contract). If renaming is needed (e.g., the existing one requires Phase-4-specific env vars), wrap rather than re-implement.

    5. Use exec.CommandContext for all kubectl/curl/strace orchestration — never bare `exec.Command` (CLAUDE.md banned-naked-polling: bound everything by ctx timeouts).
  </action>
  <verify>
    <automated>./scripts/dev.sh go build -tags=e2e ./test/e2e/... &amp;&amp; ls test/e2e/phase5_fixtures/*.yaml | wc -l | grep -q '^4$'</automated>
  </verify>
  <done>
    - `test/e2e/phase5_helpers_test.go` compiles under `-tags=e2e`.
    - `test/e2e/phase5_fixtures/` exists with 4 YAML files (environment, plugin-foo, prompt-bar, artifact-baz).
    - `grep -c '^func phase5SuiteGuard\|^func seedPhase5Fixtures\|^func straceCSSendfile\|^func getMetricsBody' test/e2e/phase5_helpers_test.go` returns 4.
    - No re-implementation of `mustAcquirePk`/`mustAcquireEk` (`grep -c '^func mustAcquirePk\|^func mustAcquireEk' test/e2e/phase5_helpers_test.go` returns 0).
    - SPDX header on the new Go file + comment-header on each YAML fixture.
  </done>
</task>

<task type="auto">
  <name>Task 3: TestPhase5Invariants umbrella — SC#1 (sendfile + headers) + SC#2 (error matrix)</name>
  <files>test/e2e/phase5_invariants_test.go</files>
  <read_first>
    - test/e2e/phase4_invariants_test.go (umbrella shape — TestPhase4Invariants + t.Run per SC)
    - test/e2e/phase5_helpers_test.go (Task 2)
    - .planning/ROADMAP.md (Phase 5 SC#1 + SC#2 prose — match verifier output verbatim)
    - .planning/phases/05-content-service-cross-component-observability/05-CONTEXT.md (D-03 outcome table)
  </read_first>
  <behavior>
    `TestPhase5Invariants(t)` umbrella that calls `phase5SuiteGuard(t)` then `t.Run("SC1_..." , ...)` ... `t.Run("SC5_...", ...)`. SC#1 + SC#2 land in this task; SC#3 + SC#4 + SC#5 in Task 4.

    **testPhase5SC1ContentSendfile(t)**:
    1. `seedPhase5Fixtures(t, ctx)` → pk, ek, env.
    2. Open the strace+curl race via `straceCSSendfile(t, ctx, "/content/plugin/foo")`. Assert true.
    3. Direct curl `/content/plugin/foo` with pk + env. Assert:
       - status 200
       - `Content-Type: application/gzip`
       - `Content-Length:` non-empty AND matches `len(body)`
       - `Cache-Control: no-store`
       - `Transfer-Encoding` absent (identity transfer per CS-06)
       - body length > 0
    4. Range header test: same curl with `Range: bytes=0-99`. Assert:
       - status 200 (NOT 206 — D-01 says CS ignores Range)
       - body length == full body length
       - no `Content-Range` header in response
    5. If-None-Match test: same curl with `If-None-Match: "*"`. Assert status 200 (not 304).
    6. If-Modified-Since test: same curl with `If-Modified-Since: <future-date>`. Assert 200.

    **testPhase5SC2ErrorMatrix(t)**:
    Each subtest is a `t.Run` inside testPhase5SC2ErrorMatrix:
    - `MissingEnvironment`: curl `/content/plugin/foo` with pk but NO `x-ach-environment` header. Assert: status 400, body parses to `{error: {code:"missing_environment", message: <str>}, request_id: "req_..."}`.
    - `InvalidKeyFormat_NoPrefix`: curl with `x-ach-key: garbage`. Assert: status 400, code=invalid_key_format.
    - `InvalidKeyFormat_Empty`: curl with empty x-ach-key header. Assert: 400, code=invalid_key_format.
    - `ExpiredOrRevoked`: curl with pk_DEADBEEF (clearly non-existent). Assert: 401, code=expired_or_revoked.
    - `UnauthorizedTeam`: revoke the test pk's owner from all teams in LiteLLM (via litellm mock or admin pk), then curl. Assert: 403, code=unauthorized_team. (Skip if LiteLLM mock teardown infra not available; document in SUMMARY.)
    - `WrongEnvironment`: curl with ek (bound to env=prod) AND `x-ach-environment: staging`. Assert: 403, code=wrong_environment.
    - `UnauthorizedContent`: curl `/content/plugin/forbidden-name` (a name NOT in env.context.plugins). Assert: 403, code=unauthorized_content. **Drift flag #2 lock-in: this MUST be 403, not 404, even though `forbidden-name` doesn't exist as a Plugin CRD.**
    - `EnvironmentNotFound`: curl with `x-ach-environment: nonexistent-env`. Assert: 404, code=environment_not_found.
    - `ContentNotFound`: curl `/content/plugin/<name-in-env.context-but-no-CRD>` — pre-seed env.context.plugins with a name that has no backing Plugin CRD. Assert: 404, code=content_not_found.

    Each subtest parses the response body via `json.Unmarshal` into an `errEnvelope` struct (helper local to this file) and asserts the `code` field exactly. Verify `request_id` matches the pattern `^req_[A-Z0-9]{26}$` (ULID).
  </behavior>
  <action>
    Create `test/e2e/phase5_invariants_test.go`. SPDX header. `//go:build e2e`. Package `e2e`.

    1. Type declarations at top:
       ```
       type errEnvelope struct {
           Error struct { Code, Message string } `json:"error"`
           RequestID string `json:"request_id"`
       }
       ```

    2. `TestPhase5Invariants(t)` umbrella:
       - Calls `phase5SuiteGuard(t)`.
       - `t.Run("SC1_ContentSendfile", testPhase5SC1ContentSendfile)` etc.

    3. Implement `testPhase5SC1ContentSendfile` per behavior. Use `http.NewRequestWithContext` + `http.DefaultClient.Do`. Read body via `io.ReadAll`. Assert headers via `resp.Header.Get(...)`. For the strace step, call `straceCSSendfile` from helpers and `t.Fatal` if false.

    4. Implement `testPhase5SC2ErrorMatrix` per behavior — 9 subtests via nested `t.Run`. Each builds a request, sends it, parses the envelope, asserts code + status.

    5. Use stdlib testing only — no Ginkgo. Phase 4's pattern uses `if resp.StatusCode != http.StatusXxx { t.Fatalf(...) }`.

    6. For the UnauthorizedTeam subtest: if removing the user from teams in live LiteLLM is infeasible in the E2E harness, document the skip via `t.Skipf("LiteLLM team-removal not scriptable in E2E harness — covered by integration test in Plan 05-05")`. This is acceptable — the team-intersect path is fully verified at the integration-test layer.
  </action>
  <verify>
    <automated>./scripts/dev.sh go build -tags=e2e ./test/e2e/... &amp;&amp; ./scripts/dev.sh go test -tags=e2e -run 'TestPhase5Invariants/SC1' -count=1 -timeout 10m ./test/e2e/... 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    - File compiles under `-tags=e2e`.
    - `grep -c '^func TestPhase5Invariants\|^func testPhase5SC1\|^func testPhase5SC2' test/e2e/phase5_invariants_test.go` returns 3.
    - SC#1 + SC#2 subtests defined; SC#3-#5 will be added in Task 4 (incremental file builds).
    - All header assertions in SC#1 cite the exact values per CS-06 (Content-Type / Content-Length / Cache-Control / no Transfer-Encoding).
    - Drift flag #2 lock-in test (UnauthorizedContent expects 403 not 404) is present and explicitly comment-documented.
    - errEnvelope JSON shape parsing works (verify with offline test fixture if cluster unavailable).
  </done>
</task>

<task type="auto">
  <name>Task 4: SC#3 §12.3 plugin precedence + SC#4 staleness + in-flight rename + SC#5 /metrics across services</name>
  <files>test/e2e/phase5_invariants_test.go</files>
  <read_first>
    - test/e2e/phase5_invariants_test.go (Task 3 — append SC#3-#5 subtests)
    - test/e2e/phase5_helpers_test.go (Task 2)
    - .planning/ROADMAP.md (Phase 5 SC#3-#5 prose)
    - .planning/phases/05-content-service-cross-component-observability/05-CONTEXT.md (D-04 step 7 staleness + D-02 in-flight rename + D-09/D-10 /metrics topology)
    - deploy/helm/ach/templates/operator-deployment.yaml (operator Pod has 2 metrics ports — verify scrape from each)
  </read_first>
  <behavior>
    Append three more sub-tests to `TestPhase5Invariants`:

    **testPhase5SC3PluginPrecedence(t)**:
    - `t.Run("CRDWinsOverMarketplace", ...)`: Pre-seed a Plugin CRD named `pluginprio` AND a marketplace_plugins row with same `name=pluginprio` but different `storage_location`. Curl `/content/plugin/pluginprio` with pk + env (where env.context.plugins includes `pluginprio`). Assert 200 + body bytes match the CRD-storage file (not the marketplace file). This requires inspecting the served bytes against fixture content.
    - `t.Run("AlphabeticallyLowestMarketplace", ...)`: Pre-seed THREE marketplace_plugins rows with same `name=mktshared` and `marketplace_name ∈ {"zzz-mkt", "anthropic-mkt", "internal-mkt"}` (insert in that order to defeat any "first-inserted-wins" bugs). Curl `/content/plugin/mktshared`. Assert 200 + the storage_location used is anthropic-mkt's (verify by serving distinct fixture bytes per marketplace).
    - `t.Run("DeletionDrainStillServes", ...)`: Apply Plugin CRD `drainable`, wait for projection row, then `kubectl delete plugin drainable` and IMMEDIATELY curl `/content/plugin/drainable` (envcache hit + projection row soft-deleted via Plan 05-04's SoftDeletePlugin). Assert 200 (still served) for the duration before finalizer drain completes. Or 503 stale_cache_expired if max_staleness ages out first — either is acceptable per CS-09.

    **testPhase5SC4StalenessAndInFlightRename(t)**:
    - `t.Run("StaleCacheExpired", ...)`: Patch the `plugins` projection row directly via psql exec (or via the Plugin CR's annotations to trigger Operator to update lsr in a controlled way; if both are infeasible in E2E, use the integration test from Plan 05-05 and skip here). Set `last_successful_refresh` to 24h-ago AND `max_staleness_seconds` to 300. Curl `/content/plugin/foo`. Assert 503, code=stale_cache_expired.
    - `t.Run("InFlightReadSurvivesRename", ...)`: Start a streaming GET that reads slowly (use `--limit-rate` on curl or sleep between chunks via a custom Go http.Response.Body wrapper). Mid-stream, `kubectl exec deploy/ach-operator -c manager -- rename /var/cache/ach/plugin/foo /var/cache/ach/plugin/foo.old && cp /var/cache/ach/plugin/foo.old /var/cache/ach/plugin/foo` — atomically swaps the inode under the CS container's open FD. Assert the slowly-streaming GET completes successfully with the ORIGINAL byte content (the inode pinned by the open FD per D-02). If this is infeasible in the kind+Helm E2E (writing to the operator's PVC mid-stream requires the operator container to support the operation), document the skip and rely on the integration test from Plan 05-05 Task 4.

    **testPhase5SC5MetricsTopology(t)**:
    - `t.Run("ForwarderMetrics", ...)`: `getMetricsBody(t, ACH_FORWARDER_URL + "/metrics")`. Grep for normative metric names: `forwarder_requests_total`, `forwarder_jwt_signed_total`, `forwarder_jwt_suppressed_total`, `forwarder_request_duration_seconds`. Assert each appears ≥ 1 time in the body. Also assert `litellm_unreachable_total{caller="forwarder"}` line is present (after Plan 04's runtime has generated at least one LiteLLM call; if not, the metric may have zero samples but should still appear in the metric family list — `prometheus.io/scrape` outputs the help line even at zero count).
    - `t.Run("ContentServiceMetrics", ...)`: `getMetricsBody(t, ACH_CONTENT_SERVICE_URL + "/metrics")`. Grep for `content_service_requests_total`, `content_service_bytes_served_total`, `content_service_request_duration_seconds`. Each ≥ 1 line. Plus `litellm_unreachable_total{caller="content_service"}`.
    - `t.Run("PlatformAPIMetrics", ...)`: `getMetricsBody(t, ACH_PLATFORM_API_URL + "/metrics")`. Plat API has no §18.5 expansion but the /metrics endpoint MUST return at least the Go runtime baseline metrics (`go_goroutines`, etc.) AND `litellm_unreachable_total{caller="platform_api"}` if Plan 05-06 Task 4 wired it; if Task 4 deferred the call sites, document that the metric family may have zero samples but the family registration is still observable.
    - `t.Run("OperatorMetrics", ...)`: `getMetricsBody(t, ACH_OPERATOR_METRICS_URL + "/metrics")`. Operator's controller-runtime metricsserver returns the standard `controller_runtime_reconcile_*` family + `workqueue_*` + `litellm_unreachable_total{caller="operator"}` (registered by Plan 05-06 Task 5). Assert at least 3 of the controller-runtime families appear AND the litellm_unreachable shared collector is registered.
    - `t.Run("NoForbiddenLabels_ContentService", ...)`: Parse CS metrics body, assert NEITHER `request_id` NOR `owner_email` appears as a label key in any CS metric family. Cardinality discipline lock-in (OBS-06).

    Note: ACH_OPERATOR_METRICS_URL points at the controller-runtime metricsserver — typically `:8443` HTTPS (with TLS auth) or `:8080` HTTP if metrics-secure=false. The E2E harness must set this env var according to the actual operator config; document the assumption in phase5SuiteGuard.
  </behavior>
  <action>
    Append to `test/e2e/phase5_invariants_test.go` (created in Task 3) — three new functions: `testPhase5SC3PluginPrecedence`, `testPhase5SC4StalenessAndInFlightRename`, `testPhase5SC5MetricsTopology`. Register each in `TestPhase5Invariants` via `t.Run`.

    1. SC#3: Build fixtures with deterministic byte content (e.g., a 4-byte unique magic per Plugin/marketplace row's tarball). Curl, read body, byte-compare against expected magic. The byte content is preseeded by the Operator's §10.3 cache-refresh loop after `kubectl apply` of the fixture; ensure the fixture URLs serve the expected bytes.

    2. SC#4 staleness path: Patch the row via the existing test helper `psqlExec(t, ctx, query string)` from Phase 3's e2e harness (verify it exists; if not, add a thin wrapper in phase5_helpers_test.go that uses `kubectl exec deploy/ach-postgres -- psql ...`).

    3. SC#4 in-flight rename: This is the highest-effort subtest. If implementing it requires more than 30% context budget, document the deferral in this subtest's body with `t.Skipf("in-flight-rename verified at integration-test layer in Plan 05-05 Task 4; E2E coverage deferred to Phase 5b")` and proceed. SC#4's staleness sub-assert is the load-bearing assertion; in-flight-rename is the supplementary D-02 lock-in.

    4. SC#5: For each /metrics endpoint, use `getMetricsBody`. Grep with `strings.Contains` for the §18.5 metric names. Assert specific counts where the metric must be NON-ZERO after Phase 4's normal traffic (e.g., `forwarder_requests_total` will be ≥ 1 after Phase 4 fixtures' UAT runs).

    5. Forbidden-labels assertion in SC#5: parse the metric text with `expfmt.TextParser` from `github.com/prometheus/common/expfmt` — already pulled via prom client_golang dep. Iterate metric families, for each `content_service_*` family inspect `metric[].label[].name` and assert it's NOT in `{"request_id", "owner_email"}`.

    Build incrementally: after each new subtest function, run `./scripts/dev.sh go build -tags=e2e ./test/e2e/...` to confirm no compile breakage.
  </action>
  <verify>
    <automated>./scripts/dev.sh go build -tags=e2e ./test/e2e/... &amp;&amp; grep -c '^func testPhase5SC3\|^func testPhase5SC4\|^func testPhase5SC5' test/e2e/phase5_invariants_test.go | grep -q '^3$'</automated>
  </verify>
  <done>
    - File compiles under `-tags=e2e`.
    - `grep -c '^func testPhase5SC' test/e2e/phase5_invariants_test.go` returns 5 (SC1..SC5).
    - SC#5 NoForbiddenLabels_ContentService subtest exists and references expfmt or equivalent label-key extraction.
    - SC#3 sub-tests for CRDWinsOverMarketplace + AlphabeticallyLowestMarketplace are explicit `t.Run` blocks.
    - SC#4 staleness subtest is implemented; in-flight-rename subtest is either implemented or has an explicit `t.Skipf` referencing Plan 05-05's integration coverage.
    - All `t.Run` subtests cite the relevant CS-XX requirement in a code comment (e.g., `// CS-08 lock-in`).
    - On a live cluster: `./scripts/dev.sh make e2e-focus RUN='TestPhase5Invariants'` exits 0 (engineer-time verification — not gated by this plan's CI green).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E test → live kind cluster | Trusted infrastructure (controlled by test runner) |
| strace -p 1 → CS process | Same-Pod container access via kubectl exec — bounded by RBAC of the test runner's kubeconfig |
| psql exec → projection row | Test-only DB mutation; same namespace as the rest of E2E |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08-01 | Tampering | strace + curl race | mitigate | strace attaches BEFORE curl runs (200ms sleep). If the race fails (curl completes before strace attaches), the test FAILS (sendfile syscall not seen) — explicit failure mode, no false-pass. Plan 05-05's integration test provides a non-racy alternative via direct file I/O assertion. |
| T-05-08-02 | Information Disclosure | strace output captures syscall args | accept | strace `-e trace=sendfile,sendfile64` only captures sendfile invocations; fd numbers + byte counts are non-sensitive. Output redirected to in-container `/tmp/strace.out` then captured locally; ephemeral. |
| T-05-08-03 | Denial of Service | E2E test on hot path | accept | Phase 5 E2E runs only in `make e2e-focus RUN='TestPhase5Invariants'` (engineer-driven) or in the full `make e2e-full` clean-room run. Not a CI hot path; not exposed to traffic. |
| T-05-08-04 | Tampering | mid-stream rename test corrupts test cluster state | accept | The rename test operates on a dedicated fixture (`/var/cache/ach/plugin/foo`) seeded specifically for the test. Cleanup is via `kubectl delete plugin foo` at test teardown. State is recreated on next test run. |
| T-05-08-05 | Repudiation | flaky sendfile assertion | mitigate | The test uses `>= 1` (any non-zero sendfile count) — extreme tolerance for io.Copy implementation drift. Plan 05-05 integration test provides higher-resolution coverage (assertion via direct *os.File.WriteTo→*net.TCPConn type assertion). |
| T-05-08-06 | Information Disclosure | /metrics body content disclosed in test logs | accept | Test logs are local to the test runner; metric values are non-sensitive (counts, durations, no PII per OBS-06 cardinality discipline). |
| T-05-08-SC | Supply Chain | `github.com/prometheus/common/expfmt` direct dep | accept | Already pulled transitively via `prometheus/client_golang` (Plan 05-01 promoted to direct). expfmt is a sibling sub-module; importing it does not add new transitive surface. |
</threat_model>

<verification>
- `./scripts/dev.sh go build -tags=e2e ./test/e2e/...` — compiles clean
- `./scripts/dev.sh go vet -tags=e2e ./test/e2e/...` — vet clean
- `./scripts/dev.sh make lint` — golangci-lint passes (includes e2e files under -tags=e2e)
- On a running kind+Helm cluster with `ACH_E2E_PHASE5=1` + port-forwarded URLs: `./scripts/dev.sh make e2e-focus RUN='TestPhase5Invariants'` exits 0
- `make wait-content-service` exits 0 on a running cluster (no more `deploy/ach-content-service` not-found error)
- ROADMAP Phase 5 SC#1..#5 each mapped 1:1 to a `testPhase5SCN` function
- SUMMARY documents which SC#4 sub-assertions actually run end-to-end vs. delegated to integration tests
</verification>

<success_criteria>
- ROADMAP Phase 5 SC#1..#5 covered by 5 named subtest functions in `test/e2e/phase5_invariants_test.go`.
- sendfile syscall verified live via strace under kubectl exec.
- /metrics scraped on all 4 services with §18.5 normative metric-name assertions.
- Drift flag #2 (cheaper-first 403-not-404) explicitly E2E-tested via UnauthorizedContent subtest.
- Drift flag #4 (co-located Pod two metric ports) explicitly E2E-tested via separate ForwarderMetrics + ContentServiceMetrics + OperatorMetrics + PlatformAPIMetrics subtests reaching different ports.
- Cardinality discipline (OBS-06 no request_id / owner_email labels) lock-in via NoForbiddenLabels subtest.
- Makefile wait-content-service no longer references a non-existent Deployment.
- Drift flag #5 (marketplace_plugins column name) E2E-tested indirectly via SC#3 marketplace-fallback subtest (CTE must compile against the real schema).
</success_criteria>

<output>
Create `.planning/phases/05-content-service-cross-component-observability/05-08-SUMMARY.md` when done. Document:
- The 5 subtest function names + their ROADMAP SC mapping.
- The strace orchestration approach (kubectl exec vs. kubectl debug ephemeral container).
- Which SC#4 subtests run E2E vs. are delegated to integration (paste skip messages if any).
- The Makefile target body diff (before/after for `wait-content-service`).
- A test run output excerpt (first 30 lines of `make e2e-focus RUN='TestPhase5Invariants'` on a green cluster) — if cluster unavailable at run-time, paste `go test -tags=e2e -list 'TestPhase5'` output as the structural verification.
- Confirmation that all 5 drift flags from PATTERNS.md are locked in by at least one subtest.
</output>
</content>
</invoke>
