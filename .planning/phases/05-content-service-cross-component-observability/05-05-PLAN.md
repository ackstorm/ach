---
phase: 05-content-service-cross-component-observability
plan: 05
type: execute
wave: 2
depends_on: [05-01, 05-02, 05-03]
files_modified:
  - internal/contentservice/handler.go
  - internal/contentservice/pipeline.go
  - internal/contentservice/authz.go
  - internal/contentservice/stream.go
  - internal/contentservice/errors.go
  - internal/contentservice/paths.go
  - internal/contentservice/paths_test.go
  - internal/contentservice/k8s.go
  - internal/contentservice/content_type_test.go
  - internal/contentservice/handler_test.go
  - internal/contentservice/pipeline_test.go
  - internal/contentservice/stream_test.go
  - internal/contentservice/authz_test.go
  - internal/contentservice/errors_test.go
  - internal/audit/events.go
  - cmd/ach/cmd/content_service.go
autonomous: true
requirements: [CS-01, CS-02, CS-03, CS-04, CS-05, CS-06, CS-07, CS-08, CS-09, CS-10, CS-11]
tags: [contentservice, authz, streaming, sendfile, security, http]

must_haves:
  truths:
    - "GET /content/{prompt|plugin|artifact}/{name} returns 200 with Content-Length exact, Cache-Control: no-store, identity transfer (no chunked, no compression), body streamed via sendfile(2)"
    - "pk_ requests without x-ach-environment return 400 missing_environment"
    - "pk_ requests whose Team membership doesn't intersect Environment.spec.authorizedTeams return 403 unauthorized_team"
    - "ek_ requests with x-ach-environment ≠ bound environment return 403 wrong_environment"
    - "Name not in spec.context.<kindPlural>[] returns 403 unauthorized_content (cheaper-first per D-04, before content resolution)"
    - "Unknown name (not in projection row AND not in marketplace_plugins) returns 404 content_not_found"
    - "Stale row (now - last_successful_refresh > max_staleness OR last_successful_refresh IS NULL) returns 503 stale_cache_expired"
    - "LiteLLM unreachable during Teams resolution returns 503 litellm_unreachable (CS-11, OBS-05 unified body code)"
    - "Range / If-None-Match / If-Modified-Since / If-Match / If-Unmodified-Since headers are silently IGNORED — full body, always 200"
    - "In-flight read survives Operator atomic rename(2) — file opened EARLY in pipeline, *os.File held until io.Copy completes (SC#4)"
    - "Every ACH-originated 4xx/5xx uses §15.5 envelope {error:{code,message},request_id} where code matches D-03 table"
    - "One audit event per request via internal/audit.EmitAudit; outcome string matches response body code"
    - "Plugin precedence via db.ResolvePluginByName (§12.3 CTE from Plan 05-02): CRD wins, else alphabetically-lowest marketplace"
    - "Drift flag #3 resolved: existing handler_test.go assertion for `Cache-Control: public, max-age=300` updated to `no-store`"
    - "Decisions covered: D-01 (custom serve, no http.ServeContent — io.Copy engages sendfile via *os.File.WriteTo), D-02 (open file early in pipeline before staleness gate — *os.File held until response stream completes, pins inode against atomic rename), D-03 (explicit error mapping — D-03 outcome table → §15.5 envelope), D-04 (cheaper-first 7-gate authz pipeline — explicit spec §5.1 divergence documented in pipeline.go doc comment), D-05 (reuse keystore.KeyResolver Phase 3 D-08 verbatim — no new keystore work), D-06 (reuse keystore.TeamsResolver Phase 4 D-17 verbatim — caller=content_service on litellm_unreachable_total), D-16 (rewrite internal/contentservice/handler.go end-to-end with pipeline-style serve()), D-17 (internal/contentservice/ package layout — handler/pipeline/authz/stream/errors/envcache/paths split), D-20 (test plan — unit + integration coverage of 7-gate authz + sendfile + §12.3 precedence)"
  artifacts:
    - path: internal/contentservice/handler.go
      provides: "RegisterRoutes + Deps struct + serve() pipeline orchestrator (REWRITTEN)"
      min_lines: 100
    - path: internal/contentservice/pipeline.go
      provides: "pipeline() function returning (resolvedRow, *errResp) — 7-step gate sequence per D-04"
      min_lines: 150
    - path: internal/contentservice/authz.go
      provides: "resolveAuthn / resolveEnv / enforceTeams / enforceAllowlist / resolveContent / checkStaleness gate functions"
      min_lines: 200
    - path: internal/contentservice/stream.go
      provides: "stream(w, r, file, contentType, size) — custom serve, sendfile-backed, no http.ServeContent"
      min_lines: 30
    - path: internal/contentservice/errors.go
      provides: "errResp type + writeError helper + D-03 outcome table + audit emission"
      min_lines: 100
    - path: internal/contentservice/k8s.go
      provides: "REMOVED — PromptContentTypeLookup obsoleted by prompts.content_type projection column"
      contains: "(file deleted)"
    - path: internal/audit/events.go
      provides: "Additive: ActionContentGet, OutcomeUnauthorizedContent, OutcomeContentNotFound, OutcomeStaleCacheExpired (and OutcomeForwarded if absent)"
      contains: "ActionContentGet"
    - path: cmd/ach/cmd/content_service.go
      provides: "STUB-PATCH ONLY — drop the NewK8sPromptLookup call so the package compiles after k8s.go deletion. Full Deps rewire is owned by Plan 05-06 Task 1."
      contains: "(no NewK8sPromptLookup reference)"
  key_links:
    - from: internal/contentservice/pipeline.go (gate 6)
      to: internal/db/plugins.go (ResolvePluginByName)
      via: "§12.3 precedence resolution"
      pattern: "db\\.ResolvePluginByName"
    - from: internal/contentservice/pipeline.go (gate 8)
      to: internal/contentservice/stream.go (stream)
      via: "open *os.File early, hold to drain"
      pattern: "os\\.Open"
    - from: internal/contentservice/errors.go (writeError)
      to: internal/platformapi/render/json.go (Error)
      via: "§15.5 envelope reuse"
      pattern: "render\\.Error"
    - from: internal/contentservice/pipeline.go (gate 4)
      to: internal/keystore/teamsresolver.go (TeamsResolver.Resolve)
      via: "Phase 4 D-17 reuse with caller=\"content_service\""
      pattern: "TeamsResolver"
---

<objective>
Rewrite the Content Service handler end-to-end per D-16: replace the §8 raw-file streamer with the full §15.6 surface — D-04 7-gate authz pipeline (cheaper-first), D-01 custom sendfile-backed serve (no http.ServeContent), D-03 explicit error mapping with §15.5 envelope, D-02 early-open inode pin for SC#4, audit emission on every request, and `db.ResolvePluginByName` §12.3 precedence on every plugin request.

Purpose: This is the load-bearing CS-01..CS-11 deliverable. Wave 1 plans (05-01 metrics, 05-02 DB helpers, 05-03 envcache) provide the imports; this plan wires them together into the new pipeline. Drift flag #3 (Cache-Control header value change) is resolved by rewriting the existing test alongside the production code.
Output: 7 new/rewritten files in `internal/contentservice/` (incl. k8s.go REMOVAL) + comprehensive test coverage (~50+ unit tests) + a compile-tolerance stub patch to `cmd/ach/cmd/content_service.go` (one-line removal of the `NewK8sPromptLookup` call — full Deps wiring is Plan 05-06 Task 1's responsibility) + additive audit-event constants in `internal/audit/events.go`.
</objective>

<scope_carveout>
**`cmd/ach/cmd/content_service.go` edit scope in THIS plan = compile-tolerance STUB PATCH ONLY:**
- Remove the `contentservice.NewK8sPromptLookup(...)` call (the function no longer exists after k8s.go deletion).
- Remove the now-unused `mgr.GetClient()` call that fed it (only if it was used SOLELY for the lookup; if other code paths consume it, leave them alone).
- Leave a `// TODO(Plan 05-06): full Deps wiring lands here` comment at the removal site.
- DO NOT rewrite the imports block, DO NOT build the new Deps struct, DO NOT mount /metrics. All of that is Plan 05-06 Task 1's exclusive scope.

Rationale: Plan 05-05 deleting `k8s.go` (and thus `NewK8sPromptLookup`) creates a build break that Plan 05-06 cannot start from. This minimal patch keeps the build green between waves 2 and 3. Plan 05-06 Task 1 will read the file again and own the full rewrite end-to-end (including any imports that this patch leaves dangling temporarily — those imports will be removed by Plan 05-06 anyway).
</scope_carveout>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-content-service-cross-component-observability/05-CONTEXT.md
@.planning/phases/05-content-service-cross-component-observability/05-PATTERNS.md
@CLAUDE.md
@internal/contentservice/handler.go
@internal/contentservice/handler_test.go
@internal/contentservice/paths.go
@internal/contentservice/content_type.go
@internal/contentservice/content_type_test.go
@internal/contentservice/k8s.go
@internal/keystore/keystore.go
@internal/keystore/teamsresolver.go
@internal/db/environments.go
@internal/db/plugins.go
@internal/db/prompts.go
@internal/db/artifacts.go
@internal/contentservice/envcache/cache.go
@internal/metrics/contentservice.go
@internal/platformapi/render/json.go
@internal/platformapi/middleware/middleware.go
@internal/audit/emit.go
@internal/audit/events.go
@cmd/ach/cmd/content_service.go

<interfaces>
<!-- Wave 1 outputs we now consume -->
From internal/db (Plan 05-02):
```go
func GetEnvironmentByName(ctx, pool, ns, name) (*EnvironmentRow, error)
func GetPromptByName(ctx, pool, ns, name) (*PromptRow, error)
func GetArtifactByName(ctx, pool, ns, name) (*ArtifactRow, error)
func ResolvePluginByName(ctx, pool, ns, name) (*PluginResolution, error)
```

From internal/contentservice/envcache (Plan 05-03):
```go
type Cache interface { Get(ctx, ns, name) (*EnvRow, error) }
type EnvRow struct {
    Namespace, Name string
    AuthorizedTeams, ContextPrompts, ContextPlugins, ContextArtifacts []string
    DeletionTimestamp *time.Time
    ResourceVersion string
}
```

From internal/metrics (Plan 05-01):
```go
type ContentServiceCollectors struct { /* private */ }
func NewContentServiceCollectors(reg *prometheus.Registry) *ContentServiceCollectors
func (c *ContentServiceCollectors) IncRequest(kind, outcome string)
func (c *ContentServiceCollectors) ObserveRequestDuration(kind string, seconds float64)
func (c *ContentServiceCollectors) AddBytesServed(kind string, n int64)
```

From internal/keystore (Phase 3 + Phase 4, reused verbatim):
```go
type KeyInfo struct {
    KeyID, OwnerEmail, Status string
    KeyType keys.BearerPrefix  // "pk_" or "ek_"
    Environment string  // only for ek_
    // ... other fields
}
type Resolver interface { Resolve(ctx, plaintext string) (*KeyInfo, error) }
type TeamsResolver interface { Resolve(ctx, email string) ([]string, error) }
```

From internal/platformapi/render:
```go
func Error(w http.ResponseWriter, status int, code, message, requestID string)
```

From internal/platformapi/middleware:
```go
func RequestID(next http.Handler) http.Handler  // chi-mountable; T-03-05-06 never preserves caller-supplied X-Request-Id
func RequestIDFromCtx(ctx context.Context) string
```

From internal/audit (Phase 2/3):
```go
type Event struct { Action, Outcome, Actor, RequestID, KeyID string; Target *Target; Extra map[string]string }
type Target struct { Kind, Name string }
func EmitAudit(ctx, logger *slog.Logger, e Event)
// New action constant needed: ActionContentGet = "content.get"
```
</interfaces>

<!-- D-03 outcome → HTTP status → code map (canonical for this plan) -->
| HTTP | code (body + audit) | trigger | gate |
|------|---------------------|---------|------|
| 400  | missing_environment      | pk_ without x-ach-environment | 2 |
| 400  | invalid_key_format       | malformed key prefix / empty header | 1 |
| 401  | expired_or_revoked       | Resolver returns (nil, nil) | 1 |
| 403  | unauthorized_team        | pk_ empty Team intersection | 4 |
| 403  | wrong_environment        | ek_ header ≠ bound env | 2 |
| 403  | unauthorized_content     | name not in env.context.<plural> | 5 |
| 404  | environment_not_found    | env row missing | 3 |
| 404  | content_not_found        | name not in projection AND not in marketplace_plugins | 6 |
| 503  | litellm_unreachable      | TeamsResolver error | 4 |
| 503  | stale_cache_expired      | staleness gate failure (incl. NULL last_successful_refresh) | 7 |
| 500  | internal_error           | any unhandled error / DB / file Open | (any) |
| 405  | (chi default 405)        | non-GET | (router) |

<spec_divergence>
Per D-04 + CONTEXT canonical-refs line 27: this plan's cheaper-first ordering (allowlist check BEFORE content resolution) DIVERGES from spec §15.6 v10 fix step order (content resolution first, then allowlist). The divergence is deliberate and user-confirmed (CONTEXT-LOG.md). pipeline.go's doc comment MUST cite the divergence in plain prose. Side effect: "name not in env.context AND not in any CRD" returns 403 unauthorized_content (allowlist fires first), NOT 404 content_not_found. Test cases lock both orderings — i.e. tests must use names that are in the CRD but not in env.context AND vice versa to prove both 403 and 404 paths.
</spec_divergence>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: stream.go (D-01 custom serve) + errors.go (D-03 mapping + audit emission) + additive audit constants</name>
  <files>internal/contentservice/stream.go, internal/contentservice/errors.go, internal/contentservice/stream_test.go, internal/contentservice/errors_test.go, internal/audit/events.go</files>
  <read_first>
    - internal/contentservice/handler.go (current §8 stub — lines 119-125 ServeContent block being replaced)
    - .planning/phases/05-content-service-cross-component-observability/05-PATTERNS.md (stream.go + errors.go sections lines 433-481)
    - .planning/phases/05-content-service-cross-component-observability/05-CONTEXT.md (D-01, D-02, D-03 outcome table, Specifics block writeFile snippet)
    - internal/platformapi/render/json.go (Error envelope writer — reused verbatim)
    - internal/platformapi/middleware/middleware.go (RequestIDFromCtx + RequestID middleware — line 45-65)
    - internal/audit/emit.go (EmitAudit signature)
    - internal/audit/events.go (Outcome* constants — reuse: OutcomeMissingEnvironment, OutcomeInvalidKeyFormat, OutcomeExpiredOrRevoked, OutcomeUnauthorizedTeam, OutcomeWrongEnvironment, OutcomeEnvironmentNotFound, OutcomeLitellmUnreachable, OutcomeInternalError. New constants to ADD additively: OutcomeUnauthorizedContent, OutcomeContentNotFound, OutcomeStaleCacheExpired)
  </read_first>
  <behavior>
    **stream.go**:
    - Function `stream(w http.ResponseWriter, r *http.Request, f *os.File, contentType string, size int64) (int64, error)`. Sets `Content-Type`, `Content-Length` (exact from `size`), `Cache-Control: no-store`. Writes status 200. Calls `io.Copy(w, f)`. Returns `(bytesWritten, err)`.
    - Does NOT call `http.ServeContent`.
    - Request headers Range/If-None-Match/If-Modified-Since/If-Match/If-Unmodified-Since are NEVER inspected (CS-08).
    - No compression middleware on this mux (decided at wiring site in Plan 05-07).
    - Doc comment cites D-01, D-02 (the open-early discipline; this function does NOT open, the caller does), CS-06, CS-08, SC#4.

    **errors.go**:
    - Type `errResp struct { HTTPStatus int; Code string; Message string }`.
    - Constants for each D-03 code (12 codes). Reuse `internal/audit.Outcome*` constants where they exist; ADD `OutcomeUnauthorizedContent = "unauthorized_content"`, `OutcomeContentNotFound = "content_not_found"`, `OutcomeStaleCacheExpired = "stale_cache_expired"`, `OutcomeInvalidKeyFormat` (verify already exists at audit/events.go:82). To add new outcomes, EDIT `internal/audit/events.go` additively — DO NOT rename existing constants. The new constants live in audit package so any consumer can match outcome strings.
    - Predefined errResp factory functions: `errMissingEnvironment()`, `errInvalidKeyFormat()`, `errExpiredOrRevoked()`, `errUnauthorizedTeam()`, `errWrongEnvironment()`, `errUnauthorizedContent()`, `errEnvironmentNotFound()`, `errContentNotFound()`, `errLitellmUnreachable()`, `errStaleCacheExpired()`, `errInternal()`. Each returns the appropriate `*errResp` with hard-coded message (T-03-02-02 — never echo upstream error text).
    - `func (d *Deps) writeError(w http.ResponseWriter, r *http.Request, kind, name string, info *keystore.KeyInfo, e *errResp)`:
      1. Read `reqID := middleware.RequestIDFromCtx(r.Context())`.
      2. Call `render.Error(w, e.HTTPStatus, e.Code, e.Message, reqID)`.
      3. Emit one audit event via `audit.EmitAudit(ctx, d.AuditLog, audit.Event{Action: audit.ActionContentGet, Outcome: e.Code, RequestID: reqID, Actor: actorFromInfo(info), KeyID: keyIDFromInfo(info), Target: &audit.Target{Kind: kind, Name: name}})`. `actorFromInfo` returns `os.Getenv("POD_NAMESPACE") + "/" + info.OwnerEmail` when info != nil else `os.Getenv("POD_NAMESPACE") + "/-"`. `keyIDFromInfo` returns info.KeyID when info != nil else empty.
      4. Inc metric: `d.Metrics.IncRequest(kind, e.Code)`.
    - New audit action constant `ActionContentGet = "content.get"` — add additively to `internal/audit/events.go` (additive constant adds to closed enum, allowed per D-19 spec-evolution rule).

    **stream_test.go**:
    - `TestStream_SetsHeaders`: build tmpfile with 1024 bytes, stream → response Content-Type matches input, Content-Length == "1024", Cache-Control == "no-store", body length == 1024.
    - `TestStream_IgnoresRangeHeader`: request has `Range: bytes=0-99`, stream → response status 200 (NOT 206), Content-Length == file size, body == full file.
    - `TestStream_IgnoresIfNoneMatch`: request has `If-None-Match: "anything"`, stream → 200 (NOT 304), full body.
    - `TestStream_IgnoresIfModifiedSince`: same shape.
    - `TestStream_NoTransferEncoding`: assert response.Header.Get("Transfer-Encoding") == "" (identity transfer per CS-06; net/http MAY add chunked if Content-Length is missing — assert we set it correctly so chunked is bypassed).

    **errors_test.go**:
    - `TestWriteError_RendersEnvelope`: build a Deps with mocked AuditLog (slog handler → buffer) and Metrics (NewContentServiceCollectors over a fresh registry). Call writeError with errMissingEnvironment. Assert response body is JSON `{"error":{"code":"missing_environment","message":"..."},"request_id":"req_..."}`, status 400.
    - `TestWriteError_EmitsAudit`: same setup, assert audit buffer contains `"action":"content.get"`, `"outcome":"missing_environment"`, `"target.kind":"prompt"` (or whatever kind was passed), `"target.name":"foo"`.
    - `TestWriteError_NilKeyInfo_BlankActor`: pass nil keystore.KeyInfo, assert audit `actor` == `"<ns>/-"`.
    - `TestWriteError_IncsMetric`: gather metrics post-call, assert `content_service_requests_total{kind="prompt",outcome="missing_environment"}` == 1.
    - `TestErrorFactories_AllCodes`: iterate 12 errResp factories, assert HTTPStatus + Code map to D-03 table verbatim.
  </behavior>
  <action>
    1. **Add audit constants** (additive — `internal/audit/events.go`): append `ActionContentGet = "content.get"` to the Action* block; append `OutcomeUnauthorizedContent = "unauthorized_content"`, `OutcomeContentNotFound = "content_not_found"`, `OutcomeStaleCacheExpired = "stale_cache_expired"` to the Outcome* block. Verify `OutcomeForwarded = "forwarded"` already exists (Phase 4 forwarder uses it); if absent, ADD additively. `internal/audit/events.go` is now declared in this plan's files_modified so the change is in scope.
    2. **stream.go**: SPDX header. Package `contentservice`. Imports `io`, `net/http`, `os`, `strconv`. Single function `stream(w, r, f, contentType, size)` returning `(int64, error)`. Body: set 3 headers, WriteHeader(200), `n, err := io.Copy(w, f)`. Return n, err.
    3. **errors.go**: SPDX header. Package `contentservice`. Imports `net/http`, `os`, plus `internal/audit`, `internal/keystore`, `internal/platformapi/middleware`, `internal/platformapi/render`. Declare `errResp` struct. Declare the 12 outcome-string constants (or import them from audit if they already exist there — prefer the audit package as the closed-vocab source of truth). 11 factory functions returning typed `*errResp`. `(d Deps) writeError(w, r, kind, name, info, e)` method (Deps defined in Task 3b). `actorFromInfo(info)` + `keyIDFromInfo(info)` helpers — defensive nil checks.
    4. **stream_test.go**: Use `net/http/httptest.NewRecorder()` + manual `os.CreateTemp` for test files. Each test creates a recorder, builds a temp file with deterministic bytes, calls `stream`, asserts headers + body. 5 tests as described.
    5. **errors_test.go**: Build a mock Deps (use a struct literal with the minimum fields — `AuditLog: slog.New(slog.NewJSONHandler(&buf, nil))`, `Metrics: metrics.NewContentServiceCollectors(metrics.NewRegistry())`). Use `bytes.Buffer` to capture audit output, then `strings.Contains` assertions on each expected attribute. Use `testutil.GatherAndCompare` or `testutil.ToFloat64` from `prometheus/client_golang/prometheus/testutil` for the metric assertion.
  </action>
  <verify>
    <automated>./scripts/dev.sh go test ./internal/contentservice/... ./internal/audit/... -count=1 -race -run 'TestStream|TestWriteError|TestErrorFactories'</automated>
  </verify>
  <done>
    - Both files compile + vet clean.
    - All 9 tests pass under `-race`.
    - `grep -c 'http.ServeContent' internal/contentservice/stream.go` returns 0 (confirms D-01).
    - `grep -c "Cache-Control.*no-store" internal/contentservice/stream.go` returns 1.
    - `grep -c 'ActionContentGet' internal/audit/events.go internal/contentservice/errors.go` returns ≥ 2.
    - `grep -c 'render.Error' internal/contentservice/errors.go` returns 1.
    - `grep -c 'EmitAudit' internal/contentservice/errors.go` returns 1.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: authz.go gate functions (resolveAuthn, resolveEnv, enforceTeams, enforceAllowlist, resolveContent, checkStaleness)</name>
  <files>internal/contentservice/authz.go, internal/contentservice/authz_test.go</files>
  <read_first>
    - .planning/phases/05-content-service-cross-component-observability/05-PATTERNS.md (authz.go section lines 385-413)
    - .planning/phases/05-content-service-cross-component-observability/05-CONTEXT.md (D-03 outcome table, D-04 gate sequence)
    - internal/keystore/keystore.go (KeyInfo struct + Resolver interface + KeyType.BearerPrefix values)
    - internal/keystore/teamsresolver.go (TeamsResolver interface, ErrNotFound handling at line 79-100)
    - internal/litellm/errors.go (ErrNotFound semantics if TeamsResolver bubbles transport errors)
    - internal/contentservice/envcache/cache.go (Plan 05-03 — EnvRow + Cache interface)
    - internal/db/plugins.go (Plan 05-02 Task 3 — ResolvePluginByName + PluginResolution)
    - internal/db/{environments,prompts,artifacts}.go (Plan 05-02 — GetXByName signatures)
    - internal/contentservice/errors.go (Task 1 — errResp + factory functions)
    - api/ach/v1alpha1/external_ref_types.go (RefreshBlock — has MaxStaleness field?)
  </read_first>
  <behavior>
    Per-gate functions, each returning `*errResp` (nil on pass-through). Functions live in `package contentservice`, called from `pipeline.go` (Task 3b).

    **resolveAuthn(ctx, d, r) (*keystore.KeyInfo, *errResp)**:
    - Read `plaintext := r.Header.Get("x-ach-key")`. Lowercase header lookup is chi-default but use canonical-cased header via `r.Header.Get`.
    - Empty plaintext → `errInvalidKeyFormat()`.
    - Prefix check: must start with `"pk_"` OR `"ek_"`. Otherwise → `errInvalidKeyFormat()`.
    - `info, err := d.Resolver.Resolve(ctx, plaintext)`. Err non-nil → `errInternal()`. info nil → `errExpiredOrRevoked()`.
    - Return `(info, nil)`.

    **resolveEnv(ctx, d, info, headerEnv string) (*envcache.EnvRow, *errResp)**:
    - If `info.KeyType == "pk_"`:
      - `headerEnv` empty → `errMissingEnvironment()`.
    - If `info.KeyType == "ek_"`:
      - `headerEnv` non-empty AND `headerEnv != info.Environment` → `errWrongEnvironment()`.
      - `headerEnv` empty → use `info.Environment` (bound).
    - resolvedEnv := `headerEnv` for pk_, `info.Environment` for ek_ (after the above branches).
    - Namespace: read from `d.Namespace` (process-level config — set in Plan 05-07).
    - `row, err := d.EnvCache.Get(ctx, d.Namespace, resolvedEnv)`. Err non-nil → `errInternal()`. row nil → `errEnvironmentNotFound()`.
    - Note CS-09: row with `DeletionTimestamp != nil` is STILL returned — content keeps serving until row is hard-deleted. The staleness gate (gate 7) eventually fires 503 once last_successful_refresh ages out, OR the resolveContent gate fires 404 if the underlying CRD's projection row is hard-deleted by Plan 05-04.
    - Return `(row, nil)`.

    **enforceTeams(ctx, d, info, envRow) *errResp**:
    - Only runs for pk_. For ek_ → return nil (no team enforcement).
    - `userTeams, err := d.Teams.Resolve(ctx, info.OwnerEmail)`. On err that wraps a transport/connection error (anything NOT `litellm.ErrNotFound`) → `errLitellmUnreachable()` + INC `litellm_unreachable_total{caller="content_service"}` via `d.LiteLLMUnreachable.WithLabelValues("content_service").Inc()`. On `errors.Is(err, litellm.ErrNotFound)` → treat as empty team list (matches Phase 4 D-17 behavior). On err == nil with empty teams → return nil userTeams.
    - Intersect `userTeams` ∩ `envRow.AuthorizedTeams`. Empty intersection → `errUnauthorizedTeam()`. Otherwise → nil.

    **enforceAllowlist(envRow, kind, name) *errResp**:
    - Pure function, no I/O.
    - Switch on kind:
      - "prompt" → check `name ∈ envRow.ContextPrompts`. Absent → `errUnauthorizedContent()`.
      - "plugin" → check `name ∈ envRow.ContextPlugins`. Absent → `errUnauthorizedContent()`.
      - "artifact" → check `name ∈ envRow.ContextArtifacts`. Absent → `errUnauthorizedContent()`.
    - Use linear scan (env.Context lists are small — typically <50 elements; no need for map indexing).

    **resolveContent(ctx, d, kind, name) (*contentRow, *errResp)** — `contentRow` is a local struct holding the fields the pipeline needs downstream:
    - Switch on kind:
      - "prompt": `row, err := db.GetPromptByName(ctx, d.Pool, d.Namespace, name)`. Err → `errInternal()`. row nil → `errContentNotFound()`. Hard-deleted soft-deletes (`row.DeletionTimestamp != nil`) — still return; CS-09 in-flight read tolerance, staleness fires next.
      - "plugin": `res, err := db.ResolvePluginByName(ctx, d.Pool, d.Namespace, name)`. Err → `errInternal()`. res nil → `errContentNotFound()`. Use `res.StorageLocation`, `res.LastSuccessfulRefresh`, `res.MaxStalenessSeconds`. Note `res.Namespace` may be the CRD namespace (Source="plugin") OR the marketplace name (Source="marketplace") — pass-through to contentRow.
      - "artifact": `row, err := db.GetArtifactByName(...)`. Same shape. Plus extract `row.Scope`.
    - Build `contentRow{StorageLocation, LastSuccessfulRefresh *time.Time, MaxStalenessSeconds, ContentType *string (prompt only), Scope string (artifact only), Source string}`.
    - Return `(row, nil)`.

    **checkStaleness(row *contentRow) *errResp**:
    - Pure function. If `row.LastSuccessfulRefresh == nil` → `errStaleCacheExpired()` (PVC-loss recovery / OP-11 condition).
    - `if time.Since(*row.LastSuccessfulRefresh) > time.Duration(row.MaxStalenessSeconds) * time.Second` → `errStaleCacheExpired()`.
    - Else nil.

    Tests (~25 cases): every gate × multiple denial paths + happy path.
  </behavior>
  <action>
    1. **authz.go**: SPDX header. Package `contentservice`. Imports: stdlib (`context`, `errors`, `net/http`, `strings`, `time`), `internal/db`, `internal/keystore`, `internal/litellm`, `internal/contentservice/envcache`.

       Top-down:
       - `contentRow` struct (unexported — pipeline.go consumes).
       - Six gate functions per behavior above. Each has a doc comment citing the relevant gate number from D-04 + the error codes it can return + the spec divergence call-out (only resolveAuthn has none; enforceAllowlist has the cheaper-first divergence note).

       For TeamsResolver error classification: use `errors.Is(err, litellm.ErrNotFound)` for the not-found branch; anything else maps to litellm_unreachable.

    2. **authz_test.go**: Use stdlib `testing` + mock implementations of the keystore/teams/envcache interfaces. Build per-gate test tables:
       - `TestResolveAuthn`: 5 cases (empty header / wrong prefix / Resolver error / Resolver nil / happy path).
       - `TestResolveEnv_PK`: 3 cases (missing header / env not found / happy path).
       - `TestResolveEnv_EK`: 3 cases (header mismatch / header absent uses bound env / env not found).
       - `TestEnforceTeams_PK`: 4 cases (LiteLLM error → 503 + INC litellm_unreachable / empty intersection / happy path / ek_ skips entirely).
       - `TestEnforceAllowlist`: 6 cases (3 kinds × {in-list, not-in-list}).
       - `TestResolveContent_Prompt`: 3 cases (nil → 404 / non-nil happy path / GetPromptByName error → 500).
       - `TestResolveContent_Plugin_CRDWins`: ResolvePluginByName returns Source="plugin".
       - `TestResolveContent_Plugin_MarketplaceFallback`: returns Source="marketplace".
       - `TestResolveContent_Plugin_None`: returns nil → 404.
       - `TestResolveContent_Artifact_ScopeRoundTrip`: returns artifact with Scope="directory".
       - `TestCheckStaleness_Fresh`: now ≈ lsr, max=300s → nil.
       - `TestCheckStaleness_Stale`: lsr 1h ago, max=300s → 503.
       - `TestCheckStaleness_NullLSR`: lsr nil → 503.

       Each mock is a struct implementing the interface with a `result`+`err` field; tests build minimal mocks with constructor helpers like `mockResolver{info: info, err: err}`.

       Use the new mock `litellmUnreachableInc` counter (passed in via Deps fixture). Counter Inc is verified via `testutil.ToFloat64`.
  </action>
  <verify>
    <automated>./scripts/dev.sh go test ./internal/contentservice/... -count=1 -race -run 'TestResolveAuthn|TestResolveEnv|TestEnforceTeams|TestEnforceAllowlist|TestResolveContent|TestCheckStaleness'</automated>
  </verify>
  <done>
    - File compiles + vets clean.
    - Tests pass under `-race`.
    - `grep -c '^func resolveAuthn\|^func resolveEnv\|^func enforceTeams\|^func enforceAllowlist\|^func resolveContent\|^func checkStaleness' internal/contentservice/authz.go` returns 6.
    - `grep -c 'litellm.ErrNotFound' internal/contentservice/authz.go` returns 1 (TeamsResolver not-found classification).
    - Mock-based unit tests cover every D-03 outcome that's reachable from authz.go gates (10 of 12 codes — content_not_found has 2 paths, environment_not_found, all 4xx codes covered).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3a: Cleanup — delete k8s.go, refactor paths.go (+ paths_test.go), apply cmd/ach/cmd/content_service.go compile-tolerance stub patch</name>
  <files>internal/contentservice/k8s.go, internal/contentservice/paths.go, internal/contentservice/paths_test.go, cmd/ach/cmd/content_service.go</files>
  <read_first>
    - internal/contentservice/k8s.go (full current file — to be REMOVED)
    - internal/contentservice/paths.go (full current file — ResolvePath returns []string; refactor to single path per scope)
    - internal/contentservice/paths_test.go (existing test assertions on old ResolvePath signature)
    - cmd/ach/cmd/content_service.go (current — line ~103 calls `contentservice.NewK8sPromptLookup(mgr.GetClient(), ns, log)`)
    - .planning/phases/05-content-service-cross-component-observability/05-PATTERNS.md (paths.go REFACTOR section lines 484-501)
    - .planning/phases/05-content-service-cross-component-observability/05-CONTEXT.md (D-Discretion — scope-based dispatch)
  </read_first>
  <behavior>
    Mechanical cleanup wave. No new production logic; pure refactor + delete + compile-tolerance.

    **k8s.go (DELETED)**:
    - Delete the file. The `PromptContentTypeLookup` function is gone; `content_type` now comes from `prompts.content_type` projection column resolved in `resolveContent` (Task 2).
    - Verify with `test -f internal/contentservice/k8s.go && echo "STILL EXISTS"` after the change — should be empty output.

    **paths.go (REFACTORED)**:
    - Old `ResolvePath(cacheRoot, kind, name)` returned `([]string, error)` with 2 candidates for artifact. New signature: `ResolvePath(cacheRoot, kind, name, scope string) (string, error)`. Single deterministic path.
      - prompt: `cacheRoot + "/prompt/" + name`.
      - plugin: `cacheRoot + "/plugin/" + name + ".tar.gz"`.
      - artifact with scope="object": `cacheRoot + "/artifact/" + name`.
      - artifact with scope="directory": `cacheRoot + "/artifact/" + name + ".tar.gz"`.
      - Invalid kind → `ErrInvalidKind`. Invalid name → `ErrInvalidName`. Invalid scope for artifact → `ErrInvalidScope = errors.New("contentservice: invalid scope")`.
    - Keep `validateName` + existing sentinel errors.

    **paths_test.go**:
    - Update every existing call site from `ResolvePath(cacheRoot, kind, name)` to `ResolvePath(cacheRoot, kind, name, scope)`. For prompt/plugin pass `""` for scope; for artifact pass `"object"` or `"directory"` matching the expected output path.
    - Add ≥ 2 new test cases for scope dispatch:
      - `TestResolvePath_Artifact_DirectoryScope`: scope="directory", returns `<root>/artifact/<name>.tar.gz`.
      - `TestResolvePath_Artifact_ObjectScope`: scope="object", returns `<root>/artifact/<name>`.
      - `TestResolvePath_Artifact_InvalidScope`: scope="invalid", returns `ErrInvalidScope`.

    **cmd/ach/cmd/content_service.go (COMPILE-TOLERANCE STUB PATCH ONLY)**:
    - The file currently calls `contentservice.NewK8sPromptLookup(mgr.GetClient(), ns, log)` (verify exact line via read). That symbol no longer exists after k8s.go deletion.
    - Edit scope is the MINIMUM needed to make `./scripts/dev.sh go build ./...` exit 0:
      1. Remove the `NewK8sPromptLookup` call AND remove its assignment target if it's only used for the lookup (e.g., the variable that was being passed to RegisterRoutes).
      2. If `mgr.GetClient()` was being consumed SOLELY by that lookup call, delete the line. If it's consumed elsewhere, leave it.
      3. Leave a `// TODO(Plan 05-06 Task 1): full Deps wiring lands here — this file gets a complete rewrite in Wave 3.` comment at the removal site.
      4. The existing `contentservice.RegisterRoutes(...)` call may still pass the old (Phase 1) Deps shape — if compilation fails because the Deps struct shape changed in Task 3b, this stub patch must also drop the now-invalid field assignment in the RegisterRoutes argument and rely on zero-value struct literal (which compiles even though the runtime would panic; Plan 05-06 wires it before any deployment). The decision tree:
         - If Plan 05-05 Task 3b changes Deps in a backward-compatible way (adds fields only, no field renames or removals): no Deps-literal edit needed here, just remove the NewK8sPromptLookup line.
         - If Plan 05-05 Task 3b removes/renames Deps fields: minimally update the literal in cmd/ach/cmd/content_service.go to compile (drop the broken field references; let the zero-value pass).
    - DO NOT: rewrite the imports block, build the new Pool/Redis/Resolver/Teams/EnvCache/Metrics chain, mount /metrics, change the http.Server setup, or otherwise expand scope. ALL of that is Plan 05-06 Task 1's exclusive responsibility.
  </behavior>
  <action>
    1. **Delete `internal/contentservice/k8s.go`** via `git rm` (or just `rm` if not yet tracked — verify with `git ls-files internal/contentservice/k8s.go`).

    2. **Refactor `internal/contentservice/paths.go`**: change `ResolvePath` signature to take `scope string`. Update the artifact branch to switch on scope. Keep `validateName` + `ErrInvalidKind` + `ErrInvalidName`. Add `ErrInvalidScope = errors.New("contentservice: invalid scope")` if artifact gets scope outside {"object","directory"}.

    3. **Update `internal/contentservice/paths_test.go`**: change existing test calls from `ResolvePath(cacheRoot, kind, name)` to `ResolvePath(cacheRoot, kind, name, scope)`. Add the 3 new scope-dispatch tests.

    4. **Apply stub patch to `cmd/ach/cmd/content_service.go`**:
       - Locate the `contentservice.NewK8sPromptLookup` call site.
       - Remove the call and the variable that received its result (if dead-on-removal).
       - If `mgr.GetClient()` is now dead, remove its assignment too. Otherwise leave it.
       - Insert the TODO comment.
       - If the `contentservice.RegisterRoutes` argument literal references removed/renamed Deps fields after Task 3b, minimally drop those field references so the literal compiles.
       - DO NOT touch anything else in the file.

    5. Verify all four edits compile: `./scripts/dev.sh go build ./...` exits 0. The repo MUST build green after this task even before Task 3b runs.
  </action>
  <verify>
    <automated>./scripts/dev.sh go build ./...</automated>
  </verify>
  <done>
    - `test -f internal/contentservice/k8s.go` returns non-zero (file is gone).
    - `grep -c 'NewK8sPromptLookup' cmd/ach/cmd/content_service.go` returns 0.
    - `grep -c 'TODO(Plan 05-06' cmd/ach/cmd/content_service.go` returns 1 (stub-patch breadcrumb in place).
    - paths.go `ResolvePath` signature now `(cacheRoot, kind, name, scope string) (string, error)` — `grep -c 'ResolvePath(cacheRoot, kind, name, scope string)' internal/contentservice/paths.go` returns 1.
    - `grep -c 'ErrInvalidScope' internal/contentservice/paths.go` returns 1.
    - paths_test.go compiles + tests pass for new scope cases: `./scripts/dev.sh go test ./internal/contentservice/... -run 'TestResolvePath' -count=1` exits 0.
    - `./scripts/dev.sh go build ./...` exits 0 — entire repo builds green (the Plan 05-05 build break created by k8s.go deletion is closed).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3b: pipeline.go (D-04 7-gate orchestrator with D-02 early file open) + Deps struct + handler.go rewrite</name>
  <files>internal/contentservice/handler.go, internal/contentservice/pipeline.go</files>
  <read_first>
    - internal/contentservice/handler.go (full current file — pattern for Deps + RegisterRoutes)
    - internal/contentservice/paths.go (post-Task-3a — new ResolvePath signature)
    - internal/contentservice/content_type.go (RETAINED — ContentTypeForFile signature unchanged)
    - .planning/phases/05-content-service-cross-component-observability/05-PATTERNS.md (handler.go REWRITE + pipeline.go sections lines 296-396)
    - .planning/phases/05-content-service-cross-component-observability/05-CONTEXT.md (D-02, D-16, D-17, D-Discretion sections)
    - internal/contentservice/authz.go (Task 2)
    - internal/contentservice/stream.go (Task 1)
    - internal/contentservice/errors.go (Task 1)
    - internal/contentservice/envcache/cache.go (Plan 05-03)
    - internal/metrics/contentservice.go (Plan 05-01)
    - internal/keystore/keystore.go (Resolver interface)
    - internal/keystore/teamsresolver.go (TeamsResolver interface)
    - github.com/jackc/pgx/v5/pgxpool (for *pgxpool.Pool type in Deps)
    - github.com/prometheus/client_golang/prometheus (for *CounterVec litellm_unreachable_total field)
  </read_first>
  <behavior>
    **handler.go (REWRITTEN)**:
    - New `Deps` struct fields: `CacheRoot string`, `Namespace string`, `Pool *pgxpool.Pool`, `EnvCache envcache.Cache`, `Resolver keystore.Resolver`, `Teams keystore.TeamsResolver`, `Metrics *metrics.ContentServiceCollectors`, `LiteLLMUnreachable *prometheus.CounterVec`, `AuditLog *slog.Logger`, `Logger *slog.Logger`.
    - Zero-value defaults in `RegisterRoutes`: Logger defaults to `slog.Default()`. The rest MUST be non-nil — RegisterRoutes does NOT panic on missing required deps (NO nil-guard panic — accepts zero values and panics at request time instead). This keeps Task 3a's stub-patched cmd/ach/cmd/content_service.go COMPILING; Plan 05-06 wires it properly before any deployment.
    - `RegisterRoutes(r chi.Router, d Deps)` registers `/healthz`, `/content/prompt/{name}`, `/content/plugin/{name}`, `/content/artifact/{name}`. `/metrics` is NOT registered here (it's mounted by the cmd-level wiring in Plan 05-06 — keep the route registration symmetric with Phase 3 platform-api which mounts metrics at the server level).
    - `serve(kind string) http.HandlerFunc` orchestrator — calls pipeline → on err call writeError → else call stream + observe duration + Inc success metric + Add bytes + emit success audit.

    **pipeline.go (NEW)**:
    - Type `resolvedRow struct { File *os.File; Size int64; ContentType string; AuditKind string; AuditName string; KeyInfo *keystore.KeyInfo }`.
    - Function `pipeline(ctx, d, kind string, r *http.Request) (*resolvedRow, *errResp)`:
      - Gate 1: `info, errR := resolveAuthn(ctx, d, r)`. errR != nil → return (nil, errR).
      - Gate 2/3: `envRow, errR := resolveEnv(ctx, d, info, r.Header.Get("x-ach-environment"))`.
      - Gate 4: pk_ only — `errR := enforceTeams(ctx, d, info, envRow)`.
      - Gate 5: `errR := enforceAllowlist(envRow, kind, name)` where `name := chi.URLParam(r, "name")`.
      - Gate 6: `contentRow, errR := resolveContent(ctx, d, kind, name)`.
      - Gate 7: `errR := checkStaleness(contentRow)`.
      - Gate 8 (D-02 EARLY OPEN): resolve on-disk path → `f, err := os.Open(path)`. On err: `os.IsNotExist(err)` → `errContentNotFound()` (the projection row claims it exists, but the cache file doesn't — could be PVC drift); else `errInternal()`. Stat via `fi, err := f.Stat()`. On stat err: close f → `errInternal()`. Compute contentType (per-kind policy per CS-06):
        - prompt: `*contentRow.ContentType` if non-nil-and-non-empty else `application/octet-stream`.
        - plugin: `application/gzip`.
        - artifact: `application/gzip` if `contentRow.Scope == "directory"` else `application/octet-stream`.
      - Return `&resolvedRow{File: f, Size: fi.Size(), ContentType: contentType, AuditKind: kind, AuditName: name, KeyInfo: info}, nil`.
    - **Doc comment MUST cite the spec divergence**: a 10-line doc block above `pipeline()` explaining that Gate 5 (allowlist) runs BEFORE Gate 6 (content resolution) — cheaper-first per D-04 — which inverts spec §15.6 v10 fix step order. Cite the side effect: "name not in env.context AND not in any CRD" yields 403 unauthorized_content (NOT 404 content_not_found). Cite the audit-dashboard implication.

    **handler.go orchestrator `serve()`** (prose only, no fenced code inside `<action>`):
    - On success path: defer File.Close, call stream → if stream error log via d.Logger.Warn (headers already sent, no envelope possible) → emit success audit via audit.EmitAudit with Outcome=audit.OutcomeForwarded → Metrics.IncRequest(kind, "forwarded") → Metrics.AddBytesServed → Metrics.ObserveRequestDuration.
    - On error path: writeError handles envelope + audit + IncRequest; then Metrics.ObserveRequestDuration.
  </behavior>
  <action>
    1. **Rewrite `internal/contentservice/handler.go`**: replace the file end-to-end. Keep SPDX header. Package `contentservice`. New Deps struct + RegisterRoutes + serve() per the behavior block. Imports: `time`, `log/slog`, `net/http`, `github.com/go-chi/chi/v5`, `github.com/jackc/pgx/v5/pgxpool`, `github.com/prometheus/client_golang/prometheus`, `internal/audit`, `internal/keystore`, `internal/contentservice/envcache`, `internal/metrics`, `internal/platformapi/middleware`. Avoid the redis client in Deps — only envcache.Cache is needed.

    2. **Create `internal/contentservice/pipeline.go`**: SPDX header. Package `contentservice`. Pipeline orchestrator function per behavior. 10-line doc block at top of `pipeline()` documenting the cheaper-first spec divergence per the `<spec_divergence>` block in <context>.

    3. **Use the new paths.go ResolvePath signature** (post-Task-3a): pass `kind, name, scope` where scope is `contentRow.Scope` for artifact, `""` for prompt/plugin.

    4. **Verify the repo still builds**: `./scripts/dev.sh go build ./...` exits 0. Task 3a left the repo green via the stub patch; this task's Deps changes MUST also keep it green. If Task 3a's stub patch references Deps fields that this task removes/renames, Task 3a's patch needs a follow-up minimal edit (drop the broken field reference) — but the simplest path is to make Task 3b's Deps purely ADDITIVE relative to Phase 1's Deps shape, so no follow-up is needed.

    5. **Recommended Deps strategy**: keep any existing Phase 1 Deps fields (e.g., `CacheRoot`, `Logger`) at their existing names so the stub-patched cmd literal still compiles. ADD new fields (Pool, EnvCache, Resolver, Teams, Metrics, LiteLLMUnreachable, AuditLog, Namespace). Remove ONLY fields that are demonstrably dead (e.g., a kubernetes client field). The stub patch in cmd/ach/cmd/content_service.go can then keep referencing CacheRoot+Logger and the build stays green with zero-value new fields.
  </action>
  <verify>
    <automated>./scripts/dev.sh go build ./... &amp;&amp; ./scripts/dev.sh go vet ./internal/contentservice/... ./cmd/ach/...</automated>
  </verify>
  <done>
    - Entire repo builds clean (`go build ./...` exits 0).
    - `grep -c 'pipeline(' internal/contentservice/pipeline.go` returns ≥ 1 (declaration).
    - `grep -c 'os.Open' internal/contentservice/pipeline.go` returns 1 (D-02 early open inside pipeline).
    - `grep -c 'http.ServeContent' internal/contentservice/` returns 0 (no fallback usage).
    - pipeline.go function doc comment contains string `cheaper-first` AND `spec §15.6` (divergence callout per D-04).
    - `grep -c 'contentservice.Deps\|^type Deps struct' internal/contentservice/handler.go` returns ≥ 1.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Integration tests for the full pipeline (testcontainers Postgres + miniredis + httptest LiteLLM) covering D-03 outcomes</name>
  <files>internal/contentservice/handler_test.go, internal/contentservice/pipeline_test.go, internal/contentservice/content_type_test.go</files>
  <read_first>
    - internal/contentservice/handler.go (Task 3b)
    - internal/contentservice/pipeline.go (Task 3b)
    - internal/contentservice/authz.go (Task 2)
    - internal/keystore/teamsresolver_test.go (httptest mock LiteLLM pattern)
    - internal/db/external_refs_test.go (testcontainers Postgres setup + //go:build integration tag)
    - internal/db/marketplace_plugins_test.go (seed marketplace_plugin row helper)
    - internal/contentservice/handler_test.go (current file — drift flag #3: replace `public, max-age=300` assertion with `no-store`)
    - internal/contentservice/content_type_test.go (current file — if it asserts the old `PromptContentTypeLookup` path, remove the dead test; if it only tests `ContentTypeForFile` (which is RETAINED), leave it untouched and drop from files_modified)
    - examples/04-environment-demo.yaml (CR fixture shapes for spec.context.* + spec.authorizedTeams)
  </read_first>
  <behavior>
    Build-tag `//go:build integration`. Spin testcontainer Postgres + miniredis + httptest LiteLLM mock per Phase 3/4 patterns.

    Coverage matrix (one subtest per D-03 outcome) — `TestPipeline_EndToEnd`:
    - `200 success / pk_`: seed pk_ in DB + Environment row in projections + Plugin projection row + cache file on disk. GET /content/plugin/foo with `x-ach-key=pk_<hex>` + `x-ach-environment=prod` + LiteLLM mock returning `{teams: ["team-a"]}` where Environment.authorized_teams contains team-a. Expect 200, Content-Type=application/gzip, Cache-Control=no-store, Content-Length matches file size, body == file bytes.
    - `200 success / ek_`: same but with ek_ bound to environment.
    - `400 missing_environment`: pk_ without x-ach-environment header.
    - `400 invalid_key_format`: header value `garbage` (no pk_/ek_ prefix).
    - `400 invalid_key_format`: empty x-ach-key header.
    - `401 expired_or_revoked`: pk_ that doesn't exist in DB (Resolver returns nil, nil).
    - `403 unauthorized_team`: pk_ valid, Environment.authorized_teams contains team-a, LiteLLM mock returns team-b only.
    - `403 wrong_environment`: ek_ bound to prod, request has x-ach-environment=staging.
    - `403 unauthorized_content`: name not in spec.context.plugins (cheaper-first — even when the Plugin CRD projection row exists).
    - `404 environment_not_found`: x-ach-environment=non-existent.
    - `404 content_not_found`: name in spec.context.plugins, but no Plugin projection row AND no marketplace_plugins row.
    - `503 stale_cache_expired`: Plugin projection row has last_successful_refresh = 24h ago + max_staleness_seconds=300.
    - `503 stale_cache_expired (NULL LSR)`: Plugin projection row has last_successful_refresh=NULL.
    - `503 litellm_unreachable`: LiteLLM mock returns 500 on /user/info.
    - `200 ignores Range header`: include `Range: bytes=0-99` → 200, full body, no 206.
    - `200 ignores If-None-Match`: include `If-None-Match: "x"` → 200, no 304.

    Plus 4 §12.3 precedence tests (`TestPipeline_PluginPrecedence`):
    - `plugin CRD wins`: both projection row + marketplace row exist; verify the served file path matches the plugin projection storage_location.
    - `marketplace fallback alphabetical`: 3 marketplace rows + no plugin projection; verify served file is from anthropic-mkt (lexicographically lowest).
    - `soft-deleted plugin → marketplace fallback`: plugin projection has deletion_timestamp != nil + marketplace row exists; served from marketplace.
    - `no match → 404 content_not_found`: name in spec.context.plugins but no plugin row and no marketplace row.

    Plus the SC#4 in-flight read survival test (`TestPipeline_InFlightReadSurvivesRename`):
    - Start a streaming GET in a goroutine that reads slowly (50ms-per-chunk via io.Pipe).
    - Mid-stream, do `os.Rename(filepath, filepath+".new")` followed by writing new file at the old path with different content.
    - Assert the goroutine receives the ORIGINAL bytes (inode pinned by the open FD).

    Plus the auditing test (`TestPipeline_EmitsOneAuditEventPerRequest`):
    - Run a successful request → audit buffer contains exactly one record with action="content.get", outcome="forwarded", target.kind="plugin", target.name="foo".
    - Run a 403 unauthorized_team → audit buffer contains exactly one record with outcome="unauthorized_team".

    Plus the Cache-Control header regression (`TestPipeline_NoStoreHeader`):
    - Assert response header `Cache-Control == "no-store"` (NOT `public, max-age=300`). Drift flag #3 lockdown.
  </behavior>
  <action>
    1. **Rewrite `internal/contentservice/handler_test.go`** end-to-end. Build tag `//go:build integration`. Package `contentservice`. Imports include testcontainers, miniredis, httptest, internal/db, internal/keystore.

       Helper functions:
       - `setupIntegration(t)` returns `(*Deps, *testFixtures)` where testFixtures has pool, mr, litellmMock httptest.Server, cleanup func.
       - `seedEnvironment(t, pool, ns, name, authorizedTeams, contextPlugins, contextPrompts, contextArtifacts)`.
       - `seedPlugin(t, pool, ns, name, storageLocation, lastSuccessfulRefresh, maxStaleness)`.
       - `seedMarketplacePlugin(t, pool, marketplaceName, name, storageLocation, lsr, ms)`.
       - `seedCacheFile(t, cacheRoot, kind, name, scope, bodyBytes)`.
       - `seedPersonalKey(t, pool, pepper, plaintext, owner, status)`.
       - `seedEnvironmentKey(t, pool, pepper, plaintext, owner, environment, status)`.
       - `doRequest(t, deps, method, path, headers map[string]string) *httptest.ResponseRecorder` — builds a chi.Router via `RegisterRoutes(r, deps)`, calls `r.ServeHTTP(rec, req)`.

    2. **Rewrite `internal/contentservice/pipeline_test.go`** with the §12.3 precedence subtests (4 cases) + SC#4 rename survival + audit + Cache-Control regression. Build tag `//go:build integration`.

    3. Use stdlib `testing` only. Per `t.Run` subtests where matrix shape applies.

    4. Use `errors.Is(...)` on errno checks, not string-matching, where the spec specifies sentinel errors (Resolver returning nil-info, etc.).

    5. **For SC#4 in-flight rename test**: Use `io.Pipe` to slow down the response body read on the client side. Inject a custom `http.ResponseWriter` wrapper that calls a "midstream" callback after the first 64 bytes are written; the callback does the `os.Rename` + write new content. Verify the goroutine's `io.ReadAll(resp.Body)` returns the original bytes.

    6. **Conditional edit on `internal/contentservice/content_type_test.go`**: Read the current file. If it references `PromptContentTypeLookup` (now deleted), delete the dead test(s). If it only exercises `ContentTypeForFile` (which is RETAINED), leave the file untouched — content_type_test.go IS in files_modified to permit the edit if needed, but the edit is a no-op when not needed.
  </action>
  <verify>
    <automated>./scripts/dev.sh go test -tags=integration ./internal/contentservice/... -count=1 -race -timeout=10m</automated>
  </verify>
  <done>
    - All integration tests pass under `-race`.
    - `grep -c '^func Test' internal/contentservice/{handler,pipeline}_test.go` returns ≥ 6 (TestPipeline_EndToEnd umbrella + TestPipeline_PluginPrecedence + TestPipeline_InFlightReadSurvivesRename + TestPipeline_EmitsOneAuditEventPerRequest + TestPipeline_NoStoreHeader + at least one helper test).
    - Subtests cover ALL 11 distinct outcome codes from D-03 (count via `grep -c 't.Run\(' internal/contentservice/handler_test.go`).
    - SC#4 test confirms in-flight bytes match pre-rename content.
    - Drift flag #3 closed: `grep -c "public, max-age=300" internal/contentservice/handler_test.go` returns 0; `grep -c "no-store" internal/contentservice/handler_test.go` returns ≥ 1.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → CS HTTP | Untrusted — caller may send arbitrary headers (`x-ach-key`, `x-ach-environment`, `Range`, `If-*`) and arbitrary path params |
| CS process → Postgres | Trusted infrastructure; pgx bound-parameter discipline preserved |
| CS process → Redis | Trusted infrastructure; envcache is best-effort |
| CS process → LiteLLM | Untrusted-availability; transport errors map to 503 litellm_unreachable |
| CS process → disk cache file | Trusted (same Pod / shared PVC RWO) — `*os.File` is opened from validated cache root + validated kind subdir + validated name |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Tampering | x-ach-key header forgery | mitigate | `keystore.Resolver.Resolve` (Phase 3 D-08) validates against `credhash`-peppered DB lookup; tampering requires hash collision or DB write access. Out of CS scope — auth contract owned by Phase 3. |
| T-05-05-02 | Information Disclosure | spec-divergence narrows leak surface | accept | Cheaper-first ordering (D-04) means Environment grant state never leaks "is this resource in the cluster" — 403 unauthorized_content fires before content resolution. Side effect documented in pipeline.go + audit dashboard. |
| T-05-05-03 | Information Disclosure | error envelope echo | mitigate | All `errResp` messages are hard-coded constants (T-03-02-02). No upstream error contents reach the client. |
| T-05-05-04 | Tampering | path traversal via {name} | mitigate | `paths.go:validateName` rejects "/", "..", leading ".". chi route registration is per-kind explicit (`/content/prompt/{name}` etc.) so the kind is router-validated. Validated name + cacheRoot + kind subdir filepath.Join is safe. |
| T-05-05-05 | Denial of Service | unbounded file read | mitigate | Content-Length is set from `f.Stat().Size()` — pre-known. Upstream §10.3 cache-refresh layer enforces size cap via `PluginMaxSizeMiB` (Phase 2). Plugin/Prompt/Artifact tarballs are bounded by config. |
| T-05-05-06 | Denial of Service | LiteLLM-down storm | mitigate | TeamsResolver miss → 503 litellm_unreachable + Inc `litellm_unreachable_total{caller="content_service"}`. Singleflight on Redis cache misses (Phase 4 D-17) caps the storm at 1 DB+LiteLLM call per email per 60s. |
| T-05-05-07 | Denial of Service | sendfile + WriteTimeout=0 stuck reader | accept | Per D-Discretion: WriteTimeout=0 to support large artifact tarballs; relies on `Request.Context()` cancellation for client-disconnect. Slowloris on the response side is bounded by client TCP buffer + kernel TCP keepalive (default 7200s) — accept as v1alpha1 risk. |
| T-05-05-08 | Repudiation | missing audit event on error | mitigate | `writeError` ALWAYS calls `audit.EmitAudit` before returning. Success path also emits. Test `TestPipeline_EmitsOneAuditEventPerRequest` regression-guards. |
| T-05-05-09 | Information Disclosure | log line leaks plaintext key | mitigate | `slog` access logger redacts `x-ach-key` to `<prefix>_***` per Phase 3 D-02 convention. CS Logger MUST be configured the same way at Plan 05-06's wiring site (cross-plan invariant). |
| T-05-05-10 | Elevation of Privilege | CS reads ACH CRDs via informer | mitigate | Phase 5 D-Discretion: NO informer in CS. Phase 1 informer manager removed in Plan 05-06. RBAC drop also lands in 05-07 (CS SA loses `get/list/watch` on ACH CRDs). |
| T-05-05-11 | Tampering | in-flight rename(2) corrupts read | mitigate | D-02 early-open: `os.Open` BEFORE staleness gate; `*os.File` pinned until `io.Copy` completes. SC#4 test `TestPipeline_InFlightReadSurvivesRename` regression-guards. |
| T-05-05-12 | Tampering | spec-divergence not audited | mitigate | pipeline.go doc comment cites the divergence; audit dashboard surfaces 403 unauthorized_content as a distinct outcome from 404 content_not_found — operators can tell which gate fired. |
| T-05-05-13 | Tampering | CRD-row preferred even when soft-deleted | mitigate | `db.ResolvePluginByName` CTE filters `WHERE deletion_timestamp IS NULL` on the plugin_match arm — soft-deleted CRD falls through to marketplace fallback. Plan 05-02 Task 4 regression-tested. |
| T-05-05-SC | Supply Chain | no new direct deps in this plan | accept | Plan reuses Wave 1 deps; `miniredis/v2` already pulled by keystore tests. |
</threat_model>

<verification>
- `./scripts/dev.sh go build ./...` — entire repo compiles
- `./scripts/dev.sh go test -tags=integration ./internal/contentservice/... -count=1 -race -timeout=10m` — all integration tests pass
- `./scripts/dev.sh go test ./internal/contentservice/... -count=1 -race -run 'TestStream|TestResolveAuthn|TestResolveEnv|TestEnforceTeams|TestEnforceAllowlist|TestResolveContent|TestCheckStaleness|TestWriteError'` — unit tests pass
- `./scripts/dev.sh make lint` — golangci-lint clean
- `./scripts/dev.sh make security` — gosec / govulncheck clean
- `grep -c 'http.ServeContent' internal/contentservice/` returns 0 (D-01 enforced)
- `grep -c 'Cache-Control.*no-store' internal/contentservice/stream.go` returns 1 (D-01 enforced)
- `grep -rc 'Cache-Control.*max-age' internal/contentservice/` returns 0 (drift flag #3 cleared)
- pipeline.go doc comment contains the cheaper-first divergence callout
- `grep -c 'NewK8sPromptLookup' cmd/ach/cmd/content_service.go` returns 0 (stub patch landed)
</verification>

<success_criteria>
- All 11 CS-XX requirements satisfied by code paths verifiable in tests.
- D-03 outcome table: every code reachable by an integration subtest.
- §15.6 surface complete: explicit per-kind chi routes, per-kind Content-Type policy, identity transfer, no-store, sendfile-backed body, Range/INM/IMS ignored.
- D-02 early-open inode pin verified by SC#4 test.
- §12.3 precedence verified by 4 plugin-precedence subtests.
- Cheaper-first spec divergence documented in code AND audit-outcome-distinct from 404.
- One audit event per request, outcome string matches response body code.
- ContentService metrics (requests, duration, bytes_served) populated on every request.
- `litellm_unreachable_total{caller="content_service"}` Inc on TeamsResolver transport failure (cross-plan: shared collector registered in Plan 05-06).
- Drift flag #3 resolved: Cache-Control header is `no-store` everywhere; old `public, max-age=300` assertion removed from tests.
- k8s.go DELETED — CS no longer needs the informer.
- cmd/ach/cmd/content_service.go compile-tolerance stub patch keeps the repo building between waves 2 and 3; Plan 05-06 Task 1 owns the full rewrite.
</success_criteria>

<output>
Create `.planning/phases/05-content-service-cross-component-observability/05-05-SUMMARY.md` when done. Document:
- The exact list of files added/modified/deleted (paste `git status --short internal/contentservice/ cmd/ach/cmd/content_service.go internal/audit/events.go`).
- Confirmation of `internal/contentservice/k8s.go` deletion.
- The pipeline divergence-callout doc comment text verbatim.
- Integration test outcome matrix (which D-03 codes have which subtest counts).
- SC#4 test approach narrative (io.Pipe + midstream rename mechanism).
- Confirmation of drift flag #3 closure: `grep 'no-store'` count + `grep 'max-age=300'` count.
- Any audit constants added (paste the diff to events.go).
- The stub-patch diff to cmd/ach/cmd/content_service.go (Task 3a output — the surgical NewK8sPromptLookup removal).
</output>
</content>
