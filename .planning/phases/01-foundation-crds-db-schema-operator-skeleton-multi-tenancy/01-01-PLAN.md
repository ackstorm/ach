---
phase: 01-foundation-crds-db-schema-operator-skeleton-multi-tenancy
plan: 01
plan_id: 01-01
type: execute
wave: 1
depends_on: []
files_modified:
  - PROJECT
  - go.mod
  - go.sum
  - Makefile
  - .golangci.yml
  - .gitignore
  - hack/boilerplate.go.txt
  - README.md
autonomous: true
requirements: []
requirements_addressed: []
objective: "Repo bootstrap via `kubebuilder init` mirroring the sister project's kubebuilder v4 layout (D-01, D-02)."

must_haves:
  truths:
    - "`make manifests generate fmt vet` exits 0 on an empty scaffold."
    - "`go build ./...` exits 0 on the scaffolded tree."
    - "`PROJECT` kubebuilder file declares `domain: ackstorm.ai`, `repo: github.com/ackstorm/ach`, `multigroup: true` (D-01)."
    - "Single `go.mod` at module `github.com/ackstorm/ach`, Go 1.23+, controller-runtime v0.19+ (D-02)."
    - "OP-01 substantively satisfied by 01-05 (Operator is sole reconciler) + 01-09 (Operator SA is sole write-verb holder). This plan ships repo scaffolding only and intentionally claims no requirement IDs."
    - "Phase 1 opts out of Nyquist sampling validation; the phase is sister-project-driven scaffolding + spec-driven CEL/SQL/RBAC, not behavioral runtime. Sampling-based validation arrives with Phase 2 (LiteLLM REST client + external-ref refresh)."
  artifacts:
    - path: "PROJECT"
      provides: "kubebuilder project metadata"
      contains: "domain: ackstorm.ai"
    - path: "go.mod"
      provides: "Go module declaration"
      contains: "module github.com/ackstorm/ach"
    - path: "Makefile"
      provides: "manifests/generate/test/lint/build targets"
      contains: "manifests:"
    - path: ".golangci.yml"
      provides: "lint configuration mirroring sister"
    - path: "hack/boilerplate.go.txt"
      provides: "Apache-2.0 header for controller-gen"
      contains: "Apache License"
  key_links:
    - from: "Makefile"
      to: "controller-gen, kustomize, envtest, golangci-lint"
      via: "go install tool definitions (sister Makefile lines 192-255)"
---

<objective>
Bootstrap the ACH Hub repository with the kubebuilder v4 scaffolding that every subsequent Phase 1 plan consumes. Establish the single `go.mod` module, Makefile target conventions, lint configuration, and Apache-2.0 boilerplate — all mirrored verbatim from the sister project `/home/jcm/Projects/ach_litellm/` per D-01 and D-02. This plan ships ZERO ACH-specific Go code; it is the scaffolding-only foundation that Plans 02 and 04 build CRD types and credential hashing on top of.

Purpose: Lock in the kubebuilder v4 conventions, build/test/lint plumbing, and module identity before any subsystem code is written.
Output: An empty-but-runnable kubebuilder v4 project at `/home/jcm/Projects/ach/` (no CRDs yet) that passes `make manifests generate fmt vet` and `go build ./...`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-crds-db-schema-operator-skeleton-multi-tenancy/01-CONTEXT.md
@.planning/phases/01-foundation-crds-db-schema-operator-skeleton-multi-tenancy/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run `kubebuilder init` and verify the seed scaffolding</name>
  <files>PROJECT, go.mod, go.sum, Makefile, README.md, .gitignore, hack/boilerplate.go.txt, cmd/main.go, .golangci.yml, .dockerignore, Dockerfile</files>
  <read_first>
    - /home/jcm/Projects/ach_litellm/PROJECT (canonical kubebuilder v4 PROJECT shape, `domain: ackstorm.ai`, `multigroup: true`)
    - /home/jcm/Projects/ach_litellm/go.mod (Go 1.23, controller-runtime v0.19.4, Ginkgo v2.19, Gomega v1.33 baseline)
    - /home/jcm/Projects/ach_litellm/Makefile (target names, controller-gen/envtest/kustomize versions, `go install tool` block lines 192-255)
    - /home/jcm/Projects/ach_litellm/.golangci.yml (lint set — verbatim lift, 47 lines)
    - /home/jcm/Projects/ach_litellm/hack/boilerplate.go.txt (Apache-2.0 header — verbatim lift, replace "ackstorm_litellm" mentions with "ACKstorm")
    - .planning/phases/01-foundation-crds-db-schema-operator-skeleton-multi-tenancy/01-PATTERNS.md (Repo bootstrap pattern + Makefile/golangci/boilerplate sections, lines 1000-1071)
    - .planning/phases/01-foundation-crds-db-schema-operator-skeleton-multi-tenancy/01-CONTEXT.md (D-01, D-02 — module path, multigroup, single go.mod)
  </read_first>
  <action>
    Execute exactly:

    ```
    cd /home/jcm/Projects/ach
    kubebuilder init --domain ackstorm.ai --repo github.com/ackstorm/ach --owner "ACKstorm"
    ```

    Required post-init edits (do not skip any):

    1. Open `PROJECT` and set `multigroup: true` (kubebuilder init defaults to false). Verify the resulting file matches the sister-project PROJECT structure (domain, repo, layout, projectName, multigroup, version). Do NOT add any `resources:` entries yet — Plan 02 adds the six kinds.
    2. Replace the kubebuilder-generated `hack/boilerplate.go.txt` with the sister-project verbatim Apache-2.0 header (PATTERNS.md "hack/boilerplate.go.txt" section, lines 1052-1071). The copyright year is `2026` and the holder is `ACKstorm.` exactly.
    3. Replace `.golangci.yml` with the sister-project file verbatim (PATTERNS.md ".golangci.yml" section, lines 1023-1047). No edits.
    4. Open the generated `Makefile` and modify the `build` target to declare the five-binary tree from D-03 (Plan 06 fills in the cmd/* trees, but the Makefile target list must be in place now). Pattern from PATTERNS.md "Makefile" section, lines 1006-1015:
       ```
       build: manifests generate fmt vet
           go build -o bin/operator ./cmd/operator
           go build -o bin/platform-api ./cmd/platform-api
           go build -o bin/forwarder ./cmd/forwarder
           go build -o bin/content-service ./cmd/content-service
           go build -o bin/ach ./cmd/ach
       ```
       Leave the kubebuilder-generated `cmd/main.go` in place for now (Plan 06 moves/replaces it). Keep every other Makefile target verbatim — especially the `Dependencies` block (lines 192-255 of sister).
    5. Append to `.gitignore`:
       ```
       /bin/
       /testbin/
       /cover.out
       /.gocache/
       ```
    6. Delete the kubebuilder-generated `Dockerfile` at repo root — Plan 10 ships five per-binary Dockerfiles (`Dockerfile.operator`, etc.).
    7. Update the generated `README.md` to a one-paragraph stub: project name (ACH — Agent Capability Hub), reference to `.planning/PROJECT.md` for full project context, reference to `.planning/ROADMAP.md` for phasing. Do not commit ASCII-art badges.

    No new external dependencies. No CRD types. No `cmd/<binary>/` directories yet.
  </action>
  <verify>
    <automated>cd /home/jcm/Projects/ach && go build ./... && make manifests generate fmt vet</automated>
  </verify>
  <acceptance_criteria>
    - File `/home/jcm/Projects/ach/PROJECT` exists and contains `domain: ackstorm.ai`, `repo: github.com/ackstorm/ach`, `multigroup: true`, `projectName: ach`, `version: "3"`.
    - File `/home/jcm/Projects/ach/go.mod` exists with first line `module github.com/ackstorm/ach` and a `go 1.23` (or higher minor) directive.
    - File `/home/jcm/Projects/ach/Makefile` contains a `build:` target listing all five `go build -o bin/<name> ./cmd/<name>` lines (operator, platform-api, forwarder, content-service, ach).
    - File `/home/jcm/Projects/ach/.golangci.yml` byte-for-byte matches `/home/jcm/Projects/ach_litellm/.golangci.yml` (verify with `diff -q`).
    - File `/home/jcm/Projects/ach/hack/boilerplate.go.txt` contains `Copyright 2026 ACKstorm.` and the Apache 2.0 URL `http://www.apache.org/licenses/LICENSE-2.0`.
    - `cd /home/jcm/Projects/ach && go build ./...` exits 0.
    - `cd /home/jcm/Projects/ach && make manifests generate fmt vet` exits 0.
    - `cd /home/jcm/Projects/ach && ls cmd/main.go` succeeds (kubebuilder placeholder; Plan 06 will reorganize).
    - No file `Dockerfile` exists at repo root (`test ! -f /home/jcm/Projects/ach/Dockerfile`).
    - `.planning/` directory is untouched.
  </acceptance_criteria>
  <done>The kubebuilder v4 scaffolding is in place, `go build ./...` and `make manifests generate fmt vet` exit 0, and the repository is ready for Plan 02 to scaffold the six CRD kinds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer→repo | Local-only scaffolding step; no remote runtime trust boundary in this plan. |
| build-time→runtime | Makefile / golangci-lint / controller-gen tooling pulled from upstream during `go install tool`. Supply chain enters via `go.mod`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | controller-gen / kustomize / envtest go-install dependencies | accept | Versions pinned in Makefile via the sister-project `go install tool` block (lines 192-255 of sister `Makefile`); no network resolution at runtime; same tool versions the sister has been running successfully against. |
| T-01-02 | Tampering | kubebuilder init scaffold integrity | accept | `kubebuilder` is a developer-machine tool; output is reviewed by diff against the sister project shape before any commit. |
| T-01-03 | Information Disclosure | accidental commit of `cover.out` or `.gocache/` | mitigate | `.gitignore` lists `/bin/`, `/testbin/`, `/cover.out`, `/.gocache/` per the explicit append in Task 1. |
| T-01-04 | Tampering | npm/pip/cargo installs | mitigate | None occur in this plan. `go install tool` pulls Go modules only; legitimacy audit is a researcher concern at the npm/pip/cargo layer per the planner spec — no such installs here. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No npm/pip/cargo installs introduced. No `[ASSUMED]`/`[SUS]` packages. |
</threat_model>

<verification>
- `go build ./...` succeeds.
- `make manifests generate fmt vet` succeeds.
- Diff `.golangci.yml` against sister verbatim.
- Confirm `PROJECT` declares `multigroup: true`.
</verification>

<success_criteria>
A reviewer can `git clone` the ACH repo at this commit, run `make manifests generate fmt vet && go build ./...`, and get a green build with no Go files in `cmd/` yet beyond the kubebuilder placeholder. The next plan (Plan 02) can run `kubebuilder create api` six times without any setup steps.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-crds-db-schema-operator-skeleton-multi-tenancy/01-01-SUMMARY.md` when done. The summary MUST record the exact controller-runtime, controller-gen, envtest, and golangci-lint versions selected (so downstream plans can confirm version-pinning consistency).
</output>
