---
phase: 01-foundation-crds-db-schema-operator-skeleton-multi-tenancy
plan: 11
plan_id: 01-11
type: execute
wave: 6
depends_on: [01-02, 01-05, 01-08, 01-09]
files_modified:
  - internal/controller/suite_test.go
  - internal/controller/environment_finalizer_test.go
  - internal/controller/plugin_finalizer_test.go
  - internal/controller/pluginmarketplace_finalizer_test.go
  - internal/controller/artifact_finalizer_test.go
  - internal/controller/prompt_finalizer_test.go
  - internal/controller/backendidentitypolicy_finalizer_test.go
  - internal/controller/cel_admission_test.go
  - test/fixtures/invalid/environment_missing_runtime.yaml
  - test/fixtures/invalid/plugin_missing_maxstaleness.yaml
  - test/fixtures/invalid/plugin_interval_exceeds_maxstaleness.yaml
  - test/fixtures/invalid/plugin_type_mismatch_subobject.yaml
  - test/fixtures/invalid/artifact_http_with_directory_scope.yaml
  - test/fixtures/invalid/backendidentitypolicy_missing_forwardidentityjwt.yaml
  - test/fixtures/invalid/environment_empty_authorized_teams.yaml
  - test/utils/utils.go
  - test/e2e/e2e_suite_test.go
  - test/e2e/phase1_invariants_test.go
  - Makefile
autonomous: true
requirements: [MULTI-03, OP-02, OP-05, OP-10, OP-11, OP-12, CRD-03, CRD-04, CRD-05, CRD-06, CRD-08]
requirements_addressed: [MULTI-03, OP-02, OP-05, OP-10, OP-11, OP-12, CRD-03, CRD-04, CRD-05, CRD-06, CRD-08]
objective: "Build the envtest + testcontainers-go integration harness asserting all five Phase 1 Success Criteria via automated tests; plus an e2e suite (build-tag-gated) for the manifest set."

must_haves:
  truths:
    - "`make test` (envtest) asserts: CEL admission ACCEPTS the six valid samples (Plan 02 Task 3) and REJECTS the seven invalid fixtures (this plan ships them)."
    - "Each of the six reconcilers passes a finalizer add+remove smoke test in envtest."
    - "Deleting an `Environment` causes EnvironmentReconciler to invoke `NoopClient.DeleteAccessGroup` then `DeleteTag` then `RemoveFinalizer` (observable via Log buffer or counter on a test double)."
    - "Deleting a `Plugin` removes a pre-created file under `<TempDir>/plugin/<name>.tar.gz` BEFORE removing the finalizer (OP-12)."
    - "MULTI-03 readiness: envtest manager `WaitForCacheSync` succeeds before any reconcile is observed."
    - "`make test-e2e` (build-tag `e2e`) on a kind cluster asserts: 3 Pods Ready; operator Pod has 2 containers; PVC bound; psql shows 4 tables; `kubectl auth can-i` returns expected verdicts for all four SAs."
  artifacts:
    - path: "internal/controller/suite_test.go"
      provides: "envtest TestMain + testcontainers-go Postgres bootstrap"
      contains: "envtest.Environment"
    - path: "internal/controller/cel_admission_test.go"
      provides: "applies fixtures + asserts accept/reject behavior"
      contains: "ApplyFixture"
    - path: "internal/controller/environment_finalizer_test.go"
      provides: "Env finalizer add+remove smoke + NoopClient call assertions"
      contains: "TestEnvironmentFinalizerAddRemove"
    - path: "test/e2e/phase1_invariants_test.go"
      provides: "e2e cluster assertions for the five Success Criteria"
      contains: "TestPhase1Invariants"
  key_links:
    - from: "internal/controller/suite_test.go"
      to: "envtest.Environment + testcontainers-go Postgres"
      via: "TestMain → setupAndRun(m) bootstrap"
      pattern: "envtest\\.Environment"
  scope_note: "Plan 11 aggregates phase-wide verification (5 tasks, ~18 files). Acceptable risk — tests are mechanical. If executor friction emerges, split Task 4 (six finalizer tests) into a follow-up plan 01-12."
---

<objective>
Build the verification harness that proves Phase 1 Success Criteria 1-5 are met. Three layers:

1. **envtest unit tests** (`internal/controller/*_test.go`): apply CRD YAMLs into envtest API server, apply valid + invalid fixture CRs, assert CEL admission outcomes, assert finalizer add/remove behavior per kind, assert external-ref reconcilers remove cached files on deletion. Run via `make test`. No Kubernetes cluster required.

2. **testcontainers-go Postgres integration** (extends `internal/controller/suite_test.go`): spins up `postgres:16-alpine`, runs `db.Migrate`, asserts the four tables + UNIQUE + CHECK constraints. Run via `make test-integration`.

3. **e2e cluster tests** (`test/e2e/phase1_invariants_test.go`, build-tag `e2e`): against a kind cluster, applies the full manifest set from `config/default/`, waits for Pods Ready, asserts SC#2 (two containers), SC#4 (psql tables), SC#5 (`kubectl auth can-i` per SA). Run via `make test-e2e`.

Purpose: Make ROADMAP Phase 1 SC #1-5 testable, automated, regression-safe.
Output: One suite_test.go + six finalizer tests + one CEL admission test + seven invalid fixtures + one e2e test + Makefile targets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-crds-db-schema-operator-skeleton-multi-tenancy/01-CONTEXT.md
@.planning/phases/01-foundation-crds-db-schema-operator-skeleton-multi-tenancy/01-PATTERNS.md
@.planning/phases/01-foundation-crds-db-schema-operator-skeleton-multi-tenancy/01-02-SUMMARY.md
@.planning/phases/01-foundation-crds-db-schema-operator-skeleton-multi-tenancy/01-03-SUMMARY.md
@.planning/phases/01-foundation-crds-db-schema-operator-skeleton-multi-tenancy/01-09-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author the seven invalid fixture CRs under `test/fixtures/invalid/`</name>
  <files>test/fixtures/invalid/environment_missing_runtime.yaml, test/fixtures/invalid/plugin_missing_maxstaleness.yaml, test/fixtures/invalid/plugin_interval_exceeds_maxstaleness.yaml, test/fixtures/invalid/plugin_type_mismatch_subobject.yaml, test/fixtures/invalid/artifact_http_with_directory_scope.yaml, test/fixtures/invalid/backendidentitypolicy_missing_forwardidentityjwt.yaml, test/fixtures/invalid/environment_empty_authorized_teams.yaml</files>
  <read_first>
    - config/samples/ach_v1alpha1_environment.yaml (Plan 02 — valid baseline)
    - config/samples/ach_v1alpha1_plugin.yaml (and four other valid samples)
    - api/ach/v1alpha1/environment_types.go (Plan 02 — CEL rules text)
    - api/ach/v1alpha1/plugin_types.go (CEL rules)
    - api/ach/v1alpha1/artifact_types.go (CEL rules incl. http/scope rule)
    - api/ach/v1alpha1/backendidentitypolicy_types.go (CEL rules)
  </read_first>
  <action>
    Write seven invalid CR manifests under `test/fixtures/invalid/`. Each must violate EXACTLY ONE CEL rule so the test asserts the targeted rejection.

    1. `environment_missing_runtime.yaml`: omits `spec.runtime` (violates `has(self.spec.runtime) && has(self.spec.context)` from CRD-02).
       ```
       apiVersion: ach.ackstorm.ai/v1alpha1
       kind: Environment
       metadata: { name: invalid-missing-runtime, namespace: ach-system }
       spec:
         authorizedTeams: ["default"]
         context: { prompts: [], plugins: [], artifacts: [] }
       ```

    2. `environment_empty_authorized_teams.yaml`: `authorizedTeams: []` (violates `size(...) >= 1`).

    3. `plugin_missing_maxstaleness.yaml`: declares `spec.refresh.interval: 1h` but omits `maxStaleness` (violates CRD-04 required).

    4. `plugin_interval_exceeds_maxstaleness.yaml`: `interval: 48h`, `maxStaleness: 24h` (violates the cross-field CEL).

    5. `plugin_type_mismatch_subobject.yaml`: `type: github` but provides `s3:` subobject (violates the discriminator CEL).

    6. `artifact_http_with_directory_scope.yaml`: `type: http`, `scope: directory` (violates CRD-05 / the http→object rule).

    7. `backendidentitypolicy_missing_forwardidentityjwt.yaml`: omits `spec.forwardIdentityJWT` (violates CRD-08 required).

    Each fixture begins with a YAML comment block citing the CEL rule it is designed to violate, e.g.:
    ```
    # FIXTURE: invalid. Expected rejection at admission.
    # Rule violated: Environment.spec.runtime REQUIRED (CRD-02 / Plan 02 Task 2)
    # Expected error substring: "Environment.spec must declare both runtime and context blocks"
    ```

    Confirm that each CR is otherwise structurally valid YAML so the only failure mode is CEL rejection.
  </action>
  <verify>
    <automated>cd /home/jcm/Projects/ach && test -d test/fixtures/invalid && ls test/fixtures/invalid/*.yaml | wc -l | grep -q '^7$'</automated>
  </verify>
  <acceptance_criteria>
    - Directory `test/fixtures/invalid/` exists with exactly seven `.yaml` files.
    - Each file is parseable YAML (`for f in test/fixtures/invalid/*.yaml; do python3 -c "import yaml; yaml.safe_load(open('$f'))" || exit 1; done`).
    - Each file's leading comment cites the CEL rule it violates.
  </acceptance_criteria>
  <done>Seven invalid fixtures exist for the CEL admission tests in Task 3.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author `internal/controller/suite_test.go` (envtest TestMain + testcontainers-go Postgres bootstrap)</name>
  <files>internal/controller/suite_test.go, test/utils/utils.go</files>
  <read_first>
    - /home/jcm/Projects/ach_litellm/internal/controller/suite_test.go (full 418-line canonical TestMain + envtest pattern)
    - /home/jcm/Projects/ach_litellm/test/utils/utils.go (Run + GetProjectDir helpers — verbatim lift)
    - .planning/phases/01-foundation-crds-db-schema-operator-skeleton-multi-tenancy/01-PATTERNS.md (lines 511-535, 1216-1222 — envtest + testcontainers-go integration)
    - internal/controller/environment_controller.go (struct shape)
    - api/ach/v1alpha1/*_types.go (scheme registration)
  </read_first>
  <action>
    Create `test/utils/utils.go` — lift sister verbatim (no changes; project-agnostic helpers `Run`, `GetProjectDir`).

    Create `internal/controller/suite_test.go` with TestMain → setupAndRun(m) pattern:

    Required content (extends sister 418-line pattern):

    1. **TestMain** that calls `setupAndRun(m)` then `os.Exit(code)`.
    2. **setupAndRun(m \*testing.M) int**:
       - `ctx, cancel := context.WithCancel(context.Background()); defer cancel()`
       - Determine CRD path via `GetProjectDir()` → `<root>/config/crd/bases`.
       - Probe for envtest assets via `findEnvtestAssets()` (sister lines 349-381 pattern: try `KUBEBUILDER_ASSETS`, then `/usr/local/kubebuilder/bin`, then `~/.local/share/kubebuilder-envtest/k8s/*`).
       - `testEnv = &envtest.Environment{ CRDDirectoryPaths: []string{crdDir}, ErrorIfCRDPathMissing: true }` + start.
       - Build scheme: `clientgoscheme.AddToScheme(scheme); achv1alpha1.AddToScheme(scheme)`.
       - **Optionally** spin up testcontainers-go Postgres: gated on env `ACH_TEST_DB=1` (developers without Docker skip this). When set: start `postgres:16-alpine`, run `db.Migrate`, expose connStr globally via package-private `var testDBPool *pgxpool.Pool`.
       - Build manager with namespace-scoped cache:
         ```
         mgr, err := manager.New(testEnv.Config, manager.Options{
             Scheme: scheme,
             Cache: cache.Options{ DefaultNamespaces: map[string]cache.Config{ WatchNamespace: {} } },
         })
         ```
         where `WatchNamespace = "ach-system"`.
       - **Ensure namespace exists**: `ensureAlreadyExists(ctx, k8sClient, &corev1.Namespace{ObjectMeta: metav1.ObjectMeta{Name: WatchNamespace}})` helper (sister lines 156-171).
       - **Register six reconcilers** on the test manager, each with a per-test `LiteLLM` field set to a counting fake. Define a `countingNoopClient` struct that wraps the real NoopClient and bumps an atomic counter per method — exported so finalizer tests can assert call ordering.
       - **Start the manager** in a goroutine; `mgr.GetCache().WaitForCacheSync(ctx)`.
       - 2-second settle window before `m.Run()`.
       - Run `m.Run()` and capture exit code.
       - Stop manager (cancel ctx), stop testEnv, stop pgContainer if running.
       - Return code.

    3. **WatchNamespace constant** = "ach-system".
    4. **Exported test helpers** for the per-kind finalizer tests: `k8sClient client.Client`, `testManager manager.Manager`, `litellmCounter *atomic.Int64` (counter incremented by countingNoopClient).
    5. **`ApplyFixture` helper**: reads a YAML file path, decodes with `yaml.Unmarshal`, creates via `k8sClient.Create`. Returns error so callers can assert "rejected by admission".

    Apache-2.0 header. Imports include `github.com/testcontainers/testcontainers-go/modules/postgres` (only used when `ACH_TEST_DB=1`).
  </action>
  <verify>
    <automated>cd /home/jcm/Projects/ach && go build ./internal/controller/... ./test/utils/...</automated>
  </verify>
  <acceptance_criteria>
    - File `internal/controller/suite_test.go` exists with `TestMain` and a separate `setupAndRun` function.
    - File `test/utils/utils.go` exists with `Run` and `GetProjectDir` exported functions.
    - `internal/controller/suite_test.go` registers all six reconcilers on the test manager (`grep -c SetupWithManager` returns 6).
    - File contains `cache.Options{ DefaultNamespaces:` (MULTI-01 enforcement in tests).
    - File defines `countingNoopClient` that satisfies `litellm.Client` and tracks call counts atomically.
    - `go build ./internal/controller/...` exits 0 (compiles but does not run — actual test exec is Task 3-5).
  </acceptance_criteria>
  <done>The envtest TestMain bootstrap exists, namespace-scoped, six reconcilers wired with a counting fake LiteLLM, optional testcontainers-go Postgres for integration runs.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author `cel_admission_test.go` — applies six valid + seven invalid fixtures, asserts outcomes</name>
  <files>internal/controller/cel_admission_test.go</files>
  <read_first>
    - internal/controller/suite_test.go (just-written — for k8sClient + ApplyFixture references)
    - config/samples/ach_v1alpha1_*.yaml (six valid samples — Plan 02 Task 3)
    - test/fixtures/invalid/*.yaml (seven invalid fixtures — Task 1)
  </read_first>
  <action>
    Create `internal/controller/cel_admission_test.go` with a single comprehensive test:

    ```
    func TestCELAdmission(t *testing.T) {
        ctx := context.Background()

        cases := []struct{
            name        string
            fixturePath string
            shouldFail  bool
            errMustContain string
        }{
            {"valid_environment", "../../config/samples/ach_v1alpha1_environment.yaml", false, ""},
            {"valid_plugin",      "../../config/samples/ach_v1alpha1_plugin.yaml", false, ""},
            {"valid_pluginmarketplace", "../../config/samples/ach_v1alpha1_pluginmarketplace.yaml", false, ""},
            {"valid_artifact",    "../../config/samples/ach_v1alpha1_artifact.yaml", false, ""},
            {"valid_prompt",      "../../config/samples/ach_v1alpha1_prompt.yaml", false, ""},
            {"valid_bip",         "../../config/samples/ach_v1alpha1_backendidentitypolicy.yaml", false, ""},

            {"invalid_env_no_runtime", "../../test/fixtures/invalid/environment_missing_runtime.yaml", true, "runtime"},
            {"invalid_env_empty_teams", "../../test/fixtures/invalid/environment_empty_authorized_teams.yaml", true, "authorizedTeams"},
            {"invalid_plugin_no_staleness", "../../test/fixtures/invalid/plugin_missing_maxstaleness.yaml", true, "maxStaleness"},
            {"invalid_plugin_interval_exceeds", "../../test/fixtures/invalid/plugin_interval_exceeds_maxstaleness.yaml", true, "interval"},
            {"invalid_plugin_type_mismatch", "../../test/fixtures/invalid/plugin_type_mismatch_subobject.yaml", true, "subobject"},
            {"invalid_artifact_http_dir", "../../test/fixtures/invalid/artifact_http_with_directory_scope.yaml", true, "scope=object"},
            {"invalid_bip_no_jwt", "../../test/fixtures/invalid/backendidentitypolicy_missing_forwardidentityjwt.yaml", true, "forwardIdentityJWT"},
        }

        for _, tc := range cases {
            t.Run(tc.name, func(t *testing.T) {
                err := ApplyFixture(ctx, tc.fixturePath)
                if tc.shouldFail {
                    if err == nil { t.Fatalf("expected admission rejection, got nil") }
                    if tc.errMustContain != "" && !strings.Contains(err.Error(), tc.errMustContain) {
                        t.Errorf("expected error containing %q, got %q", tc.errMustContain, err.Error())
                    }
                } else {
                    if err != nil { t.Fatalf("expected admission accept, got %v", err) }
                    // Cleanup: delete the created CR so subsequent tests start clean.
                }
            })
        }
    }
    ```

    Use `strings.Contains` (case-sensitive) on the API server's error message. Each invalid fixture's `errMustContain` substring matches a fragment of the CEL `message:` text defined in Plan 02 Task 2 — coordinate with Plan 02's SUMMARY.

    Reset/delete each successfully-applied CR at the end of its subtest so the next subtest does not see leftover objects.
  </action>
  <verify>
    <automated>cd /home/jcm/Projects/ach && go build ./internal/controller/... && go test -count=1 -run TestCELAdmission ./internal/controller/... 2>&1 | tee /tmp/cel-test.log | grep -E "FAIL|PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File `internal/controller/cel_admission_test.go` exists.
    - The test runs 13 subtests (6 valid + 7 invalid).
    - `go test -run TestCELAdmission ./internal/controller/...` exits 0 (when envtest assets are available — CI prerequisite).
    - On a missing envtest assets environment, the test fails with a clear error message (sister-aligned diagnostic).
  </acceptance_criteria>
  <done>CRD-01..CRD-08 CEL admission rules are asserted end-to-end against envtest.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Author six finalizer add/remove tests (one per kind)</name>
  <files>internal/controller/environment_finalizer_test.go, internal/controller/plugin_finalizer_test.go, internal/controller/pluginmarketplace_finalizer_test.go, internal/controller/artifact_finalizer_test.go, internal/controller/prompt_finalizer_test.go, internal/controller/backendidentitypolicy_finalizer_test.go</files>
  <read_first>
    - /home/jcm/Projects/ach_litellm/internal/controller/litellmconnection_finalizer_test.go (162-line sister finalizer test pattern)
    - .planning/phases/01-foundation-crds-db-schema-operator-skeleton-multi-tenancy/01-PATTERNS.md (lines 540-565 — finalizer test pattern)
    - internal/controller/suite_test.go (just-written — exposes k8sClient + litellmCounter + countingNoopClient)
    - internal/controller/finalizers.go (Plan 05 Task 2 — finalizer constants)
  </read_first>
  <action>
    Six tests, each a single `TestXxxFinalizerAddRemove`. Pattern (Environment shown in full; five siblings adapt):

    ```
    func TestEnvironmentFinalizerAddRemove(t *testing.T) {
        ctx := context.Background()
        litellmCounter.Store(0)

        cr := &achv1alpha1.Environment{
            ObjectMeta: metav1.ObjectMeta{
                Name:      "test-env-" + rand.String(5),
                Namespace: WatchNamespace,
            },
            Spec: achv1alpha1.EnvironmentSpec{
                AuthorizedTeams: []string{"default"},
                Runtime:         achv1alpha1.RuntimeBlock{},
                Context:         achv1alpha1.ContextBlock{},
            },
        }
        if err := k8sClient.Create(ctx, cr); err != nil { t.Fatalf("create: %v", err) }
        t.Cleanup(func() { _ = k8sClient.Delete(ctx, cr) })

        // Poll for finalizer added.
        require.Eventually(t, func() bool {
            var got achv1alpha1.Environment
            _ = k8sClient.Get(ctx, client.ObjectKeyFromObject(cr), &got)
            return controllerutil.ContainsFinalizer(&got, environmentFinalizer)
        }, 30*time.Second, 250*time.Millisecond, "finalizer not added")

        // Delete and assert removal.
        if err := k8sClient.Delete(ctx, cr); err != nil { t.Fatalf("delete: %v", err) }

        require.Eventually(t, func() bool {
            var got achv1alpha1.Environment
            err := k8sClient.Get(ctx, client.ObjectKeyFromObject(cr), &got)
            return apierrors.IsNotFound(err)
        }, 15*time.Second, 250*time.Millisecond, "CR not removed after finalizer drain")

        // OP-02 assertion: NoopClient.DeleteAccessGroup + DeleteTag each called once.
        if litellmCounter.Load() < 2 {
            t.Errorf("expected at least 2 LiteLLM noop calls (DeleteAccessGroup + DeleteTag), got %d", litellmCounter.Load())
        }
    }
    ```

    For Plan 05 Task 4's external-reference reconcilers, the test must ALSO assert the cached file removal:

    1. Before deletion, write a dummy file at `<test-cache-root>/plugin/<cr-name>.tar.gz` (or kind-appropriate path).
    2. After deletion, assert `os.Stat(...)` returns `IsNotExist`.

    Use `t.TempDir()` as the per-test cache root and inject via the reconciler's `CacheRoot` field at suite registration time. Suite_test.go's reconciler registration uses a single shared `testCacheRoot string` populated by suite_test.go via `t.TempDir`-equivalent (use `os.MkdirTemp` since `t.TempDir` is per-test, not per-suite).

    Six files, one per kind. For `BackendIdentityPolicyReconciler`, no file-cleanup assertion (it has no PVC presence) — just the finalizer add+remove.

    Use `stretchr/testify/require` for `Eventually` (or implement a tiny poll helper inline to keep deps minimal — preferred). The sister project uses Ginkgo; this plan uses stdlib `testing` for consistency with credhash/config/cachefs tests.

    Add `github.com/stretchr/testify` to `go.mod` only if not already present (kubebuilder may have added it).
  </action>
  <verify>
    <automated>cd /home/jcm/Projects/ach && go test -count=1 -run "TestEnvironment|TestPlugin|TestArtifact|TestPrompt|TestPluginMarketplace|TestBackendIdentityPolicy" ./internal/controller/...</automated>
  </verify>
  <acceptance_criteria>
    - Six files exist, one per kind, each containing `TestXxxFinalizerAddRemove`.
    - EnvironmentReconciler test asserts `litellmCounter.Load() >= 2` (DeleteAccessGroup + DeleteTag noop calls).
    - Four external-reference tests pre-create a file/directory under a test cache root, then assert it is gone after CR deletion (OP-12 assertion).
    - BackendIdentityPolicyReconciler test asserts finalizer add+remove without file-cleanup assertion.
    - `go test -run "TestEnvironment|TestPlugin..." ./internal/controller/...` exits 0 (when envtest assets present).
  </acceptance_criteria>
  <done>Six finalizer tests cover CRD-06 (registration) + OP-02 (Environment drain ordering) + OP-12 (external-ref cached file cleanup).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Author `test/e2e/phase1_invariants_test.go` (build-tag `e2e`) — cluster assertions for SC #1-5</name>
  <files>test/e2e/e2e_suite_test.go, test/e2e/phase1_invariants_test.go, Makefile</files>
  <read_first>
    - /home/jcm/Projects/ach_litellm/test/e2e/suite_test.go (sister e2e suite pattern, 150 lines)
    - .planning/phases/01-foundation-crds-db-schema-operator-skeleton-multi-tenancy/01-PATTERNS.md (lines 1120-1127 — e2e suite test pattern)
    - .planning/ROADMAP.md (Phase 1 Success Criteria 1-5 verbatim)
    - config/default/kustomization.yaml (Plan 08 Task 5 — manifest aggregation)
  </read_first>
  <action>
    Create `test/e2e/e2e_suite_test.go` with leading `//go:build e2e` build tag; the suite resembles sister `suite_test.go` but uses stdlib `testing` rather than Ginkgo. Required:

    1. `TestMain` that ensures `kind` and `kubectl` are available; reads `OPERATOR_IMAGE` env var (default `ach-operator:latest`); ensures a kind cluster exists (creates `ach-e2e` if not); loads the five container images into the kind cluster (`kind load docker-image`); applies `kustomize build config/default/`; waits for all Deployments to roll out.
    2. After `m.Run()`, optionally tears down the cluster (controlled by `E2E_KEEP_CLUSTER`).

    Create `test/e2e/phase1_invariants_test.go` with the same build tag. One test function: `TestPhase1Invariants(t *testing.T)`. It runs the following subtests, one per Success Criterion:

    - **SC #1 — CEL admission**: `kubectl apply -f config/samples/ach_v1alpha1_environment.yaml` exits 0; `kubectl apply -f test/fixtures/invalid/artifact_http_with_directory_scope.yaml` exits non-zero AND stderr contains `scope=object`.
    - **SC #2 — Pod topology**: `kubectl get pod -l app.kubernetes.io/component=operator -n ach-system -o jsonpath='{.items[0].status.containerStatuses[*].ready}'` returns `true true` (two trues). `kubectl get pvc ach-operator-cache -n ach-system -o jsonpath='{.status.phase}'` returns `Bound`.
    - **SC #3 — Environment drain**: create an Environment; `kubectl delete environment example -n ach-system --wait=true` returns within 30s (no hang).
    - **SC #4 — Postgres tables + pepper outside DB, sourced from Secret via wiring**: Four required checks, all in one subtest:
        1. `kubectl exec -n ach-system <postgres-pod> -- psql -U ach -c '\dt'` lists exactly 4 tables (`personal_keys`, `environment_keys`, `external_refs`, `marketplace_plugins`).
        2. `kubectl exec ... psql -c "SELECT column_name FROM information_schema.columns WHERE table_name IN ('personal_keys','environment_keys') AND column_name LIKE '%plaintext%'"` returns zero rows (DB-04: no plaintext column anywhere).
        3. `kubectl get secret ach-credential-hash-pepper -n ach-system` returns the Secret object (existence check).
        4. **Wiring assertion (B2)** — prove the Operator Deployment sources `ACH_CREDENTIAL_HASH_PEPPER` from the Secret via `valueFrom.secretKeyRef`, NOT as an inline literal:
           - `kubectl get deployment ach-operator -n ach-system -o jsonpath='{.spec.template.spec.containers[?(@.name=="manager")].env[?(@.name=="ACH_CREDENTIAL_HASH_PEPPER")].valueFrom.secretKeyRef.name}'` MUST return the literal string `ach-credential-hash-pepper` (NOT a literal pepper value, NOT empty).
           - `kubectl get deployment ach-operator -n ach-system -o yaml | awk '/name: ACH_CREDENTIAL_HASH_PEPPER/,/^      - name:/'` MUST NOT contain the substring `value:` adjacent to that env entry — only `valueFrom:` is permitted (inline literal env values are forbidden).
        5. **Placeholder-refusal probe (B2 / fulfills the T-08-01 promise in Plan 08)** — temporarily reset the Operator Deployment to use the as-shipped placeholder Secret value `REPLACE-ME-WITH-RANDOM-32-BYTES-FROM-OPENSSL-RAND-BASE64-32` (which is the literal text from `config/secrets/credential_hash_pepper_secret.yaml`), restart the Pod, and assert the operator container exits non-zero within 30s with a log line containing the substring `placeholder` or `REPLACE-ME`. After the probe, restore the prior Secret value and wait for the Deployment to roll out Ready again. If the probe cannot be made hermetic in the kind environment (e.g., the operator main currently does not screen for the literal placeholder text), the subtest MUST hard-fail with a TODO marker pointing at the operator startup path — do NOT silently pass.
    - **SC #5 — RBAC inspection**: `kubectl auth can-i create environments.ach.ackstorm.ai --as=system:serviceaccount:ach-system:ach-platform-api` returns `no`. Similar checks for forwarder + content-service. `kubectl auth can-i patch plugins.ach.ackstorm.ai --as=system:serviceaccount:ach-system:ach-platform-api` returns `yes`.

    Each subtest uses `test/utils.Run(...)` (from Task 2) to shell out to `kubectl`. Failures include the full kubectl stderr in the test failure.

    Skip the entire test if `E2E_CLUSTER` env var is unset and `kind` is not in PATH.

    Add Makefile target:
    ```
    .PHONY: test-e2e
    test-e2e:
        go test -tags=e2e -count=1 -timeout 30m ./test/e2e/...
    ```
  </action>
  <verify>
    <automated>cd /home/jcm/Projects/ach && go vet -tags=e2e ./test/e2e/... && grep -q "test-e2e:" Makefile</automated>
  </verify>
  <acceptance_criteria>
    - Files `test/e2e/e2e_suite_test.go` and `test/e2e/phase1_invariants_test.go` exist with `//go:build e2e` build tag on line 1.
    - `TestPhase1Invariants` declares five subtests (one per SC).
    - The test asserts ALL FIVE SCs by name (grep -c "SC #" in the test file ≥ 5).
    - Makefile has `test-e2e:` target.
    - `go vet -tags=e2e ./test/e2e/...` exits 0.
    - Running `make test-e2e` against a kind cluster with all images loaded asserts all five SCs (this acceptance criterion is observational; CI runs it).
  </acceptance_criteria>
  <done>Five Phase 1 Success Criteria become automated test cases. Once `make test-e2e` is green, Phase 1 is verifiably complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test→cluster | e2e tests shell out to `kubectl` and `psql` — privileges scoped to the kind cluster's local admin context. No production cluster ever runs these. |
| test→testcontainers | Postgres + Redis containers are ephemeral; lifetime bounded by `t.Cleanup`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | test asserts wrong CEL message substring and lets a real bug through | mitigate | Each invalid fixture's `errMustContain` substring matches a fragment of the CEL `message:` text defined in Plan 02 Task 2 — coordination via Plan 02's SUMMARY. Plan 11's `cel_admission_test.go` `errMustContain` table is reviewed against the SUMMARY. |
| T-11-02 | Information Disclosure | testcontainers Postgres credentials leak to log | accept | testcontainers-go uses ephemeral random credentials; container exits at test end. No production parity. |
| T-11-03 | Denial of Service | e2e tests hang on Pod-not-Ready | mitigate | Every wait has a timeout (`--timeout=2m` on kubectl rollouts; 30s polling caps in test code). Sister's `e2e_suite_test.go` ships timeouts; we mirror. |
| T-11-04 | Elevation of Privilege | e2e tests run with cluster-admin against shared cluster | mitigate | Tests target a local kind cluster (`ach-e2e`) created by `TestMain`. `E2E_KEEP_CLUSTER` env var defaults to delete-on-exit. Never run against production. |
| T-11-05 | Tampering | counting noop client miscounts (race) | mitigate | `litellmCounter` is `atomic.Int64`; each method call uses `Add(1)`. No data race possible. |
| T-11-06 | Information Disclosure | e2e test logs the placeholder pepper Secret content | accept | Plan 08 ships an obvious placeholder. Phase 7 Helm fails on placeholder; e2e tests do not need to assert pepper value, only Secret EXISTENCE (`kubectl get secret`). |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No npm/pip/cargo installs. Go modules only (`testify` if added; `testcontainers-go` already present from Plan 03). |
</threat_model>

<verification>
- `make test` runs envtest CEL admission + six finalizer tests; exits 0.
- `make test-integration` runs the testcontainers Postgres+migrate suite; exits 0.
- `make test-e2e` (build-tag-gated) runs the five SC subtests against a kind cluster; exits 0.
- Seven invalid fixtures all rejected by CEL admission with matching error substrings.
</verification>

<success_criteria>
All five Phase 1 Success Criteria from ROADMAP.md become automated tests:
- SC #1 (valid CRs accepted, invalid CRs rejected) → `TestCELAdmission`.
- SC #2 (two ready containers + PVC bound) → `TestPhase1Invariants/SC2`.
- SC #3 (Environment drain) → `TestEnvironmentFinalizerAddRemove` + `TestPhase1Invariants/SC3`.
- SC #4 (four tables + pepper in Secret) → testcontainers `TestOpenAndMigrate` + `TestPhase1Invariants/SC4`.
- SC #5 (RBAC inspection) → `TestPhase1Invariants/SC5` (`kubectl auth can-i` matrix).

A reviewer who runs `make test test-integration test-e2e` and gets three green runs has objectively shipped Phase 1.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-crds-db-schema-operator-skeleton-multi-tenancy/01-11-SUMMARY.md` when done. Include the verification command matrix: which SC is verified by which test file + function name, so Phase 2+ planners know which assertions remain green and which need extension.
</output>
