---
phase: 03-hub-identity-platform-api
plan: 11
plan_id: 03-11
type: execute
wave: 4
depends_on: [03-01, 03-02, 03-03, 03-04, 03-05, 03-06, 03-07, 03-08, 03-09, 03-10]
files_modified:
  - cmd/platform-api/main.go
  - internal/platformapi/server.go
  - internal/platformapi/server_test.go
  - internal/platformapi/runnable.go
  - internal/platformapi/doc.go
  - config/rbac/platformapi_role.yaml
  - docker-compose.yml
  - scripts/dex-config.yaml
autonomous: true
requirements: [API-01]
must_haves:
  truths:
    - "cmd/platform-api/main.go bootstraps: env var validation (ACH_BASE_URL HTTPS-only, ACH_DEX_*, ACH_REDIS_*, ACH_ADMIN_ALLOWLIST_PATH, ACH_PLATFORM_API_BIND_ADDRESS), pgxpool, go-redis, litellm.RESTClient, audit.Logger, OIDC provider, allowlist, manager.Manager (informer-only, no leader election, no controllers, no metrics-serve), chi.Mux server wrapped as manager.Runnable"
    - "internal/platformapi/server.go assembles all middleware (Plan 03-05) + mounts auth (Plan 03-07) outside authn group + mounts envkeys/environments/hydrate/admin (Plans 03-08..03-10) inside authn group; all routes under /platform/ prefix per API-01"
    - "BLK-02 wiring: middleware.Authn(deps.Resolver, deps.Allowlist, deps.Audit) — allowlist passed as positional arg so KeyContext.IsAdmin is populated uniformly for downstream handlers"
    - "BLK-03 wiring: hydrate.Deps{LiteLLM: deps.LiteLLM, ...} — hydrate handler receives the LiteLLM client as a first-class dep (consumed by internal/platformapi/teams.LookupCallerTeams per WARN-06)"
    - "config/rbac/platformapi_role.yaml ADDS get/list/watch on secrets in deployment namespace (mirrors operator_role.yaml Phase 2 D-11)"
    - "docker-compose.yml ADDS dex service under profiles:[dex] + scripts/dex-config.yaml ships a minimal Dex config for local integration testing"
    - "Process refuses to start if ACH_BASE_URL doesn't begin with https:// (T-API-04 / Hub §9.1 / CLAUDE.md security bullet — qualified for Platform API ingress)"
    - "HTTP server config: ReadHeaderTimeout 5s, ReadTimeout 30s, WriteTimeout 30s, IdleTimeout 120s, MaxHeaderBytes 1 MiB"
    - "Graceful shutdown via http.Server.Shutdown(ctx, 10s) tied to manager signal context"
    - "http.Server is configured with ReadHeaderTimeout 5s, ReadTimeout 30s, WriteTimeout 30s, IdleTimeout 120s, MaxHeaderBytes 1 MiB, graceful shutdown via http.Server.Shutdown(ctx, 10s) tied to the manager signal context per D-03."
    - "cmd/platform-api/main.go refuses to start when any of ACH_DEX_ISSUER_URL, ACH_DEX_CLIENT_ID, ACH_DEX_CLIENT_SECRET, ACH_DEX_REDIRECT_URL is unset/empty — the Dex SSO flow (Plan 03-07) cannot function without all four per D-06."
    - "controller-runtime manager.Manager runs in informer-only mode (LeaderElection: false, no controllers registered, MetricsBindAddress: \"0\"); cache is namespace-scoped per MULTI-01; mgr.WaitForCacheSync() gates /readyz per D-20."
    - "Code structure lives under internal/platformapi/{auth,envkeys,environments,hydrate,admin,store,render}/ with cmd/platform-api/main.go composing the dependency graph and internal/platformapi/server.go assembling the chi.Mux per D-24."
    - "chi.Router is the multiplexer wired in internal/platformapi/server.go; auth subtree mounted OUTSIDE authn group, envkeys/environments/hydrate/admin subtrees mounted INSIDE authn group, all routes under /platform/ prefix per D-01."
    - "Admin allowlist (loaded by Plan 03-10 LoadAllowlist at process start from ACH_ADMIN_ALLOWLIST_PATH) is passed positionally into middleware.Authn(Resolver, Allowlist, Audit) so KeyContext.IsAdmin is populated uniformly for downstream handlers (BLK-02 wiring) per D-22."
  artifacts:
    - path: "cmd/platform-api/main.go"
      provides: "Platform API process entrypoint with full Phase 3 wiring"
      contains: "manager.Manager"
    - path: "internal/platformapi/server.go"
      provides: "New(deps Deps) http.Handler that returns the composed chi.Mux"
      contains: "func New"
    - path: "internal/platformapi/runnable.go"
      provides: "serverRunnable implementing manager.Runnable for chi.Mux lifecycle"
      contains: "func (s *serverRunnable) Start"
    - path: "config/rbac/platformapi_role.yaml"
      provides: "Adds secrets get/list/watch (third rules block)"
      contains: "secrets"
    - path: "docker-compose.yml"
      provides: "Adds dex service under profiles:[dex]"
      contains: "ghcr.io/dexidp/dex"
    - path: "scripts/dex-config.yaml"
      provides: "Minimal Dex config for local UAT"
      contains: "issuer:"
  key_links:
    - from: "cmd/platform-api/main.go"
      to: "internal/platformapi.New(deps) (chi.Mux)"
      via: "mgr.Add(serverRunnable{...})"
      pattern: "mgr\\.Add"
    - from: "cmd/platform-api/main.go"
      to: "ACH_BASE_URL refuse-to-start"
      via: "strings.HasPrefix(baseURL, \"https://\") gate"
      pattern: "https://"
    - from: "internal/platformapi/server.go"
      to: "All 4 endpoint subtree packages: auth, envkeys, environments, hydrate, admin"
      via: "chi.Mux mounts + authn-gated group"
      pattern: "auth\\.|envkeys\\.|environments\\.|hydrate\\.|admin\\."
---

<objective>
Wire everything together. The Phase 1 stub `cmd/platform-api/main.go` (~75 LoC `/healthz`-only binary) becomes the full Phase 3 process: env var validation, dependency construction, informer-only manager.Manager, chi.Mux server registered as manager.Runnable. Plus the supporting infrastructure: RBAC amendment for secret-informer access, docker-compose Dex service, Dex config file.

Purpose: D-24 (code structure) + D-06 (Dex env vars) + D-20 (manager.Manager informer-only) + API-01 (all endpoints under /platform/ prefix). This plan is what makes `cmd/platform-api/` runnable in production-shape.

Output: 1 modified entrypoint + 3 new files in `internal/platformapi/` + 1 modified RBAC + 1 modified compose + 1 new Dex config.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hub-identity-platform-api/03-CONTEXT.md
@.planning/phases/03-hub-identity-platform-api/03-PATTERNS.md
@ach_hub_spec_v20260515_FINALv4.md
@.planning/phases/03-hub-identity-platform-api/03-01-PLAN.md
@.planning/phases/03-hub-identity-platform-api/03-02-PLAN.md
@.planning/phases/03-hub-identity-platform-api/03-03-PLAN.md
@.planning/phases/03-hub-identity-platform-api/03-04-PLAN.md
@.planning/phases/03-hub-identity-platform-api/03-05-PLAN.md
@.planning/phases/03-hub-identity-platform-api/03-06-PLAN.md
@.planning/phases/03-hub-identity-platform-api/03-07-PLAN.md
@.planning/phases/03-hub-identity-platform-api/03-08-PLAN.md
@.planning/phases/03-hub-identity-platform-api/03-09-PLAN.md
@.planning/phases/03-hub-identity-platform-api/03-10-PLAN.md
@cmd/operator/main.go
@cmd/platform-api/main.go
@config/rbac/platformapi_role.yaml
@config/rbac/operator_role.yaml
@docker-compose.yml
</context>

<dependencies>
All Wave 1-3 plans (03-01..03-10). This is the terminal wiring node.

External: existing controller-runtime + chi + go-oidc + redis + pgx + go-redis from prior plans. No new go.mod entries.
</dependencies>

<interfaces>
**Deps composition (cmd/platform-api/main.go assembles, package-level Deps from each subpackage consume their slice):**

```
// internal/platformapi/server.go (NEW)
type Deps struct {
    Pool       *pgxpool.Pool
    Redis      *redis.Client
    LiteLLM    litellm.Client
    Pepper     []byte
    Allowlist  map[string]struct{}
    OIDCProvider *oidc.Provider
    OAuth2Cfg  *oauth2.Config
    K8sClient  client.Client
    Store      *store.Store
    Resolver   keystore.Resolver
    Audit      *slog.Logger
    Logger     *slog.Logger
    BaseURL    string
    Namespace  string
}
```

Each subpackage's local `Deps` struct (auth.Deps, envkeys.Deps, ...) is a SUBSET of this top-level Deps. server.go constructs each subpackage's Deps from the top-level one.

**chi.Mux structure (per Patterns map):**

```
r := chi.NewRouter()
r.Use(middleware.RequestID)
r.Use(middleware.RecoverPanic(deps.Audit))
r.Use(middleware.AccessLog(deps.Logger))
r.Use(middleware.ContentTypeJSON)

// Unauthenticated endpoints
r.Get("/healthz", health)
r.Get("/livez", health)
r.Get("/readyz", readiness(deps.Pool, deps.Redis))
r.Get("/platform/auth/login", auth.LoginHandler(authDeps))
r.Get("/platform/auth/sso/callback", auth.CallbackHandler(authDeps))

// Authenticated subtree — BLK-02: Authn takes allowlist as a positional arg so
// KeyContext.IsAdmin is populated uniformly for downstream handlers.
r.Group(func(r chi.Router) {
    r.Use(middleware.Authn(deps.Resolver, deps.Allowlist, deps.Audit))
    r.Post("/platform/hydrate", hydrate.HydrateHandler(hydrateDeps))
    r.Route("/platform/env-keys", envkeys.Mount(envkeysDeps))
    r.Route("/platform/environments", environments.Mount(envDeps))
    r.Route("/platform/admin", admin.Mount(adminDeps))
})
```

Every API-01 invariant is testable here: NO route exists outside `/platform/` (except health probes and the SSO callback which lives under /platform/auth/).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement internal/platformapi/server.go + runnable.go (chi.Mux assembly + manager.Runnable wrap)</name>
  <objective>Ship the router constructor that assembles all Phase 3 endpoint subtrees with the correct middleware chain + unauthenticated carve-outs. Plus the manager.Runnable wrapper so the HTTP server lifecycle ties to the controller-runtime manager's signal context.</objective>
  <read_first>
    - .planning/phases/03-hub-identity-platform-api/03-PATTERNS.md "internal/platformapi/server.go (NEW)" — full router skeleton
    - .planning/phases/03-hub-identity-platform-api/03-CONTEXT.md D-24 (code structure) + D-02 (middleware chain order) + D-03 (http.Server config)
    - cmd/operator/main.go lines 382-387 (manager.Runnable usage analog — `mgr.Add(orphanRunnable)` pattern)
    - .planning/phases/03-hub-identity-platform-api/03-05-PLAN.md (middleware constructors: RequestID, RecoverPanic, AccessLog, ContentTypeJSON, Authn)
    - .planning/phases/03-hub-identity-platform-api/03-07-PLAN.md (auth.LoginHandler, auth.CallbackHandler)
    - .planning/phases/03-hub-identity-platform-api/03-08-PLAN.md (envkeys.Mount)
    - .planning/phases/03-hub-identity-platform-api/03-09-PLAN.md (environments.Mount + hydrate.HydrateHandler)
    - .planning/phases/03-hub-identity-platform-api/03-10-PLAN.md (admin.Mount)
    - ach_hub_spec_v20260515_FINALv4.md §15.5 + API-01 (all platform-api routes under /platform/ prefix)
    - sigs.k8s.io/controller-runtime/pkg/manager godoc — `manager.Runnable` interface: `Start(ctx context.Context) error`
  </read_first>
  <behavior>
    Tests use a fully-constructed `Deps` with fakes (no real LiteLLM/Redis/Postgres):

    - Test S-1 (mounts unauthenticated routes): GET /healthz, /livez, /readyz, /platform/auth/login, /platform/auth/sso/callback all reachable WITHOUT x-ach-key header → not rejected by Authn middleware (responses may be 200/302/400/etc depending on handler, but NOT 401 from middleware).
    - Test S-2 (mounts authenticated routes): POST /platform/hydrate, POST /platform/env-keys, GET /platform/environments, POST /platform/admin/keys/revoke WITHOUT x-ach-key → 401 (Authn middleware rejects).
    - Test S-3 (all routes under /platform/ except health probes): construct chi.Mux; enumerate all routes via `chi.Walk`; assert every path either starts with `/platform/` OR matches one of `/healthz`, `/livez`, `/readyz`. NO `/v1/...` routes (API-01 forbids `/v1/hydrate`).
    - Test S-4 (middleware order): handler instrumented to inspect ctx — verifies RequestID populated before Authn runs; verifies KeyContext populated by Authn before handler runs.
    - Test S-5 (Content-Type defaulted): handler that writes 200 with no Content-Type → response has `application/json; charset=utf-8`.
    - Test S-6 (Recover gate): handler that panics → 500 with render.Error envelope; subsequent request still works.
    - Test R-1 (serverRunnable lifecycle): create runnable with httptest-bound chi.Mux; call Start(ctx); cancel ctx; assert Start returns nil after graceful shutdown within timeout.
    - Test R-2 (serverRunnable error propagation): inject a listener that fails to bind; Start returns the error.
  </behavior>
  <action>
    Sub-step 1a — Create `internal/platformapi/doc.go` describing the package as the chi-based platform-api server with manager.Runnable lifecycle.

    Sub-step 1b — Create `internal/platformapi/server.go`:

    ```
    // Deps is the dependency bag main.go assembles and hands to New().
    type Deps struct {
        Pool         *pgxpool.Pool
        Redis        *redis.Client
        LiteLLM      litellm.Client
        Pepper       []byte
        Allowlist    map[string]struct{}
        OIDCProvider *oidc.Provider
        OAuth2Cfg    *oauth2.Config
        K8sClient    client.Client
        Store        *store.Store
        Resolver     keystore.Resolver
        Audit        *slog.Logger
        Logger       *slog.Logger
        BaseURL      string
        Namespace    string
    }

    // New returns the composed chi.Mux. The mux is wrapped by serverRunnable
    // for manager-driven lifecycle.
    func New(deps Deps) http.Handler {
        r := chi.NewRouter()

        // Middleware chain (D-02 outer -> inner)
        r.Use(middleware.RequestID)
        r.Use(middleware.RecoverPanic(deps.Audit, deps.Logger))
        r.Use(middleware.AccessLog(deps.Logger))
        r.Use(middleware.ContentTypeJSON)

        // Health probes (unauthenticated)
        r.Get("/healthz", healthHandler)
        r.Get("/livez", healthHandler)
        r.Get("/readyz", readyHandler(deps.Pool, deps.Redis))

        // SSO endpoints (unauthenticated; D-02 carve-out)
        authDeps := auth.Deps{
            OIDCProvider: deps.OIDCProvider,
            OAuth2Cfg:    deps.OAuth2Cfg,
            LiteLLM:      deps.LiteLLM,
            Pool:         deps.Pool,
            Pepper:       deps.Pepper,
            Audit:        deps.Audit,
            Logger:       deps.Logger,
            Namespace:    deps.Namespace,
        }
        r.Get("/platform/auth/login", auth.LoginHandler(authDeps))
        r.Get("/platform/auth/sso/callback", auth.CallbackHandler(authDeps))

        // Authenticated subtree — BLK-02: Authn takes the allowlist positional
        // arg so KeyContext.IsAdmin is uniformly populated for downstream handlers.
        r.Group(func(r chi.Router) {
            r.Use(middleware.Authn(deps.Resolver, deps.Allowlist, deps.Audit))

            // BLK-03: hydrate.Deps now exposes LiteLLM litellm.Client as a
            // first-class field (Plan 03-09 ships the contract).
            hydrateDeps := hydrate.Deps{
                Store:     deps.Store,
                LiteLLM:   deps.LiteLLM,
                BaseURL:   deps.BaseURL,
                Allowlist: deps.Allowlist,
                Audit:     deps.Audit,
                Namespace: deps.Namespace,
            }
            r.Post("/platform/hydrate", hydrate.HydrateHandler(hydrateDeps))

            envkeysDeps := envkeys.Deps{
                Pool: deps.Pool, LiteLLM: deps.LiteLLM, Store: deps.Store,
                Redis: deps.Redis, Pepper: deps.Pepper,
                Audit: deps.Audit, Logger: deps.Logger,
                Namespace: deps.Namespace,
            }
            r.Route("/platform/env-keys", envkeys.Mount(envkeysDeps))

            // WARN-06: environments.Deps now carries LiteLLM (for internal/platformapi/teams.LookupCallerTeams).
            envDeps := environments.Deps{
                Store:     deps.Store,
                LiteLLM:   deps.LiteLLM,
                Allowlist: deps.Allowlist,
                Audit:     deps.Audit,
                Namespace: deps.Namespace,
            }
            r.Route("/platform/environments", environments.Mount(envDeps))

            adminDeps := admin.Deps{
                Pool: deps.Pool, LiteLLM: deps.LiteLLM, Redis: deps.Redis,
                K8sClient: deps.K8sClient, Allowlist: deps.Allowlist,
                Audit: deps.Audit, Logger: deps.Logger,
                Pepper: deps.Pepper, Namespace: deps.Namespace,
            }
            r.Route("/platform/admin", admin.Mount(adminDeps))
        })

        return r
    }

    func healthHandler(w http.ResponseWriter, _ *http.Request) {
        w.WriteHeader(http.StatusOK)
    }

    func readyHandler(pool *pgxpool.Pool, redisClient *redis.Client) http.HandlerFunc {
        return func(w http.ResponseWriter, r *http.Request) {
            // Quick health probe: ping DB + Redis.
            ctx, cancel := context.WithTimeout(r.Context(), 2*time.Second)
            defer cancel()
            if err := pool.Ping(ctx); err != nil { http.Error(w, "db unreachable", http.StatusServiceUnavailable); return }
            if err := redisClient.Ping(ctx).Err(); err != nil { http.Error(w, "redis unreachable", http.StatusServiceUnavailable); return }
            w.WriteHeader(http.StatusOK)
        }
    }
    ```

    Sub-step 1c — Create `internal/platformapi/runnable.go`:

    ```
    // serverRunnable implements sigs.k8s.io/controller-runtime/pkg/manager.Runnable
    // so the HTTP server lifecycle ties to the manager's signal context.
    type serverRunnable struct {
        srv    *http.Server
        logger *slog.Logger
    }

    func NewRunnable(addr string, handler http.Handler, logger *slog.Logger) *serverRunnable {
        return &serverRunnable{
            srv: &http.Server{
                Addr:              addr,
                Handler:           handler,
                ReadHeaderTimeout: 5 * time.Second,
                ReadTimeout:       30 * time.Second,
                WriteTimeout:      30 * time.Second,
                IdleTimeout:       120 * time.Second,
                MaxHeaderBytes:    1 << 20,  // 1 MiB
            },
            logger: logger,
        }
    }

    func (s *serverRunnable) Start(ctx context.Context) error {
        errCh := make(chan error, 1)
        go func() { errCh <- s.srv.ListenAndServe() }()

        select {
        case err := <-errCh:
            if err == http.ErrServerClosed { return nil }
            return err
        case <-ctx.Done():
            s.logger.Info("platform-api: signal received, draining")
            shutdownCtx, cancel := context.WithTimeout(context.Background(), 10*time.Second)
            defer cancel()
            return s.srv.Shutdown(shutdownCtx)
        }
    }

    func (s *serverRunnable) NeedLeaderElection() bool { return false }
    ```

    Sub-step 1d — Create `internal/platformapi/server_test.go` with tests S-1..S-6 + R-1..R-2. For S-3 (route enumeration) use:
    ```
    chi.Walk(mux, func(method, route string, handler http.Handler, _ ...func(http.Handler) http.Handler) error {
        // assert route starts with /platform/ OR matches /healthz|/livez|/readyz
        ...
        return nil
    })
    ```
  </action>
  <verify>
    <automated>./scripts/dev.sh go build ./internal/platformapi/...</automated>
    <automated>./scripts/dev.sh go vet ./internal/platformapi/...</automated>
    <automated>./scripts/dev.sh go test ./internal/platformapi/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Files exist: `internal/platformapi/{doc.go, server.go, runnable.go, server_test.go}`.
    - `grep -nE '^func New\(deps Deps\) http\.Handler' internal/platformapi/server.go` returns exactly one match.
    - `grep -nE '"/healthz"|"/livez"|"/readyz"' internal/platformapi/server.go | wc -l` ≥ 3.
    - `grep -nE '"/platform/auth/login"|"/platform/auth/sso/callback"' internal/platformapi/server.go | wc -l` ≥ 2 (outside authn group).
    - `grep -nE 'r\.Group\(' internal/platformapi/server.go` returns at least one match (authn-gated subtree).
    - `grep -nE 'middleware\.Authn\(deps\.Resolver, deps\.Allowlist, deps\.Audit\)' internal/platformapi/server.go` returns at least one match (BLK-02 — Authn takes allowlist as positional arg).
    - `grep -nE 'LiteLLM:\s*deps\.LiteLLM' internal/platformapi/server.go` returns at least one match in the hydrate.Deps construction (BLK-03 — wiring of first-class LiteLLM field on hydrate.Deps per Plan 03-09 contract).
    - `grep -nE 'NeedLeaderElection' internal/platformapi/runnable.go` returns at least one match (manager.Runnable interface satisfaction).
    - `grep -nE 'ReadHeaderTimeout:\s*5\s*\*\s*time\.Second' internal/platformapi/runnable.go` returns at least one match (D-03 invariant).
    - `grep -nE 'MaxHeaderBytes:\s*1\s*<<\s*20|MaxHeaderBytes:\s*1048576' internal/platformapi/runnable.go` returns at least one match.
    - `grep -nE 'srv\.Shutdown' internal/platformapi/runnable.go` returns at least one match (graceful shutdown).
    - `./scripts/dev.sh go test ./internal/platformapi/... -count=1` exits 0 with all 8 behaviors passing.
    - Test S-3 (no /v1/ routes) acceptance gate verifies API-01.
  </acceptance_criteria>
  <done>server.go assembles chi.Mux with correct middleware order + authn carve-outs; runnable.go wraps as manager.Runnable with D-03 http.Server config; 8 tests pass; API-01 enforced via route enumeration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite cmd/platform-api/main.go (env var validation, manager.Manager, all deps wired)</name>
  <objective>Replace the Phase 1 stub with the full Phase 3 entrypoint. Validate every required env var (refuse-to-start on missing/non-HTTPS base URL), construct all deps in the right order (pepper -> DB -> Redis -> LiteLLM -> OIDC -> allowlist -> manager -> serverRunnable), then mgr.Start with signal-handler context.</objective>
  <read_first>
    - cmd/platform-api/main.go (Phase 1 stub — what's being replaced; ~75 LoC)
    - cmd/operator/main.go (analog for manager construction, env var validation, OS signal handling; ~510 LoC — Phase 1+2)
    - .planning/phases/03-hub-identity-platform-api/03-PATTERNS.md "cmd/platform-api/main.go (REWRITE)" — full pattern map including env var fail-fast + manager construction (LeaderElection: false, MetricsBindAddress: "0", informer-only)
    - .planning/phases/03-hub-identity-platform-api/03-CONTEXT.md D-06 (Dex env vars: ACH_DEX_ISSUER_URL, ACH_DEX_CLIENT_ID, ACH_DEX_CLIENT_SECRET, ACH_DEX_REDIRECT_URL) + D-20 (manager.Manager informer-only + LeaderElection: false)
    - .planning/phases/03-hub-identity-platform-api/03-04-PLAN.md (internal/keys discipline — main.go MUST NOT log plaintext)
    - .planning/phases/03-hub-identity-platform-api/03-05-PLAN.md (keystore.NewCachedResolver constructor)
    - .planning/phases/03-hub-identity-platform-api/03-06-PLAN.md (store.New constructor + manager.Manager interaction)
    - .planning/phases/03-hub-identity-platform-api/03-10-PLAN.md (admin.LoadAllowlist signature)
    - internal/config/config.go (existing Phase 1+2 helpers — MustEnvNonEmpty, EnvOr)
    - internal/credhash/pepperenv/ (Phase 1 — LoadPepper signature)
    - ach_hub_spec_v20260515_FINALv4.md §9.1 (ACH_BASE_URL MUST begin with https://; component refuses to start otherwise) + §5.1 (resolver short-circuit ordering)
  </read_first>
  <behavior>
    Test via integration tests under build-tag `integration`:

    - Test C-1 (refuse-to-start on missing ACH_BASE_URL): set all env vars EXCEPT ACH_BASE_URL; run binary; expect non-zero exit + stderr "ACH_BASE_URL required". (For test: extract the env-var validation into a `validateConfig()` function so it can be unit-tested without spawning a subprocess.)
    - Test C-2 (refuse-to-start on non-HTTPS ACH_BASE_URL): set ACH_BASE_URL="http://foo"; expect refuse-to-start with stderr "ACH_BASE_URL must be https://".
    - Test C-3 (refuse-to-start on missing pepper): pepper invalid; expect refuse-to-start (carry-forward from Phase 1 plan 01-06 placeholder check).
    - Test C-4 (refuse-to-start on missing ACH_DEX_*): each of the four Dex env vars missing causes refuse-to-start.
    - Test C-5 (refuse-to-start on missing ACH_REDIS_ADDR): expect refuse-to-start.
    - Test C-6 (refuse-to-start on missing ACH_DB_URL): expect refuse-to-start (carry-forward Phase 1).
    - Test C-7 (refuse-to-start on missing ACH_LITELLM_*): expect refuse-to-start (carry-forward Phase 2).
    - Test C-8 (ACH_ADMIN_ALLOWLIST_PATH missing file): allowlist path doesn't exist → start succeeds with empty allowlist + WARN log (per D-23).
    - Test C-9 (happy path startup): all env vars set, all deps available; binary starts, /readyz returns 200, /healthz returns 200. Use httptest to drive the server lifecycle without spawning a real binary; OR use a Ginkgo SetupSuite with a real `go run` invocation gated by integration tag.

    Recommend extracting `validateConfig()`, `buildDeps(...)`, and `runServer(deps)` as separate functions so each can be unit-tested without subprocess spawning. main() is then a thin glue layer.
  </behavior>
  <action>
    Sub-step 2a — Read the Phase 1 stub `cmd/platform-api/main.go` to confirm what's being replaced (just `/healthz` mux + http.Server + signal handler).

    Sub-step 2b — Rewrite `cmd/platform-api/main.go` per Patterns map "cmd/platform-api/main.go (REWRITE)":

    Structure:
    1. `func main()` is the entry point.
    2. Extract `validateConfig() (*Config, error)` — fail-fast env var validation:
       - ACH_BASE_URL via `config.MustEnvNonEmpty` — refuse-to-start if missing.
       - `strings.HasPrefix(baseURL, "https://")` — refuse-to-start with explicit error if not.
       - ACH_DB_URL, ACH_LITELLM_BASE_URL, ACH_LITELLM_MASTER_KEY — fail-fast.
       - ACH_DEX_ISSUER_URL, ACH_DEX_CLIENT_ID, ACH_DEX_CLIENT_SECRET, ACH_DEX_REDIRECT_URL — fail-fast.
       - ACH_REDIS_ADDR — fail-fast. ACH_REDIS_PASSWORD, ACH_REDIS_TLS, ACH_REDIS_DB — optional.
       - ACH_ADMIN_ALLOWLIST_PATH — optional, default `/etc/ach/admins/admins.txt`.
       - ACH_PLATFORM_API_BIND_ADDRESS — optional, default `:8080`.
       - POD_NAMESPACE — required for actor composition + namespace-scoped manager cache.
       - Credhash pepper via `pepperenv.Load` — fail-fast (refuse placeholder per Phase 1 plan 01-06 invariant).
    3. Extract `buildDeps(ctx, cfg *Config) (*Deps, *manager.Manager, error)`:
       - Open pgxpool via `db.Open(ctx, cfg.DBURL)`.
       - Create go-redis Client via `redis.NewClient(&redis.Options{Addr: cfg.RedisAddr, ...})`.
       - Create litellm.RESTClient via `litellm.NewRESTClient(cfg.LiteLLMBaseURL, cfg.LiteLLMMasterKey, slog-logger-handle)`.
       - Create audit logger via `audit.NewLogger(os.Stdout)`.
       - Create OIDC provider via `oidc.NewProvider(ctx, cfg.DexIssuerURL)`.
       - Create OAuth2 config via `oauth2.Config{ClientID: cfg.DexClientID, ClientSecret: cfg.DexClientSecret, Endpoint: provider.Endpoint(), RedirectURL: cfg.DexRedirectURL, Scopes: []string{"openid","email","profile"}}`.
       - Load allowlist via `admin.LoadAllowlist(cfg.AllowlistPath, slog-logger)`.
       - Create `manager.Manager` per Patterns map:
         ```
         mgr, err := ctrl.NewManager(ctrl.GetConfigOrDie(), ctrl.Options{
             Scheme:                 scheme,  // includes achv1alpha1
             LeaderElection:         false,
             HealthProbeBindAddress: "0",  // probes via chi.Mux's /readyz instead
             MetricsBindAddress:     "0",
             Cache: cache.Options{
                 DefaultNamespaces: map[string]cache.Config{cfg.Namespace: {}},
             },
         })
         ```
       - Pre-warm Secret informer + Environment informer + the other five ACH CRD informers via `mgr.GetCache().GetInformer(ctx, &kind{})`.
       - Construct `store.New(mgr.GetClient(), cfg.Namespace, ctrl.Log.WithName("store"))`.
       - Construct `keystore.NewDBResolver(pool, pepper)` then `keystore.NewCachedResolver(dbResolver, redisClient, pepper)`.
       - Compose top-level `Deps` struct.
    4. Extract `runServer(ctx, deps, mgr, bindAddr)`:
       - `httpHandler := platformapi.New(deps)`.
       - `runnable := platformapi.NewRunnable(bindAddr, httpHandler, deps.Logger)`.
       - `mgr.Add(runnable)`.
       - `return mgr.Start(ctx)`.
    5. main() glue:
       - `cfg, err := validateConfig()`; on err → log + os.Exit(1).
       - `ctx := ctrl.SetupSignalHandler()`.
       - `deps, mgr, err := buildDeps(ctx, cfg)`; on err → log + os.Exit(1).
       - `if err := runServer(ctx, deps, mgr, cfg.BindAddr); err != nil { log + os.Exit(1) }`.

    Sub-step 2c — Write `cmd/platform-api/main_test.go` with tests C-1..C-9 covering the validateConfig + buildDeps + integration smoke. Use `t.Setenv` for env var manipulation. For C-9 use httptest-bound server + skip the real `mgr.Start` (since envtest is required for a real manager; defer the real e2e to Plan 03-12).

    A simpler approach: tests C-1..C-8 unit-test `validateConfig()` only (no manager construction). Test C-9 becomes an envtest-tag integration test that constructs the full manager + chi.Mux against an envtest control plane.
  </action>
  <verify>
    <automated>./scripts/dev.sh go build ./cmd/platform-api/...</automated>
    <automated>./scripts/dev.sh go vet ./cmd/platform-api/...</automated>
    <automated>./scripts/dev.sh go test ./cmd/platform-api/... -count=1</automated>
    <automated>./scripts/dev.sh make build</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/platform-api/main.go` no longer contains the Phase 1 stub's `http.NewServeMux` line (verified by `grep -c 'http\.NewServeMux' cmd/platform-api/main.go` equaling 0).
    - `grep -nE 'ACH_BASE_URL' cmd/platform-api/main.go` returns at least one match.
    - `grep -nE 'strings\.HasPrefix.*"https://"' cmd/platform-api/main.go` returns at least one match (HTTPS-only gate).
    - `grep -nE 'ACH_DEX_' cmd/platform-api/main.go | wc -l` ≥ 4 (all four Dex env vars).
    - `grep -nE 'ACH_REDIS_' cmd/platform-api/main.go` returns at least one match.
    - `grep -nE 'pepperenv\.Load' cmd/platform-api/main.go` returns at least one match.
    - `grep -nE 'oidc\.NewProvider' cmd/platform-api/main.go` returns at least one match.
    - `grep -nE 'admin\.LoadAllowlist' cmd/platform-api/main.go` returns at least one match.
    - `grep -nE 'LeaderElection:\s*false' cmd/platform-api/main.go` returns at least one match (D-20 invariant).
    - `grep -nE 'MetricsBindAddress:\s*"0"' cmd/platform-api/main.go` returns at least one match (no /metrics in Phase 3).
    - `grep -nE 'mgr\.Add\(' cmd/platform-api/main.go` returns at least one match.
    - `grep -nE 'ctrl\.SetupSignalHandler' cmd/platform-api/main.go` returns at least one match.
    - `./scripts/dev.sh make build` exits 0 (full cross-package build succeeds — proves no import cycles, no signature mismatches).
    - `./scripts/dev.sh go test ./cmd/platform-api/... -count=1` exits 0 with all 9 behaviors passing (subprocess spawn-and-check OR validateConfig() unit tests).
  </acceptance_criteria>
  <done>main.go rewritten: env var fail-fast for every Phase 3 knob; pepper validation; pgxpool/redis/litellm/oidc/allowlist all constructed; informer-only manager.Manager with LeaderElection=false + MetricsBindAddress=0; chi.Mux wrapped as manager.Runnable; signal-driven graceful shutdown. make build succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Amend config/rbac/platformapi_role.yaml + ADD dex service to docker-compose.yml + create scripts/dex-config.yaml</name>
  <objective>Ship the deployment-layer pieces: RBAC carve-out for secret-informer reads (mirrors operator role's Phase 2 D-11 addition), a Dex service under profiles:[dex] for local UAT, and a minimal Dex config file.</objective>
  <read_first>
    - config/rbac/platformapi_role.yaml (existing — Phase 1 plan 01-09 created; verify the two existing rules-blocks: get/list/watch on ACH CRDs + patch on four external-ref kinds)
    - config/rbac/operator_role.yaml (existing — observe the Phase 2 D-11 secret-informer carve-out at lines 44-46)
    - docker-compose.yml (existing — Phase 02.2 D-04 pattern: litellm + litellm-db under profiles:[litellm])
    - .planning/phases/03-hub-identity-platform-api/03-PATTERNS.md sections "config/rbac/platformapi_role.yaml (MODIFY)" + "docker-compose.yml (MODIFY)" — exact yaml fragments
    - .planning/phases/03-hub-identity-platform-api/03-CONTEXT.md (Phase 2's Secret informer pattern — Phase 3 reads the same Dex client secret rotation watch)
    - ach_hub_spec_v20260515_FINALv4.md §17 (LiteLLM decoupling — Dex is separate; ACH does not co-deploy Dex in production; local UAT only) + §5.2 (informer-cache discipline)
    - github.com/dexidp/dex documentation for the minimal config.yaml shape (issuer, storage, web.http, oauth2.skipApprovalScreen, connectors[mockCallback], staticClients)
  </read_first>
  <behavior>
    - Test K-1 (RBAC amendment): after edit, `grep -c 'resources: \["secrets"\]' config/rbac/platformapi_role.yaml` ≥ 1 (third rules-block present).
    - Test K-2 (RBAC still namespace-scoped): `grep -c '^kind: Role$' config/rbac/platformapi_role.yaml` ≥ 1 AND `grep -c '^kind: ClusterRole$' config/rbac/platformapi_role.yaml` equals 0 (MULTI-01 invariant: Role not ClusterRole).
    - Test K-3 (preserved existing rules): the two prior rules-blocks (CRD reads + force-refresh patch) still present; verified by `grep -c 'resources: \["plugins", "prompts", "artifacts", "pluginmarketplaces"\]' config/rbac/platformapi_role.yaml` ≥ 1.
    - Test C-1 (docker-compose dex service): `docker-compose --profile dex config 2>&1 | grep -c 'image: ghcr.io/dexidp/dex'` ≥ 1.
    - Test C-2 (dex profile-gated): `docker-compose config 2>&1 | grep -c 'image: ghcr.io/dexidp/dex'` equals 0 (NOT present without --profile dex).
    - Test C-3 (existing litellm profile preserved): `docker-compose --profile litellm config 2>&1 | grep -c 'image: ghcr.io/berriai/litellm-database'` ≥ 1.
    - Test C-4 (existing postgres/redis services unchanged): `docker-compose config 2>&1 | grep -c 'image: postgres'` ≥ 1.
    - Test D-1 (dex-config.yaml exists + valid YAML): file exists; `python -c "import yaml; yaml.safe_load(open('scripts/dex-config.yaml'))"` exits 0 (use `./scripts/dev.sh` python or system python).
    - Test D-2 (dex-config minimal fields): contains `issuer:`, `web:`, `connectors:`, `staticClients:` top-level keys.
  </behavior>
  <action>
    Sub-step 3a — Read `config/rbac/platformapi_role.yaml` to see current shape (Phase 1 plan 01-09 / Phase 1 SUMMARYs reference lines 11-20 + 25-31).

    Sub-step 3b — APPEND a third rules-block to `config/rbac/platformapi_role.yaml` after the existing patch rule (Patterns map line 1212):
    ```yaml
    # Phase 3 D-04: Dex client secret rotation observation. Namespace-scoped
    # per MULTI-01 (Role, not ClusterRole). Mirrors operator_role.yaml.
    - apiGroups: [""]
      resources: ["secrets"]
      verbs: ["get", "list", "watch"]
    ```

    DO NOT modify the existing two rules-blocks. DO NOT promote to ClusterRole.

    Sub-step 3c — Read `docker-compose.yml` to see current shape. ADD a `dex:` service block under `services:` (alongside `litellm:`, `litellm-db:`, `postgres:`, `redis:`) per Patterns map "docker-compose.yml (MODIFY)":

    ```yaml
    dex:
      profiles: ["dex"]
      image: ghcr.io/dexidp/dex:v2.41.1   # pin to current stable; verify on releases page
      container_name: ach-dev-dex
      volumes:
        - ./scripts/dex-config.yaml:/etc/dex/config.yaml:ro
      ports:
        - "${DEX_PORT:-5556}:5556"
      command: ["dex", "serve", "/etc/dex/config.yaml"]
      healthcheck:
        test: ["CMD-SHELL", "wget -qO- http://localhost:5556/dex/healthz || exit 1"]
        interval: 5s
        timeout: 3s
        retries: 30
        start_period: 10s
    ```

    DO NOT modify `postgres`, `redis`, `litellm`, or `litellm-db` services. ADD dex AFTER all existing services. Verify pin: check `https://github.com/dexidp/dex/releases` for current stable; if v2.41.1 isn't current, use the latest v2.x stable. Document in SUMMARY.

    Sub-step 3d — Create `scripts/dex-config.yaml`:

    ```yaml
    # Minimal Dex config for local UAT — NOT for production.
    # Production deployments use a Helm chart (Phase 7 / DIST-04) or
    # an external Dex deployment.
    #
    # Phase 3 plan 03-11 D-04: scripts/dex-config.yaml mirrors
    # scripts/litellm-config.yaml shape — small YAML, version-pinned,
    # local UAT only.

    issuer: http://localhost:5556/dex

    storage:
      type: memory

    web:
      http: 0.0.0.0:5556

    oauth2:
      skipApprovalScreen: true

    staticClients:
      - id: ach-platform-api
        secret: dev-secret-not-for-prod
        name: 'ACH Platform API (dev)'
        redirectURIs:
          - http://localhost:8080/platform/auth/sso/callback
          - https://localhost:8080/platform/auth/sso/callback

    connectors:
      - type: mockCallback
        id: mock
        name: Example
    ```

    Note: the `mockCallback` connector returns a static user (kilgore@kilgore.trout) for tests; production would use OIDC/LDAP/etc.

    Sub-step 3e — Write test `scripts/test-rbac-dex.sh` (or extend Plan 03-12's invariants suite) covering K-1..K-3 + C-1..C-4 + D-1..D-2. Tests use shell + grep + python (yaml.safe_load) — keep them simple. For C-1..C-4 use `docker-compose config` to render the merged config without bringing services up.
  </action>
  <verify>
    <automated>./scripts/dev.sh test -f config/rbac/platformapi_role.yaml</automated>
    <automated>./scripts/dev.sh test -f scripts/dex-config.yaml</automated>
    <automated>./scripts/dev.sh grep -c 'resources: \["secrets"\]' config/rbac/platformapi_role.yaml</automated>
    <automated>./scripts/dev.sh grep -c '^kind: ClusterRole$' config/rbac/platformapi_role.yaml</automated>
    <automated>./scripts/dev.sh docker-compose config | grep -c 'image: ghcr.io/dexidp/dex' || true</automated>
    <automated>./scripts/dev.sh docker-compose --profile dex config | grep -c 'image: ghcr.io/dexidp/dex'</automated>
    <automated>./scripts/dev.sh python3 -c "import yaml; yaml.safe_load(open('scripts/dex-config.yaml'))"</automated>
  </verify>
  <acceptance_criteria>
    - `config/rbac/platformapi_role.yaml`: contains three rules-blocks (CRD reads + force-refresh patch + secrets get/list/watch).
    - `grep -c 'resources: \["secrets"\]' config/rbac/platformapi_role.yaml` ≥ 1.
    - `grep -c '^kind: ClusterRole$' config/rbac/platformapi_role.yaml` equals 0.
    - `docker-compose --profile dex config` includes the `dex` service.
    - `docker-compose config` (no profile) does NOT include the `dex` service (profile-gated).
    - `scripts/dex-config.yaml` is valid YAML (python yaml.safe_load passes).
    - `scripts/dex-config.yaml` contains the four top-level keys: issuer, web, staticClients, connectors.
    - No modification to docker-compose's existing postgres/redis/litellm/litellm-db services (verified by `git diff docker-compose.yml` showing only additions in the `services:` block AFTER the existing services).
  </acceptance_criteria>
  <done>RBAC amendment ships (secret-informer carve-out namespace-scoped); docker-compose dex service ships under profiles:[dex] (keeps `make dev-up` lean); scripts/dex-config.yaml ships as a valid YAML for local UAT; existing services unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS env vars -> ACH process | fail-fast validation: refuse-to-start on missing or non-HTTPS critical vars |
| K8s manager.Manager -> ACH process | informer cache; signal-context drives graceful shutdown |
| docker-compose dex profile -> local UAT | dev-only; production uses Helm-deployed external Dex |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11-01 | Tampering | misconfigured ACH_BASE_URL (HTTP) | mitigate | strings.HasPrefix(baseURL, "https://") refuse-to-start gate (Test C-2 acceptance gate). T-API-04 alignment |
| T-03-11-02 | Information Disclosure | log of env-var values at startup | mitigate | Log only env-var NAMES that were validated; never values. Pepper, master keys, Dex client secret NEVER logged. main.go does NOT print the connection string (Phase 1 plan 01-10 carry-forward) |
| T-03-11-03 | Tampering | accidental cross-namespace cache | mitigate | manager.Cache.DefaultNamespaces = {cfg.Namespace: {}}. Verified by Plan 03-06 store tests (Task 1 Test 3 — namespace isolation) |
| T-03-11-04 | Denial of Service | leader election creating multi-replica conflict | mitigate | LeaderElection: false hardcoded (D-20 invariant). Test acceptance gate: grep verifies the false constant |
| T-03-11-05 | Tampering | header smuggling / oversized headers | mitigate | http.Server config: MaxHeaderBytes 1 MiB; ReadHeaderTimeout 5s (gosec G112 carry-forward + Phase 3 D-03). T-RESOURCE-01 alignment |
| T-03-11-06 | Denial of Service | slow client | mitigate | ReadTimeout 30s, WriteTimeout 30s, IdleTimeout 120s. T-RESOURCE-02 alignment |
| T-03-11-07 | Tampering | RBAC over-privilege via ClusterRole | mitigate | `grep -c '^kind: ClusterRole$' config/rbac/platformapi_role.yaml` equals 0 (Test K-2 acceptance gate). Role-only per MULTI-01 |
| T-03-11-08 | Information Disclosure | Dex local-UAT secret leak to production | mitigate | scripts/dex-config.yaml hardcodes `secret: dev-secret-not-for-prod` and the file lives in scripts/ (developer-facing). Production deployment uses Helm-rendered config (Phase 7 DIST-04). Documented in dex-config.yaml header comment |
| T-03-11-SC | Tampering | npm/pip/cargo installs | mitigate | No new go.mod entries. Dex docker image pinned (ghcr.io/dexidp/dex:v2.41.1) — pin to a specific tag, never `latest`. Package legitimacy: dexidp is the CNCF Dex project canonical org |
</threat_model>

<verification>
**Phase-level criteria mapped to plan tasks:**
- Phase SC#1..SC#6 — every plan-defined endpoint mounts through Task 1's server.go; smoke test in Plan 03-12 confirms behavioral equivalence.
- API-01 (all endpoints under /platform/ prefix) — Task 1 Test S-3 acceptance gate enumerates all routes.
- D-03 (http.Server timeouts + MaxHeaderBytes) — Task 1 runnable.go acceptance gates.
- D-06 (Dex env vars fail-fast) — Task 2 main.go.
- D-20 (manager.Manager informer-only, LeaderElection: false) — Task 2 acceptance gates.
- MULTI-01 (namespace-scoped Role; never ClusterRole) — Task 3 acceptance gates.
- MULTI-02 carve-out (force-refresh patch on four kinds) — already in Phase 1; Task 3 doesn't change.
- Hub §9.1 (HTTPS-only ACH_BASE_URL) — Task 2 refuse-to-start gate.

**Plan-level verification:**
- `./scripts/dev.sh make build` exits 0 (proves no import cycles, no signature mismatches across all Phase 3 packages).
- `./scripts/dev.sh make test` exits 0 (proves all unit tests across all Phase 3 packages pass).
- `./scripts/dev.sh docker-compose --profile dex config` succeeds (proves dex service is well-formed compose YAML).
- `./scripts/dev.sh grep -c '^kind: ClusterRole$' config/rbac/platformapi_role.yaml` equals 0.
</verification>

<success_criteria>
- All three tasks pass `<verify>` blocks.
- Full Phase 3 codebase builds: `./scripts/dev.sh make build` exits 0.
- Full unit-test suite across Phase 3 packages passes: `./scripts/dev.sh make test` exits 0.
- The Phase 1 stub `cmd/platform-api/main.go` is now a full Phase 3 entrypoint with: env var fail-fast (8+ vars), pepper validation, dependency construction in correct order, informer-only manager.Manager, chi.Mux wrapped as manager.Runnable, signal-driven graceful shutdown.
- API-01 testable via route enumeration in server_test.go (Task 1 Test S-3): no /v1/ routes; everything under /platform/ or health probes.
- HTTPS-only refuse-to-start verified (Task 2 Test C-2).
- LeaderElection: false enforced (Task 2 acceptance gate).
- Local UAT path: `./scripts/dev.sh docker-compose --profile dex up -d dex` brings up Dex; `curl http://localhost:5556/dex/.well-known/openid-configuration` returns the OIDC discovery document.
</success_criteria>

<output>
Create `.planning/phases/03-hub-identity-platform-api/03-11-SUMMARY.md` when done — record:
1. The final Dex image pin chosen (v2.41.1 vs current stable).
2. Any divergence from the Phase 02.2 docker-compose D-04 pattern (e.g. did `dex` need a `depends_on` clause? Should be self-contained per dex-config.yaml's `storage: memory`).
3. Any signature mismatches encountered during `make build` that required minor patches to upstream plans (and which plan/file).
4. Confirmation that `make test` passes across all 12 Phase 3 plans' code; if any test from earlier plans broke due to a Plan 03-11 wiring change, document the fix.
5. The final list of new env vars added to docker-compose .env example (`ACH_BASE_URL`, `ACH_DEX_*`, etc.) so engineers can run the stack locally.
</output>
