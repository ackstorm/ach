---
phase: 03-hub-identity-platform-api
plan: 01
plan_id: 03-01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/litellm/users.go
  - internal/litellm/keygen.go
  - internal/litellm/types.go
  - internal/litellm/client.go
  - internal/litellm/noop.go
  - internal/litellm/client_test.go
autonomous: true
requirements: [API-02]
must_haves:
  truths:
    - "internal/litellm.Client interface exposes UserNew, UserInfoByEmail, TeamMemberAdd, KeyGenerate"
    - "RESTClient implements all four new methods through makeRequest (REL-04 drain+close discipline preserved)"
    - "NoopClient stubs return canned values keeping the compile-time interface canary green"
    - "UserInfo type carries Teams []string field (BLK-01 contract — Plans 03-08 + 03-09 consume info.Teams for the §8.2 step-4 team-intersection check; KEY-11)"
  artifacts:
    - path: "internal/litellm/users.go"
      provides: "UserNew + UserInfoByEmail + TeamMemberAdd methods on RESTClient"
      contains: "func (c *RESTClient) UserNew"
    - path: "internal/litellm/keygen.go"
      provides: "KeyGenerate method on RESTClient"
      contains: "func (c *RESTClient) KeyGenerate"
    - path: "internal/litellm/types.go"
      provides: "UserNewRequest, UserInfo (with Teams []string), TeamMember, TeamMemberAddRequest, KeyGenerateRequest, KeyGenerateResponse"
      contains: "type KeyGenerateRequest struct"
  key_links:
    - from: "internal/litellm/client.go (Client interface)"
      to: "internal/litellm/noop.go (NoopClient stubs)"
      via: "compile-time canary var _ Client = (*NoopClient)(nil)"
      pattern: "var _ Client = \\(\\*NoopClient\\)\\(nil\\)"
    - from: "internal/litellm/keygen.go"
      to: "personal_keys.litellm_token / environment_keys.litellm_token"
      via: "KeyGenerateResponse.Token field"
      pattern: "Token\\s+string"
---

<objective>
Extend the existing `internal/litellm` client surface with the four methods Phase 3's SSO and env-keys flows require: `UserNew`, `UserInfoByEmail`, `TeamMemberAdd`, `KeyGenerate`. This closes the Phase 02.2 D-02 prerequisite — every active ACH key will carry a non-null `litellm_token` from this plan forward.

Purpose: Phase 3 D-25 mandates new LiteLLM REST methods so the SSO callback (Plan 03-07) can verify-or-create a LiteLLM user and the env-keys create handler (Plan 03-08) can mint LiteLLM virtual keys with ACH-supplied bearer plaintext.

Output: 2 new Go files + 3 modified Go files in `internal/litellm/`. All new methods funnel through the existing `makeRequest` so transport discipline (drain+close, typed 401, no-body-in-error-strings) is automatic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hub-identity-platform-api/03-CONTEXT.md
@.planning/phases/03-hub-identity-platform-api/03-PATTERNS.md
@ach_hub_spec_v20260515_FINALv4.md
@internal/litellm/client.go
@internal/litellm/noop.go
@internal/litellm/types.go
@internal/litellm/team.go
@internal/litellm/restclient.go
@internal/litellm/errors.go
</context>

<dependencies>
None — this plan is Wave 1, no inter-plan dependencies.

External: existing `internal/litellm.RESTClient.makeRequest` (Phase 2), `internal/litellm.ErrNotFound` (Phase 2).
</dependencies>

<interfaces>
Existing `Client` interface (`internal/litellm/client.go:61-105`) — Phase 3 appends four method signatures. Compile-time canary `var _ Client = (*RESTClient)(nil)` at line 107 and the matching canary in `noop.go` enforce drift.

Existing `RESTClient.makeRequest(ctx, method, path, body)` signature accepts `nil` body for GETs; returns `([]byte, error)` where the body is the raw response after `redactingRoundTripper` redaction.

Phase 02.2 column shape (already shipped in migration `db/migrations/000003_litellm_token.up.sql`):
- `personal_keys.litellm_token text NULL`
- `environment_keys.litellm_token text NULL`

The `KeyGenerateResponse.Token` field this plan introduces is the value Phase 3 INSERT paths will write into both columns.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 3 LiteLLM request/response types to types.go</name>
  <objective>Introduce six new Go structs that model the request/response shapes of LiteLLM's /user/new, /user/info, /team/member_add, /key/generate endpoints — matching the existing convention in internal/litellm/types.go.</objective>
  <read_first>
    - internal/litellm/types.go (existing — observe `NewTeamRequest`/`UpdateTeamRequest`/`TeamListEntry` patterns at lines 89-148 and the Phase 02.2 `UserKeyInfo`/`ListUserKeysResponse` patterns at lines 270-290)
    - internal/litellm/team.go (analog: `CreateTeam` request/response idiom)
    - .planning/phases/03-hub-identity-platform-api/03-PATTERNS.md section "internal/litellm/types.go (MODIFY)" — exact struct shapes
    - .planning/phases/03-hub-identity-platform-api/03-CONTEXT.md D-25 — method signatures driving the type shapes
    - ach_hub_spec_v20260515_FINALv4.md §6.3, §16.1 — KEY-10 invariant (max_budget always nil for ACH)
  </read_first>
  <behavior>
    - Test 1: `KeyGenerateRequest{Key: "pk_xyz", MaxBudget: nil}` JSON-marshals with `"key":"pk_xyz"` and omits `max_budget` entirely (omitempty + nil pointer).
    - Test 2: `KeyGenerateRequest{Key: "pk_xyz", MaxBudget: float64Ptr(0)}` JSON-marshals with `"max_budget":0` present (pointer-to-zero is intentionally serialized; ACH never does this but the type must support it for round-trip).
    - Test 3: `TeamMemberAddRequest{TeamID: "default", Member: TeamMember{UserID: "u-1", Role: "user"}}` JSON-marshals as `{"team_id":"default","member":{"user_id":"u-1","role":"user"}}`.
    - Test 4: `UserInfo{}` JSON-unmarshals from `{"user_id":"u-1","user_email":"a@b.c","teams":["default","ops"]}` with all three fields populated (UserID, UserEmail, Teams). Teams round-trips both directions; omitted in JSON when nil/empty (omitempty).
  </behavior>
  <action>
    Append six exported structs to `internal/litellm/types.go` (do NOT touch existing struct definitions):

    1. `UserNewRequest` with fields `UserEmail string \`json:"user_email"\``, `UserID string \`json:"user_id,omitempty"\``, `Teams []string \`json:"teams,omitempty"\``.
    2. `UserInfo` with fields `UserID string \`json:"user_id"\``, `UserEmail string \`json:"user_email"\``, `Teams []string \`json:"teams,omitempty"\``. The `Teams` field is required: Phase 3 Plans 03-08 + 03-09 consume `info.Teams` for the §8.2 step-4 team-intersection check (KEY-11). LiteLLM v1.83.10 returns `teams` as a string array on `/user/info` per D-25.
    3. `TeamMember` with fields `UserID string \`json:"user_id"\``, `Role string \`json:"role,omitempty"\``.
    4. `TeamMemberAddRequest` with fields `TeamID string \`json:"team_id"\``, `Member TeamMember \`json:"member"\``.
    5. `KeyGenerateRequest` with fields `UserID string`, `Key string`, `KeyAlias string`, `Models []string`, `MaxBudget *float64`, `Tags []string`, `AccessGroups []string` — all with appropriate `json:"...,omitempty"` tags per the Patterns map. `MaxBudget` MUST be `*float64` (NOT `float64`) — the pointer encodes "JSON null vs not present" so Phase 3 D-25 can pass `nil` to enforce KEY-10 (ACH never sets max_budget).
    6. `KeyGenerateResponse` with fields `Key string \`json:"key"\``, `Token string \`json:"token"\``, `UserID string \`json:"user_id,omitempty"\``, `KeyAlias string \`json:"key_alias,omitempty"\``, `ExpiresAt string \`json:"expires,omitempty"\``.

    Write JSON round-trip tests in `internal/litellm/client_test.go` (extend existing file; do NOT create a new test file). Use table-driven `TestPhase3TypesJSON` with subcases for the four behaviors above.
  </action>
  <verify>
    <automated>./scripts/dev.sh go build ./internal/litellm/...</automated>
    <automated>./scripts/dev.sh go test ./internal/litellm/... -run TestPhase3TypesJSON -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `./scripts/dev.sh go build ./internal/litellm/...` exits 0.
    - `grep -nE 'type (UserNewRequest|UserInfo|TeamMember|TeamMemberAddRequest|KeyGenerateRequest|KeyGenerateResponse) struct' internal/litellm/types.go | wc -l` equals 6.
    - `grep -nE 'MaxBudget \*float64' internal/litellm/types.go` outputs exactly one match (KEY-10 pointer-type invariant).
    - `grep -nE 'Teams\s+\[\]string\s+\`json:"teams,omitempty"\`' internal/litellm/types.go` returns at least one match in the UserInfo struct (BLK-01 contract — consumed by Plans 03-08 + 03-09 for §8.2 team-intersection).
    - `./scripts/dev.sh go test ./internal/litellm/... -run TestPhase3TypesJSON -count=1` exits 0 with all four subcases passing.
    - No existing type in `internal/litellm/types.go` is modified (verified by `git diff` showing only appended lines).
  </acceptance_criteria>
  <done>Six new structs ship in types.go with correct JSON tags; TestPhase3TypesJSON proves marshal/unmarshal round-trips behave per the four behaviors; build remains green across the package.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement UserNew, UserInfoByEmail, TeamMemberAdd in internal/litellm/users.go</name>
  <objective>Ship three RESTClient methods that wrap LiteLLM's user/team management endpoints, all funneling through makeRequest so transport-layer redaction and 4xx-handling are inherited.</objective>
  <read_first>
    - internal/litellm/team.go (analog: CreateTeam at lines 19-29 — POST + JSON unmarshal envelope)
    - internal/litellm/restclient.go (existing makeRequest at lines 110-168; observe ctx/method/path/body signature and error wrapping)
    - internal/litellm/errors.go (ErrNotFound sentinel — 404-handling convention for GET endpoints)
    - internal/litellm/keyinfo.go (Phase 02.2 G1 fix — observe how GET /key/list uses url.QueryEscape and returns ErrNotFound on 404 via makeRequest 4xx wrapping)
    - .planning/phases/03-hub-identity-platform-api/03-PATTERNS.md section "internal/litellm/users.go" — exact method bodies
    - .planning/phases/03-hub-identity-platform-api/03-CONTEXT.md D-25 — semantic contracts
    - ach_hub_spec_v20260515_FINALv4.md §17 — LiteLLM decoupling (REST only; default Team is deployer concern)
  </read_first>
  <behavior>
    - Test 1 (UserNew happy path): httptest.Server responds 200 with `{"user_id":"u-1","user_email":"a@b.c"}`; UserNew returns &UserInfo{UserID:"u-1", UserEmail:"a@b.c"}, no error.
    - Test 2 (UserInfoByEmail not found): httptest.Server responds 404 on /user/info?user_email=missing@x; UserInfoByEmail returns (nil, error) where the error contains "404" or wraps ErrNotFound (whichever convention current makeRequest enforces — assert on the actual return shape; do NOT change makeRequest).
    - Test 3 (UserInfoByEmail happy path): httptest.Server responds 200 with valid JSON; UserInfoByEmail returns *UserInfo, nil.
    - Test 4 (TeamMemberAdd happy path): httptest.Server responds 200; TeamMemberAdd returns nil. Captures the request body and asserts `team_id=default, member.user_id=u-1, member.role=user`.
    - Test 5 (TeamMemberAdd duplicate-add 4xx): httptest.Server responds 400; TeamMemberAdd returns the wrapped error. Caller-side de-dup is the SSO handler's responsibility (D-25), NOT this method's.
  </behavior>
  <action>
    Create new file `internal/litellm/users.go` with package `litellm`. Implement three methods on `*RESTClient`:

    1. `func (c *RESTClient) UserNew(ctx context.Context, req *UserNewRequest) (*UserInfo, error)` — POST `/user/new` with `req`, decode response into `UserInfo`. Mirror `CreateTeam` (team.go lines 19-29) verbatim except for the path and types.

    2. `func (c *RESTClient) UserInfoByEmail(ctx context.Context, email string) (*UserInfo, error)` — GET `/user/info?user_email=<url.QueryEscape(email)>`, body=nil. Decode into `UserInfo`. Use `net/url.QueryEscape` for the email parameter (same idiom as keyinfo.go).

    3. `func (c *RESTClient) TeamMemberAdd(ctx context.Context, teamID, userID, role string) error` — POST `/team/member_add` with `&TeamMemberAddRequest{TeamID: teamID, Member: TeamMember{UserID: userID, Role: role}}`. Return `err` directly from `makeRequest` (no response body to decode for the success path; only assert err==nil).

    DO NOT modify `makeRequest`. The 404-handling convention is whatever makeRequest currently returns; SSO handler (Plan 03-07) will adapt with errors.Is/strings.Contains as needed.

    Write tests in a new `internal/litellm/users_test.go` using `httptest.NewServer` (analog: `internal/litellm/team_test.go`). Use table-driven structure with the five behaviors above. Use `RESTClient` constructed with the httptest server URL as `liteLLMBaseURL`.
  </action>
  <verify>
    <automated>./scripts/dev.sh go build ./internal/litellm/...</automated>
    <automated>./scripts/dev.sh go test ./internal/litellm/... -run TestUser -count=1</automated>
  </verify>
  <acceptance_criteria>
    - File `internal/litellm/users.go` exists with `package litellm` declaration.
    - `grep -cE '^func \(c \*RESTClient\) (UserNew|UserInfoByEmail|TeamMemberAdd)' internal/litellm/users.go` equals 3.
    - `grep -cE 'url\.QueryEscape' internal/litellm/users.go` is at least 1 (UserInfoByEmail uses it).
    - `grep -cE 'c\.makeRequest' internal/litellm/users.go` equals 3 (all three methods funnel through makeRequest).
    - `./scripts/dev.sh go test ./internal/litellm/... -run TestUser -count=1` exits 0.
    - File `internal/litellm/users_test.go` exists and uses `httptest.NewServer` (verified by `grep -c 'httptest.NewServer' internal/litellm/users_test.go` ≥ 1).
  </acceptance_criteria>
  <done>users.go ships three Client methods through makeRequest; users_test.go covers happy + not-found + duplicate-add paths via httptest.Server; build remains green across the package; no other file in `internal/litellm/` is touched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement KeyGenerate in internal/litellm/keygen.go and extend Client interface + NoopClient</name>
  <objective>Ship the KeyGenerate REST method, extend the Client interface with all four Phase 3 methods, and add NoopClient stubs so the compile-time canary stays green.</objective>
  <read_first>
    - internal/litellm/keygen.go (does NOT exist — create new)
    - internal/litellm/team.go (analog: CreateTeam — same POST/JSON-decode shape)
    - internal/litellm/client.go (existing Client interface at lines 61-105; canary at line 107)
    - internal/litellm/noop.go (existing NoopClient stubs at lines 51-98; canary at line 104)
    - internal/litellm/restclient.go (makeRequest at lines 110-168)
    - .planning/phases/03-hub-identity-platform-api/03-PATTERNS.md sections "internal/litellm/keygen.go", "internal/litellm/client.go (MODIFY)", "internal/litellm/noop.go (MODIFY)"
    - .planning/phases/03-hub-identity-platform-api/03-CONTEXT.md D-25 (signature contract) + D-13 (ACH-supplied bearer plaintext via the `key` field)
    - ach_hub_spec_v20260515_FINALv4.md §6.3 + §16.1 — KEY-10 invariant
  </read_first>
  <behavior>
    - Test 1 (KeyGenerate echoes caller-supplied key): httptest.Server responds 200 with `{"key":"pk_abc","token":"litellm-hex-xyz","user_id":"u-1"}`; KeyGenerate returns response with Key="pk_abc", Token="litellm-hex-xyz".
    - Test 2 (KeyGenerate omits max_budget): caller passes `KeyGenerateRequest{Key:"pk_abc", UserID:"u-1", MaxBudget: nil}`; assertion on captured request body confirms `"max_budget"` key does NOT appear in JSON (omitempty + nil pointer).
    - Test 3 (NoopClient.KeyGenerate echoes key + synthesizes token): `noop.KeyGenerate(ctx, &KeyGenerateRequest{Key: "pk_xxx", UserID: "u-1"})` returns response with `Key="pk_xxx"` and `Token="noop-token-u-1"`.
    - Test 4 (NoopClient.UserInfoByEmail returns ErrNotFound by default): `noop.UserInfoByEmail(ctx, "any@x")` returns `(nil, ErrNotFound)`.
    - Test 5 (canary compile-time): `var _ Client = (*RESTClient)(nil)` and `var _ Client = (*NoopClient)(nil)` both compile (asserted via `go vet`).
  </behavior>
  <action>
    Sub-step 3a — Create new file `internal/litellm/keygen.go`:
    - `func (c *RESTClient) KeyGenerate(ctx context.Context, req *KeyGenerateRequest) (*KeyGenerateResponse, error)` — POST `/key/generate`, decode response. Mirror CreateTeam shape verbatim. Add a doc comment referencing Phase 3 D-13 (ACH-supplied plaintext via `req.Key`) + KEY-10 (max_budget is *float64 = nil always for ACH).

    Sub-step 3b — Modify `internal/litellm/client.go` Client interface (lines 61-105):
    - Append four method signatures: `UserNew`, `UserInfoByEmail`, `TeamMemberAdd`, `KeyGenerate` — copy signatures verbatim from Plan 03-PATTERNS line 1051-1071.
    - DO NOT touch any existing method signature or the compile-time canary at line 107.

    Sub-step 3c — Modify `internal/litellm/noop.go`:
    - Append four stub methods on `*NoopClient` per the Patterns map "noop.go (MODIFY)" section. Each method MUST call `c.Log.Info("stub: ...")` and return a canned value:
      - `UserNew` returns `&UserInfo{UserID: "noop-" + req.UserEmail, UserEmail: req.UserEmail}, nil`.
      - `UserInfoByEmail` returns `(nil, ErrNotFound)` — drives unit tests through the UserNew branch deterministically (D-25). Tests that need the existing-user path (e.g. Plan 03-08 Test 1) construct a stand-in `*UserInfo{UserID:..., UserEmail:..., Teams: []string{"a"}}` via an in-test fake — the canned stub is the no-info default.
      - `TeamMemberAdd` returns `nil` unconditionally.
      - `KeyGenerate` returns `&KeyGenerateResponse{Key: req.Key, Token: "noop-token-" + req.UserID, UserID: req.UserID}, nil`.
    - DO NOT touch the canary at line 104.

    Sub-step 3d — Write tests in `internal/litellm/keygen_test.go` (new file) covering test 1 + test 2 above using httptest.Server. Append the NoopClient assertions (test 3 + test 4) to `internal/litellm/client_test.go` in a `TestNoopClient_Phase3` table-driven function. Test 5 is automatic via `go vet`.
  </action>
  <verify>
    <automated>./scripts/dev.sh go build ./internal/litellm/...</automated>
    <automated>./scripts/dev.sh go vet ./internal/litellm/...</automated>
    <automated>./scripts/dev.sh go test ./internal/litellm/... -run TestKeyGenerate -count=1</automated>
    <automated>./scripts/dev.sh go test ./internal/litellm/... -run TestNoopClient_Phase3 -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `./scripts/dev.sh go vet ./internal/litellm/...` exits 0 (canary compile-time assertions hold).
    - `grep -cE '^func \(c \*RESTClient\) KeyGenerate' internal/litellm/keygen.go` equals 1.
    - `grep -nE '^\s+(UserNew|UserInfoByEmail|TeamMemberAdd|KeyGenerate)\(' internal/litellm/client.go | wc -l` is at least 4 (interface signatures present).
    - `grep -nE '^func \(c \*NoopClient\) (UserNew|UserInfoByEmail|TeamMemberAdd|KeyGenerate)' internal/litellm/noop.go | wc -l` equals 4.
    - `grep -cE 'var _ Client = \(\*(RESTClient|NoopClient)\)\(nil\)' internal/litellm/client.go internal/litellm/noop.go` is at least 2 (canaries preserved).
    - All four `./scripts/dev.sh go test ...` invocations exit 0.
    - Captured request body in Test 2 (max_budget=nil) verifies the field is absent from the marshaled JSON.
  </acceptance_criteria>
  <done>KeyGenerate method ships, Client interface extended with four Phase 3 methods, NoopClient stubs added and canaries green; tests prove KeyGenerate echoes caller-supplied key, omits max_budget when nil, and NoopClient's defaults drive the SSO first-time-user branch via ErrNotFound.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Platform API → LiteLLM REST | LiteLLM master key (HMAC-bearer) crosses this hop via Authorization header; redacted by `internal/litellm/transport.go` redactingRoundTripper before logging |
| ACH process → bearer namespace | ACH owns `pk_*`/`ek_*` prefixes; LiteLLM never issues a key in these namespaces (D-13) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Information Disclosure | KeyGenerate request body | mitigate | Caller-supplied `Key` field (ACH plaintext) flows over HTTPS only; `transport.go redactingRoundTripper` lines 42-98 already redacts request bodies to {method, path, status, latency_ms}; nothing else needed at this layer |
| T-03-01-02 | Tampering | Client interface drift | mitigate | Compile-time canary `var _ Client = (*RESTClient)(nil)` + `var _ Client = (*NoopClient)(nil)` fail the build if any method signature drifts (carry-forward Phase 2 invariant) |
| T-03-01-03 | Spoofing | LiteLLM upstream identity | accept | Master-key HMAC bearer + TLS terminate at LiteLLM ingress; ACH does NOT verify LiteLLM's cert pin (deployment-layer concern) |
| T-03-01-04 | Repudiation | LiteLLM-side audit gap | accept | LiteLLM owns its own audit trail; ACH emits no audit event from KeyGenerate alone — caller (SSO/envkeys handler) emits at success/failure. This plan introduces no audit emission |
| T-03-01-SC | Tampering | npm/pip/cargo installs | mitigate | None — this plan adds NO new go.mod entries (uses stdlib + existing internal/litellm dependencies). RESEARCH.md package legitimacy gate not required |
</threat_model>

<verification>
**Phase-level criteria mapped to plan tasks:**

- Phase SC#1 (SSO login creates LiteLLM user + adds to default Team) — this plan supplies the underlying REST methods. Plan 03-07 (SSO handler) orchestrates the flow.
- API-02 (first-SSO LiteLLM user + default Team membership + missing-team failure) — substrate landed here. The failure-mode (`500 default_team_missing`) is emitted by the SSO handler (Plan 03-07) when `TeamMemberAdd` returns a non-success on the literal "default" team ID.
- KEY-10 (no max_budget on first SSO) — type-level enforcement via `*float64` pointer with omitempty; callers pass `nil`.

**Plan-level verification:**
- `./scripts/dev.sh go build ./internal/litellm/...` exits 0
- `./scripts/dev.sh go vet ./internal/litellm/...` exits 0
- `./scripts/dev.sh go test ./internal/litellm/... -count=1` exits 0 across all `TestUser*`, `TestKeyGenerate*`, `TestPhase3Types*`, `TestNoopClient_Phase3` cases
- `grep -rn '^func .*KeyGenerate\|^func .*UserNew\|^func .*UserInfoByEmail\|^func .*TeamMemberAdd' internal/litellm/users.go internal/litellm/keygen.go internal/litellm/noop.go | wc -l` ≥ 8 (RESTClient + NoopClient implementations)
</verification>

<success_criteria>
- All three tasks pass their `<verify>` blocks.
- `./scripts/dev.sh make test` (the package-wide invocation) exits 0 with the four new test functions reported as PASS.
- No file outside `internal/litellm/` is modified by this plan.
- `git diff --stat internal/litellm/` shows exactly 6 files changed: 2 created (`users.go`, `keygen.go`), 3 modified (`types.go`, `client.go`, `noop.go`), 2-3 test files (created/extended).
</success_criteria>

<output>
Create `.planning/phases/03-hub-identity-platform-api/03-01-SUMMARY.md` when done — record any LiteLLM-side response-shape surprises (e.g. fields LiteLLM v1.83.10 returns that ACH ignores) so Plan 03-07/03-08 know what to expect.
</output>
