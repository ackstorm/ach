---
phase: 03-hub-identity-platform-api
plan: 06
plan_id: 03-06
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/platformapi/store/store.go
  - internal/platformapi/store/store_test.go
  - internal/platformapi/store/doc.go
  - internal/platformapi/store/types.go
autonomous: true
requirements: [API-08]
must_haves:
  truths:
    - "internal/platformapi/store reads Environment + external-ref CRDs via controller-runtime's cache-backed client.Client; no API-server round trips at request time"
    - "Store.GetEnvironment returns (nil, nil) on absent + (env, nil) when found; nil deletionTimestamp check is the caller's responsibility"
    - "Store.ListAuthorizedEnvironments filters by spec.authorizedTeams[] ∩ caller teams; admin variant returns all in namespace"
    - "Store.EnvironmentAccessGroupSynced returns the boolean status of Environment.status.conditions[Type=AccessGroupSynced] (false when missing)"
    - "Store.EnvironmentTerminating returns true when env.DeletionTimestamp != nil"
    - "internal/platformapi/store/ ships GetEnvironment, ListAuthorizedEnvironments, EnvironmentAccessGroupSynced, EnvironmentTerminating reader helpers backed by the informer cache — every Phase 3 Environment read goes through Store, no API-server round trips at request time per D-21."
  artifacts:
    - path: "internal/platformapi/store/store.go"
      provides: "Store struct + GetEnvironment + ListAuthorizedEnvironments + EnvironmentAccessGroupSynced + EnvironmentTerminating"
      contains: "type Store struct"
    - path: "internal/platformapi/store/types.go"
      provides: "EnvironmentView (read-only projection) + condition helper"
      contains: "type EnvironmentView struct"
  key_links:
    - from: "internal/platformapi/store/store.go"
      to: "sigs.k8s.io/controller-runtime/pkg/client.Client (informer-cached)"
      via: "s.client.Get / s.client.List with client.InNamespace(s.ns)"
      pattern: "client\\.InNamespace"
    - from: "Store.ListAuthorizedEnvironments"
      to: "Environment.spec.authorizedTeams[]"
      via: "intersection with callerTeams slice"
      pattern: "authorizedTeams|AuthorizedTeams"
---

<objective>
Ship `internal/platformapi/store/` — the informer-backed reader package that gives Phase 3 handlers sub-millisecond access to Environment + condition + deletionTimestamp state. Every Environment read in Plan 03-08 (env-keys flows), Plan 03-09 (hydrate + environments list), and Plan 03-10 (admin) goes through Store.

Purpose: D-20 + D-21. Hub spec §5.2 mandates informer-cached reads (no API-server round trips at request time); centralizing the read API in one package keeps that discipline observable in code review and testable in envtest.

Output: 4 new files in new `internal/platformapi/store/` package. Zero new go.mod entries (controller-runtime + the existing api/ach/v1alpha1 types are already in go.mod).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hub-identity-platform-api/03-CONTEXT.md
@.planning/phases/03-hub-identity-platform-api/03-PATTERNS.md
@ach_hub_spec_v20260515_FINALv4.md
@internal/snapshot/snapshot.go
@cmd/operator/main.go
</context>

<dependencies>
None — Wave 1 plan (no inter-plan deps on Phase 3). Depends on existing api/ach/v1alpha1 types from Phase 1.

External: existing `sigs.k8s.io/controller-runtime/pkg/client` (Phase 1 dependency).
</dependencies>

<interfaces>
**Consumed (from Phase 1 codebase):**

- `api/ach/v1alpha1.Environment` and `api/ach/v1alpha1.EnvironmentList` — CRD type definitions from Phase 1 plan 01-02
- `sigs.k8s.io/controller-runtime/pkg/client.Client` — cached-read client; consumer obtains via `mgr.GetClient()` after informer sync
- `metav1.Condition` — for condition introspection

**Exposed (new):**

```
type Store struct {
    client client.Client
    ns     string
    log    logr.Logger
}

func New(c client.Client, ns string, log logr.Logger) *Store

func (s *Store) GetEnvironment(ctx context.Context, name string) (*achv1alpha1.Environment, error)
func (s *Store) ListAuthorizedEnvironments(ctx context.Context, callerTeams []string, isAdmin bool) ([]achv1alpha1.Environment, error)
func (s *Store) EnvironmentAccessGroupSynced(ctx context.Context, name string) (bool, error)
func (s *Store) EnvironmentTerminating(ctx context.Context, name string) (bool, error)
```

The handler plans pass `mgr.GetClient()` from Plan 03-11's cmd/platform-api/main.go wiring. The Store is namespace-scoped per MULTI-01 (`s.ns` from `POD_NAMESPACE` env var).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Store struct + GetEnvironment + EnvironmentTerminating + EnvironmentAccessGroupSynced</name>
  <objective>Ship the three single-Environment read helpers. GetEnvironment is the foundation; EnvironmentTerminating wraps the deletionTimestamp check; EnvironmentAccessGroupSynced reads the condition slice the Phase 2 operator writes.</objective>
  <read_first>
    - cmd/operator/main.go lines 305-370 (existing — manager construction, namespace-scoped cache, Secret informer pre-warm)
    - internal/snapshot/snapshot.go (Phase 2 — atomic.Pointer pattern; NOT used here but mental model carry-over)
    - api/ach/v1alpha1/environment_types.go (Phase 1 — Environment + EnvironmentSpec + EnvironmentStatus shapes; verify Conditions field is `[]metav1.Condition`)
    - api/ach/v1alpha1/zz_generated_deepcopy.go (Phase 1 — observe DeepCopy patterns; required for client.Client.Get to populate the destination)
    - .planning/phases/03-hub-identity-platform-api/03-PATTERNS.md sections "internal/platformapi/store/store.go (NEW)" — full struct + method shapes
    - .planning/phases/03-hub-identity-platform-api/03-CONTEXT.md D-20 + D-21 — informer-only manager + cache-served reads
    - ach_hub_spec_v20260515_FINALv4.md §5.2 (informer-cache discipline) + §6.5 (deletionTimestamp semantics) + §6.6 (AccessGroupSynced condition)
    - .planning/phases/02-external-refs-marketplace-operator-reconciliation/02-CONTEXT.md (Phase 2 — verify how AccessGroupSynced condition is currently set by Environment reconciler)
  </read_first>
  <behavior>
    Use controller-runtime's envtest pattern (mirror `internal/controller/ach/main_wiring_envtest_test.go` if it exists; otherwise mirror the test stub):

    - Test 1 (GetEnvironment present): seed an Environment named "prod" in namespace "ach-system"; Store.GetEnvironment(ctx, "prod") returns (*Environment, nil) with Name="prod", Spec/Status populated.
    - Test 2 (GetEnvironment absent): no Environment seeded; Store.GetEnvironment(ctx, "missing") returns (nil, nil) — apierrors.IsNotFound mapped to (nil, nil) for caller ergonomics.
    - Test 3 (GetEnvironment other-namespace isolation): seed Environment "prod" in "other-ns" namespace; Store configured with ns="ach-system"; Store.GetEnvironment returns (nil, nil).
    - Test 4 (EnvironmentTerminating present nil deletionTimestamp): Store.EnvironmentTerminating returns (false, nil).
    - Test 5 (EnvironmentTerminating non-nil deletionTimestamp): seed env with finalizer + DeletionTimestamp; Store.EnvironmentTerminating returns (true, nil).
    - Test 6 (EnvironmentTerminating absent): Store.EnvironmentTerminating returns (false, nil) — absent treated as not-terminating; handler maps this to env_not_found via GetEnvironment.
    - Test 7 (EnvironmentAccessGroupSynced true): seed env with status.conditions = [{Type:"AccessGroupSynced", Status:ConditionTrue, ...}]; returns (true, nil).
    - Test 8 (EnvironmentAccessGroupSynced false explicit): seed env with status.conditions = [{Type:"AccessGroupSynced", Status:ConditionFalse, Reason:"LiteLLMUnreachable"}]; returns (false, nil).
    - Test 9 (EnvironmentAccessGroupSynced missing condition): env with empty Status.Conditions; returns (false, nil) — Phase 3 §8.2 step 3 treats missing as not-ready (503 not_ready).
    - Test 10 (EnvironmentAccessGroupSynced absent env): nonexistent env name; returns (false, nil) — caller did the existence check earlier via GetEnvironment.
  </behavior>
  <action>
    Sub-step 1a — Create `internal/platformapi/store/doc.go`:
    ```
    // Package store ships the informer-backed reader helpers for Platform API
    // handlers. Every Phase 3 read of an Environment / Plugin / Prompt / Artifact /
    // PluginMarketplace / BackendIdentityPolicy CR goes through this package so
    // the cache-served discipline (Hub §5.2) is observable in code review.
    //
    // The constructor accepts a controller-runtime client.Client — the caller
    // (cmd/platform-api/main.go) MUST construct it via mgr.GetClient() AFTER
    // mgr.GetCache() has completed initial list-and-watch sync; otherwise reads
    // fall back to the API server, defeating the cache promise.
    //
    // Namespace scope per MULTI-01: the constructor takes ns from POD_NAMESPACE;
    // all reads are scoped via client.InNamespace(ns).
    package store
    ```

    Sub-step 1b — Create `internal/platformapi/store/store.go`:

    ```
    type Store struct {
        client client.Client
        ns     string
        log    logr.Logger
    }

    func New(c client.Client, ns string, log logr.Logger) *Store {
        return &Store{client: c, ns: ns, log: log}
    }
    ```

    Implement `GetEnvironment(ctx, name)`:
    ```
    env := &achv1alpha1.Environment{}
    if err := s.client.Get(ctx, types.NamespacedName{Namespace: s.ns, Name: name}, env); err != nil {
        if apierrors.IsNotFound(err) {
            return nil, nil
        }
        return nil, fmt.Errorf("store: GetEnvironment(%s): %w", name, err)
    }
    return env, nil
    ```

    Implement `EnvironmentTerminating(ctx, name)`:
    - Call GetEnvironment. If (nil, nil) return (false, nil). If err, propagate.
    - Return `env.DeletionTimestamp != nil`.

    Implement `EnvironmentAccessGroupSynced(ctx, name)`:
    - Call GetEnvironment. If (nil, nil) return (false, nil). If err, propagate.
    - Loop over `env.Status.Conditions`: if `c.Type == "AccessGroupSynced"` return `(c.Status == metav1.ConditionTrue, nil)`.
    - If no matching condition found: return (false, nil).

    Sub-step 1c — Create test file `internal/platformapi/store/store_test.go` using envtest.

    Phase 3 store tests can reuse the existing `internal/controller/ach/*_envtest_test.go` setup pattern — share the envtest binary via Ginkgo BeforeSuite, OR pull in the testenv helper from Phase 1 plan 01-11. Use Ginkgo only if the existing tests use Ginkgo; otherwise stdlib testing.TestMain. Check `internal/controller/ach/` for the existing convention (Phase 1 plan 01-11 used stdlib testing).

    Use `setup.NewEnvtest(t).Start()` style fixture; create test client via `mgr.GetClient()`; seed Environments via `client.Create(ctx, env)`. After seeding, wait for cache to receive the events (`Eventually` polling or `cache.WaitForCacheSync` + explicit Get round-trip).
  </action>
  <verify>
    <automated>./scripts/dev.sh go build ./internal/platformapi/store/...</automated>
    <automated>./scripts/dev.sh go vet ./internal/platformapi/store/...</automated>
    <automated>./scripts/dev.sh go test ./internal/platformapi/store/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Files exist: `internal/platformapi/store/{doc.go, store.go, store_test.go}`.
    - `grep -nE 'type Store struct' internal/platformapi/store/store.go` returns exactly one match.
    - `grep -nE 'func \(s \*Store\) GetEnvironment\(' internal/platformapi/store/store.go` returns exactly one match.
    - `grep -nE 'func \(s \*Store\) EnvironmentTerminating\(' internal/platformapi/store/store.go` returns exactly one match.
    - `grep -nE 'func \(s \*Store\) EnvironmentAccessGroupSynced\(' internal/platformapi/store/store.go` returns exactly one match.
    - `grep -nE 'apierrors\.IsNotFound' internal/platformapi/store/store.go` returns at least one match.
    - `grep -nE 'AccessGroupSynced' internal/platformapi/store/store.go` returns at least one match.
    - `grep -nE 'metav1\.ConditionTrue' internal/platformapi/store/store.go` returns at least one match.
    - `./scripts/dev.sh go test ./internal/platformapi/store/... -count=1` exits 0 with all 10 behaviors passing.
    - Test 3 verifies namespace isolation (other-ns Environment is invisible).
  </acceptance_criteria>
  <done>Store.GetEnvironment / EnvironmentTerminating / EnvironmentAccessGroupSynced ship; namespace-scoped reads verified; envtest demonstrates each helper returns the expected shape for present/absent/terminating/various-condition-states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ListAuthorizedEnvironments + EnvironmentView projection</name>
  <objective>Ship the list helper that filters Environments by caller's LiteLLM team membership (or returns all in namespace for admins). This is the read primitive that powers API-08 (GET /platform/environments).</objective>
  <read_first>
    - internal/platformapi/store/store.go (Task 1 — Store struct, list-via-client.List pattern)
    - .planning/phases/03-hub-identity-platform-api/03-PATTERNS.md section "internal/platformapi/environments/handler.go" — informs the projection shape (`hasIntersect` helper + `toView` mapper)
    - .planning/phases/03-hub-identity-platform-api/03-CONTEXT.md D-21 — namespace-scoped reads only; admin sees all
    - ach_hub_spec_v20260515_FINALv4.md §15.5 (GET /platform/environments contract: filter by authorizedTeams ∩ caller teams; admin sees all; conditions[] verbatim)
    - ach_hub_spec_v20260515_FINALv4.md §6.6 (condition reasons closed set — verbatim in response)
    - api/ach/v1alpha1/environment_types.go (Phase 1 — verify EnvironmentSpec.AuthorizedTeams field shape; expected `[]string`)
  </read_first>
  <behavior>
    Use envtest with seeded Environments:

    - Test 1 (intersection-positive): seed env "prod" with authorizedTeams=["alpha","beta"]; caller teams=["beta","gamma"]; isAdmin=false. ListAuthorizedEnvironments returns [env "prod"].
    - Test 2 (intersection-empty): seed env "prod" with authorizedTeams=["alpha"]; caller teams=["beta"]; isAdmin=false. ListAuthorizedEnvironments returns [].
    - Test 3 (admin override): seed envs "prod" + "stage" with authorizedTeams=["alpha"]; caller teams=["beta"]; isAdmin=true. ListAuthorizedEnvironments returns both envs.
    - Test 4 (empty authorizedTeams): seed env "prod" with authorizedTeams=[]; caller teams=["any"]; isAdmin=false. ListAuthorizedEnvironments returns [] (empty authorizedTeams means no one is authorized; intersection of empty with anything is empty).
    - Test 5 (admin + empty authorizedTeams): same env; isAdmin=true. ListAuthorizedEnvironments returns [env "prod"].
    - Test 6 (namespace isolation): seed env "prod" in "ach-system" + env "prod-other" in "other-ns"; Store ns="ach-system". ListAuthorizedEnvironments (admin or non-admin) only sees "ach-system" envs.
    - Test 7 (conditions[] preserved): seed env with multiple conditions ({Type:"AccessGroupSynced", Status:True}, {Type:"ContentReady", Status:False, Reason:"StaleCacheExpired"}); returned env carries both conditions verbatim.
    - Test 8 (terminating envs visible): seed env with DeletionTimestamp set; ListAuthorizedEnvironments INCLUDES the env (drain semantics are Phase 5 / CS-09 concern; listing during drain is allowed).
  </behavior>
  <action>
    Sub-step 2a — Append to `internal/platformapi/store/store.go`:

    ```
    func (s *Store) ListAuthorizedEnvironments(ctx context.Context, callerTeams []string, isAdmin bool) ([]achv1alpha1.Environment, error) {
        var list achv1alpha1.EnvironmentList
        if err := s.client.List(ctx, &list, client.InNamespace(s.ns)); err != nil {
            return nil, fmt.Errorf("store: ListAuthorizedEnvironments: %w", err)
        }
        out := make([]achv1alpha1.Environment, 0, len(list.Items))
        for _, env := range list.Items {
            if isAdmin || hasIntersect(env.Spec.AuthorizedTeams, callerTeams) {
                out = append(out, env)
            }
        }
        return out, nil
    }

    // hasIntersect returns true if a and b share at least one element.
    // Used for authorizedTeams ∩ callerTeams check.
    func hasIntersect(a, b []string) bool {
        if len(a) == 0 || len(b) == 0 { return false }
        set := make(map[string]struct{}, len(a))
        for _, s := range a { set[s] = struct{}{} }
        for _, s := range b {
            if _, ok := set[s]; ok { return true }
        }
        return false
    }
    ```

    Sub-step 2b — Create `internal/platformapi/store/types.go` with `EnvironmentView`:

    ```
    // EnvironmentView is the JSON projection of an Environment for the
    // GET /platform/environments response. Conditions are copied verbatim
    // from .status.conditions per API-08 / Hub §6.6.
    type EnvironmentView struct {
        Name              string             `json:"name"`
        Spec              EnvironmentSpecView `json:"spec"`
        Conditions        []metav1.Condition `json:"conditions"`
        DeletionTimestamp *metav1.Time       `json:"deletionTimestamp,omitempty"`
    }

    type EnvironmentSpecView struct {
        AuthorizedTeams []string                 `json:"authorizedTeams,omitempty"`
        Runtime         achv1alpha1.EnvironmentRuntime `json:"runtime"`
        Context         achv1alpha1.EnvironmentContext `json:"context"`
    }

    func ToEnvironmentView(env achv1alpha1.Environment) EnvironmentView {
        return EnvironmentView{
            Name: env.Name,
            Spec: EnvironmentSpecView{
                AuthorizedTeams: env.Spec.AuthorizedTeams,
                Runtime:         env.Spec.Runtime,
                Context:         env.Spec.Context,
            },
            Conditions:        env.Status.Conditions,
            DeletionTimestamp: env.DeletionTimestamp,
        }
    }
    ```

    (Field names for Runtime/Context match the actual Phase 1 CRD shape — verify the exact field names in api/ach/v1alpha1/environment_types.go; adjust if Phase 1 uses different casing.)

    Sub-step 2c — Append test cases to `internal/platformapi/store/store_test.go` covering the 8 behaviors above.
  </action>
  <verify>
    <automated>./scripts/dev.sh go build ./internal/platformapi/store/...</automated>
    <automated>./scripts/dev.sh go vet ./internal/platformapi/store/...</automated>
    <automated>./scripts/dev.sh go test ./internal/platformapi/store/... -run "TestListAuthorizedEnvironments|TestToEnvironmentView" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - File `internal/platformapi/store/types.go` exists with `type EnvironmentView struct`.
    - `grep -nE 'func \(s \*Store\) ListAuthorizedEnvironments\(' internal/platformapi/store/store.go` returns exactly one match.
    - `grep -nE 'func hasIntersect\(' internal/platformapi/store/store.go` returns exactly one match.
    - `grep -nE 'client\.InNamespace\(s\.ns\)' internal/platformapi/store/store.go` returns at least one match.
    - `grep -nE 'isAdmin \|\| hasIntersect' internal/platformapi/store/store.go` returns at least one match (short-circuit logic).
    - `grep -nE 'func ToEnvironmentView\(' internal/platformapi/store/types.go` returns exactly one match.
    - `grep -nE 'env\.Status\.Conditions' internal/platformapi/store/types.go` returns at least one match (conditions[] verbatim per API-08).
    - `./scripts/dev.sh go test ./internal/platformapi/store/... -run "TestListAuthorizedEnvironments|TestToEnvironmentView" -count=1` exits 0 with all 8 behaviors passing.
    - Test 3 verifies admin override; Test 6 verifies namespace isolation; Test 7 verifies conditions[] verbatim copy.
  </acceptance_criteria>
  <done>ListAuthorizedEnvironments ships with team-intersection filtering + admin override; EnvironmentView projection preserves conditions[] verbatim per API-08; envtest demonstrates filter behavior across 8 cases; types.go centralizes the projection for the handler plan to import.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller-runtime cache -> Store | informer-cached reads; mutating via Store is impossible (client.Client interface used only for Get/List) |
| Store -> handler | EnvironmentView projection (read-only); namespace scope baked into Store constructor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Tampering | cross-namespace read | mitigate | Every read uses `client.InNamespace(s.ns)`. The Store struct takes `ns` from constructor (cmd/platform-api/main.go reads `POD_NAMESPACE`). Test 6 verifies isolation |
| T-03-06-02 | Information Disclosure | non-admin sees unauthorized env | mitigate | `hasIntersect(env.Spec.AuthorizedTeams, callerTeams) || isAdmin` short-circuit. Empty authorizedTeams ∩ any callerTeams returns false (Test 4 acceptance gate). Admin override is single boolean (no team check) — must be set by handler ONLY after the admin allowlist check (Plan 03-10's responsibility) |
| T-03-06-03 | Tampering | stale cache read | accept | Phase 2 reconciler writes conditions on every reconcile + every 5min requeue (OP-13); Phase 3 readers serve cached state with ≤5min staleness. Hub §5.2 explicitly designs for this; the cache is the runtime source of truth |
| T-03-06-04 | Denial of Service | unbounded ListEnvironments | accept | controller-runtime cache size is bounded by the namespace's Environment count (deployment-layer concern). No request-time DB-side query; just iterating an in-memory slice |
| T-03-06-SC | Tampering | npm/pip/cargo installs | mitigate | No new go.mod entries. Reuses existing controller-runtime + api/ach/v1alpha1 from Phase 1 |
</threat_model>

<verification>
**Phase-level criteria mapped to plan tasks:**
- API-08 (GET /platform/environments filters by authorizedTeams ∩ caller teams; admin sees all; conditions[] verbatim) — Task 2 ships the read primitive; Plan 03-09 wires the handler.
- D-21 (informer-cached reader helpers) — Task 1 + Task 2 ship the four named helpers verbatim.
- Hub §5.2 (informer-cache discipline; readiness gate on cache sync) — Plan 03-11's main.go ensures `mgr.WaitForCacheSync()` before serving; this plan delivers the reader.

**Plan-level verification:**
- `./scripts/dev.sh go build ./internal/platformapi/store/...` exits 0
- `./scripts/dev.sh go vet ./internal/platformapi/store/...` exits 0
- `./scripts/dev.sh go test ./internal/platformapi/store/... -count=1` exits 0
- `grep -rE 's\.client\.(Create|Update|Delete|Patch)' internal/platformapi/store/` returns ZERO matches (read-only invariant — Store cannot mutate)
</verification>

<success_criteria>
- All two tasks pass `<verify>` blocks.
- Read-only invariant: `grep -rE 's\.client\.(Create|Update|Delete|Patch)' internal/platformapi/store/` returns 0 — Store offers ONLY Get + List.
- Namespace isolation testable (Test 3 + Test 6).
- API-08 substrate ready: ListAuthorizedEnvironments returns the correct subset for any (callerTeams, isAdmin) combination.
- Handler plans 03-08 (env-keys §8.2 step 2 GetEnvironment + step 3 EnvironmentAccessGroupSynced) and 03-09 (Hydrate + Environments list) can construct `Store` via the constructor and call the four helpers without further setup.
</success_criteria>

<output>
Create `.planning/phases/03-hub-identity-platform-api/03-06-SUMMARY.md` when done — record the exact field names in `EnvironmentSpec.AuthorizedTeams` / `EnvironmentSpec.Runtime` / `EnvironmentSpec.Context` (case + JSON tags) so Plan 03-09's hydrate handler maps them correctly to the v15.1 response shape.
</output>
