---
phase: 03-hub-identity-platform-api
plan: 08
plan_id: 03-08
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-03, 03-04, 03-05, 03-06]
files_modified:
  - internal/platformapi/envkeys/handler.go
  - internal/platformapi/envkeys/handler_test.go
  - internal/platformapi/envkeys/mount.go
  - internal/platformapi/envkeys/doc.go
autonomous: true
requirements: [KEY-05, KEY-08, KEY-11, API-05, API-06, API-07]
must_haves:
  truths:
    - "POST /platform/env-keys runs the §8.2 8-step flow: caller-type=pk_, GetEnvironment (404), Terminating (404), AccessGroupSynced (503 not_ready), authorizedTeams ∩ caller (403), idempotent LiteLLM user provision, KeyGenerate with server-side ek_ + access_groups=[<env>] + max_budget=nil, INSERT inside tx with LiteLLM compensation on failure"
    - "WARN-03 commit: D-12 step 7 retry addresses ONLY ekid_ PK collisions (regen new ulid, reuse plaintext+LiteLLM key); credential_hash UNIQUE collisions surface as 500 db_insert_failed with LiteLLM compensation (no retry — collision probability ~1/2^128)"
    - "GET /platform/env-keys filters by caller's owner_email for non-admin; admin may pass ?owner_email=<email>; non-admin passing owner_email -> 400 invalid_argument"
    - "GET /platform/env-keys/{key_id} returns the row if owned by caller (non-admin) or any (admin); 404 unknown; 400 if {key_id} doesn't match ekid_ prefix"
    - "DELETE /platform/env-keys/{key_id} validates ekid_ prefix BEFORE lookup (400 invalid_argument); runs §8.5 flow (LiteLLM-first); 204 only AFTER LiteLLM ack; 403 not_key_owner; 503 litellm_unreachable"
    - "Plaintext returned exactly once at POST response; never anywhere else"
    - "Every state-changing op emits audit (ActionEkCreate / ActionEkRevoke) with correct outcome + key.id=ekid_*"
    - "ACH generates the ek_ bearer plaintext server-side via crypto/rand BEFORE calling LiteLLM, then supplies it to /key/generate via the caller-supplied `key` field — ACH remains the sole authority over the `pk_*`/`ek_*` bearer namespace and the §8.2 step 6 plaintext return needs no extra round-trip parse per D-13."
  artifacts:
    - path: "internal/platformapi/envkeys/handler.go"
      provides: "CreateHandler, ListHandler, GetHandler, RevokeHandler"
      contains: "func CreateHandler"
    - path: "internal/platformapi/envkeys/mount.go"
      provides: "Mount(deps) returns chi.Router subtree for /platform/env-keys"
      contains: "func Mount"
  key_links:
    - from: "CreateHandler"
      to: "store.GetEnvironment + store.EnvironmentTerminating + store.EnvironmentAccessGroupSynced (Plan 03-06)"
      via: "deps.Store accessor"
      pattern: "deps\\.Store\\."
    - from: "CreateHandler"
      to: "litellm.KeyGenerate + db.InsertEnvironmentKey + litellm.RevokeKey (compensation)"
      via: "8-step §8.2 sequence per D-12"
      pattern: "KeyGenerate|InsertEnvironmentKey|RevokeKey"
    - from: "RevokeHandler"
      to: "db.GetEnvironmentKey -> litellm.RevokeKey -> db.RevokeEnvironmentKey -> redis.DEL"
      via: "§8.5 LiteLLM-first sequence per D-15"
      pattern: "GetEnvironmentKey|RevokeKey|RevokeEnvironmentKey"
---

<objective>
Ship the env-keys endpoint quartet under `/platform/env-keys`: POST (§8.2 8-step create), GET list (caller-filtered + admin override), GET by id, DELETE (§8.5 LiteLLM-first revoke). These endpoints embody Phase 3's most complex orchestration — server-side ek_ generation, idempotent LiteLLM coordination, asymmetric revocation with LiteLLM as the load-bearing barrier.

Purpose: KEY-05 + KEY-08 + KEY-11 + API-05 + API-06 + API-07 + D-12 + D-13 + D-15. The §8.2 + §8.5 flows are the most-cited test scenarios in Hub spec's success criteria; getting the ordering right (Create: LiteLLM-then-DB-with-compensation; Revoke: LiteLLM-then-DB) is the entire phase's load-bearing invariant for asymmetric revocation.

Output: 4 new files in new `internal/platformapi/envkeys/` package. Zero new go.mod entries (consumes packages from upstream Phase 3 plans).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hub-identity-platform-api/03-CONTEXT.md
@.planning/phases/03-hub-identity-platform-api/03-PATTERNS.md
@ach_hub_spec_v20260515_FINALv4.md
@.planning/phases/03-hub-identity-platform-api/03-01-PLAN.md
@.planning/phases/03-hub-identity-platform-api/03-02-PLAN.md
@.planning/phases/03-hub-identity-platform-api/03-03-PLAN.md
@.planning/phases/03-hub-identity-platform-api/03-04-PLAN.md
@.planning/phases/03-hub-identity-platform-api/03-05-PLAN.md
@.planning/phases/03-hub-identity-platform-api/03-06-PLAN.md
@.planning/phases/03-hub-identity-platform-api/03-07-PLAN.md
@internal/orphan/runnable.go
@internal/credhash/credhash.go
</context>

<dependencies>
- 03-01 (LiteLLM client) — KeyGenerate + RevokeKey + UserInfoByEmail + UserNew
- 03-02 (Audit + Render) — audit constants, render.JSON/Error
- 03-03 (DB helpers) — InsertEnvironmentKey, GetEnvironmentKey, RevokeEnvironmentKey, ListEnvironmentKeysByOwner, ListEnvironmentKeysByOwnerWithFilter
- 03-04 (keys) — NewBearer(PrefixEk), NewKeyID(PrefixEkid), ClassifyBearer (for validating client-supplied input)
- 03-05 (middleware) — KeyContextFromCtx, RequestIDFromCtx, ActorFromCtx + the Authn-gated mount point
- 03-06 (store) — GetEnvironment, EnvironmentTerminating, EnvironmentAccessGroupSynced
</dependencies>

<interfaces>
**Mount signature consumed by Plan 03-11's server.go:**

```
r.Group(func(r chi.Router) {
    r.Use(middleware.Authn(deps.Resolver, deps.Audit))
    // ...
    r.Route("/platform/env-keys", envkeys.Mount(deps))
})
```

`envkeys.Mount(deps Deps) func(r chi.Router)` returns a closure that registers:
- `r.Post("/", envkeys.CreateHandler(deps))`
- `r.Get("/", envkeys.ListHandler(deps))`
- `r.Get("/{key_id}", envkeys.GetHandler(deps))`
- `r.Delete("/{key_id}", envkeys.RevokeHandler(deps))`

**Caller-team membership lookup (KEY-11 step 4):**

For the §8.2 step 4 `authorizedTeams[] ∩ caller's LiteLLM Teams ≠ ∅` check, Phase 3 needs to know the caller's LiteLLM team memberships. The keystore's `KeyInfo` does NOT carry team memberships (those are dynamic). The §8.2 step 4 lookup is a NEW Phase 3 concern — but per D-12 + the broader Hub design, this team-membership lookup is "the cached Team-membership lookup, ≤60s TTL — same cache the Forwarder uses in Phase 4". Phase 3 introduces the cache surface and a placeholder lookup that calls LiteLLM directly for now (Phase 4 will replace with the proper cached lookup that shares Redis).

Phase 3 simplification: import the shared helper `teams.LookupCallerTeams(ctx, deps.LiteLLM, ownerEmail)` from `internal/platformapi/teams/lookup.go` (shipped by Plan 03-05 Task 3 per WARN-06). The helper calls `litellm.UserInfoByEmail` and returns `info.Teams` (the `Teams []string` field is in the Plan 03-01 contract per BLK-01; no UserInfo extension is needed here). Phase 4 will replace the per-request LiteLLM call with a Redis-cached lookup that shares the keystore's 60s TTL; Phase 3 calls LiteLLM directly per request.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement CreateHandler — §8.2 8-step ek_ create flow</name>
  <objective>Ship the most complex Phase 3 endpoint: server-side ek_ generation, idempotent LiteLLM user verify-or-create, KeyGenerate with caller-supplied key + access_groups=[<environment>], transactional INSERT with LiteLLM compensation on failure.</objective>
  <read_first>
    - .planning/phases/03-hub-identity-platform-api/03-PATTERNS.md sections "internal/platformapi/envkeys/handler.go" — CreateHandler skeleton + DB INSERT idiom + LiteLLM compensation pattern
    - .planning/phases/03-hub-identity-platform-api/03-CONTEXT.md D-12 (8-step flow verbatim) + D-13 (server-side plaintext)
    - .planning/phases/03-hub-identity-platform-api/03-07-PLAN.md Task 3 (SSO CallbackHandler — same compensation pattern; learn from its tests)
    - .planning/phases/03-hub-identity-platform-api/03-03-PLAN.md (InsertEnvironmentKey signature + EkInsertRow shape)
    - .planning/phases/03-hub-identity-platform-api/03-06-PLAN.md (store helpers contract)
    - internal/orphan/runnable.go (Phase 2 — RevokeKey error-handling idiom for the compensation step)
    - internal/credhash/credhash.go (Hash signature)
    - ach_hub_spec_v20260515_FINALv4.md §8.2 (the 8-step flow) + §8.3 (error matrix) + §15.5 (POST /platform/env-keys response shape)
    - ach_hub_spec_v20260515_FINALv4.md §6.6 (AccessGroupSynced condition — value source for §8.2 step 3)
  </read_first>
  <behavior>
    Use httptest fakes for LiteLLM + Store + DB (testcontainers-go for the DB tier; envtest for Store; httptest.Server for LiteLLM mock). Or use unit tests with fakes implementing the small interfaces (`fakeStore`, `fakeLiteLLM`, `fakePool`). Prefer the latter for fast unit-loop testing; integration tests come in Plan 03-12.

    - Test 1 (happy path): pk_ caller; env "prod" exists, not terminating, AccessGroupSynced=True, authorizedTeams=["a"], caller teams=["a"]. LiteLLM UserInfoByEmail returns existing user; KeyGenerate returns {key (echo), token}; InsertEnvironmentKey succeeds.
      - Response: 200 with body `{"key_id":"ekid_<26>","plaintext":"ek_<26>","environment":"prod","name":"<from-request>","owner_email":"<caller>","created_at":"<RFC3339>"}`.
      - Audit emitted: ActionEkCreate, OutcomeCreated, key.id=ekid_*, target.kind=environment, target.name=prod.
      - DB row inserted with status=active, litellm_user_id, litellm_token populated.

    - Test 2 (ek_ caller rejected): Authn injected ek_ KeyContext; CreateHandler returns 401 invalid_key_type without further work; no DB, no LiteLLM calls.

    - Test 3 (Environment not found): pk_ caller; env="missing". GetEnvironment returns (nil, nil). Response 404 environment_not_found; audit outcome=environment_not_found.

    - Test 4 (Environment terminating): GetEnvironment returns env with DeletionTimestamp set. Response 404 environment_not_found (terminating treated as not-found per D-12 step 2); audit same.

    - Test 5 (AccessGroupSynced=False): GetEnvironment returns env, Terminating=false, AccessGroupSynced returns (false, nil). Response 503 not_ready; audit outcome=not_ready.

    - Test 6 (authorizedTeams intersection empty): env.Spec.AuthorizedTeams=["x"]; callerTeams=["y"]. Response 403 unauthorized_team; audit outcome=unauthorized_team.

    - Test 7 (first-time LiteLLM user — UserNew called): UserInfoByEmail returns ErrNotFound; UserNew returns 200; TeamMemberAdd(default,user_id,user) succeeds; KeyGenerate proceeds. (Identical to SSO Test 1 except scoped to env-keys; verifies the same idempotent path).

    - Test 8 (KeyGenerate failure): KeyGenerate returns transport error. Response 503 litellm_unreachable; audit outcome=litellm_unreachable; InsertEnvironmentKey NOT called.

    - Test 9 (DB INSERT failure + LiteLLM compensation): KeyGenerate succeeds; InsertEnvironmentKey returns non-transient error; assert deps.LiteLLM.RevokeKey(token) is called exactly once; response 500 db_insert_failed; audit outcome=db_insert_failed.

    - Test 10 (DB INSERT unique-violation retry — WARN-03 split into 10a + 10b):
      - Test 10a (forced credential_hash UNIQUE collision → 500 + LiteLLM revoked): InsertEnvironmentKey returns 23505 on the `environment_keys_credential_hash_key` constraint (simulate via a constraint-name check or by seeding a row with the same credential_hash). Handler classifies as credential_hash collision; calls `deps.LiteLLM.RevokeKey(token)` exactly once (compensation defer); response 500 db_insert_failed; audit OutcomeDbInsertFailed. NO retry attempted; NewKeyID called once.
      - Test 10b (forced ekid_ PK collision → retry with new ulid succeeds): InsertEnvironmentKey returns 23505 on the `environment_keys_pkey` constraint on the first call (simulate via constraint-name check or by pre-seeding a row with the same ekid_); the second InsertEnvironmentKey with a freshly generated ekid_ (same credential_hash, same plaintext, same LiteLLM token) succeeds. Response 200; assert NewKeyID called exactly twice; assert deps.LiteLLM.RevokeKey called ZERO times (the LiteLLM key is reused).
      - Test 10c (ekid_ PK collision on the RETRY too → 500 + LiteLLM revoked): InsertEnvironmentKey returns 23505 on the pkey constraint on BOTH calls. After the second failure, handler calls deps.LiteLLM.RevokeKey(token) once and returns 500 db_insert_failed. Audit OutcomeDbInsertFailed.

    Decision (per WARN-03): D-12 step 7 "retried 1× with new ulid" addresses the **ekid_ PK collision ONLY**, NOT the credential_hash collision. Rationale: credential_hash collisions across the 256-bit HMAC-SHA-256 output space are astronomically unlikely (1 in ~2^128 even at billion-row scale) — the retry mechanism cannot meaningfully reduce this probability without regenerating the plaintext (which then requires compensating the already-issued LiteLLM key). Treat the two collision classes asymmetrically:
    - ekid_ PK collision (pg code 23505 on the `personal_keys_pkey` / `environment_keys_pkey` constraint OR detected via constraint name match): retry once with a fresh `keys.NewKeyID(keys.PrefixEkid)` and the SAME plaintext + credential_hash. The retry reuses the LiteLLM key already issued; no compensation needed.
    - credential_hash UNIQUE collision (pg code 23505 on the `*_credential_hash_key` constraint): treated as a hard failure — return 500 with audit OutcomeDbInsertFailed. The compensating `deps.LiteLLM.RevokeKey(token)` runs in the same compensation defer block as Test 9. No retry.

    - Test 11 (plaintext exactly once in response): inspect response body string — generated plaintext appears EXACTLY ONE TIME in the body. Headers carry no plaintext.

    - Test 12 (DisallowUnknownFields): request body `{"environment":"prod","name":"k1","extra_field":"x"}` rejected with 400 invalid_argument before any other work.
  </behavior>
  <action>
    Sub-step 1a — Create `internal/platformapi/envkeys/doc.go` declaring the package + discipline doc (mirrors auth/doc.go shape; emphasize the §8.2 sequence + LiteLLM-first compensation).

    Sub-step 1b — Create `internal/platformapi/envkeys/handler.go` with `Deps` struct:
    ```
    type Deps struct {
        Pool       *pgxpool.Pool
        LiteLLM    litellm.Client
        Store      *store.Store
        Redis      *redis.Client
        Pepper     []byte
        Audit      *slog.Logger
        Logger     *slog.Logger
        Namespace  string
    }

    // CreateRequest is the POST body shape (D-16 idiom — DisallowUnknownFields).
    type CreateRequest struct {
        Environment string `json:"environment"`
        Name        string `json:"name"`
    }

    // CreateResponse is the §15.5 POST response shape.
    type CreateResponse struct {
        KeyID       string `json:"key_id"`
        Plaintext   string `json:"plaintext"`
        Environment string `json:"environment"`
        Name        string `json:"name"`
        OwnerEmail  string `json:"owner_email"`
        CreatedAt   string `json:"created_at"`
    }
    ```

    Implement `CreateHandler(deps Deps) http.HandlerFunc` per Hub §8.2:

    1. Caller-type guard: read KeyContext via middleware.KeyContextFromCtx(ctx). If `keyCtx.KeyType != keys.PrefixPk` → audit + 401 invalid_key_type.
    2. Strict JSON decode with DisallowUnknownFields → CreateRequest. Empty `environment` → 400 invalid_argument. Empty `name` → 400 invalid_argument.
    3. `env, err := deps.Store.GetEnvironment(ctx, req.Environment)`. If (nil, nil) → audit + 404 environment_not_found.
    4. `term, err := deps.Store.EnvironmentTerminating(ctx, req.Environment)`. If term → audit + 404 environment_not_found.
    5. `synced, err := deps.Store.EnvironmentAccessGroupSynced(ctx, req.Environment)`. If !synced → audit + 503 not_ready.
    6. Team-membership lookup (D-12 step 4 + WARN-06): import the shared helper `import achteams "achkitstorm.ai/internal/platformapi/teams"` and call `achteams.LookupCallerTeams(ctx, deps.LiteLLM, keyCtx.OwnerEmail)`. On transport error → audit OutcomeLitellmUnreachable + 503 litellm_unreachable. Otherwise compute `hasIntersect(env.Spec.AuthorizedTeams, callerTeams)`. If false → audit OutcomeUnauthorizedTeam + 403 unauthorized_team.
    7. Idempotent LiteLLM user provision (D-12 step 5): same idiom as SSO CallbackHandler Task 3 — UserInfoByEmail → if ErrNotFound, UserNew → TeamMemberAdd. NEVER sets max_budget.
    8. Generate plaintext + keyID:
       - `plaintext, err := keys.NewBearer(keys.PrefixEk)` → "ek_<26>".
       - `keyID, err := keys.NewKeyID(keys.PrefixEkid)` → "ekid_<26>".
       - `credentialHash, err := credhash.Hash(deps.Pepper, []byte(plaintext))`.
    9. LiteLLM KeyGenerate (D-12 step 6):
       ```
       keyResp, err := deps.LiteLLM.KeyGenerate(ctx, &litellm.KeyGenerateRequest{
           UserID:       userID,
           Key:          plaintext,
           MaxBudget:    nil,            // KEY-10
           AccessGroups: []string{env.Name},  // D-12 step 6
           Tags:         []string{env.Name},  // §6.3 ek_ Environment tag
       })
       ```
       Error → 503 litellm_unreachable.
    10. INSERT (D-12 step 7):
        ```
        err := db.InsertEnvironmentKey(ctx, deps.Pool, db.EkInsertRow{
            KeyID: keyID, CredentialHash: credentialHash, Environment: req.Environment,
            OwnerEmail: keyCtx.OwnerEmail, Name: req.Name, Status: "active",
            LiteLLMUserID: &userID, LiteLLMToken: &keyResp.Token,
        })
        ```
        If err != nil:
          - If unique-violation on ekid_ (constraint name OR pg code 23505 + key_id PK violation) → retry once with new ekid_ via keys.NewKeyID. If retry also fails → compensate + 500 db_insert_failed.
          - Other failures → compensate (deps.LiteLLM.RevokeKey(ctx, keyResp.Token), best-effort log) + audit OutcomeDbInsertFailed + 500.
    11. Emit audit ActionEkCreate, OutcomeCreated, key.id=keyID, target.kind=environment, target.name=req.Environment.
    12. render.JSON(w, 200, CreateResponse{...}) — plaintext returned EXACTLY ONCE.

    Import the shared helper from Plan 03-05 Task 3: `import achteams "achkitstorm.ai/internal/platformapi/teams"` then call `achteams.LookupCallerTeams(ctx, deps.LiteLLM, keyCtx.OwnerEmail)`. The helper reads `info.Teams` from the `litellm.UserInfo` struct (Teams field shipped by Plan 03-01 per BLK-01). On `ErrNotFound` it returns `([], nil)` (caller has no LiteLLM user yet → no teams); on other transport error it returns `(nil, err)` (this handler emits 503 litellm_unreachable).

    DO NOT define `lookupCallerTeams` inline in this file — Plan 03-05 Task 3 owns the helper (per WARN-06; teams package). This handler imports it.

    Sub-step 1c — Append `TestCreateHandler` test cases (1-12 above) to `internal/platformapi/envkeys/handler_test.go`.
  </action>
  <verify>
    <automated>./scripts/dev.sh go build ./internal/platformapi/envkeys/...</automated>
    <automated>./scripts/dev.sh go vet ./internal/platformapi/envkeys/...</automated>
    <automated>./scripts/dev.sh go test ./internal/platformapi/envkeys/... -run "TestCreateHandler" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - File `internal/platformapi/envkeys/handler.go` exists with `func CreateHandler(deps Deps) http.HandlerFunc`.
    - `grep -nE 'keyCtx\.KeyType != keys\.PrefixPk' internal/platformapi/envkeys/handler.go` returns at least one match (caller-type guard).
    - `grep -nE 'deps\.Store\.GetEnvironment' internal/platformapi/envkeys/handler.go` returns at least one match.
    - `grep -nE 'deps\.Store\.EnvironmentTerminating' internal/platformapi/envkeys/handler.go` returns at least one match.
    - `grep -nE 'deps\.Store\.EnvironmentAccessGroupSynced' internal/platformapi/envkeys/handler.go` returns at least one match.
    - `grep -nE 'keys\.NewBearer\(keys\.PrefixEk\)' internal/platformapi/envkeys/handler.go` returns at least one match.
    - `grep -nE 'AccessGroups:\s*\[\]string\{env\.Name\}|AccessGroups:.*req\.Environment' internal/platformapi/envkeys/handler.go` returns at least one match.
    - `grep -nE 'MaxBudget:\s*nil' internal/platformapi/envkeys/handler.go` returns at least one match (KEY-10).
    - `grep -nE 'deps\.LiteLLM\.RevokeKey' internal/platformapi/envkeys/handler.go` returns at least one match (compensation).
    - `grep -nE 'audit\.ActionEkCreate' internal/platformapi/envkeys/handler.go` returns at least one match.
    - `grep -nE 'DisallowUnknownFields' internal/platformapi/envkeys/handler.go` returns at least one match (D-16).
    - `grep -cE '^func lookupCallerTeams\(' internal/platformapi/envkeys/handler.go` equals 0 (WARN-06 — no inline helper; the shared helper from Plan 03-05 Task 3 is imported).
    - `grep -nE 'achteams\.LookupCallerTeams|teams\.LookupCallerTeams' internal/platformapi/envkeys/handler.go` returns at least one match (WARN-06 — shared helper import).
    - `./scripts/dev.sh go test ./internal/platformapi/envkeys/... -run "TestCreateHandler" -count=1` exits 0 with all 12 behaviors passing.
    - Test 11 (plaintext exactly once) acceptance gate.
    - Test 10a (credential_hash collision → 500 + LiteLLM revoked) acceptance gate (per WARN-03 commit).
    - Test 10b (ekid_ PK collision → retry with new ulid succeeds, NO LiteLLM revoke) acceptance gate (per WARN-03 commit).
  </acceptance_criteria>
  <done>CreateHandler implements §8.2 8-step flow verbatim with caller-type guard, GetEnvironment/Terminating/AccessGroupSynced ordering, team-intersection check, idempotent LiteLLM user provision, server-side ek_ + ekid_ generation, KeyGenerate with caller-supplied key + access_groups + max_budget=nil + tags, transactional INSERT with LiteLLM compensation, ulid retry on PK collision, audit emission on every branch. 12 tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ListHandler + GetHandler (API-06 + per-row read)</name>
  <objective>Ship the read endpoints: GET /platform/env-keys returns paginated rows (caller-scoped non-admin; admin filter via ?owner_email=); GET /platform/env-keys/{key_id} returns a single row (or 404 / 400 invalid_argument on prefix mismatch / 403 not_key_owner).</objective>
  <read_first>
    - .planning/phases/03-hub-identity-platform-api/03-PATTERNS.md "internal/platformapi/envkeys/handler.go" — list shape + pagination patterns
    - .planning/phases/03-hub-identity-platform-api/03-CONTEXT.md API-06 + admin-allowlist context (D-22, D-23 — admin determination)
    - .planning/phases/03-hub-identity-platform-api/03-03-PLAN.md (ListEnvironmentKeysByOwner + ListEnvironmentKeysByOwnerWithFilter signatures)
    - ach_hub_spec_v20260515_FINALv4.md §15.5 (env-keys list shape; ?limit + ?cursor pagination; next_cursor field)
    - .planning/phases/03-hub-identity-platform-api/03-04-PLAN.md (keys.PkidKeyIDPrefix + keys.EkidKeyIDPrefix constants)
  </read_first>
  <behavior>
    - Test L-1 (list non-admin happy path): pk_ caller owner_email=a@b; seed 3 rows for a@b + 1 row for c@d; ListHandler returns only 3 rows.
    - Test L-2 (list non-admin with ?owner_email rejected): pk_ caller; ?owner_email=c@d in URL. Response: 400 invalid_argument; audit OutcomeInternalError or new outcome.
    - Test L-3 (list admin happy path): admin pk_ caller; no ?owner_email. Response: all rows in namespace.
    - Test L-4 (list admin with ?owner_email filter): admin; ?owner_email=c@d. Response: only c@d's rows.
    - Test L-5 (list ek_ caller rejected): ek_ caller → 401 invalid_key_type (management endpoint per API-11).
    - Test L-6 (list pagination): seed 7 rows; ?limit=3 returns 3 rows + next_cursor populated; follow-up call with ?cursor=<next> returns next 3 rows; final call returns remaining 1 + next_cursor=null.
    - Test L-7 (list default limit=100): no ?limit param → default 100; ?limit=600 → 400 invalid_argument (exceeds 500 max).

    - Test G-1 (get happy path non-admin owner): pk_ caller; row exists for caller. Response: 200 with row fields.
    - Test G-2 (get prefix mismatch): GET /platform/env-keys/pkid_xxx (wrong prefix). Response: 400 invalid_argument; lookup NOT performed (verify DB query count 0).
    - Test G-3 (get plaintext rejected): GET /platform/env-keys/ek_xxx (plaintext, not key_id). Response: 400 invalid_argument.
    - Test G-4 (get owned by different caller): row owned by c@d; caller owner=a@b, non-admin. Response: 403 not_key_owner; audit OutcomeNotKeyOwner.
    - Test G-5 (get owned by different caller, admin): admin caller. Response: 200.
    - Test G-6 (get unknown key_id): GET /platform/env-keys/ekid_unknownxxx. Response: 404 (use OutcomeEnvironmentNotFound or a new "not_found" — per Hub §15.5 the not-found case is `404 not_found` or `404 environment_not_found`; for env-keys-get use generic 404 + audit OutcomeNotKeyOwner OR similar; document chosen outcome in SUMMARY).
  </behavior>
  <action>
    Append to `internal/platformapi/envkeys/handler.go`:

    `ListHandler(deps Deps) http.HandlerFunc`:
    1. Caller-type guard: pk_ only.
    2. Parse query: `?limit=` (default 100, max 500 — 400 if exceeded), `?cursor=` (opaque), `?owner_email=` (admin-only).
    3. Determine admin status: read `keyCtx.IsAdmin` directly. Plan 03-05 ships `KeyContext.IsAdmin bool` per BLK-02, populated by `middleware.Authn` against the allowlist loaded in Plan 03-11. No fallback path — there is no `deps.Allowlist` on this handler.
    4. If admin: call `db.ListEnvironmentKeysByOwnerWithFilter(ctx, pool, optionalOwnerEmail, limit, cursor)`. If non-admin: call `db.ListEnvironmentKeysByOwner(ctx, pool, keyCtx.OwnerEmail, limit, cursor)`.
    5. Render JSON: `{"items": [...], "next_cursor": <string or null>}`.

    `GetHandler(deps Deps) http.HandlerFunc`:
    1. Caller-type guard: pk_ only.
    2. Read `chi.URLParam(r, "key_id")`. Validate `strings.HasPrefix(keyID, keys.EkidKeyIDPrefix)`. If not → 400 invalid_argument; NO DB call.
    3. `row, err := db.GetEnvironmentKey(ctx, pool, keyID)`. If (nil, nil) → 404.
    4. Owner check: `row.OwnerEmail == keyCtx.OwnerEmail || keyCtx.IsAdmin`. Else → 403 not_key_owner + audit OutcomeNotKeyOwner.
    5. Render JSON with row fields (NO plaintext; NO credential_hash; key_id + environment + name + owner_email + status + created_at).

    Append test cases L-1..L-7 + G-1..G-6 to handler_test.go.
  </action>
  <verify>
    <automated>./scripts/dev.sh go build ./internal/platformapi/envkeys/...</automated>
    <automated>./scripts/dev.sh go test ./internal/platformapi/envkeys/... -run "TestListHandler|TestGetHandler" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '^func ListHandler\(' internal/platformapi/envkeys/handler.go` returns exactly one match.
    - `grep -nE '^func GetHandler\(' internal/platformapi/envkeys/handler.go` returns exactly one match.
    - `grep -nE 'strings\.HasPrefix\(keyID, keys\.EkidKeyIDPrefix\)' internal/platformapi/envkeys/handler.go` returns at least one match (prefix gate per API-07/GetHandler).
    - `grep -nE 'invalid_argument' internal/platformapi/envkeys/handler.go` returns at least one match.
    - `grep -nE 'not_key_owner' internal/platformapi/envkeys/handler.go` returns at least one match.
    - `./scripts/dev.sh go test ./internal/platformapi/envkeys/... -run "TestListHandler|TestGetHandler" -count=1` exits 0 with all 13 behaviors passing.
    - Test L-2 verifies non-admin owner_email rejection.
    - Test G-2 verifies prefix mismatch returns 400 BEFORE DB lookup.
  </acceptance_criteria>
  <done>ListHandler + GetHandler ship with caller-scope + admin override + pagination + per-row owner check; non-admin owner_email filtering rejected with 400; prefix mismatch on key_id rejected before DB; plaintext/credential_hash absent from responses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement RevokeHandler + Mount (§8.5 LiteLLM-first revoke + chi router subtree)</name>
  <objective>Ship the asymmetric-revocation endpoint and the chi router mount-point. RevokeHandler runs §8.5 verbatim: LiteLLM ack → DB flip → Redis DEL → 204 No Content. Mount returns the chi.Router subtree the cmd/platform-api wiring (Plan 03-11) installs at /platform/env-keys.</objective>
  <read_first>
    - .planning/phases/03-hub-identity-platform-api/03-PATTERNS.md "internal/platformapi/envkeys/handler.go" — RevokeHandler skeleton + §8.5 sequence
    - .planning/phases/03-hub-identity-platform-api/03-CONTEXT.md D-15 (§8.5 5-step verbatim) + KEY-08 (LiteLLM-first invariant)
    - .planning/phases/03-hub-identity-platform-api/03-03-PLAN.md (GetEnvironmentKey + RevokeEnvironmentKey signatures)
    - .planning/phases/03-hub-identity-platform-api/03-05-PLAN.md (redisCachedResolver cache key shape: `"ach:key:" + credentialHashHex`)
    - internal/orphan/runnable.go (Phase 2 — LiteLLM-first sequencing call-site)
    - ach_hub_spec_v20260515_FINALv4.md §8.5 (the 5-step flow) + §15.5 (DELETE /platform/env-keys/{key_id} response shape: 204 No Content; not_key_owner; invalid_argument prefix gate)
    - ach_hub_spec_v20260515_FINALv4.md §5.1 (Redis cache invalidation via DEL)
  </read_first>
  <behavior>
    - Test R-1 (happy path): caller=owner, ek_ row exists, status=active. RevokeHandler:
      - Step 1: GetEnvironmentKey → row with credential_hash + litellm_token captured. NO DB UPDATE yet.
      - Step 2: deps.LiteLLM.RevokeKey(ctx, row.LiteLLMToken) returns nil.
      - Step 3: db.RevokeEnvironmentKey(ctx, pool, keyID) flips status='revoked', revoked_at=now().
      - Step 4: deps.Redis.Del(ctx, "ach:key:" + credential_hash_hex) returns nil.
      - Step 5: audit ActionEkRevoke + OutcomeRevoked + key.id=keyID.
      - Response: 204 No Content; no body.

    - Test R-2 (prefix mismatch): DELETE /platform/env-keys/pkid_xxx. Response: 400 invalid_argument; NO DB / LiteLLM / Redis work.

    - Test R-3 (unknown key_id): DELETE /platform/env-keys/ekid_unknown. Response: 404 (chosen outcome); audit emitted.

    - Test R-4 (owner check fails): row owner=c@d; caller=a@b, non-admin. Response: 403 not_key_owner; audit OutcomeNotKeyOwner; DB / LiteLLM / Redis unchanged.

    - Test R-5 (LiteLLM unreachable): RevokeKey returns network error. Response: 503 litellm_unreachable; audit OutcomeLitellmUnreachable; DB row STAYS active (no flip); Redis NOT DEL'd; caller may retry — KEY-08 invariant.

    - Test R-6 (DB flip failure post-LiteLLM-ack): RevokeKey succeeds; RevokeEnvironmentKey returns error. Response: 500 internal_error; audit OutcomeInternalError; Redis NOT DEL'd. The LiteLLM-side key is revoked; the DB row is in a partial state — this is the documented post-LiteLLM-ack failure mode per §8.5. Operator next reconcile will eventually catch up via the orphan-cleanup loop reading active DB rows.

    - Test R-7 (admin override): admin caller, row owned by c@d. RevokeHandler proceeds (full happy-path); audit ActionEkRevoke with actor=<admin>.

    - Test R-8 (already-revoked row): row.status=='revoked'. GetEnvironmentKey returns the row (status field is in the response). RevokeHandler:
      - Should EITHER: (a) return 404 (already-revoked = not findable), OR (b) return 204 idempotently. Per §8.5 → likely (b) is more idempotent but Hub spec says "DELETE returns 204 only after LiteLLM acknowledges" — so already-revoked should still call LiteLLM RevokeKey (which LiteLLM treats as idempotent or returns 4xx) and either succeed-no-op (204) or fail-as-litellm-error (503).
      - Decision: prefer (a) 404 to avoid double-emitting audit events. Document choice in SUMMARY. Use Outcome=OutcomeEnvironmentNotFound or a new outcome.

    Implement chi router mount:
    - Test M-1 (Mount registers all four routes): construct a chi.Router, call Mount; verify routes table has POST /, GET /, GET /{key_id}, DELETE /{key_id}.
  </behavior>
  <action>
    Append `RevokeHandler(deps Deps) http.HandlerFunc` to `internal/platformapi/envkeys/handler.go` per Hub §8.5:

    1. Caller-type guard: pk_ only.
    2. `keyID := chi.URLParam(r, "key_id")`. Validate ekid_ prefix; else 400 invalid_argument; NO DB.
    3. `row, err := db.GetEnvironmentKey(ctx, pool, keyID)`. If (nil, nil) → 404 + audit.
    4. Owner check: `row.OwnerEmail == keyCtx.OwnerEmail || keyCtx.IsAdmin`. Else → 403 not_key_owner + audit.
    5. Already-revoked check: `row.Status != "active"` → 404 + audit (decision per Test R-8).
    6. **LiteLLM-first (D-15):** `if err := deps.LiteLLM.RevokeKey(ctx, *row.LiteLLMToken); err != nil { audit OutcomeLitellmUnreachable + 503 + RETURN }`. DB row stays active so retry works cleanly.
    7. **DB flip:** `_, err := db.RevokeEnvironmentKey(ctx, pool, keyID)`. On err → audit OutcomeInternalError + 500.
    8. **Redis DEL (best-effort):** `if err := deps.Redis.Del(ctx, "ach:key:" + row.CredentialHash).Err(); err != nil { deps.Logger.Warn(...) }`. Do NOT fail the request on Redis err — 60s TTL is the worst case bound.
    9. Audit ActionEkRevoke + OutcomeRevoked + key.id=keyID + target.kind=environment + target.name=row.Environment.
    10. `w.WriteHeader(http.StatusNoContent)`. No body.

    Implement `Mount(deps Deps) func(chi.Router)` in `internal/platformapi/envkeys/mount.go`:
    ```
    func Mount(deps Deps) func(r chi.Router) {
        return func(r chi.Router) {
            r.Post("/", CreateHandler(deps))
            r.Get("/", ListHandler(deps))
            r.Get("/{key_id}", GetHandler(deps))
            r.Delete("/{key_id}", RevokeHandler(deps))
        }
    }
    ```

    Append test cases R-1..R-8 + M-1 to handler_test.go.
  </action>
  <verify>
    <automated>./scripts/dev.sh go build ./internal/platformapi/envkeys/...</automated>
    <automated>./scripts/dev.sh go vet ./internal/platformapi/envkeys/...</automated>
    <automated>./scripts/dev.sh go test ./internal/platformapi/envkeys/... -run "TestRevokeHandler|TestMount" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '^func RevokeHandler\(' internal/platformapi/envkeys/handler.go` returns exactly one match.
    - `grep -nE '^func Mount\(' internal/platformapi/envkeys/mount.go` returns exactly one match.
    - Code-ordering check: in RevokeHandler, the LITERAL token "deps.LiteLLM.RevokeKey" appears BEFORE the literal token "db.RevokeEnvironmentKey" (LiteLLM-first invariant per KEY-08). Verified via:
      - `awk '/deps\.LiteLLM\.RevokeKey/{print "litellm:" NR} /db\.RevokeEnvironmentKey/{print "db:" NR}' internal/platformapi/envkeys/handler.go` shows the litellm line number is LESS THAN the db line number.
    - `grep -nE 'http\.StatusNoContent' internal/platformapi/envkeys/handler.go` returns at least one match (204 invariant).
    - `grep -nE 'deps\.Redis\.Del' internal/platformapi/envkeys/handler.go` returns at least one match (Redis invalidate).
    - `grep -nE '"ach:key:" \+ row\.CredentialHash' internal/platformapi/envkeys/handler.go` returns at least one match (cache-key shape matches keystore.go).
    - `grep -cE 'r\.Post|r\.Get|r\.Delete' internal/platformapi/envkeys/mount.go` ≥ 4 (four routes mounted).
    - `./scripts/dev.sh go test ./internal/platformapi/envkeys/... -run "TestRevokeHandler|TestMount" -count=1` exits 0 with all 9 behaviors passing.
    - Test R-5 verifies LiteLLM-unreachable leaves DB row active (KEY-08 invariant — caller may retry).
  </acceptance_criteria>
  <done>RevokeHandler enforces §8.5 LiteLLM-first sequence (asymmetric revocation per KEY-08); prefix gate + owner check + admin override + already-revoked + retry-on-litellm-fail all tested; Mount registers four chi routes; full plan: 12+13+9 = 34 test cases all pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| handler -> LiteLLM REST | virtual-key revoke crosses the LiteLLM API; LiteLLM is the load-bearing barrier (KEY-08) |
| handler -> Redis | DEL invalidation; best-effort, 60s TTL is fallback |
| handler -> Postgres | INSERT (create) or UPDATE (revoke) — transactional |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08-01 | Tampering | revocation race (ek_) | mitigate | LiteLLM-first ordering: deps.LiteLLM.RevokeKey BEFORE db.RevokeEnvironmentKey BEFORE redis.Del. T-KEY-05 alignment. Acceptance gate compares grep line numbers |
| T-03-08-02 | Information Disclosure | plaintext leaked via List/Get | mitigate | Response includes key_id + environment + name + owner_email + status + created_at — NO plaintext, NO credential_hash, NO litellm_token. Verified by inspecting CreateResponse + the analogous List/Get response shapes |
| T-03-08-03 | Tampering | prefix-confusion attack | mitigate | GetHandler + RevokeHandler validate `strings.HasPrefix(keyID, keys.EkidKeyIDPrefix)` BEFORE any DB call. Rejects pkid_/pk_/ek_/random inputs |
| T-03-08-04 | Spoofing | revoke-someone-else's-key | mitigate | Owner check: `row.OwnerEmail == keyCtx.OwnerEmail || keyCtx.IsAdmin`. Test R-4 acceptance gate. Admin path requires Plan 03-10's allowlist check populating keyCtx.IsAdmin |
| T-03-08-05 | Information Disclosure | non-admin sees others' env-keys | mitigate | ListHandler uses `db.ListEnvironmentKeysByOwner(callerEmail, ...)` for non-admin; rejects ?owner_email parameter for non-admin (Test L-2 acceptance gate) |
| T-03-08-06 | Denial of Service | DB INSERT compensation orphans LiteLLM key | mitigate | Compensation: deps.LiteLLM.RevokeKey(token) called on InsertEnvironmentKey failure (Test 9 acceptance gate). Plus orphan-cleanup loop (Phase 2) catches missed compensations on next tick (Phase 02.2 D-02 = precise via ListActiveACHKeyTokens) |
| T-03-08-07 | Tampering | replay of revoked key | accept | Revoked rows have status='revoked'; Redis cache may still serve the stale 'active' KeyInfo for up to 60s (KEY-08 designed bound). The 60s window is acceptable per Hub §5.1 |
| T-03-08-08 | Information Disclosure | plaintext in audit Extra | mitigate | Audit emission uses only key_id (ekid_) and target.kind/target.name; never plaintext. T-AUDIT-01 alignment; Plan 03-02 EmitAudit doc-comment enforces caller discipline |
| T-03-08-SC | Tampering | npm/pip/cargo installs | mitigate | No new go.mod entries. Reuses all packages from upstream Phase 3 plans |
</threat_model>

<verification>
**Phase-level criteria mapped to plan tasks:**
- Phase SC#3 (POST /platform/env-keys runs §8.2 8-step) — Task 1 implements verbatim.
- Phase SC#4 (asymmetric revocation: ek_ LiteLLM-first) — Task 3 implements + grep gate on line ordering.
- KEY-05 (ek_ bound to one Environment, not re-checked per request) — Task 1 INSERT stores Environment; no subsequent reauthorization.
- KEY-08 (ek_ LiteLLM-first revocation) — Task 3 acceptance gate.
- KEY-11 (§8.2 flow with informer cache check + idempotent verify-or-create) — Task 1 acceptance gates.
- API-05 (POST response shape) — Task 1 CreateResponse.
- API-06 (List caller-filtered; admin override; non-admin owner_email rejection) — Task 2.
- API-07 (DELETE prefix gate BEFORE lookup; 204 after LiteLLM ack; 403 not_key_owner) — Task 3.

**Plan-level verification:**
- `./scripts/dev.sh go build ./internal/platformapi/envkeys/...` exits 0
- `./scripts/dev.sh go vet ./internal/platformapi/envkeys/...` exits 0
- `./scripts/dev.sh go test ./internal/platformapi/envkeys/... -count=1` exits 0
- LiteLLM-first ordering gate: `awk '/deps\\.LiteLLM\\.RevokeKey/{print "litellm:" NR} /db\\.RevokeEnvironmentKey/{print "db:" NR}' internal/platformapi/envkeys/handler.go` shows litellm line < db line
</verification>

<success_criteria>
- All three tasks pass `<verify>` blocks.
- KEY-08 acceptance gate (LiteLLM-first ordering) verifiable via grep line-numbers.
- 34+ test cases collectively cover §8.2 + §8.5 flows + every error matrix entry from §8.3.
- Plain text emission count check (Test 11): single occurrence of generated plaintext in response.
- Compensation invariants:
  - Create: LiteLLM RevokeKey called exactly once on InsertEnvironmentKey failure.
  - Revoke: DB UPDATE NEVER runs before LiteLLM RevokeKey ack.
</success_criteria>

<output>
Create `.planning/phases/03-hub-identity-platform-api/03-08-SUMMARY.md` when done — record:
1. The chosen behavior for Test R-8 (already-revoked: 404 vs 204) with rationale.
2. Confirmation that the WARN-03 commit was implemented: ekid_ PK collisions retry once; credential_hash UNIQUE collisions surface as 500 with LiteLLM compensation (no retry).
3. Confirmation that the team-membership lookup imports `internal/platformapi/teams` (the shared helper shipped by Plan 03-05 Task 3 per WARN-06) and does NOT inline the LiteLLM call.
4. The mechanism by which KeyContext.IsAdmin is populated (Authn-middleware extension in Plan 03-05 vs Deps.Allowlist lookup in handler — relevant for Plan 03-10 admin handler coordination).
</output>
